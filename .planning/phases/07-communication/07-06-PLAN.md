---
phase: 07-communication
plan: 06
type: execute
wave: 6
depends_on: ["07-05"]
files_modified:
  - apps/web/src/components/messaging/ReadReceiptIndicator.tsx
  - apps/web/src/components/messaging/ReadReceiptDetail.tsx
  - apps/web/src/components/messaging/MessageAttachmentUpload.tsx
  - apps/web/src/components/messaging/MessageAttachmentDisplay.tsx
  - apps/web/src/components/messaging/PollCreator.tsx
  - apps/web/src/components/messaging/PollDisplay.tsx
  - apps/web/src/components/messaging/PollResultBar.tsx
  - apps/web/src/components/messaging/AbsenceQuickAction.tsx
  - apps/web/src/hooks/usePoll.ts
  - apps/web/src/hooks/useReadReceipts.ts
  - apps/web/src/components/messaging/MessageBubble.tsx
  - apps/web/src/components/messaging/ComposeDialog.tsx
  - apps/web/src/components/messaging/MessageReplyInput.tsx
  - apps/web/src/components/messaging/ConversationList.tsx
  - apps/web/src/hooks/__tests__/useMessages.test.ts
autonomous: false
requirements:
  - COMM-03
  - COMM-04
  - COMM-05
  - COMM-06

must_haves:
  truths:
    - "Sender sees read receipt indicators (single check, double check gray/blue/green) on sent messages"
    - "Clicking read receipt icon shows Popover with list of who has read and who has not"
    - "Messages display file attachments with download link and type icon"
    - "Compose dialog and reply input support attaching files (PDF, JPG, PNG, DOC, DOCX)"
    - "Polls render inline in message bubbles with vote buttons and result bars"
    - "Parents see 'Abwesenheit melden' quick-action that creates excuse + system message"
  artifacts:
    - path: "apps/web/src/components/messaging/ReadReceiptIndicator.tsx"
      provides: "Inline read receipt icon with 4 states"
      contains: "ReadReceiptIndicator"
    - path: "apps/web/src/components/messaging/PollDisplay.tsx"
      provides: "Inline poll rendering with vote buttons and results"
      contains: "PollDisplay"
    - path: "apps/web/src/components/messaging/AbsenceQuickAction.tsx"
      provides: "Quick-action button for parent absence reporting"
      contains: "AbsenceQuickAction"
    - path: "apps/web/src/components/messaging/MessageAttachmentUpload.tsx"
      provides: "File attachment upload component"
      contains: "MessageAttachmentUpload"
  key_links:
    - from: "apps/web/src/components/messaging/MessageBubble.tsx"
      to: "apps/web/src/components/messaging/ReadReceiptIndicator.tsx"
      via: "Inline render for sender's messages"
      pattern: "ReadReceiptIndicator"
    - from: "apps/web/src/components/messaging/MessageBubble.tsx"
      to: "apps/web/src/components/messaging/PollDisplay.tsx"
      via: "Inline render when message.type === 'POLL'"
      pattern: "PollDisplay"
    - from: "apps/web/src/components/messaging/AbsenceQuickAction.tsx"
      to: "/api/v1/schools/:schoolId/absence-report"
      via: "apiFetch POST for absence reporting"
      pattern: "absence-report"
---

<objective>
Complete the frontend by adding read receipt indicators (COMM-03), file attachment upload/display (COMM-04), absence quick-action (COMM-05), inline poll creation and voting (COMM-06), and a human verification checkpoint. This finishes Phase 7.

Purpose: All 6 COMM requirements are now visible and interactive in the UI.
Output: ReadReceiptIndicator, MessageAttachmentUpload/Display, PollCreator/Display/ResultBar, AbsenceQuickAction, frontend test stubs, human verification.
</objective>

<execution_context>
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-communication/07-CONTEXT.md
@.planning/phases/07-communication/07-RESEARCH.md
@.planning/phases/07-communication/07-UI-SPEC.md
@.planning/phases/07-communication/07-05-SUMMARY.md

<interfaces>
<!-- Key types and components from prior plans -->

From packages/shared/src/types/messaging.ts (Plan 01):
```typescript
export interface MessageDto { id, conversationId, senderId, senderName, body, type, createdAt, attachments: MessageAttachmentDto[], poll: PollDto | null, readCount?, totalRecipients? }
export interface MessageAttachmentDto { id, filename, mimeType, sizeBytes }
export interface PollDto { id, question, type, deadline, isClosed, options: PollOptionDto[], userVoteOptionIds: string[] }
export interface PollOptionDto { id, text, voteCount, voters?: {userId, name}[] }
export type PollType = 'SINGLE_CHOICE' | 'MULTIPLE_CHOICE';
```

From apps/web/src/components/messaging/MessageBubble.tsx (Plan 05):
```typescript
export function MessageBubble({ message, isOwn, isGroupChat }: { message: MessageDto; isOwn: boolean; isGroupChat: boolean })
// Currently renders body, sender name, timestamp. Needs: ReadReceiptIndicator, MessageAttachmentDisplay, PollDisplay inline.
```

From apps/web/src/components/messaging/ComposeDialog.tsx (Plan 05):
```typescript
export function ComposeDialog({ open, onOpenChange, schoolId }: { open: boolean; onOpenChange: (open: boolean) => void; schoolId: string })
// Currently has broadcast/direct tabs, body, subject. Needs: attachment upload area, poll toggle.
```

From apps/web/src/components/messaging/MessageReplyInput.tsx (Plan 05):
```typescript
export function MessageReplyInput({ onSend, disabled }: { onSend: (body: string) => void; disabled?: boolean })
// Currently has textarea + send button. Needs: paperclip button for attachment.
```

From apps/web/src/components/classbook/ExcuseForm.tsx (Phase 5 -- reused for COMM-05):
```typescript
export function ExcuseForm({ ... }) // Existing Phase 5 excuse form
```

API endpoints:
```
POST   /api/v1/schools/:schoolId/conversations/:id/messages/:mid/attachments  # Upload
GET    /api/v1/schools/:schoolId/conversations/:id/messages/:mid/attachments/:aid/download  # Download
POST   /api/v1/schools/:schoolId/polls/:pid/votes
PATCH  /api/v1/schools/:schoolId/polls/:pid/close
GET    /api/v1/schools/:schoolId/polls/:pid/results
POST   /api/v1/schools/:schoolId/absence-report
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read receipts, file attachments, polls, absence quick-action components</name>
  <files>
    apps/web/src/components/messaging/ReadReceiptIndicator.tsx,
    apps/web/src/components/messaging/ReadReceiptDetail.tsx,
    apps/web/src/components/messaging/MessageAttachmentUpload.tsx,
    apps/web/src/components/messaging/MessageAttachmentDisplay.tsx,
    apps/web/src/components/messaging/PollCreator.tsx,
    apps/web/src/components/messaging/PollDisplay.tsx,
    apps/web/src/components/messaging/PollResultBar.tsx,
    apps/web/src/components/messaging/AbsenceQuickAction.tsx,
    apps/web/src/hooks/usePoll.ts,
    apps/web/src/hooks/useReadReceipts.ts,
    apps/web/src/hooks/__tests__/useMessages.test.ts
  </files>
  <read_first>
    apps/web/src/components/messaging/MessageBubble.tsx,
    apps/web/src/components/messaging/ComposeDialog.tsx,
    apps/web/src/components/messaging/MessageReplyInput.tsx,
    apps/web/src/components/messaging/ConversationList.tsx,
    apps/web/src/components/classbook/ExcuseForm.tsx,
    apps/web/src/components/classbook/FileUploadField.tsx,
    apps/web/src/hooks/useMessages.ts,
    apps/web/src/lib/api-fetch.ts,
    apps/web/src/hooks/useAuth.ts
  </read_first>
  <action>
1. **ReadReceiptIndicator** (COMM-03) -- Per UI-SPEC read receipt visual encoding:

   Props: `{ readCount: number; totalRecipients: number; isOwn: boolean; messageId: string; onShowDetail: () => void }`.

   Render logic (only visible when isOwn):
   - readCount === 0 and totalRecipients === 0: Single Check icon in muted-foreground (sent). aria-label="Gesendet".
   - readCount === 0: CheckCheck (double) in muted-foreground (delivered). aria-label="Zugestellt".
   - readCount > 0 && readCount < totalRecipients: CheckCheck in primary color (partial read). aria-label="{readCount} von {totalRecipients} gelesen".
   - readCount >= totalRecipients: CheckCheck in success color (all read). aria-label="Gelesen".

   Import Check, CheckCheck from lucide-react. On click: call onShowDetail (opens Popover).

2. **ReadReceiptDetail** (COMM-03) -- Popover content per UI-SPEC:

   Props: `{ messageId: string; schoolId: string }`.
   Uses useReadReceipts hook to fetch read/unread lists from backend.
   Two sections: "Gelesen" (names + timestamps) and "Nicht gelesen" (names). Summary "X von Y gelesen" at top.
   Scrollable via ScrollArea if list is long.

3. **useReadReceipts hook** (apps/web/src/hooks/useReadReceipts.ts):
   ```typescript
   export function useReadReceipts(schoolId: string, conversationId: string, messageId: string) {
     return useQuery({
       queryKey: ['readReceipts', messageId],
       queryFn: async () => {
         // Fetch message detail with read receipt data
         // The message list already returns readCount/totalRecipients for sender's messages
         // For the detail popover, fetch from a dedicated endpoint or parse from message recipients
       },
     });
   }
   ```

4. **MessageAttachmentUpload** (COMM-04) -- Per UI-SPEC:

   Props: `{ files: File[]; onAdd: (files: File[]) => void; onRemove: (index: number) => void; maxFiles: number; maxSizeBytes: number }`.

   Renders: file picker button (Paperclip icon + "Datei anfuegen" label), hint text "PDF, JPG, PNG, DOC oder DOCX (max. 5 MB pro Datei)", list of attached files with filename, size (formatBytes), remove X button. Validates: extension in ['.pdf', '.jpg', '.jpeg', '.png', '.doc', '.docx']. Size <= 5MB. Max 5 files. Shows inline error messages per UI-SPEC copywriting. Reuse Phase 5 FileUploadField component if compatible, otherwise create inline.

5. **MessageAttachmentDisplay** (COMM-04) -- Per UI-SPEC:

   Props: `{ attachments: MessageAttachmentDto[]; schoolId: string; conversationId: string; messageId: string }`.

   Renders each attachment: file type icon (FileText for PDF/DOC, ImageIcon for JPG/PNG), filename (truncated 30 chars), size, download Button (Download icon). Download URL: `/api/v1/schools/${schoolId}/conversations/${conversationId}/messages/${messageId}/attachments/${attachment.id}/download`. Image attachments show 48x48 thumbnail (use the download URL with an `<img>` tag).

6. **PollCreator** (COMM-06, D-09, D-11) -- Per UI-SPEC:

   Props: `{ onPollChange: (poll: PollInput | null) => void; initialPoll?: PollInput }`.
   Where PollInput = { question: string; type: PollType; options: string[]; deadline?: string }.

   Renders: question Input (placeholder "Ihre Frage eingeben"), type Select ("Einzelauswahl"/"Mehrfachauswahl"), option rows (start with 2, add up to 10, remove when >2), optional deadline native date input, "Option hinzufuegen" secondary button. Validation: question required, min 2 options, max 10 options. Calls onPollChange on every field change.

7. **PollDisplay** (COMM-06, D-09, D-10, D-11, D-12) -- Per UI-SPEC:

   Props: `{ poll: PollDto; currentUserId: string; isSender: boolean; schoolId: string }`.

   Renders: question (label weight), status badge ("Offen"/"Abgeschlossen"), deadline warning if <=24h. Before voting: radio buttons (single choice) or checkboxes (multiple choice) per option. "Stimme abgeben" button for multi-choice. After voting: PollResultBar per option. Sender/admin: click option expands voter names. Others: anonymous counts only. Uses usePoll hook for vote mutation.

8. **PollResultBar** (COMM-06) -- Per UI-SPEC poll result bar colors:

   Props: `{ option: PollOptionDto; totalVotes: number; isUserVote: boolean }`.

   Horizontal bar: default `bg-primary/20`, user's vote `bg-primary/40`. Text: option text, vote count, percentage. Percentage at end of bar.

9. **usePoll hook** (apps/web/src/hooks/usePoll.ts):
   ```typescript
   export function usePollResults(schoolId: string, pollId: string) { /* useQuery */ }
   export function useCastVote(schoolId: string, pollId: string) { /* useMutation POST */ }
   export function useClosePoll(schoolId: string, pollId: string) { /* useMutation PATCH */ }
   ```

10. **AbsenceQuickAction** (COMM-05, D-13, D-14, D-15) -- Per UI-SPEC:

    Props: `{ schoolId: string }`.

    Visible only to 'eltern' role. Button with AlertCircle icon + "Abwesenheit melden" label. On click: opens Dialog containing Phase 5 ExcuseForm component (imported from apps/web/src/components/classbook/ExcuseForm.tsx). On submit: POST to /api/v1/schools/{schoolId}/absence-report with studentId, dateFrom, dateTo, reason. Toast: "Abwesenheit gemeldet und Klassenvorstand benachrichtigt".

11. **Frontend test stubs** (apps/web/src/hooks/__tests__/useMessages.test.ts):
    ```typescript
    import { describe, it } from 'vitest';
    describe('useMessages', () => {
      it.todo('returns paginated messages for a conversation');
      it.todo('useSendMessage posts to correct endpoint');
      it.todo('useMarkRead sends PATCH to /read');
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: apps/web/src/components/messaging/ReadReceiptIndicator.tsx
    - File exists: apps/web/src/components/messaging/ReadReceiptDetail.tsx
    - File exists: apps/web/src/components/messaging/MessageAttachmentUpload.tsx
    - File exists: apps/web/src/components/messaging/MessageAttachmentDisplay.tsx
    - File exists: apps/web/src/components/messaging/PollCreator.tsx
    - File exists: apps/web/src/components/messaging/PollDisplay.tsx
    - File exists: apps/web/src/components/messaging/PollResultBar.tsx
    - File exists: apps/web/src/components/messaging/AbsenceQuickAction.tsx
    - grep "ReadReceiptIndicator" apps/web/src/components/messaging/ReadReceiptIndicator.tsx returns a match
    - grep "CheckCheck" apps/web/src/components/messaging/ReadReceiptIndicator.tsx returns a match (double check icon)
    - grep "PollCreator" apps/web/src/components/messaging/PollCreator.tsx returns a match
    - grep "PollDisplay" apps/web/src/components/messaging/PollDisplay.tsx returns a match
    - grep "AbsenceQuickAction" apps/web/src/components/messaging/AbsenceQuickAction.tsx returns a match
    - grep "Abwesenheit melden" apps/web/src/components/messaging/AbsenceQuickAction.tsx returns a match
    - grep "useCastVote" apps/web/src/hooks/usePoll.ts returns a match
    - File exists: apps/web/src/hooks/__tests__/useMessages.test.ts
    - TSC compiles without errors
  </acceptance_criteria>
  <done>ReadReceiptIndicator with 4 states, ReadReceiptDetail Popover, MessageAttachmentUpload/Display, PollCreator with single/multi choice and deadline, PollDisplay with vote buttons and result bars, PollResultBar, AbsenceQuickAction for parents, usePoll and useReadReceipts hooks, frontend test stubs all implemented and type-check.</done>
</task>

<task type="auto">
  <name>Task 2: Wire new components into existing pages</name>
  <files>
    apps/web/src/components/messaging/MessageBubble.tsx,
    apps/web/src/components/messaging/ComposeDialog.tsx,
    apps/web/src/components/messaging/MessageReplyInput.tsx,
    apps/web/src/components/messaging/ConversationList.tsx
  </files>
  <read_first>
    apps/web/src/components/messaging/MessageBubble.tsx,
    apps/web/src/components/messaging/ComposeDialog.tsx,
    apps/web/src/components/messaging/MessageReplyInput.tsx,
    apps/web/src/components/messaging/ConversationList.tsx,
    apps/web/src/components/messaging/ReadReceiptIndicator.tsx,
    apps/web/src/components/messaging/MessageAttachmentDisplay.tsx,
    apps/web/src/components/messaging/PollDisplay.tsx,
    apps/web/src/components/messaging/MessageAttachmentUpload.tsx,
    apps/web/src/components/messaging/PollCreator.tsx,
    apps/web/src/components/messaging/AbsenceQuickAction.tsx
  </read_first>
  <action>
1. **Update MessageBubble** to include:
   - ReadReceiptIndicator after timestamp (only when isOwn=true). Pass message.readCount, message.totalRecipients. Wrap in Popover with ReadReceiptDetail as content.
   - MessageAttachmentDisplay below body text (when message.attachments.length > 0).
   - PollDisplay inline in bubble (when message.type === 'POLL' and message.poll is not null). Pass poll, currentUserId, isSender.

2. **Update ComposeDialog** to include:
   - MessageAttachmentUpload component below body textarea. Manage files state with useState<File[]>([]). Pass files, onAdd, onRemove, maxFiles=5, maxSizeBytes=5*1024*1024.
   - "Umfrage anfuegen" toggle Button below attachment area. When toggled on, render PollCreator inline. Manage pollData state with useState<PollInput|null>(null).
   - On submit: if files exist, first POST the conversation/message, then upload each file via POST to /attachments endpoint using FormData.
   - On submit: if pollData exists, include pollData in the CreateConversationDto body.

3. **Update MessageReplyInput** to include:
   - Paperclip button (Paperclip icon) before send button. On click: opens system file picker. Manage attached files in local state. Display attached file previews above textarea. On send: upload files after message creation.

4. **Update ConversationList** to include:
   - AbsenceQuickAction button in the header area (visible only to 'eltern' role). Position: secondary action next to "Neue Nachricht" button.

5. **Ensure all components use UI-SPEC copywriting** -- Verify all German strings match the copywriting contract exactly (formal "Sie", no exclamation marks, ASCII-transliterated umlauts in source).
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "ReadReceiptIndicator" apps/web/src/components/messaging/MessageBubble.tsx returns a match
    - grep "MessageAttachmentDisplay" apps/web/src/components/messaging/MessageBubble.tsx returns a match
    - grep "PollDisplay" apps/web/src/components/messaging/MessageBubble.tsx returns a match
    - grep "MessageAttachmentUpload" apps/web/src/components/messaging/ComposeDialog.tsx returns a match
    - grep "PollCreator" apps/web/src/components/messaging/ComposeDialog.tsx returns a match
    - grep "Umfrage anfuegen" apps/web/src/components/messaging/ComposeDialog.tsx returns a match
    - grep "Paperclip\|paperclip" apps/web/src/components/messaging/MessageReplyInput.tsx returns a match
    - grep "AbsenceQuickAction" apps/web/src/components/messaging/ConversationList.tsx returns a match
    - TSC compiles without errors
  </acceptance_criteria>
  <done>All new components wired into existing pages: ReadReceiptIndicator + MessageAttachmentDisplay + PollDisplay in MessageBubble, MessageAttachmentUpload + PollCreator in ComposeDialog, Paperclip attachment in MessageReplyInput, AbsenceQuickAction in ConversationList header. Full frontend compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete messaging flow</name>
  <files>none</files>
  <action>
Human verification of the complete Phase 7 communication feature. All automated implementation is done in Tasks 1 and 2. This checkpoint pauses execution for the user to test the full messaging flow end-to-end.

What was built: Complete Phase 7 Communication -- thread-based messaging with broadcast/direct support, read receipts, file attachments, inline polls, absence reporting quick-action, and real-time Socket.IO updates.

Steps for verification:

**Prerequisites:** Start the full stack per app startup procedure (Docker Compose for infra, API server, Vite dev server).

1. **Sidebar navigation (D-05):** Log in with any test user. Verify "Nachrichten" appears in sidebar with MessageSquare icon. Click it to navigate to /messages.

2. **Broadcast message (COMM-01):** Log in as a Lehrer. Click "Neue Nachricht". Select "Rundnachricht" tab. Pick scope "Klasse", select a class. Enter subject + body. Click "Nachricht senden". Verify toast "Nachricht gesendet". Verify conversation appears in list.

3. **Direct message (COMM-02):** Click "Neue Nachricht". Select "Direktnachricht" tab. Search for a parent name. Enter message body. Send. Verify conversation appears.

4. **Message delivery (D-06):** Open a second browser/incognito as the recipient (Eltern). Navigate to /messages. Verify the message appears in their conversation list. Click to open. Verify messages show correctly (sender's messages on left).

5. **Read receipts (COMM-03):** In the sender's browser, check the message. It should show single/double check icon. When the recipient opens the message, verify the sender's read receipt indicator changes (from gray to green for direct, or shows "1 von N gelesen" for broadcast). Click the icon to verify the Popover shows read/unread names.

6. **File attachment (COMM-04):** Compose a new message. Click the paperclip icon. Attach a PDF file. Send. Verify recipient sees the attachment with filename, size, and download button. Click download to verify the file downloads correctly.

7. **Poll (COMM-06):** Compose a new message. Toggle "Umfrage anfuegen". Enter a question, add 3 options, set type to "Einzelauswahl". Send. Verify the poll renders inline. As a recipient, vote on an option. Verify results update. As the sender, verify you see named voters.

8. **Absence reporting (COMM-05):** Log in as Eltern. On /messages, find the "Abwesenheit melden" button. Click it. Fill in the excuse form. Submit. Verify toast "Abwesenheit gemeldet und Klassenvorstand benachrichtigt". Verify the Klassenvorstand receives a direct message with the absence details.

9. **Mobile responsive (D-06):** Resize browser to mobile width (<640px). Verify conversation list shows full-width. Tap a conversation to navigate to detail view. Verify back button returns to list.

10. **Real-time (D-08):** With two browsers open, send a message from one. Verify it appears instantly in the other without refresh.
  </action>
  <verify>Human tester confirms all 10 verification steps pass. Type "approved" or describe issues.</verify>
  <done>All 6 COMM requirements verified end-to-end by human tester. Phase 7 Communication is complete.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p apps/web/tsconfig.json` compiles without errors
2. `cd apps/api && pnpm test -- --run` all backend tests pass
3. Full messaging flow verified by human tester
4. All 6 COMM requirements demonstrable in the UI
</verification>

<success_criteria>
- Read receipt indicators show 4 states (sent, delivered, partial read, all read) per COMM-03
- File attachments upload/download working with validation per COMM-04
- Absence quick-action creates excuse + system message per COMM-05
- Polls render inline with vote buttons, results, deadline, named/anonymous visibility per COMM-06
- All components wired into MessageBubble, ComposeDialog, MessageReplyInput, ConversationList
- Human verification confirms complete messaging flow works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/07-communication/07-06-SUMMARY.md`
</output>
