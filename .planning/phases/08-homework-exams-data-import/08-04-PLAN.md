---
phase: 08-homework-exams-data-import
plan: 04
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - apps/api/src/modules/calendar/calendar.module.ts
  - apps/api/src/modules/calendar/calendar.service.ts
  - apps/api/src/modules/calendar/calendar.controller.ts
  - apps/api/src/modules/calendar/sis.service.ts
  - apps/api/src/modules/calendar/sis.controller.ts
  - apps/api/src/modules/calendar/guards/sis-api-key.guard.ts
  - apps/api/src/modules/calendar/dto/sis-response.dto.ts
  - apps/api/src/modules/calendar/dto/calendar-token.dto.ts
  - apps/api/src/modules/calendar/__tests__/calendar.service.spec.ts
  - apps/api/src/modules/calendar/__tests__/sis.service.spec.ts
  - apps/api/prisma/seed.ts
  - apps/api/src/app.module.ts
autonomous: true
requirements:
  - IMPORT-03
  - IMPORT-04

must_haves:
  truths:
    - "User can generate a personal iCal subscription URL containing timetable, homework, and exams"
    - "Calendar apps (Google Calendar, Apple Calendar) can fetch the ICS via token-authenticated URL"
    - "External SIS systems can read students, teachers, and classes via API key-protected endpoints"
    - "User can revoke and regenerate their calendar token"
  artifacts:
    - path: "apps/api/src/modules/calendar/calendar.service.ts"
      provides: "iCal generation with timetable + homework + exam events"
      exports: ["CalendarService"]
    - path: "apps/api/src/modules/calendar/calendar.controller.ts"
      provides: "Token-authenticated /api/v1/calendar/:token.ics endpoint"
      exports: ["CalendarController"]
    - path: "apps/api/src/modules/calendar/sis.service.ts"
      provides: "Read-only data access for SIS integration"
      exports: ["SisService"]
    - path: "apps/api/src/modules/calendar/sis.controller.ts"
      provides: "/api/v1/sis/ endpoints with API key auth"
      exports: ["SisController"]
    - path: "apps/api/src/modules/calendar/guards/sis-api-key.guard.ts"
      provides: "X-Api-Key header guard for SIS endpoints"
      exports: ["SisApiKeyGuard"]
  key_links:
    - from: "apps/api/src/modules/calendar/calendar.service.ts"
      to: "ical-generator"
      via: "ICS content generation with timetable + homework + exams"
      pattern: "ical.*createEvent"
    - from: "apps/api/src/modules/calendar/calendar.controller.ts"
      to: "apps/api/src/modules/calendar/calendar.service.ts"
      via: "Token lookup then ICS generation"
      pattern: "findByToken.*generateIcs"
    - from: "apps/api/src/modules/calendar/guards/sis-api-key.guard.ts"
      to: "prisma.sisApiKey"
      via: "API key validation in guard"
      pattern: "prisma\\.sisApiKey\\.findFirst"
---

<objective>
iCal calendar subscription + SIS read-only API: CalendarService generates ICS with timetable/homework/exams, CalendarController serves token-authenticated .ics files, SisService provides read-only student/teacher/class data, SisApiKeyGuard protects SIS endpoints. CASL permission seeds for Phase 8 entities.

Purpose: Delivers IMPORT-03 (iCal/ICS export) and IMPORT-04 (SIS API). Users subscribe to their personal calendar. External systems read school data via API.
Output: CalendarModule registered in AppModule with calendar and SIS subsystems. Tests pass.
</objective>

<execution_context>
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-homework-exams-data-import/08-CONTEXT.md
@.planning/phases/08-homework-exams-data-import/08-RESEARCH.md
@.planning/phases/08-homework-exams-data-import/08-01-SUMMARY.md
@.planning/phases/08-homework-exams-data-import/08-02-SUMMARY.md

<interfaces>
<!-- ical-generator already installed, used in Phase 4 -->
From apps/api/src/modules/timetable/timetable-export.service.ts:
- Uses ical-generator to create ICS with timetable lessons
- Pattern: ical({ name, timezone: 'Europe/Vienna', prodId, method: ICalCalendarMethod.PUBLISH })
- createEvent for each lesson

<!-- HomeworkService and ExamService from Plan 02 -->
From apps/api/src/modules/homework/homework.service.ts:
- findBySchool(schoolId, opts): returns Homework[]
From apps/api/src/modules/homework/exam.service.ts:
- findByClass(classId): returns Exam[]

<!-- @Public() decorator for bypassing JWT -->
From apps/api/src/modules/auth/decorators/public.decorator.ts:
- @Public() -- bypasses JwtAuthGuard

<!-- Seed file pattern -->
From apps/api/prisma/seed.ts:
- Permission seeds follow pattern: { action: 'manage', subject: 'all' } for admin
- Role-specific arrays: schulleitung, lehrer, eltern, schueler permissions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CalendarService + CalendarController (iCal with token auth) + tests</name>
  <read_first>
    - apps/api/src/modules/calendar/__tests__/calendar.service.spec.ts
    - apps/api/src/modules/timetable/timetable-export.service.ts
    - apps/api/src/modules/homework/homework.service.ts
    - apps/api/src/modules/homework/exam.service.ts
    - apps/api/src/modules/auth/decorators/public.decorator.ts
    - apps/api/prisma/schema.prisma
  </read_first>
  <files>
    apps/api/src/modules/calendar/calendar.service.ts,
    apps/api/src/modules/calendar/calendar.controller.ts,
    apps/api/src/modules/calendar/dto/calendar-token.dto.ts,
    apps/api/src/modules/calendar/__tests__/calendar.service.spec.ts
  </files>
  <behavior>
    CalendarService:
    - generateToken(userId, schoolId): Creates CalendarToken with crypto.randomUUID(). Returns token DTO.
    - findByToken(token): Looks up CalendarToken. Returns null if not found (revoked or invalid).
    - revokeAndRegenerate(userId, schoolId): Deletes existing token for userId+schoolId, creates new one.
    - generateIcs(userId, schoolId): Generates ICS string containing:
      (a) Recurring timetable lessons for the user (weekly events per school year)
      (b) Homework due dates as all-day VEVENT (summary: "HA: {title}")
      (c) Exam dates as all-day VEVENT (summary: "Pruefung: {title}")
      Uses Europe/Vienna timezone. Returns string.
    - findTokenForUser(userId, schoolId): Returns existing token or null.

    CalendarController:
    - GET /api/v1/calendar/:token.ics (@Public): Token-authenticated. Looks up token, generates ICS, returns with Content-Type text/calendar.
    - POST /api/v1/schools/:schoolId/calendar/token (JWT-protected): Creates token for current user.
    - DELETE /api/v1/schools/:schoolId/calendar/token (JWT-protected): Revokes and regenerates.
    - GET /api/v1/schools/:schoolId/calendar/token (JWT-protected): Gets current token info.
  </behavior>
  <action>
1. Create CalendarTokenDto (apps/api/src/modules/calendar/dto/calendar-token.dto.ts):
   ```typescript
   export class CalendarTokenResponseDto {
     @ApiProperty() id!: string;
     @ApiProperty() token!: string;
     @ApiProperty() calendarUrl!: string;
     @ApiProperty() createdAt!: string;
   }
   ```

2. Create CalendarService (apps/api/src/modules/calendar/calendar.service.ts):
   - Constructor: inject PrismaService. Import ical from 'ical-generator'.
   - generateToken(userId, schoolId):
     ```typescript
     const token = crypto.randomUUID();
     const record = await this.prisma.calendarToken.create({
       data: { userId, schoolId, token },
     });
     return { ...record, calendarUrl: `/api/v1/calendar/${token}.ics` };
     ```
   - findByToken(token): prisma.calendarToken.findUnique({ where: { token } }).
   - findTokenForUser(userId, schoolId): prisma.calendarToken.findFirst({ where: { userId, schoolId } }).
   - revokeAndRegenerate(userId, schoolId):
     Delete existing: prisma.calendarToken.deleteMany({ where: { userId, schoolId } }).
     Create new: return this.generateToken(userId, schoolId).
   - generateIcs(userId, schoolId):
     Create ical calendar: ical({ name: 'SchoolFlow Kalender', timezone: 'Europe/Vienna', prodId: { company: 'SchoolFlow', product: 'Kalender', language: 'DE' }, method: ICalCalendarMethod.PUBLISH }).
     Query timetable lessons: prisma.timetableLesson.findMany for user's classes (resolve user -> person -> student -> schoolClass OR teacher -> timetable lessons). For each lesson, create recurring VEVENT (weekly).
     Query homework: prisma.homework.findMany for user's relevant classSubjects. For each, create all-day event on dueDate with summary "HA: {title}" and description "Fach: {subjectName}".
     Query exams: prisma.exam.findMany for user's relevant classes. For each, create all-day event on date with summary "Pruefung: {title}" and description "Fach: {subjectName}, Dauer: {duration || '-'} min".
     Return cal.toString().

3. Create CalendarController (apps/api/src/modules/calendar/calendar.controller.ts):
   - @Controller() with two route sets:
   - @Public() @Get('api/v1/calendar/:token.ics'): Calls calendarService.findByToken(token). If null, throw NotFoundException. Generate ICS via calendarService.generateIcs(). Set reply headers: Content-Type: 'text/calendar; charset=utf-8', Content-Disposition: 'attachment; filename="schoolflow.ics"'. Send ICS string. (D-09: no JWT required, token in URL path per RESEARCH Pattern 3).
   - @Post('api/v1/schools/:schoolId/calendar/token') @CheckPermissions({ action: 'create', subject: 'calendar-token' }): Creates token for @CurrentUser().
   - @Get('api/v1/schools/:schoolId/calendar/token') @CheckPermissions({ action: 'read', subject: 'calendar-token' }): Returns existing token for current user (or null).
   - @Delete('api/v1/schools/:schoolId/calendar/token') @CheckPermissions({ action: 'delete', subject: 'calendar-token' }): Revokes and regenerates.

4. Convert Wave 0 calendar.service.spec.ts stubs to real tests:
   - Mock PrismaService with calendarToken methods.
   - Test generateToken creates UUID token.
   - Test generateIcs produces valid ICS string (contains VCALENDAR, VEVENT).
   - Test generateIcs includes homework events with "HA:" prefix.
   - Test generateIcs includes exam events with "Pruefung:" prefix.
   - Test generateIcs uses Europe/Vienna timezone.
   - Test revokeAndRegenerate deletes old + creates new.
   - Test findByToken returns null for nonexistent token.
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/apps/api && pnpm vitest run src/modules/calendar/__tests__/calendar.service.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep "generateIcs" apps/api/src/modules/calendar/calendar.service.ts` matches
    - `grep "crypto.randomUUID" apps/api/src/modules/calendar/calendar.service.ts` matches
    - `grep "@Public" apps/api/src/modules/calendar/calendar.controller.ts` matches
    - `grep "text/calendar" apps/api/src/modules/calendar/calendar.controller.ts` matches
    - `grep "HA:" apps/api/src/modules/calendar/calendar.service.ts` matches (homework prefix)
    - `grep "Pruefung:" apps/api/src/modules/calendar/calendar.service.ts` matches (exam prefix)
    - Vitest runs calendar.service.spec.ts with 0 failures
  </acceptance_criteria>
  <done>
    CalendarService generates ICS with timetable + homework + exams. CalendarController serves token-authenticated .ics endpoint. Token CRUD (create/read/revoke) protected by JWT. IMPORT-03 backend complete.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SisService + SisApiKeyGuard + SisController + CalendarModule + CASL seeds</name>
  <read_first>
    - apps/api/src/modules/calendar/__tests__/sis.service.spec.ts
    - apps/api/src/modules/auth/permissions/permissions.guard.ts
    - apps/api/prisma/seed.ts
    - apps/api/src/app.module.ts
  </read_first>
  <files>
    apps/api/src/modules/calendar/sis.service.ts,
    apps/api/src/modules/calendar/sis.controller.ts,
    apps/api/src/modules/calendar/guards/sis-api-key.guard.ts,
    apps/api/src/modules/calendar/dto/sis-response.dto.ts,
    apps/api/src/modules/calendar/calendar.module.ts,
    apps/api/src/modules/calendar/__tests__/sis.service.spec.ts,
    apps/api/prisma/seed.ts,
    apps/api/src/app.module.ts
  </files>
  <behavior>
    SisApiKeyGuard:
    - Reads X-Api-Key header from request. If missing, returns false.
    - Queries prisma.sisApiKey.findFirst({ where: { key: apiKey, isActive: true } }).
    - If no match, returns false. If match, sets request.sisSchoolId = key.schoolId and updates lastUsed timestamp.
    - Returns true on valid key.

    SisService:
    - createApiKey(schoolId, name, userId): Creates SisApiKey with crypto.randomUUID() as key.
    - revokeApiKey(id): Sets isActive=false.
    - listApiKeys(schoolId): Returns active keys (key field masked except at creation).
    - getStudents(schoolId): Returns students with person data (name, dateOfBirth not decrypted).
    - getTeachers(schoolId): Returns teachers with person data and qualifications.
    - getClasses(schoolId): Returns classes with student counts.

    SisController:
    - GET /api/v1/sis/students: @UseGuards(SisApiKeyGuard) @Public() -- reads schoolId from request.sisSchoolId.
    - GET /api/v1/sis/teachers: same pattern.
    - GET /api/v1/sis/classes: same pattern.
    - POST /api/v1/schools/:schoolId/sis/api-keys: JWT-protected, admin-only. Creates API key.
    - GET /api/v1/schools/:schoolId/sis/api-keys: JWT-protected, admin-only. Lists keys.
    - DELETE /api/v1/schools/:schoolId/sis/api-keys/:id: JWT-protected, admin-only. Revokes key.
  </behavior>
  <action>
1. Create SisApiKeyGuard (apps/api/src/modules/calendar/guards/sis-api-key.guard.ts):
   - @Injectable() implements CanActivate.
   - Inject PrismaService.
   - canActivate(context: ExecutionContext): Extract request from context.switchToHttp().getRequest(). Read apiKey from request.headers['x-api-key']. If !apiKey return false. Query prisma.sisApiKey.findFirst({ where: { key: apiKey, isActive: true } }). If !key return false. Set request.sisSchoolId = key.schoolId. Update lastUsed: prisma.sisApiKey.update({ where: { id: key.id }, data: { lastUsed: new Date() } }). Return true.

2. Create SIS response DTOs (apps/api/src/modules/calendar/dto/sis-response.dto.ts):
   ```typescript
   export class SisStudentDto {
     @ApiProperty() id!: string;
     @ApiProperty() firstName!: string;
     @ApiProperty() lastName!: string;
     @ApiProperty() className!: string;
   }
   export class SisTeacherDto {
     @ApiProperty() id!: string;
     @ApiProperty() firstName!: string;
     @ApiProperty() lastName!: string;
     @ApiProperty() shortName!: string;
     @ApiProperty() subjects!: string[];
   }
   export class SisClassDto {
     @ApiProperty() id!: string;
     @ApiProperty() name!: string;
     @ApiProperty() level!: number;
     @ApiProperty() studentCount!: number;
   }
   export class SisApiKeyResponseDto {
     @ApiProperty() id!: string;
     @ApiProperty() name!: string;
     @ApiProperty() isActive!: boolean;
     @ApiProperty() lastUsed!: string | null;
     @ApiProperty() createdAt!: string;
     @ApiProperty({ description: 'Only returned at creation time' }) key?: string;
   }
   ```

3. Create SisService (apps/api/src/modules/calendar/sis.service.ts):
   - Inject PrismaService.
   - createApiKey(schoolId, name, userId): crypto.randomUUID() as key. prisma.sisApiKey.create. Return with key visible.
   - revokeApiKey(id): prisma.sisApiKey.update({ where: { id }, data: { isActive: false } }).
   - listApiKeys(schoolId): prisma.sisApiKey.findMany({ where: { schoolId } }). Return without key field (masked).
   - getStudents(schoolId): prisma.student.findMany({ where: { person: { schoolId } }, include: { person: true, schoolClasses: true } }). Map to SisStudentDto with firstName, lastName from person.
   - getTeachers(schoolId): prisma.teacher.findMany({ where: { person: { schoolId } }, include: { person: true, qualifications: { include: { subject: true } } } }). Map to SisTeacherDto.
   - getClasses(schoolId): prisma.schoolClass.findMany({ where: { schoolId }, include: { _count: { select: { students: true } } } }). Map to SisClassDto.

4. Create SisController (apps/api/src/modules/calendar/sis.controller.ts):
   - @ApiTags('sis') @Controller()
   - SIS data endpoints (API key auth):
     - @Public() @UseGuards(SisApiKeyGuard) @Get('api/v1/sis/students'): Extract schoolId from request.sisSchoolId (custom property set by guard). Return sisService.getStudents(schoolId).
     - @Public() @UseGuards(SisApiKeyGuard) @Get('api/v1/sis/teachers'): same.
     - @Public() @UseGuards(SisApiKeyGuard) @Get('api/v1/sis/classes'): same.
   - API key management endpoints (JWT auth):
     - @Post('api/v1/schools/:schoolId/sis/api-keys') @CheckPermissions({ action: 'manage', subject: 'sis-api-key' }): Create key.
     - @Get('api/v1/schools/:schoolId/sis/api-keys') @CheckPermissions({ action: 'manage', subject: 'sis-api-key' }): List keys.
     - @Delete('api/v1/schools/:schoolId/sis/api-keys/:id') @CheckPermissions({ action: 'manage', subject: 'sis-api-key' }): Revoke key.

5. Create CalendarModule (apps/api/src/modules/calendar/calendar.module.ts):
   - @Module with imports: [HomeworkModule] (for data access in CalendarService).
   - Providers: [CalendarService, SisService, SisApiKeyGuard].
   - Controllers: [CalendarController, SisController].
   - Exports: [CalendarService, SisService].

6. Register CalendarModule in AppModule imports.

7. Add CASL permission seeds to apps/api/prisma/seed.ts for Phase 8 entities:
   - Admin: manage:all already covers everything (no new entries needed for admin).
   - Schulleitung: add { action: 'manage', subject: 'homework' }, { action: 'manage', subject: 'exam' }, { action: 'read', subject: 'import' }.
   - Lehrer: add { action: 'create', subject: 'homework' }, { action: 'read', subject: 'homework' }, { action: 'update', subject: 'homework' }, { action: 'delete', subject: 'homework' }, same pattern for 'exam'. Add { action: 'create', subject: 'calendar-token' }, { action: 'read', subject: 'calendar-token' }, { action: 'delete', subject: 'calendar-token' }.
   - Eltern: add { action: 'read', subject: 'homework' }, { action: 'read', subject: 'exam' }, { action: 'create', subject: 'calendar-token' }, { action: 'read', subject: 'calendar-token' }, { action: 'delete', subject: 'calendar-token' }.
   - Schueler: add { action: 'read', subject: 'homework' }, { action: 'read', subject: 'exam' }, { action: 'create', subject: 'calendar-token' }, { action: 'read', subject: 'calendar-token' }, { action: 'delete', subject: 'calendar-token' }.

8. Convert Wave 0 sis.service.spec.ts stubs to real tests:
   - Mock PrismaService with sisApiKey, student, teacher, schoolClass methods.
   - Test createApiKey produces UUID key.
   - Test SisApiKeyGuard returns false without header.
   - Test SisApiKeyGuard returns false with inactive key.
   - Test SisApiKeyGuard returns true and sets request.sisSchoolId.
   - Test getStudents/getTeachers/getClasses return mapped DTOs.
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/apps/api && pnpm nest build 2>&1 | tail -5 && pnpm vitest run src/modules/calendar/__tests__/ 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep "CalendarModule" apps/api/src/app.module.ts` matches
    - `grep "SisApiKeyGuard" apps/api/src/modules/calendar/guards/sis-api-key.guard.ts` matches
    - `grep "x-api-key" apps/api/src/modules/calendar/guards/sis-api-key.guard.ts` matches
    - `grep "sisSchoolId" apps/api/src/modules/calendar/guards/sis-api-key.guard.ts` matches
    - `grep "homework" apps/api/prisma/seed.ts` matches (permission seeds)
    - `grep "calendar-token" apps/api/prisma/seed.ts` matches (permission seeds)
    - `pnpm nest build` exits 0
    - Vitest runs sis.service.spec.ts with 0 failures
  </acceptance_criteria>
  <done>
    SIS API delivers read-only student/teacher/class data via API key auth. Calendar token CRUD + ICS generation complete. CASL seeds cover all Phase 8 subjects (homework, exam, import, calendar-token, sis-api-key). IMPORT-03 and IMPORT-04 backend requirements met.
  </done>
</task>

</tasks>

<verification>
- `cd apps/api && pnpm nest build` exits 0
- `cd apps/api && pnpm test -- --run` passes all calendar and sis specs
- `curl -H "X-Api-Key: test" http://localhost:3000/api/v1/sis/students` returns 401 (invalid key) when running
- iCal endpoint returns Content-Type: text/calendar
</verification>

<success_criteria>
Personal iCal subscription URL works with token auth, includes timetable + homework + exams. SIS read-only API protected by API key. CASL permissions seeded for all Phase 8 entities. All backend requirements complete.
</success_criteria>

<output>
After completion, create `.planning/phases/08-homework-exams-data-import/08-04-SUMMARY.md`
</output>
