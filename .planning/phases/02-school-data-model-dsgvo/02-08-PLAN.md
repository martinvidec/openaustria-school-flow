---
phase: 02-school-data-model-dsgvo
plan: 08
type: execute
wave: 1
depends_on: ["02-07"]
files_modified:
  - apps/api/src/modules/dsgvo/dsgvo.module.ts
  - apps/api/src/modules/dsgvo/export/data-export.service.ts
  - apps/api/src/modules/dsgvo/deletion/data-deletion.service.ts
autonomous: true
requirements: [FOUND-02, FOUND-03, FOUND-04, FOUND-05, DSGVO-01, DSGVO-02, DSGVO-03, DSGVO-04, DSGVO-05, DSGVO-06]
gap_closure: true

must_haves:
  truths:
    - "Production TypeScript build (nest build) exits 0 with zero type errors"
    - "BullMQ retention cron job uses 'pattern' property (not 'cron') per BullMQ v5 API"
    - "Prisma JSON field assignments use explicit InputJsonValue casts where TSC requires them"
  artifacts:
    - path: "apps/api/src/modules/dsgvo/dsgvo.module.ts"
      provides: "BullMQ v5-compatible repeat option"
      contains: "pattern: '0 2 * * *'"
    - path: "apps/api/src/modules/dsgvo/export/data-export.service.ts"
      provides: "Type-safe Prisma JSON field assignment"
      contains: "as unknown as Prisma.InputJsonValue"
    - path: "apps/api/src/modules/dsgvo/deletion/data-deletion.service.ts"
      provides: "Type-safe audit metadata update"
      contains: "as Prisma.InputJsonValue"
  key_links:
    - from: "dsgvo.module.ts"
      to: "BullMQ RepeatOptions"
      via: "repeat.pattern property"
      pattern: "pattern:\\s*'0 2 \\* \\* \\*'"
    - from: "data-export.service.ts"
      to: "prisma.dsgvoJob.update resultData"
      via: "InputJsonValue cast"
      pattern: "as unknown as Prisma\\.InputJsonValue"
    - from: "data-deletion.service.ts"
      to: "prisma.auditEntry.update metadata"
      via: "InputJsonValue cast"
      pattern: "as Prisma\\.InputJsonValue"
---

<objective>
Fix 3 TypeScript build errors in DSGVO module files that prevent production `nest build` (TSC) from completing successfully.

Purpose: Phase 02 verification found that while all 136 tests pass via Vitest/SWC, the production TypeScript compiler exits with 3 type errors. A Docker production build would fail. This gap closure fixes the errors with ~5 lines of code changes.

Output: Clean `nest build` exit 0 with all DSGVO module files compiling without type errors.
</objective>

<execution_context>
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-school-data-model-dsgvo/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix BullMQ v5 repeat option and Prisma InputJsonValue type casts</name>
  <files>
    apps/api/src/modules/dsgvo/dsgvo.module.ts,
    apps/api/src/modules/dsgvo/export/data-export.service.ts,
    apps/api/src/modules/dsgvo/deletion/data-deletion.service.ts
  </files>
  <read_first>
    apps/api/src/modules/dsgvo/dsgvo.module.ts,
    apps/api/src/modules/dsgvo/export/data-export.service.ts,
    apps/api/src/modules/dsgvo/deletion/data-deletion.service.ts,
    .planning/phases/02-school-data-model-dsgvo/02-VERIFICATION.md
  </read_first>
  <action>
    Three targeted fixes. Each is a single-line change:

    **Fix 1: dsgvo.module.ts line 50** -- BullMQ v5 renamed `repeat.cron` to `repeat.pattern`.
    Change:
      `repeat: { cron: '0 2 * * *' },`
    To:
      `repeat: { pattern: '0 2 * * *' },`

    **Fix 2: data-export.service.ts line 204** -- The object `{ jsonExport, pdfBase64 }` assigned to Prisma's `resultData` (a `JsonValue` field) is typed as `PersonExportData` which TSC cannot narrow to `Prisma.InputJsonValue`. Add Prisma import and cast.
    At the top of the file, add import:
      `import { Prisma } from '../../../config/database/generated/client.js';`
    Change line 204 from:
      `resultData: { jsonExport, pdfBase64 },`
    To:
      `resultData: { jsonExport, pdfBase64 } as unknown as Prisma.InputJsonValue,`

    **Fix 3: data-deletion.service.ts line 130** -- The `sanitized` variable returned from `sanitizeAuditMetadata` has type `unknown`, which TSC rejects for Prisma's `metadata` field (expects `InputJsonValue`). Add Prisma import and cast.
    At the top of the file, add import:
      `import { Prisma } from '../../../config/database/generated/client.js';`
    Change line 130 from:
      `data: { metadata: sanitized },`
    To:
      `data: { metadata: sanitized as Prisma.InputJsonValue },`
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/apps/api && npx nest build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dsgvo.module.ts contains `pattern: '0 2 * * *'` and does NOT contain `cron: '0 2 * * *'`
    - data-export.service.ts contains `as unknown as Prisma.InputJsonValue` on the resultData line
    - data-export.service.ts contains `import { Prisma } from '../../../config/database/generated/client.js'`
    - data-deletion.service.ts contains `as Prisma.InputJsonValue` on the metadata line
    - data-deletion.service.ts contains `import { Prisma } from '../../../config/database/generated/client.js'`
    - `cd apps/api && npx nest build` exits 0 with zero type errors
    - `pnpm --filter @schoolflow/api test -- --run` still passes all 136 tests with 0 failures
  </acceptance_criteria>
  <done>Production TypeScript build (`nest build`) exits 0 with zero errors. All 136 existing tests continue to pass. The three DSGVO module type errors from VERIFICATION.md are resolved.</done>
</task>

</tasks>

<verification>
Run both verification commands in sequence:
1. `cd apps/api && npx nest build` -- must exit 0
2. `pnpm --filter @schoolflow/api test -- --run` -- must show 136/136 pass, 0 failures

Both must succeed for the gap to be considered closed.
</verification>

<success_criteria>
- `nest build` (TSC production build) exits 0 with no type errors
- All 136 existing tests pass unchanged
- VERIFICATION.md truth #10 ("Production TypeScript build compiles with zero errors") is now satisfiable
</success_criteria>

<output>
After completion, create `.planning/phases/02-school-data-model-dsgvo/02-08-SUMMARY.md`
</output>
