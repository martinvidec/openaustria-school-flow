---
phase: 02-school-data-model-dsgvo
plan: 07
type: execute
wave: 4
depends_on: ["02-02", "02-03", "02-04", "02-05", "02-06"]
files_modified:
  - apps/api/src/modules/auth/casl/casl-ability.factory.ts
  - apps/api/src/modules/auth/casl/casl-ability.factory.spec.ts
  - apps/api/src/modules/audit/audit.interceptor.ts
  - apps/api/prisma/seed.ts
  - apps/api/src/app.module.ts
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - DSGVO-01
  - DSGVO-02
  - DSGVO-03
  - DSGVO-05

must_haves:
  truths:
    - "CASL ability factory defines permissions for all new subjects (teacher, student, class, subject, consent, retention, export, dsfa, person)"
    - "Seed data creates default RBAC permissions for all 5 roles covering all Phase 2 entities"
    - "Audit interceptor recognizes new sensitive resources (teacher, student, consent, export)"
    - "All modules compile together and the full test suite passes"
    - "Seed data includes sample school with teachers, classes, students, subjects for development"
  artifacts:
    - path: "apps/api/src/modules/auth/casl/casl-ability.factory.ts"
      provides: "Updated ability factory with Phase 2 subjects"
    - path: "apps/api/prisma/seed.ts"
      provides: "Seed data with RBAC permissions + sample school data"
    - path: "apps/api/src/modules/audit/audit.interceptor.ts"
      provides: "Updated sensitive resources list"
  key_links:
    - from: "apps/api/prisma/seed.ts"
      to: "prisma.role + prisma.permission"
      via: "Seed script creates permissions for new subjects"
      pattern: "permission.*create.*teacher|student|class|subject|consent"
    - from: "apps/api/src/modules/auth/casl/casl-ability.factory.ts"
      to: "apps/api/src/modules/teacher/teacher.controller.ts + class.controller.ts + subject.controller.ts + dsgvo controllers"
      via: "@CheckPermissions decorator references these subjects"
      pattern: "action.*subject.*teacher|student|class|subject|consent|retention|export|dsfa"
---

<objective>
Wire all Phase 2 modules together: update CASL permissions for new entities, extend audit interceptor for new sensitive resources, create seed data with RBAC permissions and sample school data, and verify full integration.

Purpose: Plans 02-06 created individual modules. This plan ensures they work together: RBAC permissions allow the right roles to access the right entities, audit logging captures sensitive operations on new entities, and seed data provides a working development environment.

Output: Updated CASL factory, extended audit interceptor, comprehensive seed data, full test suite passing.
</objective>

<execution_context>
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-school-data-model-dsgvo/02-CONTEXT.md

<interfaces>
From apps/api/src/modules/auth/casl/casl-ability.factory.ts:
- Uses AbilityBuilder<PureAbility<[string, string]>>
- Loads role permissions from prisma.permission table
- Loads user overrides from prisma.permissionOverride table
- Subjects are strings: currently 'school', 'timetable', 'classbook', 'grades', 'all'
- New subjects needed: 'teacher', 'student', 'class', 'subject', 'consent', 'retention', 'export', 'dsfa', 'person'

From apps/api/src/modules/audit/audit.interceptor.ts:
- SENSITIVE_RESOURCES array defines which resources trigger read logging
- Current: ['grades', 'student', 'teacher', 'user']
- Need to add: 'consent', 'export', 'person'

From apps/api/prisma/schema.prisma:
- Role model: id, name, displayName, description, permissions Permission[]
- Permission model: id, roleId FK, action, subject, conditions Json?, inverted, reason
- 5 roles: admin, schulleitung, lehrer, eltern, schueler

Phase 2 @CheckPermissions subjects used in controllers:
- teacher.controller.ts: subject: 'teacher'
- student.controller.ts: subject: 'student'
- class.controller.ts: subject: 'class'
- group.controller.ts: subject: 'class' (groups are sub-resource)
- subject.controller.ts: subject: 'subject'
- consent.controller.ts: subject: 'consent'
- retention.controller.ts: subject: 'retention'
- dsfa.controller.ts: subject: 'dsfa'
- data-deletion.controller.ts: subject: 'person'
- data-export.controller.ts: subject: 'export'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CASL permissions and audit interceptor for Phase 2 entities</name>
  <files>
    apps/api/src/modules/auth/casl/casl-ability.factory.ts
    apps/api/src/modules/auth/casl/casl-ability.factory.spec.ts
    apps/api/src/modules/audit/audit.interceptor.ts
  </files>
  <read_first>
    apps/api/src/modules/auth/casl/casl-ability.factory.ts
    apps/api/src/modules/auth/casl/casl-ability.factory.spec.ts
    apps/api/src/modules/audit/audit.interceptor.ts
    apps/api/src/modules/audit/audit.service.ts
  </read_first>
  <action>
    **Update CASL ability factory** at `apps/api/src/modules/auth/casl/casl-ability.factory.ts`:
    - No code changes needed in the factory itself -- it dynamically loads permissions from the database
    - The factory already handles any subject string via `builder.can(perm.action, perm.subject)`
    - The only change: update the spec file to test that Phase 2 subjects work correctly

    **Update CASL spec** at `apps/api/src/modules/auth/casl/casl-ability.factory.spec.ts`:
    - Add test cases for Phase 2 subjects:
      - Test admin can manage all Phase 2 subjects (teacher, student, class, subject, consent, retention, export, dsfa, person)
      - Test lehrer can read teacher/student/class/subject but cannot delete
      - Test lehrer can read consent for own students only (conditions: { teacherSchoolId: '{{ id }}' } -- or simplified: teachers can read)
      - Test eltern can read student (own child only), read consent (own), request export (own)
      - Test schueler can read class, read subject, read own consent

    **Update audit interceptor** at `apps/api/src/modules/audit/audit.interceptor.ts`:
    - Find the SENSITIVE_RESOURCES array (or equivalent pattern that determines which resources trigger read logging)
    - Add new sensitive resources: 'consent', 'export', 'person', 'retention'
    - The resulting array should be: ['grades', 'student', 'teacher', 'user', 'consent', 'export', 'person', 'retention']
    - This ensures that reads on consent records, export data, person data, and retention policies are audit-logged (sensitive DSGVO data)
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && pnpm --filter @schoolflow/api test -- --run -t "CaslAbility"</automated>
  </verify>
  <acceptance_criteria>
    - casl-ability.factory.spec.ts contains test cases for subjects 'teacher', 'student', 'class', 'subject', 'consent', 'retention', 'export', 'dsfa', 'person'
    - casl-ability.factory.spec.ts tests admin can manage all Phase 2 subjects
    - casl-ability.factory.spec.ts tests lehrer permissions on Phase 2 subjects
    - casl-ability.factory.spec.ts tests eltern restricted to own child's data
    - audit.interceptor.ts SENSITIVE_RESOURCES array includes 'consent', 'export', 'person', 'retention'
    - CASL tests pass: exit 0
  </acceptance_criteria>
  <done>CASL permissions tested for all Phase 2 subjects across 5 roles, audit interceptor recognizes new sensitive DSGVO resources</done>
</task>

<task type="auto">
  <name>Task 2: Seed data with RBAC permissions for Phase 2 entities and sample school data</name>
  <files>
    apps/api/prisma/seed.ts
    apps/api/package.json
    apps/api/src/app.module.ts
  </files>
  <read_first>
    apps/api/prisma/seed.ts
    apps/api/prisma/schema.prisma
    apps/api/package.json
    apps/api/src/app.module.ts
    apps/api/src/modules/subject/templates/austrian-stundentafeln.ts
  </read_first>
  <action>
    **Create or update seed.ts** at `apps/api/prisma/seed.ts`:

    The seed script uses PrismaClient directly (not the NestJS app). Import from the generated client path.

    **Section 1: Roles (upsert existing 5 roles)**
    ```typescript
    const roles = [
      { name: 'admin', displayName: 'Administrator', description: 'Full system access' },
      { name: 'schulleitung', displayName: 'Schulleitung', description: 'School management' },
      { name: 'lehrer', displayName: 'Lehrer', description: 'Teacher access' },
      { name: 'eltern', displayName: 'Eltern', description: 'Parent access' },
      { name: 'schueler', displayName: 'Schueler', description: 'Student access' },
    ];
    ```

    **Section 2: Permissions for Phase 2 subjects**

    admin role -- full CRUD on everything:
    - { action: 'manage', subject: 'all' } (already covers everything)

    schulleitung role:
    - teacher: create, read, update, delete
    - student: create, read, update, delete
    - class: create, read, update, delete
    - subject: create, read, update, delete
    - consent: create, read, update
    - retention: create, read, update, delete
    - export: create, read
    - dsfa: create, read, update, delete
    - person: read, delete (for DSGVO deletion)

    lehrer role:
    - teacher: read (own profile + colleagues in same school)
    - student: read, update (students in own classes)
    - class: read (own classes)
    - subject: read
    - consent: read (for own students)
    - export: create (own data export only)

    eltern role:
    - student: read (own children only, conditions: { parentId: '{{ id }}' })
    - class: read (child's class)
    - subject: read
    - consent: create, read, update (own consent records)
    - export: create, read (own data export)

    schueler role:
    - class: read (own class)
    - subject: read
    - consent: read (own consent)
    - export: create, read (own data)

    **Section 3: Sample school data for development**
    Create a sample AHS Unterstufe school:
    - School: "BG/BRG Musterstadt", type AHS_UNTER, with default time grid
    - SchoolYear: 2025/2026, startDate: 2025-09-01, endDate: 2026-07-04, semesterBreak: 2026-02-07
    - 3 Teachers as Person+Teacher:
      - "Max Mustermann" (D, E), 100% employment, WE target 20
      - "Anna Lehrerin" (M, PH, CH), 75% employment, WE target 15, reduction: KLASSENVORSTAND 1.5 WE
      - "Peter Sportlich" (BSP, BU), 100% employment, WE target 20
    - 4 Subjects: Deutsch (D, I), Mathematik (M, II), Englisch (E, I), Bewegung und Sport (BSP, IVa)
    - 2 Classes: "1A" (yearLevel 1) and "1B" (yearLevel 1)
    - 6 Students (3 per class) as Person+Student
    - Apply AHS_UNTER year 1 Stundentafel template subjects to both classes (create ClassSubject entries)
    - 1 Retention policy override: kommunikation = 730 days (2 years instead of default 1)

    **Section 4: Default retention policies**
    Create default RetentionPolicy entries for the sample school:
    - noten: 21900 days (60 years), isDefault=true
    - anwesenheit: 1825 days (5 years), isDefault=true
    - kommunikation: 365 days (1 year), isDefault=true
    - audit_mutation: 1095 days (3 years), isDefault=true
    - audit_sensitive_read: 365 days (1 year), isDefault=true
    - personal_data: 1825 days (5 years), isDefault=true
    - health_data: 365 days (1 year), isDefault=true

    **Update package.json** -- add prisma seed script if not present:
    ```json
    "prisma": {
      "seed": "tsx prisma/seed.ts"
    }
    ```
    Ensure tsx is available (add as devDependency if needed: `pnpm add -D tsx`).

    **Verify AppModule** -- ensure all modules are imported:
    - PrismaModule, AuthModule, AuditModule, HealthModule, SchoolModule (from Phase 1)
    - QueueModule, EncryptionModule (or via DsgvoModule) (from Plan 01)
    - TeacherModule (from Plan 02)
    - StudentModule, ClassModule (from Plan 03)
    - SubjectModule (from Plan 04)
    - DsgvoModule (from Plans 05+06)
    - If any are missing, add them

    **Run seed:**
    ```bash
    cd apps/api && npx prisma db seed
    ```
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && pnpm --filter @schoolflow/api test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - prisma/seed.ts exists and creates 5 roles with upsert
    - prisma/seed.ts creates permissions for subjects: teacher, student, class, subject, consent, retention, export, dsfa, person
    - prisma/seed.ts creates schulleitung permissions for all Phase 2 subjects
    - prisma/seed.ts creates lehrer permissions (read teacher, read/update student, read class, read subject)
    - prisma/seed.ts creates eltern permissions (read student with conditions, consent CRUD)
    - prisma/seed.ts creates sample school "BG/BRG Musterstadt" with type AHS_UNTER
    - prisma/seed.ts creates 3 sample teachers with Person records
    - prisma/seed.ts creates 6 sample students assigned to 2 classes
    - prisma/seed.ts creates 4+ subjects
    - prisma/seed.ts creates default retention policies for 7 categories
    - app.module.ts imports ALL Phase 2 modules (TeacherModule, StudentModule, ClassModule, SubjectModule, DsgvoModule)
    - Full test suite passes: `pnpm --filter @schoolflow/api test -- --run` exits 0
  </acceptance_criteria>
  <done>Complete RBAC permissions seeded for all 5 roles across all Phase 2 entities, sample school data for development, all modules wired in AppModule, full test suite green</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @schoolflow/api test -- --run` exits 0 (full test suite)
2. `npx nest build` compiles with 0 errors
3. Seed script runs without errors: `cd apps/api && npx prisma db seed`
</verification>

<success_criteria>
- CASL permissions cover all Phase 2 subjects for all 5 roles
- Role hierarchy: admin > schulleitung > lehrer > eltern > schueler
- Audit interceptor logs reads on consent, export, person, retention as sensitive
- Seed data provides usable development environment with sample school
- All Phase 2 modules compile and wire together in AppModule
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-school-data-model-dsgvo/02-07-SUMMARY.md`
</output>
