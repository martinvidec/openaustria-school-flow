---
phase: 17.1-ci-stabilization-followup
plan: 04
type: execute
wave: 3
depends_on:
  - 17.1-02
files_modified:
  # Conditional — exact backend file determined by live-stack-repro outcome:
  # - apps/api/prisma/seed.ts (Phase-15.1 seed UUID alignment touched persons; user search joins person.firstName/lastName)
  # - apps/api/src/modules/user-directory/user-directory.service.ts (search query implementation — fallback if seed-only fix insufficient)
  - apps/web/e2e/admin-users-list.spec.ts
  - apps/web/e2e/admin-user-overrides.spec.ts
  - apps/web/e2e/admin-user-permissions.spec.ts
  - apps/web/e2e/admin-user-person-link.spec.ts
  - apps/web/e2e/admin-user-roles.spec.ts
  - apps/web/e2e/admin-user-silent-4xx.spec.ts
  - .planning/phases/17-ci-stabilization/17-TRIAGE.md
autonomous: true
requirements: []
tags:
  - best-effort
  - cluster-B
  - phase-13-regression
  - admin-users-search
  - live-stack-repro

must_haves:
  decisions_honored:
    - "D-01: best-effort tier (may be skipped if 17.1-02 smoke-PR was GREEN without it)"
    - "D-03: Wave 3 parallel-with-17.1-03; depends_on [17.1-02]"
    - "D-04: autonomous: true with [BLOCKING] live-stack-repro task per CONTEXT D-11"
    - "D-11: live-stack repro REQUIRED before code edits (curl against running API)"
    - "D-13: single fixture-fix likely unblocks all 15 tests; check Phase-15.1 seed UUID alignment + person.firstName/lastName join"
    - "D-20: skip-with-reason annotation default; spec-file deletion only if duplicate or feature removed"
  truths:
    - "Live-stack repro of GET /admin/users?search=lehrer-user (or admin-user / schulleitung-user) returns NON-empty data array containing the expected fixture user (or unable-to-reproduce documented in TRIAGE) [honors D-11, D-13]"
    - "All 15 specs across 6 Cluster B files (USER-01-LIST-01..05, USER-02-ROLES-01..04, USER-03-PERM-01..02, USER-04-OVR-01..05, USER-05-LINK-01..03, USER-SILENT-01..02 + the helpers behind the search-fixture lookup) re-run 3/3 PASS on desktop project, OR skip preserved with documented Phase 17.1 reason [honors D-20]"
    - "17-TRIAGE.md rows #cluster-13-* (6 rows) have Resolution column updated (fixed-in-commit OR unable-to-fix-deferred)"
  artifacts:
    - path: ".planning/phases/17-ci-stabilization/17-TRIAGE.md"
      provides: "Updated 6 #cluster-13-* rows with fix evidence or deferral note"
      contains: "live-stack repro"
    - path: "apps/web/e2e/admin-users-list.spec.ts"
      provides: "Either skip annotation removed (if fixed) or skip preserved with new Phase 17.1 reason"
      contains: "describe("
  key_links:
    - from: "apps/web/e2e/helpers/loginAsRole.ts (user-search fixture)"
      to: "GET /api/v1/admin/users?search=..."
      via: "user-directory search endpoint"
      pattern: "GET /admin/users \\(search="
    - from: "user-directory.service.ts search query"
      to: "person.firstName + person.lastName join"
      via: "Prisma where clause"
      pattern: "person:\\s*\\{"
---

<objective>
**BEST-EFFORT** plan (per CONTEXT D-01) — runs ONLY if Plan 17.1-02 Task 3 classified as RED-WITH-CLUSTERS and Cluster B is among the firing clusters. If smoke-PR went GREEN without this plan, this plan is OPTIONAL deferral material.

Fix the Phase 13 admin-user search fixture regression that breaks 15 tests across 6 specs (`admin-users-list`, `-overrides`, `-permissions`, `-person-link`, `-roles`, `-silent-4xx`). All 6 specs share the same root-cause symptom: `Error: GET /admin/users (search=lehrer-user|admin-user|schulleitung-user)` — the search-fixture lookup fails. Phase 13 SUMMARYs do NOT mark these as failing → regression candidate.

Single-root-cause hypothesis (per CONTEXT D-13): Phase-15.1 seed UUID alignment touched persons; user search joins `person.firstName/lastName`. A single fixture fix likely unblocks the entire 15-test cluster.

Per CONTEXT D-04: this plan is `autonomous: true` BUT requires `[BLOCKING] live-stack-repro` BEFORE any code edits. Without confirmed local repro, the executor MUST mark unable-to-reproduce + skip-with-reason in 17-TRIAGE.md — no speculative fixes.

Per CONTEXT D-14: 30-min budget per phase (repro / fix / spec re-run).

Per memory `feedback_admin_requirements_need_ui_evidence.md`: code-only debugging without confirmed live repro WILL produce an "I fixed something else" PR. Live-stack repro is the diagnostic — DO NOT guess.
</objective>

<execution_context>
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-ci-stabilization-followup/17.1-CONTEXT.md
@.planning/phases/17-ci-stabilization/17-TRIAGE.md
@.planning/phases/17-ci-stabilization/deferred-items.md

# Source-of-truth files for the suspected fix sites
@apps/api/src/modules/user-directory/user-directory.controller.ts
@apps/api/src/modules/user-directory/user-directory.service.ts
@apps/api/src/modules/user-directory/dto/user-directory-query.dto.ts
@apps/web/e2e/admin-users-list.spec.ts

<interfaces>
GET /api/v1/admin/users?search=<query>
  Returns: { data: User[], total: number, page: number, pageSize: number } (paged response shape; verify exact contract via apps/api/src/modules/user-directory/user-directory.controller.ts)
  search filter joins: user.person.firstName + user.person.lastName + user.email (verify the exact JOIN columns via grep on user-directory.service.ts)

Test fixtures expected to match search:
  - search=lehrer-user   -> expects ≥1 user with email containing 'lehrer-user' OR person.firstName/lastName matching the German Lehrer-fixture name
  - search=admin-user    -> expects ≥1 user with email containing 'admin-user' OR person matching the Admin-fixture name
  - search=schulleitung-user -> expects ≥1 user with email containing 'schulleitung-user' OR person matching the Schulleitung-fixture name

Phase-15.1 seed UUID realignment (per CONTEXT D-13) touched persons. The hypothesis: either (a) the seed-user/seed-person link is broken (User row exists, Person row exists, but the foreign key was reassigned to wrong UUID), OR (b) the seed-person.firstName/lastName values were modified and no longer contain the search-string substrings the tests expect, OR (c) the user-directory.service.ts search query was modified between Phase 13 and Phase 15.1 to no longer search the joined columns.

Discrimination: the curl response will tell us. If `data: []` for ALL three search queries -> seed/fixture problem. If `data: [...]` but the wrong shape (missing person fields) -> service-query problem.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: [BLOCKING] Live-stack repro of GET /admin/users?search=... — capture response shape (30-min budget)</name>
  <read_first>
    - apps/api/src/modules/user-directory/user-directory.controller.ts (find the GET /admin/users handler; identify the @Get path + search query param + response DTO)
    - apps/api/src/modules/user-directory/user-directory.service.ts (find the findAll/search method; identify which person columns are joined for the search filter — this is the diagnostic anchor for outcome (c) below)
    - apps/api/src/modules/user-directory/dto/user-directory-query.dto.ts (find the search query DTO + any IsOptional decorators that may permit silent-leak — cross-reference with memory `project_useTeachers_tenant_leak.md`)
    - apps/api/prisma/seed.ts (grep for `lehrer-user`, `admin-user`, `schulleitung-user` — locate the seed users + their joined Person rows; document the persons.firstName/lastName/email field values + the user.personId UUID alignment)
    - apps/web/e2e/admin-users-list.spec.ts (line 38 USER-01-LIST-01 — see what the test expects; usually `expect(response.data.length).toBeGreaterThan(0)` after the search query)
    - apps/web/e2e/helpers/loginAsRole.ts (the shared search-fixture helper used by all 6 Cluster B specs; understand what it queries for and what it expects in the response)
    - .planning/phases/17.1-ci-stabilization-followup/17.1-CONTEXT.md (D-11 BLOCKING repro; D-13 single-fixture-fix hypothesis; D-14 30-min budget)
    - User memory `reference_app_startup.md` (full-stack startup command sequence)
  </read_first>
  <files>(none — read-only repro + diagnostic capture)</files>
  <action>
    1. Start full local stack per memory `reference_app_startup.md`. Wait until `curl -s http://localhost:3000/api/v1/health` returns ok.

    2. Authenticate as admin via Keycloak (per `apps/web/e2e/helpers/login.ts`). Capture JWT into `$TOKEN`.

    3. Confirm the seed users exist in DB:
       `pnpm --filter @schoolflow/api exec prisma studio` → manual lookup, OR via psql directly:
       `docker compose exec postgres psql -U schoolflow -d schoolflow_dev -c "SELECT u.id, u.email, p.\"firstName\", p.\"lastName\" FROM \"User\" u LEFT JOIN \"Person\" p ON u.\"personId\" = p.id WHERE u.email LIKE '%-user%' LIMIT 10;"`
       Document the rows returned. Three discriminating outcomes:
       (i) Returns 3+ rows with intact email + person.firstName/lastName -> fixture data is fine; problem is in the search QUERY. Proceed to step 4 to confirm.
       (ii) Returns rows but with NULL person.firstName/lastName -> Phase-15.1 seed UUID realignment broke the user.personId FK or wiped Person fields. Proceed to step 4 to confirm.
       (iii) Returns 0 rows for `email LIKE '%-user%'` -> the seed user fixtures DO NOT EXIST in DB. Major fixture regression. Proceed to step 4 to confirm.

    4. Repro the failing search query for each of the 3 fixture handles:
       ```
       for QUERY in lehrer-user admin-user schulleitung-user; do
         echo "=== search=$QUERY ===";
         curl -s "http://localhost:3000/api/v1/admin/users?search=$QUERY" -H "Authorization: Bearer $TOKEN" | jq '. | { length: (.data | length), firstItem: .data[0] }';
       done
       ```

    5. Classify outcome:
       (a) All 3 queries return `data: []` AND DB step 3 showed seed users exist -> SERVICE-QUERY problem (Path-A in Task 2: fix `user-directory.service.ts` search filter to include the right columns).
       (b) All 3 queries return `data: []` AND DB step 3 showed seed users MISSING or with NULL person fields -> SEED problem (Path-B in Task 2: fix `apps/api/prisma/seed.ts`).
       (c) Queries return data but the response shape is wrong (e.g., missing pagination wrapper, or `firstName` field renamed) -> SHAPE problem (Path-C in Task 2: fix the response DTO mapping in `user-directory.service.ts` or `user-directory.controller.ts`).
       (d) Queries return correct data -> 422-not-reproducing-locally; CI-only failure; mark `unable-to-reproduce` and skip to Task 4.

    6. Update `17-TRIAGE.md` rows `#cluster-13-users-list`, `#cluster-13-overrides`, `#cluster-13-permissions`, `#cluster-13-person-link`, `#cluster-13-roles`, `#cluster-13-silent-4xx` Local-Repro column. Replace `n/a — parallel-worktree environment` with: `live-stack repro on <date>: GET /admin/users?search=lehrer-user returns <code> with shape <shape-summary>; classified as <a/b/c/d>`.

    Time-box: 30 min. If exceeded -> mark `unable-to-reproduce-in-30min` -> skip to Task 4.
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && curl -s "http://localhost:3000/api/v1/admin/users?search=lehrer-user" -H "Authorization: Bearer ${TOKEN:-PLACEHOLDER}" 2>/dev/null | jq '. | { length: (.data | length // 0) }' 2>/dev/null || echo "API or jq unavailable"</automated>
  </verify>
  <acceptance_criteria>
    - Live API responds at `http://localhost:3000/api/v1/health` (returns 200 + ok body).
    - DB-direct query returns evidence of seed-user existence (or absence).
    - All 3 search queries (`lehrer-user`, `admin-user`, `schulleitung-user`) attempted; concrete data lengths captured.
    - One of four outcomes recorded in TRIAGE: (a) service-query problem, (b) seed problem, (c) shape problem, (d) unable-to-reproduce.
    - `grep "live-stack repro" .planning/phases/17-ci-stabilization/17-TRIAGE.md` returns ≥ 6 NEW occurrences (one per Cluster B row).
    - 30-min wallclock budget honored.
  </acceptance_criteria>
  <done>Cluster B repro outcome classified (one of a/b/c/d); 6 TRIAGE rows updated with the live evidence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply targeted fix per Task 1 outcome — seed.ts (Path-B) OR user-directory.service.ts (Path-A/C) (30-min budget)</name>
  <read_first>
    - Task 1 outcome classification (a/b/c/d) — DETERMINES which file is touched
    - Path-A or Path-C: apps/api/src/modules/user-directory/user-directory.service.ts (find findAll/search method body; identify the where clause that joins person fields)
    - Path-B: apps/api/prisma/seed.ts (find the seed-user creation around lehrer-user/admin-user/schulleitung-user)
    - User memory `project_useTeachers_tenant_leak.md` (related pattern — `where: { x: undefined }` = no Prisma filter; check if a similar pattern hides here)
    - User memory `feedback_restart_api_after_migration.md` (restart API after seed changes)
    - CLAUDE.md "Database migrations — hard rule": SCHEMA changes require migration files. SEED.TS DATA changes do NOT modify schema. SERVICE/CONTROLLER code changes do NOT modify schema.
  </read_first>
  <files>
    # Exactly ONE of (determined by Task 1 outcome):
    # apps/api/prisma/seed.ts                                    (Path-B)
    # apps/api/src/modules/user-directory/user-directory.service.ts (Path-A or Path-C)
  </files>
  <action>
    GUARD: Only run if Task 1 outcome was (a), (b), or (c). For (d) unable-to-reproduce -> SKIP and go to Task 4.

    **Path-A (service-query problem -> fix user-directory.service.ts):**
    1. Find the search filter Prisma query. Diagnose what it currently joins (likely `email` only after the regression vs `email + person.firstName + person.lastName` before).
    2. Restore the join across `email + person.firstName + person.lastName + person.middleName` (or whatever Phase 13 had per its SUMMARY).
    3. Use Prisma `OR` filter pattern: `where: { OR: [{ email: { contains: search } }, { person: { firstName: { contains: search } } }, { person: { lastName: { contains: search } } }] }`.
    4. Restart API; re-run the curl from Task 1 step 4.

    **Path-B (seed problem -> fix seed.ts):**
    1. Find the lehrer-user/admin-user/schulleitung-user seed entities.
    2. Restore the broken FK (user.personId -> existing person.id) OR restore the wiped person.firstName/lastName values.
    3. Re-seed: `cd apps/api && pnpm prisma db seed`. Restart API per memory.
    4. Re-run the curl from Task 1 step 4.

    **Path-C (shape problem -> fix DTO mapping in user-directory.service.ts or .controller.ts):**
    1. Find the response shape mismatch (e.g., missing pagination wrapper, renamed field).
    2. Restore the shape per Phase-13 SUMMARY (`{ data, total, page, pageSize }`).
    3. Re-run the curl. Confirm shape matches what specs expect.
    4. Restart API.

    Confirm fix with curl: `curl -s "http://localhost:3000/api/v1/admin/users?search=lehrer-user" -H "Authorization: Bearer $TOKEN" | jq '.data | length'` returns ≥ 1.

    Commit (atomic, single file):
    `git add <single-file>`
    `git commit -m "fix(17.1-cluster-B): resolve admin-users search returning empty — <Path-A: restored person field joins | Path-B: realigned seed user.personId FK | Path-C: restored {data,total,page,pageSize} response shape>"` (HEREDOC body referencing Cluster B + the path taken + the curl-verified result)

    Time-box: 30 min. If exceeded -> revert via `git checkout -- <file>`, mark `unable-to-fix-in-30-min` in TRIAGE, proceed to Task 4.

    PROHIBITED: schema.prisma modifications. If diagnosis points there, abort + escalate.
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && curl -s "http://localhost:3000/api/v1/admin/users?search=lehrer-user" -H "Authorization: Bearer ${TOKEN:-PLACEHOLDER}" 2>/dev/null | jq '.data | length // 0' 2>/dev/null && git diff HEAD~1 HEAD apps/api/prisma/schema.prisma 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - GUARD-CONDITIONAL: Task 1 outcome was (a), (b), or (c). For (d), this task correctly SKIPPED.
    - If executed: `git diff --stat HEAD~1 HEAD` shows EXACTLY one file modified.
    - If executed: `git log --oneline -1` shows commit subject starting with `fix(17.1-cluster-B):`.
    - If executed: re-running the curl returns `data: [...]` with length ≥ 1 for `search=lehrer-user`.
    - If executed: ZERO modifications to `apps/api/prisma/schema.prisma` — verifiable via verify command's `wc -l` on schema diff returning 0.
    - 30-min time-box honored.
  </acceptance_criteria>
  <done>Single atomic fix commit landed; curl returns non-empty data; OR task correctly skipped with documented reason in TRIAGE.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove skip annotations + verify all 15 Cluster B tests PASS 3/3 + update TRIAGE (30-min budget)</name>
  <read_first>
    - Task 2 outcome (commit landed vs skipped)
    - All 6 Cluster B spec files (find the describe-block-level `test.skip(true, 'Phase 17 deferred: ...')` annotation in each)
    - .planning/phases/17-ci-stabilization/17-TRIAGE.md (find #cluster-13-* rows; update Resolution column)
  </read_first>
  <files>
    apps/web/e2e/admin-users-list.spec.ts
    apps/web/e2e/admin-user-overrides.spec.ts
    apps/web/e2e/admin-user-permissions.spec.ts
    apps/web/e2e/admin-user-person-link.spec.ts
    apps/web/e2e/admin-user-roles.spec.ts
    apps/web/e2e/admin-user-silent-4xx.spec.ts
    .planning/phases/17-ci-stabilization/17-TRIAGE.md
  </files>
  <action>
    GUARD: Only run if Task 2 produced a successful fix commit (curl returns non-empty data). If Task 2 skipped -> SKIP and go to Task 4.

    1. Remove the `test.skip(true, 'Phase 17 deferred: ...')` annotation from each of the 6 spec files. Locate via `grep -n "Phase 17 deferred" apps/web/e2e/admin-user*.spec.ts`. Each file has ONE describe-block-level test.skip; remove that single line (the describe block stays).

    2. Re-run all 15 Cluster B tests with `--repeat-each=3`:
       ```
       pnpm --filter @schoolflow/web exec playwright test \
         admin-users-list.spec.ts \
         admin-user-overrides.spec.ts \
         admin-user-permissions.spec.ts \
         admin-user-person-link.spec.ts \
         admin-user-roles.spec.ts \
         admin-user-silent-4xx.spec.ts \
         --project=desktop --repeat-each=3
       ```
       Expected: 45/45 PASS (15 tests × 3 repeats). If subset fails -> Task 2 fix only PARTIALLY closed Cluster B; revert skip-annotation removal for the failing-spec subset; mark TRIAGE `partial-fix — X of 15 tests still failing`; proceed to Task 4 for the failing subset.

    3. Update `17-TRIAGE.md` 6 rows for Cluster B. Resolution column for each: "Fixed in commit `<sha-from-Task-2>`. Live-stack repro confirmed non-empty data; spec re-run 3/3 PASS on `<date>`."

    4. Commit (second atomic commit of this plan):
       `git add apps/web/e2e/admin-user*.spec.ts apps/web/e2e/admin-users-list.spec.ts .planning/phases/17-ci-stabilization/17-TRIAGE.md`
       `git commit -m "test(17.1-cluster-B): re-enable 15 specs after Cluster B regression fix; update TRIAGE"` (HEREDOC body listing the 6 spec files + 15 test IDs + 45/45 local validation)

    Time-box: 30 min. If `--repeat-each=3` blows the budget -> revert removals, mark `partial-fix-needs-investigation`, proceed to Task 4.
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && pnpm --filter @schoolflow/web exec playwright test admin-users-list.spec.ts admin-user-overrides.spec.ts admin-user-permissions.spec.ts admin-user-person-link.spec.ts admin-user-roles.spec.ts admin-user-silent-4xx.spec.ts --project=desktop --repeat-each=3 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - GUARD-CONDITIONAL: Task 2 fix commit landed. If skipped, this task correctly SKIPPED.
    - If executed: `grep -c "Phase 17 deferred" apps/web/e2e/admin-users-list.spec.ts apps/web/e2e/admin-user-overrides.spec.ts apps/web/e2e/admin-user-permissions.spec.ts apps/web/e2e/admin-user-person-link.spec.ts apps/web/e2e/admin-user-roles.spec.ts apps/web/e2e/admin-user-silent-4xx.spec.ts | grep -c ":0$"` returns 6 (zero matches per file = annotation removed).
    - If executed: Playwright output shows `45 passed` (15 tests × 3 repeats) AND `0 failed`.
    - If executed: `grep -c "Fixed in commit" .planning/phases/17-ci-stabilization/17-TRIAGE.md` returns ≥ 6 NEW occurrences in Cluster B row block.
    - If executed: `git log --oneline -1` shows commit `test(17.1-cluster-B): re-enable 15 specs after Cluster B regression fix`.
  </acceptance_criteria>
  <done>15 Cluster B tests pass 3/3; TRIAGE updated; or task correctly skipped/reverted with documented reason.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Skip-with-reason preservation if any task skipped — update TRIAGE Resolution + spec annotations with Phase 17.1 deferral note</name>
  <read_first>
    - Task 1, 2, 3 outcomes
    - .planning/phases/17-ci-stabilization/17-TRIAGE.md (Cluster B rows that did not get a fix-evidence update in Task 3)
    - Existing skip annotation pattern in spec files (`// Phase 17 deferred: <reason>` -> update to `Phase 17.1 deferred:` per CONTEXT D-14 + D-20)
  </read_first>
  <files>
    # Conditional — touched only if 17.1 deferral is needed:
    # apps/web/e2e/admin-user*.spec.ts (skip annotations updated if not removed in Task 3)
    .planning/phases/17-ci-stabilization/17-TRIAGE.md
  </files>
  <action>
    GUARD: Only run if Task 1 outcome was (d), OR Task 2 was skipped/reverted, OR Task 3 was skipped/reverted. If Tasks 1-3 all succeeded with a clean fix -> SKIP this task.

    1. For each Cluster B spec where the skip annotation REMAINS, UPDATE the existing annotation in the spec file from `// Phase 17 deferred: <reason>` to `// Phase 17.1 deferred: <reason> — see 17-TRIAGE.md row #cluster-13-<sub-id>` per CONTEXT D-14 pattern. Per D-20: do NOT delete; preserve as regression detector.

    2. Update `17-TRIAGE.md` rows for Cluster B (6 rows). Resolution column gets a NEW Phase 17.1 entry per row, e.g.:
       - `#cluster-13-users-list`: Resolution += " | Phase 17.1 attempt: <unable-to-reproduce | fix-exceeded-30min | partial-fix>. Queue for Phase 17.2 backlog."
       - 5 sibling rows: same pattern.

    3. Per CONTEXT D-02: best-effort cluster fixes that miss the smoke-PR window are NOT a phase-failure trigger. Document the deferral cleanly.

    4. Commit:
       `git add apps/web/e2e/admin-user*.spec.ts apps/web/e2e/admin-users-list.spec.ts .planning/phases/17-ci-stabilization/17-TRIAGE.md`
       `git commit -m "docs(17.1-cluster-B): preserve skip-with-reason; queue for Phase 17.2 backlog"` (HEREDOC body documenting WHICH outcome triggered deferral + WHAT next investigation step would be)
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && grep -c "Phase 17.1 deferred\|Phase 17.1 attempt" .planning/phases/17-ci-stabilization/17-TRIAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - GUARD-CONDITIONAL: Only runs if Tasks 1-3 did not produce a clean fix. If they did, this task correctly SKIPPED.
    - If executed: `grep -c "Phase 17.1 deferred\|Phase 17.1 attempt" .planning/phases/17-ci-stabilization/17-TRIAGE.md` returns ≥ 1 NEW occurrence (any of the 6 Cluster B rows).
    - If executed: each affected spec file has annotation updated from `Phase 17 deferred` to `Phase 17.1 deferred`.
    - If executed: zero spec files DELETED (per D-20).
    - If executed: commit subject `docs(17.1-cluster-B): preserve skip-with-reason; queue for Phase 17.2 backlog`.
  </acceptance_criteria>
  <done>Deferral documented in TRIAGE + spec annotations; specs preserved; user has clear hand-off for 17.2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test E2E helper -> live API | curl repro crosses helper code -> HTTP -> API DTO validation -> Prisma -> DB |
| user-directory.service.ts search filter -> Prisma WHERE clause | Search query is a tenant-scoped operation; misalignment may cause data leak (per memory `project_useTeachers_tenant_leak.md`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-14 | Tampering | apps/api/prisma/seed.ts (Path-B) | mitigate | Seed data is dev-only. Changes go through PR review. ASVS L1 N/A for test fixtures. |
| T-17.1-15 | Information Disclosure | apps/api/src/modules/user-directory/user-directory.service.ts (Path-A search query) | mitigate | When restoring `person.firstName/lastName` joins, the executor MUST verify the schoolId tenant scope is preserved. Per memory `project_useTeachers_tenant_leak.md`: `where: { x: undefined }` = no Prisma filter = silent cross-tenant leak. Add an explicit assertion: the new search filter MUST AND-combine with `schoolId: <current-school>` (or equivalent tenant scope). Failure to do so is a security regression that this task body MUST flag for revert. |
| T-17.1-16 | Tampering | apps/api/prisma/schema.prisma | mitigate (forbidden) | This plan PROHIBITS schema modifications per CLAUDE.md hard rule. Acceptance criterion explicitly checks `git diff schema.prisma` returns zero lines. |
| T-17.1-17 | Information Disclosure | curl JWT in command-line history | accept | Short-lived (15-min) admin token. Local-only stack. Acceptable risk; do NOT paste full JWT into TRIAGE.md (truncate). |
| T-17.1-18 | Repudiation | Cluster B regression provenance | mitigate | Per CONTEXT D-13, the regression is between Phase 13 final and PR #1 baseline (Phase 15.1 seed UUID realignment is the prime suspect). Task 1 step 6 MUST document which Phase introduced the regression — if diagnostic evidence indicates a Phase OTHER than 15.1, the developer note in TRIAGE must call this out (helps future audits). |
</threat_model>

<verification>
After Tasks 1-4 complete (with appropriate skips per guards):

1. `grep -c "live-stack repro" .planning/phases/17-ci-stabilization/17-TRIAGE.md` returns ≥ 6 NEW occurrences (Task 1 evidence captured for each Cluster B row).
2. If Tasks 2+3 executed: `pnpm --filter @schoolflow/web exec playwright test admin-users-list.spec.ts admin-user-{overrides,permissions,person-link,roles,silent-4xx}.spec.ts --project=desktop --repeat-each=3` shows 45 passed + 0 failed.
3. If Task 4 executed: `grep -c "Phase 17.1 deferred\|Phase 17.1 attempt" .planning/phases/17-ci-stabilization/17-TRIAGE.md` returns ≥ 1 NEW occurrence.
4. ZERO modifications to `apps/api/prisma/schema.prisma` across the entire plan: `git diff <plan-base-sha> HEAD apps/api/prisma/schema.prisma | wc -l` returns 0.
5. If Path-A taken: confirm tenant-scope is preserved in the restored search filter — `grep -A5 "search:" apps/api/src/modules/user-directory/user-directory.service.ts | grep -E "(schoolId|tenantId|currentSchool)"` returns ≥ 1 (no silent cross-tenant leak per memory `project_useTeachers_tenant_leak.md`).
</verification>

<success_criteria>
- [ ] Task 1 — live-stack repro attempted within 30-min budget; outcome (a/b/c/d) documented in 6 Cluster B TRIAGE rows
- [ ] Task 2 — IF Task 1 was (a/b/c): targeted single-file fix landed within 30-min budget; OR correctly skipped
- [ ] Task 3 — IF Task 2 fix landed: 15 specs re-run 3/3 PASS, skip annotations removed, TRIAGE Resolution updated
- [ ] Task 4 — IF Tasks 1-3 did not produce a clean fix: skip-with-reason preserved (NOT deleted per D-20), TRIAGE updated, queued for 17.2
- [ ] ZERO `apps/api/prisma/schema.prisma` modifications across the entire plan (CLAUDE.md hard rule)
- [ ] If Path-A taken: tenant-scope verified preserved in the restored search filter (no silent cross-tenant leak per memory `project_useTeachers_tenant_leak.md`)
- [ ] No `--admin` overrides used; no force-pushes to main; no branch switching
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-ci-stabilization-followup/17.1-04-SUMMARY.md` with:
- Cluster B repro outcome (curl evidence + DB-direct evidence)
- Fix path taken (Path-A service-query / Path-B seed / Path-C shape / unable-to-fix-deferred)
- Spec re-run result (45/45 pass OR partial OR all-deferred)
- Tenant-scope verification (Path-A only) — confirms restored search filter does NOT introduce silent cross-tenant leak
- Commits landed (subjects + SHAs) — typically 0-3 commits depending on outcome path
- Hand-off note: PHASE-17.1-COMPLETE-FOR-CLUSTER-B (if 45/45 pass) OR DEFERRED-TO-17.2-CLUSTER-B (if any unable-to-fix path taken)
</output>
