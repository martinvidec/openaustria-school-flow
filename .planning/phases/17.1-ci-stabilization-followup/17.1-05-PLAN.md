---
phase: 17.1-ci-stabilization-followup
plan: 05
type: execute
wave: 4
depends_on:
  - 17.1-03
  - 17.1-04
files_modified:
  # Cluster C
  - apps/web/e2e/admin-audit-log-detail.spec.ts
  - apps/web/e2e/admin-audit-log-filter.spec.ts
  - apps/web/e2e/global-setup.ts  # conditional — only if Cluster C audit-fixture seeding needed
  # Cluster D
  - apps/web/e2e/admin-import.spec.ts
  # Cluster E
  - apps/web/e2e/admin-timetable-edit-dnd.spec.ts
  # Cluster F
  - apps/web/e2e/silent-4xx.spec.ts
  - apps/web/e2e/screenshots.spec.ts
  # TRIAGE update
  - .planning/phases/17-ci-stabilization/17-TRIAGE.md
autonomous: true
requirements: []
tags:
  - best-effort
  - cluster-C
  - cluster-D
  - cluster-E
  - cluster-F
  - 30-min-budget
  - mop-up

must_haves:
  decisions_honored:
    - "D-01: best-effort tier (may short-circuit if 17.1-02 smoke-PR was GREEN)"
    - "D-03: Wave 4 sequential; depends_on [17.1-03, 17.1-04]"
    - "D-04: autonomous: true with [BLOCKING] live-stack-repro task per CONTEXT D-11 (carries to per-cluster verification)"
    - "D-14: 30-min-fix-or-skip per cluster; goal is CI-stable, not bug-zero"
    - "D-15: Cluster C — audit-log-detail = missing-fixture (likely global-setup.ts seeding); audit-log-filter = .first() qualifier (sub-30-min)"
    - "D-16: Cluster D admin-import = HIGHEST-RISK for budget; explicit executor-judgment gate to skip if Mantine Stepper deep-dive surfaces"
    - "D-17: Clusters E + F = timing-sensitive; try waitForLoadState('networkidle') + expect.poll() first"
    - "D-20: skip-with-reason annotation default; spec-file deletion only if duplicate or feature removed"
  truths:
    - "Each of 4 clusters (C/D/E/F) has been investigated within its 30-min budget; outcome is either FIXED (skip removed + 3/3 spec re-pass) OR DEFERRED (Phase 17.1 deferred annotation + TRIAGE update) [honors D-14, D-20]"
    - "17-TRIAGE.md rows for #cluster-15-audit-detail, #cluster-15-audit-filter, #cluster-10.5-import, #cluster-04-dnd, #cluster-10.2-silent-4xx, #cluster-10.2-screenshot-school05 each have an updated Resolution column reflecting the per-cluster outcome"
    - "Per CONTEXT D-14: Wallclock per cluster ≤ 30 min. The phase exists to MAKE CI GREEN, not to debug Mantine Stepper for 4 hours."
  artifacts:
    - path: ".planning/phases/17-ci-stabilization/17-TRIAGE.md"
      provides: "Updated Resolution column for 6 mop-up cluster rows"
      contains: "Phase 17.1"
  key_links:
    - from: "Per-cluster 30-min budget"
      to: "outcome (FIXED OR DEFERRED)"
      via: "literal timer + skip-with-reason fallback"
      pattern: "Phase 17.1 deferred|Fixed in commit"
---

<objective>
**BEST-EFFORT** plan (per CONTEXT D-01) — runs ONLY if Plan 17.1-02 Task 3 classified as RED-WITH-CLUSTERS and any of Clusters C/D/E/F is among the firing clusters. If smoke-PR went GREEN without this plan, this plan is OPTIONAL deferral material.

**Per plan-checker warning 4 (success-path short-circuit):** If Plan 17.1-02 SUMMARY indicates `Status: GREEN` for the smoke-PR re-run, the executor SHOULD short-circuit Task 1 with a single TRIAGE update note: `Plan 17.1-05 not required — smoke-PR GREEN per 17.1-02-SUMMARY (Truth 1 satisfied without Cluster C/D/E/F fixes; deferred clusters remain skip-annotated as carry-forward).` This avoids burning the 150-min worst-case wallclock budget on plan tasks whose firing condition was not met.

Mop-up of 4 deferred clusters from Phase 17 (per CONTEXT D-14, D-15, D-16, D-17):
- **Cluster C** (2 tests): `admin-audit-log-detail.spec.ts:42` (AUDIT-VIEW-02 missing-fixture per Phase-17 CONTEXT D-08) + `admin-audit-log-filter.spec.ts:104` (selector strict-mode dual-render — `.first()` qualifier likely fix; sub-30-min trivial)
- **Cluster D** (3 tests): `admin-import.spec.ts:71/91/116` (IMPORT-UNTIS-01, IMPORT-CSV-01..02; 1m timeouts on Mantine Stepper or file-upload preview) — HIGHEST RISK for 30-min budget per CONTEXT D-16
- **Cluster E** (1 test): `admin-timetable-edit-dnd.spec.ts:110` (REGRESSION-DND-COLLISION; pointer-event timing in headless Playwright)
- **Cluster F** (2 tests): `silent-4xx.spec.ts:43` (SILENT-4XX-01 toast-render timing) + `screenshots.spec.ts:172` (SCHOOL-05 screenshot capture timing — UAT-only, non-blocking)

Per CONTEXT D-14: each cluster gets a 30-min investigation box. If not fixed in 30 min → skip-with-reason annotation update (`// Phase 17.1 deferred: <reason> — see 17-TRIAGE.md row N`) AND row update in 17-TRIAGE.md. NOT all 4 clusters must be fixed; goal is CI-stable, not bug-zero.

Per CONTEXT D-17: Cluster E + F are timing-sensitive Playwright issues — try `await page.waitForLoadState('networkidle')` + `expect.poll()` patterns first. If 30-min budget exhausted → skip-with-reason.

Per CONTEXT D-15: Cluster C audit-log-detail likely needs an audit-fixture-seeding step in `apps/web/e2e/global-setup.ts`. Cluster C audit-log-filter is a `.first()` qualifier — sub-30-min trivial fix.

Sequential per-cluster execution (depends_on 17.1-03 + 17.1-04 — runs after both, but neither's success is gating; this plan can proceed even if 03/04 deferred).
</objective>

<execution_context>
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-ci-stabilization-followup/17.1-CONTEXT.md
@.planning/phases/17-ci-stabilization/17-TRIAGE.md
@.planning/phases/17-ci-stabilization/deferred-items.md

# 6 spec files in scope
@apps/web/e2e/admin-audit-log-detail.spec.ts
@apps/web/e2e/admin-audit-log-filter.spec.ts
@apps/web/e2e/admin-import.spec.ts
@apps/web/e2e/admin-timetable-edit-dnd.spec.ts
@apps/web/e2e/silent-4xx.spec.ts
@apps/web/e2e/screenshots.spec.ts

# Audit-fixture seeding entry point (Cluster C path)
@apps/web/e2e/global-setup.ts

<interfaces>
30-min-per-cluster budget protocol:
1. Set a literal timer (per CONTEXT specifics: "set a literal timer").
2. Investigate the failure: read the spec, run it locally with `--repeat-each=3`, observe stack trace, hypothesize root cause.
3. Try ONE small fix attempt within budget. If it works (3/3 PASS) → commit + remove skip annotation.
4. If timer expires without 3/3 PASS → revert the attempted fix, UPDATE the existing skip annotation from `// Phase 17 deferred: <reason>` to `// Phase 17.1 deferred: <reason> — see 17-TRIAGE.md row N`, UPDATE TRIAGE Resolution column, commit the deferral.

Per-cluster execution order (sequential within this plan, NOT parallel — 30-min budgets stack):
- Task 1 (Cluster C — quickest, two-pronged: filter trivial + detail fixture-needed)
- Task 2 (Cluster F — toast/screenshot timing; lowest risk)
- Task 3 (Cluster E — DnD timing; medium risk)
- Task 4 (Cluster D — Mantine Stepper; HIGHEST risk per D-16)
- Task 5 (TRIAGE consolidation — single TRIAGE update commit if multiple per-cluster commits accumulated)

Per CONTEXT D-20: skip-with-reason is the DEFAULT — never delete a spec, even if deferred multiple times. Spec preservation is the regression-detector hedge for re-enablement when the deferred fix lands.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Cluster C — admin-audit-log-detail (missing-fixture) + admin-audit-log-filter (selector strict-mode `.first()`) (30-min budget per sub-cluster)</name>
  <read_first>
    - apps/web/e2e/admin-audit-log-detail.spec.ts:42 (AUDIT-VIEW-02 — depends on pre-seeded audit-create rows; per CONTEXT D-15 likely needs `global-setup.ts` audit-fixture seeding)
    - apps/web/e2e/admin-audit-log-filter.spec.ts:104 (AUDIT-VIEW-01 — `getByRole('button', { name: 'Filter zurücksetzen' })` resolves to 2 elements after Phase 17-04 DataList migration cascade; per CONTEXT D-15 sub-30-min trivial fix)
    - apps/web/e2e/global-setup.ts (current entry point; identify where seed-audit-rows could be added; reference Phase-15 audit-log spec for the create-action shape)
    - .planning/phases/17.1-ci-stabilization-followup/17.1-CONTEXT.md (D-14 30-min protocol, D-15 Cluster C path)
  </read_first>
  <files>
    apps/web/e2e/admin-audit-log-filter.spec.ts
    apps/web/e2e/admin-audit-log-detail.spec.ts
    apps/web/e2e/global-setup.ts  # conditional
  </files>
  <action>
    **Sub-cluster C1: admin-audit-log-filter (selector strict-mode) — 30-min budget**

    1. Open `apps/web/e2e/admin-audit-log-filter.spec.ts:104`. Find the `getByRole('button', { name: 'Filter zurücksetzen' })` call.

    2. Apply trivial `.first()` qualifier:
       - Before: `await page.getByRole('button', { name: 'Filter zurücksetzen' }).click();`
       - After:  `await page.getByRole('button', { name: 'Filter zurücksetzen' }).first().click();`

    3. ALTERNATIVELY (preferred if straightforward): use a unique `data-testid` selector. Inspect the rendered DOM to confirm both buttons exist (one desktop, one mobile after DataList migration); add a `data-testid` to ONE of them in the source component, then update the spec to use it. This is the more durable fix — but if the source-component change widens scope beyond 30 min, fall back to `.first()`.

    4. Remove the describe-block-level skip annotation (`test.skip(true, 'Phase 17 deferred: ...')`).

    5. Re-run with `--repeat-each=3`:
       `pnpm --filter @schoolflow/web exec playwright test admin-audit-log-filter.spec.ts --project=desktop --repeat-each=3`
       Expected: 3/3 PASS for AUDIT-VIEW-01.

    6. If PASS: commit + update TRIAGE row `#cluster-15-audit-filter` Resolution column to "Fixed in commit `<sha>` — `.first()` qualifier on Filter zurücksetzen button selector. 3/3 PASS local."

    7. If FAIL within budget: revert (`git checkout -- apps/web/e2e/admin-audit-log-filter.spec.ts`), update annotation to `Phase 17.1 deferred:`, update TRIAGE Resolution to `Phase 17.1 attempt: <observed-failure>. Queue for 17.2.`

    **Sub-cluster C2: admin-audit-log-detail (missing-fixture) — 30-min budget**

    1. Open `apps/web/e2e/admin-audit-log-detail.spec.ts:42`. Find `seedAuditEntryLegacy: seed DB has zero action=create audit rows`.

    2. Per Phase-17.1 CONTEXT D-15 (carries Phase-17 D-08 missing-fixture pattern) + D-20: this is classified as missing-fixture. Per Phase-17 CONTEXT D-08, missing-fixture defaults to skip-with-reason — DO NOT auto-fix unless adding a fixture-seeding step is trivial. **Note on namespace:** Phase-17 D-08 = missing-fixture parking; Phase-17.1 D-08 = MERGE ORDER (different namespace). Always prefix with the phase number when citing carry-forward decisions.

    3. Quick attempt: open `apps/web/e2e/global-setup.ts`. If a clean Phase-15-style fixture-add step is obvious (e.g., a few lines wrapping `prisma.auditLog.create({ ... })` with create-action audit rows), attempt it within budget.

    4. If global-setup.ts modification is trivial: re-run `admin-audit-log-detail.spec.ts --repeat-each=3`. If PASS → commit + update TRIAGE.

    5. If non-trivial (touches multiple files OR exceeds 30 min): UPDATE the existing skip annotation from `Phase 17 deferred` to `Phase 17.1 deferred: missing-fixture — global-setup.ts audit-row seeding deferred to Phase 17.2 backlog (per Phase-17 CONTEXT D-08 missing-fixture parking pattern, NOT Phase-17.1 D-08 which is about merge order)`. Update TRIAGE row `#cluster-15-audit-detail` Resolution column accordingly.

    Combined commit (if any sub-cluster succeeded):
    `git add apps/web/e2e/admin-audit-log-{filter,detail}.spec.ts apps/web/e2e/global-setup.ts .planning/phases/17-ci-stabilization/17-TRIAGE.md`
    `git commit -m "fix(17.1-cluster-C): <sub-cluster outcomes — filter .first() / detail fixture-seeded OR deferred>"` (HEREDOC body documenting BOTH sub-clusters' outcomes)
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && pnpm --filter @schoolflow/web exec playwright test admin-audit-log-filter.spec.ts admin-audit-log-detail.spec.ts --project=desktop --repeat-each=3 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - For C1 (filter): EITHER `grep -c "Phase 17 deferred" apps/web/e2e/admin-audit-log-filter.spec.ts` returns 0 AND playwright shows 3/3 PASS for AUDIT-VIEW-01, OR `grep -c "Phase 17.1 deferred" apps/web/e2e/admin-audit-log-filter.spec.ts` returns ≥ 1 AND TRIAGE row `#cluster-15-audit-filter` Resolution column updated.
    - For C2 (detail): EITHER `grep -c "Phase 17 deferred" apps/web/e2e/admin-audit-log-detail.spec.ts` returns 0 AND playwright shows 3/3 PASS for AUDIT-VIEW-02, OR `grep -c "Phase 17.1 deferred" apps/web/e2e/admin-audit-log-detail.spec.ts` returns ≥ 1 AND TRIAGE row `#cluster-15-audit-detail` Resolution column updated.
    - Per-sub-cluster 30-min budget honored (no single sub-cluster blocks the next one).
  </acceptance_criteria>
  <done>Both sub-clusters of C have a definitive outcome (FIXED or Phase-17.1-DEFERRED); TRIAGE updated for both rows.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cluster F — silent-4xx (toast timing) + screenshots SCHOOL-05 (capture timing) (30-min budget per sub-cluster)</name>
  <read_first>
    - apps/web/e2e/silent-4xx.spec.ts:43 (SILENT-4XX-01 — Stammdaten PUT 4xx red-toast assertion timing out)
    - apps/web/e2e/screenshots.spec.ts:172 (SCHOOL-05 — 1m timeout on screenshot capture; per CONTEXT D-17: UAT-only suite, non-blocking)
    - .planning/phases/17.1-ci-stabilization-followup/17.1-CONTEXT.md (D-17 — try `waitForLoadState('networkidle')` + `expect.poll()` first; if 30-min exhausted → skip-with-reason)
  </read_first>
  <files>
    apps/web/e2e/silent-4xx.spec.ts
    apps/web/e2e/screenshots.spec.ts
  </files>
  <action>
    **Sub-cluster F1: silent-4xx.spec.ts:43 — 30-min budget**

    1. Open the spec at line 43. Identify the toast-render assertion (likely `expect(page.getByText(/error|Fehler|...|toast/)).toBeVisible()`).

    2. Apply timing-hardening per CONTEXT D-17:
       - Add `await page.waitForLoadState('networkidle');` before the toast assertion (waits for the PUT response + any subsequent re-render).
       - OR wrap the assertion in `expect.poll(async () => ..., { timeout: 5000, intervals: [100, 200, 500] })` to retry the visibility check.

    3. Remove describe-block-level skip annotation; re-run `--repeat-each=3`:
       `pnpm --filter @schoolflow/web exec playwright test silent-4xx.spec.ts --project=desktop --repeat-each=3`

    4. If 3/3 PASS: commit + update TRIAGE row `#cluster-10.2-silent-4xx`.

    5. If FAIL within budget: revert, update annotation to `Phase 17.1 deferred:`, update TRIAGE Resolution.

    **Sub-cluster F2: screenshots.spec.ts:172 (SCHOOL-05) — 30-min budget**

    1. Open the spec at line 172. Identify the screenshot capture call (likely `await page.screenshot({ path: '...' })`).

    2. Per CONTEXT D-17: this is UAT-only (non-blocking). The deferred-items.md notes "screenshot specs are non-blocking — UAT-only".

    3. Quick attempt: add `await page.waitForLoadState('networkidle');` before the screenshot call + bump timeout to 90s. Re-run.

    4. If 3/3 PASS within budget: commit + update TRIAGE row `#cluster-10.2-screenshot-school05`.

    5. If FAIL OR if the spec is genuinely UAT-only and not in scope for CI green: prefer SKIP path. Update annotation to `Phase 17.1 deferred: screenshot capture timing — UAT-only suite, non-blocking; queue for 17.2 backlog`. Update TRIAGE Resolution.

    Combined commit:
    `git add apps/web/e2e/silent-4xx.spec.ts apps/web/e2e/screenshots.spec.ts .planning/phases/17-ci-stabilization/17-TRIAGE.md`
    `git commit -m "fix(17.1-cluster-F): <silent-4xx / screenshots SCHOOL-05 outcomes>"` (HEREDOC body documenting BOTH sub-clusters)
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && pnpm --filter @schoolflow/web exec playwright test silent-4xx.spec.ts screenshots.spec.ts --project=desktop --repeat-each=3 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - For F1: EITHER `grep -c "Phase 17 deferred" apps/web/e2e/silent-4xx.spec.ts` returns 0 AND 3/3 PASS for SILENT-4XX-01, OR `grep -c "Phase 17.1 deferred" apps/web/e2e/silent-4xx.spec.ts` returns ≥ 1 AND TRIAGE updated.
    - For F2: EITHER `grep -c "Phase 17 deferred" apps/web/e2e/screenshots.spec.ts` returns 0 AND 3/3 PASS for SCHOOL-05, OR `grep -c "Phase 17.1 deferred" apps/web/e2e/screenshots.spec.ts` returns ≥ 1 AND TRIAGE updated.
    - Per-sub-cluster 30-min budget honored.
  </acceptance_criteria>
  <done>Both Cluster F sub-clusters have definitive outcome; TRIAGE updated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Cluster E — admin-timetable-edit-dnd (REGRESSION-DND-COLLISION pointer-event timing) (30-min budget)</name>
  <read_first>
    - apps/web/e2e/admin-timetable-edit-dnd.spec.ts:110 (REGRESSION-DND-COLLISION — `element(s) not found` on pointer-drag landing assertion)
    - .planning/phases/17.1-ci-stabilization-followup/17.1-CONTEXT.md (D-17 timing-pattern hints)
  </read_first>
  <files>
    apps/web/e2e/admin-timetable-edit-dnd.spec.ts
  </files>
  <action>
    1. Open the spec at line 110. Identify the pointer-drag-landing assertion. Typical pattern: `await source.dragTo(target)` followed by `await expect(landingZone.locator('...')).toBeVisible()`.

    2. Apply timing-hardening per CONTEXT D-17:
       - Try (a) `await page.waitForLoadState('networkidle');` after the drag.
       - Try (b) `expect.poll(async () => { ... }, { timeout: 3000 })` on the landing assertion.
       - Try (c) `await page.mouse.move(targetX, targetY); await page.waitForTimeout(100);` between the drag start and end to give pointer events time to register in headless mode (last-resort timing hack).

    3. Remove describe-block-level skip annotation; re-run `--repeat-each=3`:
       `pnpm --filter @schoolflow/web exec playwright test admin-timetable-edit-dnd.spec.ts --project=desktop --repeat-each=3`

    4. If 3/3 PASS: commit + update TRIAGE row `#cluster-04-dnd`.

    5. If FAIL within budget: revert, update annotation to `Phase 17.1 deferred: DnD pointer-event timing in headless Playwright — needs DnD-helper update or page-stable wait protocol; queue for 17.2 backlog`. Update TRIAGE Resolution.

    Commit:
    `git add apps/web/e2e/admin-timetable-edit-dnd.spec.ts .planning/phases/17-ci-stabilization/17-TRIAGE.md`
    `git commit -m "fix(17.1-cluster-E): <DnD pointer-timing fix OR deferred>"` (HEREDOC body)
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && pnpm --filter @schoolflow/web exec playwright test admin-timetable-edit-dnd.spec.ts --project=desktop --repeat-each=3 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - EITHER `grep -c "Phase 17 deferred" apps/web/e2e/admin-timetable-edit-dnd.spec.ts` returns 0 AND 3/3 PASS for REGRESSION-DND-COLLISION, OR `grep -c "Phase 17.1 deferred" apps/web/e2e/admin-timetable-edit-dnd.spec.ts` returns ≥ 1 AND TRIAGE row `#cluster-04-dnd` Resolution column updated.
    - 30-min wallclock budget honored.
  </acceptance_criteria>
  <done>Cluster E has a definitive outcome (FIXED or Phase-17.1-DEFERRED); TRIAGE updated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Cluster D — admin-import wizard (Mantine Stepper / file-upload preview) (30-min budget — HIGHEST RISK per CONTEXT D-16)</name>
  <read_first>
    - apps/web/e2e/admin-import.spec.ts:71/91/116 (IMPORT-UNTIS-01, IMPORT-CSV-01, IMPORT-CSV-02 — 1m timeouts on `locator.click` for full wizard flows)
    - .planning/phases/17.1-ci-stabilization-followup/17.1-CONTEXT.md (D-16: HIGHEST RISK; if 30 min isn't enough → skip-with-reason and document as "Mantine-Stepper investigation" follow-up for Phase 23; D-17 timing patterns)
    - .planning/phases/17.1-ci-stabilization-followup/17.1-CONTEXT.md Claude's Discretion: "Whether to attempt Plan 17.1-05 Cluster D at all if 30-min budget is the only blocker — defer to executor judgment based on time pressure at run time"
  </read_first>
  <files>
    apps/web/e2e/admin-import.spec.ts
  </files>
  <action>
    1. EXECUTOR JUDGMENT GATE (per CONTEXT Claude's Discretion): If at this point the prior 3 tasks have already consumed >2.5 hours, OR if the smoke-PR is already documented as GREEN-without-D, OR if user signal indicates time pressure → SKIP this task entirely. Document in TRIAGE: `Cluster D investigation declined for time-pressure reasons; queue for Phase 17.2 / Phase 23 Mantine-Stepper investigation.`

    2. If proceeding: open `admin-import.spec.ts` lines 71, 91, 116. Identify the `locator.click()` call timing out at 60s.

    3. Quick diagnostic attempts (per CONTEXT D-17):
       - (a) Add `await page.waitForLoadState('networkidle');` before the click. Re-run ONE test (IMPORT-UNTIS-01) — does it now pass within 60s?
       - (b) Bump per-action timeout to 90s for this spec only: `await locator.click({ timeout: 90_000 });`.
       - (c) Add `expect.poll(async () => locator.isVisible(), { timeout: 30_000 })` before the click — confirms render-ready before interacting.

    4. If ANY attempt reaches 3/3 PASS within budget: apply the same fix to all 3 tests in the spec, remove skip annotation, commit, update TRIAGE row `#cluster-10.5-import`.

    5. If 30 min exhausted (HIGHEST PROBABILITY per D-16): revert all attempts, update annotation to `Phase 17.1 deferred: Mantine Stepper render delay or file-upload preview hang — needs deep investigation; queue for Phase 23 Mantine-Stepper investigation backlog`, update TRIAGE row `#cluster-10.5-import`.

    Commit:
    `git add apps/web/e2e/admin-import.spec.ts .planning/phases/17-ci-stabilization/17-TRIAGE.md`
    `git commit -m "fix(17.1-cluster-D): <wizard-timing fix OR deferred to Phase 23 Mantine-Stepper investigation>"` (HEREDOC body)
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && pnpm --filter @schoolflow/web exec playwright test admin-import.spec.ts --project=desktop --repeat-each=3 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - EITHER `grep -c "Phase 17 deferred" apps/web/e2e/admin-import.spec.ts` returns 0 AND 3/3 PASS for all 3 IMPORT tests, OR `grep -c "Phase 17.1 deferred" apps/web/e2e/admin-import.spec.ts` returns ≥ 1 AND TRIAGE row `#cluster-10.5-import` Resolution column updated.
    - If the executor-judgment gate (step 1) declined the task: TRIAGE Resolution explicitly documents the declination reason.
    - 30-min wallclock budget honored (this is the cluster MOST LIKELY to hit budget — verify the deferral path triggers cleanly).
  </acceptance_criteria>
  <done>Cluster D has a definitive outcome; TRIAGE updated; Phase-23 follow-up note (if deferred) clearly written.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Final TRIAGE consolidation + per-cluster outcome summary commit</name>
  <read_first>
    - Tasks 1-4 outcomes (which clusters fixed vs deferred)
    - .planning/phases/17-ci-stabilization/17-TRIAGE.md (verify ALL 6 rows from Clusters C/D/E/F have updated Resolution columns)
  </read_first>
  <files>.planning/phases/17-ci-stabilization/17-TRIAGE.md</files>
  <action>
    1. Verify every Cluster C/D/E/F row in 17-TRIAGE.md has an updated Resolution column reflecting Phase 17.1 outcome. Six rows:
       - `#cluster-15-audit-detail`
       - `#cluster-15-audit-filter`
       - `#cluster-10.5-import`
       - `#cluster-04-dnd`
       - `#cluster-10.2-silent-4xx`
       - `#cluster-10.2-screenshot-school05`

    2. If any row was NOT updated by Tasks 1-4 (e.g., a task was entirely skipped without TRIAGE write), retroactively add a "Phase 17.1: not investigated within plan budget — queue for 17.2" note for that row.

    3. Add a summary block at the bottom of `## Smoke-PR result` section (or append a new `## Phase 17.1 Mop-Up Summary` section) documenting:
       - Which clusters were FIXED (with commit SHAs)
       - Which clusters were DEFERRED (with reason + 17.2/23 routing)
       - Total wallclock spent on this plan (helps future budget calibration)
       - Any sub-cluster that surfaced new failure modes worth flagging

    4. Final consolidation commit:
       `git add .planning/phases/17-ci-stabilization/17-TRIAGE.md`
       `git commit -m "docs(17.1-mop-up): consolidate Cluster C/D/E/F outcomes — <X fixed / Y deferred>"` (HEREDOC body listing the 6 row outcomes)
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && grep -E "(#cluster-15-audit|#cluster-10.5-import|#cluster-04-dnd|#cluster-10.2-silent-4xx|#cluster-10.2-screenshot)" .planning/phases/17-ci-stabilization/17-TRIAGE.md | grep -cE "(Fixed in commit|Phase 17.1 deferred|Phase 17.1 attempt|Phase 17.1: not investigated)"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 Cluster C/D/E/F rows in TRIAGE have an updated Resolution column with one of: `Fixed in commit`, `Phase 17.1 deferred`, `Phase 17.1 attempt`, or `Phase 17.1: not investigated within plan budget`.
    - Verify command returns ≥ 6.
    - Final consolidation commit subject `docs(17.1-mop-up): consolidate Cluster C/D/E/F outcomes`.
    - Zero spec files DELETED in this plan (per D-20: skip preserves the regression detector).
  </acceptance_criteria>
  <done>TRIAGE document is consistent: every mop-up cluster row has a definitive Phase 17.1 outcome annotation; future readers can trace each test's destiny.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test E2E spec -> live API | Each cluster's failure crosses the test → API boundary; fix attempts modify only the test side (per scope guardrail D-22/D-23: no source-component changes except where strictly necessary, e.g., adding a data-testid for Cluster C1 selector unification). |
| global-setup.ts -> seed DB (Cluster C2 only) | Audit-fixture seeding writes test data to dev DB; no production surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-19 | Tampering | apps/web/e2e/global-setup.ts (Cluster C2 only) | mitigate | Audit-fixture seeding additions are dev-only test data. Restricted to `prisma.auditLog.create({ ... })` calls with synthetic actor/action. ASVS L1 N/A. |
| T-17.1-20 | Tampering | apps/web/e2e/*.spec.ts timing patterns | accept | Adding `waitForLoadState`, `expect.poll`, or timeout bumps does NOT change production code. The risk surface is "false-positive test pass" (timing accommodation hides a real bug). Mitigation: spec re-run `--repeat-each=3` ensures stability across 3 runs (CONTEXT D-05 threshold). |
| T-17.1-21 | Repudiation | Cluster outcome traceability | mitigate | Task 5 consolidates ALL 6 row Resolution updates in TRIAGE so future readers can trace each test's destiny without re-reading task SUMMARYs. |
| T-17.1-22 | Information Disclosure | n/a | accept | No PII, no secrets, no production data. Test-side changes only. |
| T-17.1-23 | Denial of Service | Per-cluster 30-min budget | mitigate (process-level) | The 30-min-per-cluster budget IS the DoS mitigation against "executor spends 4 hours on Mantine Stepper". Cluster D has explicit executor-judgment gate to decline if time-pressure indicates. Per CONTEXT D-16. |
</threat_model>

<verification>
After all 5 tasks complete:

1. `grep -cE "(#cluster-15-audit|#cluster-10.5-import|#cluster-04-dnd|#cluster-10.2-silent-4xx|#cluster-10.2-screenshot)" .planning/phases/17-ci-stabilization/17-TRIAGE.md` returns ≥ 6 (all 6 rows present).
2. Same grep with the additional filter `| grep -cE "(Fixed in commit|Phase 17.1 deferred|Phase 17.1 attempt|Phase 17.1: not investigated)"` returns 6 (all 6 rows have a definitive Phase 17.1 outcome).
3. Zero spec files deleted: `git diff --diff-filter=D --name-only <plan-base-sha> HEAD apps/web/e2e/*.spec.ts | wc -l` returns 0.
4. Per-cluster commits are atomic: `git log --oneline <plan-base-sha>..HEAD | wc -l` returns 1-5 (one per fired task).
5. ZERO modifications to `apps/api/prisma/schema.prisma`: `git diff <plan-base-sha> HEAD apps/api/prisma/schema.prisma | wc -l` returns 0.
</verification>

<success_criteria>
- [ ] Task 1 — Cluster C investigated within 30-min sub-cluster budgets; both sub-clusters (filter + detail) have FIXED or Phase-17.1-DEFERRED outcome
- [ ] Task 2 — Cluster F investigated within 30-min sub-cluster budgets; both sub-clusters (silent-4xx + screenshots) have FIXED or Phase-17.1-DEFERRED outcome
- [ ] Task 3 — Cluster E investigated within 30-min budget; FIXED or Phase-17.1-DEFERRED outcome
- [ ] Task 4 — Cluster D investigated within 30-min budget OR declined per executor-judgment gate; outcome documented
- [ ] Task 5 — TRIAGE consolidation commit landed; all 6 rows have a definitive Phase 17.1 outcome
- [ ] Zero spec files deleted (per D-20)
- [ ] Zero apps/api/prisma/schema.prisma modifications
- [ ] No `--admin` overrides used; no force-pushes; no branch switching
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-ci-stabilization-followup/17.1-05-SUMMARY.md` with:
- Per-cluster outcome table:
  | Cluster | Spec(s) | Outcome | Wallclock | Commit SHA / Deferral Reason |
  |---------|---------|---------|-----------|------------------------------|
  | C1 (filter) | admin-audit-log-filter.spec.ts | FIXED / DEFERRED | <minutes> | <sha or reason> |
  | C2 (detail) | admin-audit-log-detail.spec.ts | FIXED / DEFERRED | <minutes> | <sha or reason> |
  | F1 (silent-4xx) | silent-4xx.spec.ts | FIXED / DEFERRED | <minutes> | <sha or reason> |
  | F2 (SCHOOL-05) | screenshots.spec.ts | FIXED / DEFERRED | <minutes> | <sha or reason> |
  | E (DnD) | admin-timetable-edit-dnd.spec.ts | FIXED / DEFERRED | <minutes> | <sha or reason> |
  | D (import wizard) | admin-import.spec.ts | FIXED / DEFERRED / DECLINED | <minutes> | <sha or reason> |
- Total wallclock for the plan
- Hand-off note: if all 6 sub-clusters FIXED → PHASE-17.1-COMPLETE-FOR-MOP-UP. If any DEFERRED → DEFERRALS-QUEUED-FOR-17.2 (or Phase 23 for Mantine-Stepper).
</output>
