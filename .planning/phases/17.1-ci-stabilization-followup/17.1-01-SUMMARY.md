---
phase: 17.1-ci-stabilization-followup
plan: 01
subsystem: testing
tags: [typescript, tanstack-query-v5, vitest, ci, type-narrowing, dashboard]

# Dependency graph
requires:
  - phase: 16-school-admin-dashboard
    provides: DashboardChecklist component + UseQueryResult-based unit-test pattern (vitest)
  - phase: 17-ci-stabilization
    provides: smoke-PR `chore/ci-smoke-noop` (PR #9) that surfaced the TS build-blocker via `Build Web` failure in run 25251866945
provides:
  - "Type-clean partial UseQueryResult mocks in DashboardChecklist.test.tsx (per-variant `as const` literal narrowing)"
  - "Restored `tsc -p tsconfig.app.json --noEmit` exit 0 across all of apps/web (CI build-gate is no longer pre-flight-fail)"
  - "Atomic-commit precedent for the `as Q['status']` → `as const` pattern (reusable for any future TanStack Query v5 partial-mock test)"
affects: [17.1-02, 17, ci, smoke-pr, future-dashboard-tests, future-tanstack-query-v5-mocks]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TanStack Query v5 partial-mock pattern: use `status: '<variant>' as const` instead of `as Q['status']` to preserve discriminated-union variant narrowing under `Partial<UseQueryResult>`"

key-files:
  created: []
  modified:
    - apps/web/src/components/admin/dashboard/DashboardChecklist.test.tsx

key-decisions:
  - "Used `as const` literal cast (Plan §interfaces option 1) — smallest possible diff (9 single-token swaps, zero structural change)"
  - "Did NOT lift makeQueryResult factory to apps/web/src/test-utils/queryMocks.ts (per CONTEXT D-07: extraction allowed only if zero other files need touching; no other files were investigated within this plan's scope)"
  - "Single atomic commit per CONTEXT D-07 — no bundled refactors, no scope creep"
  - "Acceptance ran the FULL `tsc -p tsconfig.app.json --noEmit` per CONTEXT D-06 — not just a per-file check — to guarantee no other apps/web TS errors hide behind this fix"

patterns-established:
  - "Pattern: TanStack Query v5 discriminated-union narrowing via `as const` — for any test that supplies a `Partial<UseQueryResult>` mock, the `status` field MUST be cast with `as const` (not `as Q['status']`) so the variant literal is preserved and assignable to the per-variant `Partial<...>` shape"

requirements-completed: []  # Plan 17.1-01 has no requirement IDs in frontmatter (tech-debt fix)

# Metrics
duration: 3min
completed: 2026-05-02
---

# Phase 17.1 Plan 01: DashboardChecklist Type-Narrowing Fix Summary

**Replaced 9 occurrences of `status: '<X>' as Q['status']` with `as const` in DashboardChecklist.test.tsx, eliminating 8 TS2345 errors that blocked `pnpm --filter @schoolflow/web build` and consequently the entire CI pipeline (Build Web → Playwright).**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-05-02T17:14:39Z
- **Completed:** 2026-05-02T17:17:58Z
- **Tasks:** 2 (Task 1: apply fix at 9 sites; Task 2: vitest verify + atomic commit)
- **Files modified:** 1

## Accomplishments

- **TS build-gate restored:** `pnpm --filter @schoolflow/web exec tsc -p tsconfig.app.json --noEmit` exits **0** (TSC_EXIT=0 captured in Task-1 verify and re-confirmed post-commit). Before fix: 8 TS2345 errors at lines 84/100/122/137/175/192/209/224. After fix: 0 errors anywhere in apps/web.
- **Behavior preserved:** All 8 vitest tests in `DashboardChecklist.test.tsx` pass — `Test Files  1 passed (1)` AND `Tests  8 passed (8)` (944ms total). Zero behavioral change; only the type-cast tokens differ.
- **Single-file atomic commit:** Diff contained to `DashboardChecklist.test.tsx` (1 file changed, 9 insertions(+), 9 deletions(-)). Honors D-07.
- **Unblocks Wave 2:** Plan 17.1-02 (worktree-merge + smoke-PR re-run) is no longer blocked at `Build Web` — every CI run going forward can now reach Playwright.

## Task Commits

1. **Task 1+2 (atomic — per D-07): Apply per-variant `as const` literal cast at all 9 sites + vitest verify + commit** — `20e0b81` (fix)

The plan structured Task 1 as "apply edit" and Task 2 as "vitest + commit," but per CONTEXT D-07 the entire fix is a SINGLE atomic commit. The two tasks share `20e0b81` because the commit is the joint deliverable (no Task-1-only commit was created).

**Plan metadata commit:** TBD (this SUMMARY.md + STATE.md + ROADMAP.md update — landed by orchestrator final-commit step below).

## Files Created/Modified

- `apps/web/src/components/admin/dashboard/DashboardChecklist.test.tsx` — Replaced 9 occurrences of `status: '<X>' as Q['status']` with `status: '<X>' as const` (factory default at line 34 + 8 call sites at lines 87, 102, 124, 139, 177, 195, 212, 226). Type-system effect: each variant literal is preserved per discriminated-union variant of `UseQueryResult<TData, TError>`, so `Partial<Q>` accepts the partial mock without widening `status` to the full `'pending' | 'success' | 'error'` union.

## Decisions Made

- **Fix path = Option 1 (`as const`):** Per Plan §interfaces, Option 1 was preferred over Option 2 (outer `as Partial<Q>` cast) because it produces the smallest diff (9 single-token swaps) and preserves type-safety for all other fields in the partial mock. Option 2 would have suppressed type-checking for the entire object literal — strictly worse.
- **No mock-factory extraction:** Per CONTEXT D-07, the `makeQueryResult` factory was NOT lifted to `apps/web/src/test-utils/queryMocks.ts`. The lift would have required investigating other test files for the same pattern (out of scope for this plan), and CONTEXT D-07 explicitly forbids bundled refactors. The factory stays in-file; a future plan can lift it if other files surface the same pattern.
- **No new test infrastructure:** Vitest 4.1.2 already installed; no test-framework changes needed.

## Deviations from Plan

**None — plan executed exactly as written.**

Cross-checked against Plan §acceptance_criteria:
- 0 remaining `as Q['status']` patterns (verified via grep — count = 0)
- 9 `as const` occurrences (verified via grep — count = 9; Plan asked for ≥ 8, factory default takes the count to 9)
- Single file changed (verified via `git show --stat HEAD`)
- No new `@ts-ignore` / `@ts-expect-error` introduced (verified via diff grep)
- No file deletions in commit (verified via `git diff --diff-filter=D HEAD~1 HEAD` — empty)

Cross-checked against Plan §verification:
- `tsc -p tsconfig.app.json --noEmit` exits 0 (final re-run after commit also 0)
- Vitest reports `Tests  8 passed (8)`
- `git log --oneline -1` shows the exact `fix(dashboard): resolve UseQueryResult discriminated-union type narrowing in DashboardChecklist tests` commit subject
- `git status --short` for the modified file is clean (only pre-existing dirty files remain — STATE.md, untracked test artifacts — which are unrelated)

## Issues Encountered

None — type-fix applied cleanly on first attempt; vitest passed on first run; tsc passed on first run. No fix-attempt iteration needed.

## Per CONTEXT D-06: full apps/web tsc surfaced no OTHER errors

The plan's quick-fail safeguard required documenting any OTHER pre-existing TS errors that the fix surfaces elsewhere in apps/web (those would be deferred to Phase 17.2 per D-06). **Result: NONE.** The full `tsc -p tsconfig.app.json --noEmit` exits 0 with zero errors anywhere — not just in DashboardChecklist.test.tsx. No deferred items added to `.planning/phases/17-ci-stabilization/deferred-items.md` from this plan.

## User Setup Required

None — no external service configuration required.

## Hand-off Note

**Ready for Plan 17.1-02 to merge into the worktree-merge sequence.** The TS build-blocker is resolved; CI's `Build Web` step will now succeed on PR #9 (smoke-PR) once it picks up commit `20e0b81`. Per CONTEXT D-08, the merge order remains FIXED: 17-01 → 17-02 → 17-03 → 17-04 → 17-05, with this fix riding on the `gsd/phase-17-ci-stabilization` branch (per user directive: same-branch strategy for Phase 17.1).

## Next Phase Readiness

- TS-compile-gate: GREEN
- Vitest unit-tests: PASSING (8/8 in DashboardChecklist.test.tsx)
- Plan 17.1-02 dependencies satisfied: yes
- No blockers; no concerns

## Self-Check: PASSED

- File `apps/web/src/components/admin/dashboard/DashboardChecklist.test.tsx`: FOUND (modified)
- Commit `20e0b81`: FOUND in `git log --oneline -1` on branch `gsd/phase-17-ci-stabilization`
- TSC exit code captured: 0 (final re-run post-commit)
- Vitest pass count captured: 8/8

---
*Phase: 17.1-ci-stabilization-followup*
*Completed: 2026-05-02*
