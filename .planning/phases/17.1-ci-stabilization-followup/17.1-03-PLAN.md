---
phase: 17.1-ci-stabilization-followup
plan: 03
type: execute
wave: 3
depends_on:
  - 17.1-02
files_modified:
  # Conditional — exact backend file determined by live-stack-repro outcome:
  # - apps/api/prisma/seed.ts (data-only seed-class-1a ID alignment) — if cross-reference-missing
  # - apps/web/e2e/helpers/constraints.ts (params shape) — if period-out-of-range
  - apps/web/e2e/admin-solver-tuning-restrictions.spec.ts
  - apps/web/e2e/admin-solver-tuning-preferences.spec.ts
  - apps/web/e2e/admin-solver-tuning-audit.spec.ts
  - .planning/phases/17-ci-stabilization/17-TRIAGE.md
autonomous: true
requirements: []
tags:
  - best-effort
  - cluster-A
  - phase-14-regression
  - constraint-templates-422
  - live-stack-repro

must_haves:
  decisions_honored:
    - "D-01: best-effort tier (may be skipped if 17.1-02 smoke-PR was GREEN without it)"
    - "D-03: Wave 3 parallel-with-17.1-04; depends_on [17.1-02]"
    - "D-04: autonomous: true with [BLOCKING] live-stack-repro task per CONTEXT D-11"
    - "D-11: live-stack repro REQUIRED before code edits (curl against running API)"
    - "D-12: discriminate two error URIs (cross-reference-missing vs period-out-of-range) — fix path follows URI, not guess"
    - "D-20: skip-with-reason annotation default; spec-file deletion only if duplicate or feature removed"
  truths:
    - "Live-stack repro of POST /constraint-templates returns NOT 422 after the targeted fix (or unable-to-reproduce documented in TRIAGE) [honors D-11, D-12]"
    - "All 5 specs in Cluster A (E2E-SOLVER-04, -06, -07, -09, -11) re-run 3/3 PASS on desktop project with skip annotation REMOVED — OR skip preserved with documented reason if unable-to-fix in 30 min [honors D-20]"
    - "17-TRIAGE.md row #cluster-14-422 + 4 sibling rows have Resolution column updated (fixed-in-commit OR unable-to-fix-deferred)"
  artifacts:
    - path: ".planning/phases/17-ci-stabilization/17-TRIAGE.md"
      provides: "Updated #cluster-14-422 row + 4 sibling rows with fix evidence or deferral note"
      contains: "live-stack repro"
    - path: "apps/web/e2e/admin-solver-tuning-restrictions.spec.ts"
      provides: "Either skip annotation removed (if fixed) or skip preserved with new Phase 17.1 reason"
      contains: "describe("
  key_links:
    - from: "apps/web/e2e/helpers/constraints.ts"
      to: "POST /api/v1/schools/:schoolId/constraint-templates"
      via: "createConstraintTemplate request"
      pattern: "POST /constraint-templates"
    - from: "validateCrossReference (api)"
      to: "seed-class-1a fixture"
      via: "schoolId/classId reference check"
      pattern: "schoolflow://errors/cross-reference-missing|period-out-of-range"
---

<objective>
**BEST-EFFORT** plan (per CONTEXT D-01) — runs ONLY if Plan 17.1-02 Task 3 classified as RED-WITH-CLUSTERS and Cluster A is among the firing clusters. If smoke-PR went GREEN without this plan, this plan is OPTIONAL deferral material (queue to Phase 17.2 backlog or Phase 23-Backlog).

Fix the Phase 14 `POST /constraint-templates` 422 regression that breaks 5 tests across 3 specs. Phase-14-03 SUMMARY documents these as PASSING at end of Phase 14 — this is a regression introduced between Phase 14 final and PR #1 baseline.

Single-root-cause hypothesis (per CONTEXT D-12): the 422 either fires `schoolflow://errors/cross-reference-missing` (fix `seed-class-1a` ID alignment in `apps/api/prisma/seed.ts`) OR `schoolflow://errors/period-out-of-range` (adjust `apps/web/e2e/helpers/constraints.ts` `params` shape to match the post-Phase-14 DTO contract). The `r.json().type` URI from a live-stack repro IS the diagnostic — DO NOT guess.

Per CONTEXT D-04: this plan is `autonomous: true` BUT requires `[BLOCKING] live-stack-repro` BEFORE any code edits. Without confirmed local repro, the executor MUST mark unable-to-reproduce + skip-with-reason in 17-TRIAGE.md — no speculative fixes.

Per CONTEXT D-14 (carried-forward 30-min protocol): repro budget = 30 min, fix-application budget = 30 min, spec re-validation budget = 30 min. If any one of those exceeds budget → revert + skip-with-reason + queue for 17.2.
</objective>

<execution_context>
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-ci-stabilization-followup/17.1-CONTEXT.md
@.planning/phases/17-ci-stabilization/17-TRIAGE.md
@.planning/phases/17-ci-stabilization/deferred-items.md
@.planning/phases/14-solver-tuning/14-03-SUMMARY.md

# Source-of-truth files for the suspected fix sites
@apps/api/src/modules/timetable/constraint-template.service.ts
@apps/web/e2e/helpers/constraints.ts
@apps/web/e2e/admin-solver-tuning-restrictions.spec.ts

<interfaces>
POST /api/v1/schools/:schoolId/constraint-templates
  Body: { templateType: 'NO_LESSONS_AFTER' | 'SUBJECT_MORNING' | ..., params: {...} }
  Returns 200/201 with TemplateDto on success
  Returns 422 with problem+json on validation error:
    type: "schoolflow://errors/cross-reference-missing" -> seed-class-1a or other entity ID does not exist (Path-A: fix seed.ts)
    type: "schoolflow://errors/period-out-of-range"     -> params shape (e.g., dayOfWeek/period) outside allowed range (Path-B: fix constraints.ts helper)
    type: "schoolflow://errors/<other>"                 -> NEW sub-cluster, document, do NOT auto-fix

CONSTRAINT_SCHOOL_ID + the seed-class-1a ID used by the helper are defined at the top of `apps/web/e2e/helpers/constraints.ts`. First action of Task 1 is to grep these constants and verify they match `apps/api/prisma/seed.ts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: [BLOCKING] Live-stack repro of POST /constraint-templates 422 — capture problem+json type URI (30-min budget)</name>
  <read_first>
    - apps/web/e2e/helpers/constraints.ts (find CONSTRAINT_SCHOOL_ID, CONSTRAINT_API base URL, the createConstraintTemplate POST body shape, and templateType values used by Cluster A specs)
    - apps/web/e2e/admin-solver-tuning-restrictions.spec.ts lines 42, 176 (E2E-SOLVER-04 + -06 params)
    - apps/web/e2e/admin-solver-tuning-preferences.spec.ts lines 41, 185 (E2E-SOLVER-07 + -09 params)
    - apps/api/src/modules/timetable/constraint-template.service.ts (search for `validateCrossReference`, `period-out-of-range`, `cross-reference-missing` to map error URIs to validation logic)
    - apps/api/prisma/seed.ts (grep for `class-1a`, `1a`, `Klasse 1a` — locate seed-class-1a fixture; confirm its ID, schoolId, and any referenced subject IDs)
    - .planning/phases/14-solver-tuning/14-03-SUMMARY.md (confirms 5 tests as PASSING at end of Phase 14 — regression baseline)
    - .planning/phases/17.1-ci-stabilization-followup/17.1-CONTEXT.md (D-11 BLOCKING repro; D-12 error URI is the diagnostic; D-14 30-min budget)
    - User memory `reference_app_startup.md` (full-stack startup command sequence)
  </read_first>
  <files>(none — read-only repro + diagnostic capture)</files>
  <action>
    1. Start full local stack per memory `reference_app_startup.md`. Wait until `curl -s http://localhost:3000/api/v1/health` returns ok.

    2. Authenticate as admin via Keycloak (per `apps/web/e2e/helpers/login.ts` conventions). Capture JWT into `$TOKEN`. Verify token length > 200 chars.

    3. Identify constants:
       `grep -n "CONSTRAINT_SCHOOL_ID\|CONSTRAINT_API" apps/web/e2e/helpers/constraints.ts`
       `grep -nE "id: '[^']*1a'|id: '[^']*class[^']*1a'" apps/api/prisma/seed.ts`
       Document the values in TRIAGE evidence.

    4. Repro NO_LESSONS_AFTER (used by E2E-SOLVER-04, -06, -11):
       `curl -i -X POST "http://localhost:3000/api/v1/schools/${SCHOOL_ID}/constraint-templates" -H "Authorization: Bearer ${TOKEN}" -H "Content-Type: application/json" -d '<EXACT body from admin-solver-tuning-restrictions.spec.ts:42>'`

    5. Repro SUBJECT_MORNING (used by E2E-SOLVER-07, -09):
       `curl -i -X POST "http://localhost:3000/api/v1/schools/${SCHOOL_ID}/constraint-templates" -H "Authorization: Bearer ${TOKEN}" -H "Content-Type: application/json" -d '<EXACT body from admin-solver-tuning-preferences.spec.ts:41>'`

    6. Capture response. Three outcomes drive next-task routing:
       (a) Returns 200/201 -> 422 does NOT reproduce locally -> CI-only failure -> mark `unable-to-reproduce` -> skip Task 2 -> go to Task 4.
       (b) Returns 422 with `type: "schoolflow://errors/cross-reference-missing"` -> Path-A in Task 2 (fix seed.ts).
       (c) Returns 422 with `type: "schoolflow://errors/period-out-of-range"` -> Path-B in Task 2 (fix constraints.ts).
       (d) Returns 422 with NEW URI -> grep apps/api/src/modules/timetable/ for the URI string; document; surface to user; do NOT auto-fix.

    7. Update `17-TRIAGE.md` row `#cluster-14-422` Local-Repro column. Replace `n/a — parallel-worktree environment (no live API on :3000)` with: `live-stack repro on <date>: POST /constraint-templates returns <code> with type=<URI>`. Include the curl command + truncated response body (≤200 chars) inline.

    Time-box: 30 min total for steps 1-7. If exceeded -> mark `unable-to-reproduce-in-30min` -> skip to Task 4.
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/api/v1/health</automated>
  </verify>
  <acceptance_criteria>
    - Live API responds at `http://localhost:3000/api/v1/health` (returns 200 + ok body).
    - Both NO_LESSONS_AFTER + SUBJECT_MORNING POSTs attempted; concrete status code captured (not connection-refused).
    - One of four outcomes recorded in TRIAGE: (a) unable-to-reproduce, (b) cross-reference-missing URI captured, (c) period-out-of-range URI captured, (d) NEW URI captured.
    - `grep "live-stack repro" .planning/phases/17-ci-stabilization/17-TRIAGE.md` returns ≥ 1 NEW occurrence (the row now reflects live evidence).
    - 30-min wallclock budget honored (per D-14).
  </acceptance_criteria>
  <done>422 reproduction outcome classified (one of a/b/c/d); TRIAGE row updated with the live evidence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply targeted fix per Task 1 outcome — seed.ts data-fix (Path-A) OR constraints.ts helper-fix (Path-B) (30-min budget)</name>
  <read_first>
    - Task 1 outcome classification (a/b/c/d) — DETERMINES which file is touched
    - Path-A: apps/api/prisma/seed.ts (find seed-class-1a / school fixture; verify ID alignment)
    - Path-B: apps/web/e2e/helpers/constraints.ts + apps/api/src/modules/timetable/dto/* (find params construction + DTO contract)
    - .planning/phases/17.1-ci-stabilization-followup/17.1-CONTEXT.md (D-12 error URI is the diagnostic)
    - CLAUDE.md "Database migrations — hard rule": SCHEMA changes require migration files; SEED.TS DATA changes do NOT modify schema and need no migration. If fix turns out to need a schema change -> ABORT and escalate.
    - User memory `feedback_restart_api_after_migration.md` (restart API after seed changes — Prisma client cache)
  </read_first>
  <files>
    # Exactly ONE of (determined by Task 1 outcome):
    # apps/api/prisma/seed.ts
    # apps/web/e2e/helpers/constraints.ts
  </files>
  <action>
    GUARD: Only run if Task 1 outcome was (b) or (c). For (a) unable-to-reproduce or (d) NEW URI -> SKIP and go to Task 4.

    **Path-A (cross-reference-missing -> fix seed.ts):**
    1. The 422 response body names the missing reference (e.g., "classId 'class-1a' not found"). Locate the corresponding entity in `apps/api/prisma/seed.ts`.
    2. Either ADD the missing seed entity OR ALIGN the ID to match what the helper expects (favor ID alignment over new entity creation — smaller change).
    3. Re-seed: `cd apps/api && pnpm prisma db seed` (or per local convention from CLAUDE.md).
    4. Restart API per memory `feedback_restart_api_after_migration.md` (Prisma client binds at boot).
    5. Re-run the curl repro from Task 1 step 4. Confirm now returns 200/201.

    **Path-B (period-out-of-range -> fix constraints.ts helper):**
    1. Open `apps/api/src/modules/timetable/dto/` and find the DTO for `params` of the templateType in question (NO_LESSONS_AFTER vs SUBJECT_MORNING). Document the expected shape (e.g., `{ dayOfWeek: 1-5, period: 1-N }`).
    2. Open `apps/web/e2e/helpers/constraints.ts`. Find the `params` construction in the helper.
    3. Adjust the `params` to match the DTO contract. Typically a single-line change.
    4. Re-run the curl repro from Task 1. Confirm now returns 200/201.
    5. NO API restart needed (test-side helper-only change).

    Commit (atomic, single file):
    `git add <single-file>`
    `git commit -m "fix(17.1-cluster-A): resolve POST /constraint-templates 422 — <one-line description>"` (full body per template — see Plan 17.1-01 for HEREDOC pattern; reference Cluster A and the URI captured in Task 1)

    Time-box: 30 min for fix application + curl re-verify. If exceeded -> `git checkout -- <file>`, mark `unable-to-fix-in-30-min` in TRIAGE, proceed to Task 4.

    PROHIBITED: schema.prisma modifications. If the diagnosis points there, escalate — do NOT touch schema in a tech-debt-closure plan.
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && git diff --stat HEAD~1 HEAD 2>/dev/null | tail -3 && git diff HEAD~1 HEAD apps/api/prisma/schema.prisma 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - GUARD-CONDITIONAL: Task 1 outcome was (b) or (c). For (a)/(d), this task correctly SKIPPED.
    - If executed: `git diff --stat HEAD~1 HEAD` shows EXACTLY one file modified.
    - If executed: `git log --oneline -1` shows commit subject starting with `fix(17.1-cluster-A):`.
    - If executed: re-running the curl POST returns 200 or 201 (NOT 422).
    - If executed: ZERO modifications to `apps/api/prisma/schema.prisma` — verifiable via the verify command's `wc -l` returning 0 lines for the schema diff.
    - 30-min time-box honored: if exceeded, revert + Task 4 path taken.
  </acceptance_criteria>
  <done>Single atomic fix commit landed; curl repro returns 200/201; OR task correctly skipped with documented reason in TRIAGE.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove skip annotations + verify all 5 Cluster A tests PASS 3/3 + update TRIAGE (30-min budget)</name>
  <read_first>
    - Task 2 outcome (commit landed vs skipped)
    - apps/web/e2e/admin-solver-tuning-restrictions.spec.ts (find describe-block-level `test.skip(true, 'Phase 17 deferred: ...')` annotation)
    - apps/web/e2e/admin-solver-tuning-preferences.spec.ts (same)
    - apps/web/e2e/admin-solver-tuning-audit.spec.ts (same)
    - .planning/phases/17-ci-stabilization/17-TRIAGE.md (find #cluster-14-422 + 4 sibling rows; update Resolution column)
  </read_first>
  <files>
    apps/web/e2e/admin-solver-tuning-restrictions.spec.ts
    apps/web/e2e/admin-solver-tuning-preferences.spec.ts
    apps/web/e2e/admin-solver-tuning-audit.spec.ts
    .planning/phases/17-ci-stabilization/17-TRIAGE.md
  </files>
  <action>
    GUARD: Only run if Task 2 produced a successful fix commit. If Task 2 was skipped (Task 1 outcomes a/d, or 30-min exceeded) -> SKIP and go to Task 4.

    1. Remove the `test.skip(true, 'Phase 17 deferred: ...')` annotation from each of the 3 spec files. Locate via `grep -n "Phase 17 deferred" apps/web/e2e/admin-solver-tuning-*.spec.ts`. Each file has ONE describe-block-level test.skip line; remove that single line (the describe block stays, the inner tests re-activate).

    2. Re-run all 5 Cluster A tests with `--repeat-each=3` (per CONTEXT D-05 real-bug threshold):
       `pnpm --filter @schoolflow/web exec playwright test admin-solver-tuning-restrictions.spec.ts admin-solver-tuning-preferences.spec.ts admin-solver-tuning-audit.spec.ts --project=desktop --repeat-each=3`
       Expected: 15/15 PASS (5 tests × 3 repeats). If ANY test fails -> the root cause was NOT what Task 2 fixed; revert skip-annotation removals, mark TRIAGE as `partial-fix — Task 2 unblocked X/5 tests`, proceed to Task 4 for the unblocked subset.

    3. Update `17-TRIAGE.md` rows (5 rows total per Cluster A — see TRIAGE for exact row markers):
       - `#cluster-14-422`: Resolution -> "Fixed in commit `<sha-from-Task-2>`. Live-stack repro confirmed 200/201; spec re-run 3/3 PASS on `<date>`."
       - `#cluster-14-422-siblings`: same Resolution.
       - `#cluster-14-422-preferences-07`: same Resolution.
       - `#cluster-14-422-preferences-09`: same Resolution.
       - `#cluster-14-422-audit-11`: same Resolution.

    4. Commit (second atomic commit of this plan):
       `git add apps/web/e2e/admin-solver-tuning-*.spec.ts .planning/phases/17-ci-stabilization/17-TRIAGE.md`
       `git commit -m "test(17.1-cluster-A): re-enable 5 specs after Cluster A regression fix; update TRIAGE"` (full HEREDOC body referencing the 5 test IDs and the 15/15 local validation result)

    Time-box: 30 min for spec re-runs. If `--repeat-each=3` blows the budget -> revert skip-annotation removals, mark `partial-fix-needs-investigation`, proceed to Task 4.
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && pnpm --filter @schoolflow/web exec playwright test admin-solver-tuning-restrictions.spec.ts admin-solver-tuning-preferences.spec.ts admin-solver-tuning-audit.spec.ts --project=desktop --repeat-each=3 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - GUARD-CONDITIONAL: Task 2 commit landed. If Task 2 was skipped, this task is correctly SKIPPED.
    - If executed: `grep -c "Phase 17 deferred" apps/web/e2e/admin-solver-tuning-restrictions.spec.ts` returns 0 (annotation removed).
    - If executed: same `grep -c` on the other 2 spec files returns 0.
    - If executed: Playwright output shows `15 passed` (5 tests × 3 repeats) AND `0 failed`.
    - If executed: `grep -c "Fixed in commit" .planning/phases/17-ci-stabilization/17-TRIAGE.md` returns ≥ 5 NEW occurrences in the Cluster A row block.
    - If executed: `git log --oneline -1` shows commit `test(17.1-cluster-A): re-enable 5 specs after Cluster A regression fix`.
  </acceptance_criteria>
  <done>5 Cluster A tests pass 3/3; TRIAGE updated with fix evidence; or task correctly skipped/reverted with documented reason.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Skip-with-reason preservation if any task skipped — update TRIAGE Resolution column with Phase 17.1 deferral note</name>
  <read_first>
    - Task 1, 2, 3 outcomes
    - .planning/phases/17-ci-stabilization/17-TRIAGE.md (Cluster A rows that did not get a fix-evidence update in Task 3)
    - Existing skip annotation pattern in spec files (`// Phase 17 deferred: <reason> — see 17-TRIAGE.md row N`) — update to `Phase 17.1 deferred:` per CONTEXT D-14 + D-20
  </read_first>
  <files>
    # Conditional — touched only if 17.1 deferral is needed:
    # apps/web/e2e/admin-solver-tuning-restrictions.spec.ts (skip annotation update if not removed in Task 3)
    # apps/web/e2e/admin-solver-tuning-preferences.spec.ts
    # apps/web/e2e/admin-solver-tuning-audit.spec.ts
    .planning/phases/17-ci-stabilization/17-TRIAGE.md
  </files>
  <action>
    GUARD: Only run if Task 1 outcome was (a) or (d), OR Task 2 was skipped/reverted, OR Task 3 was skipped/reverted. If Tasks 1-3 all succeeded with a clean fix -> SKIP this task (deferral path not needed).

    1. For each Cluster A spec where the skip annotation REMAINS (Task 3 did not remove it), UPDATE the existing annotation in the spec file from `// Phase 17 deferred: <reason>` to `// Phase 17.1 deferred: <reason> — see 17-TRIAGE.md row #cluster-14-422` per CONTEXT D-14 pattern (replacing `Phase 17` with `Phase 17.1`). Per CONTEXT D-20: do NOT delete the spec; preserve as a regression detector for re-enablement.

    2. Update `17-TRIAGE.md` rows for Cluster A. Resolution column gets a NEW Phase 17.1 entry per row, e.g.:
       - `#cluster-14-422`: Resolution += " | Phase 17.1 attempt: <unable-to-reproduce | fix-exceeded-30min | new-URI surfaced for user triage>. Queue for Phase 17.2 backlog."
       - 4 sibling rows: same.

    3. If the smoke-PR went GREEN despite Cluster A still firing in the per-spec re-run (i.e., CI environment hides it but local repro confirms), document this discrepancy. Per CONTEXT D-02: "Best-effort cluster fixes that miss the smoke-PR window are NOT a phase-failure trigger." The deferral is acceptable.

    4. Commit:
       `git add apps/web/e2e/admin-solver-tuning-*.spec.ts .planning/phases/17-ci-stabilization/17-TRIAGE.md`
       `git commit -m "docs(17.1-cluster-A): preserve skip-with-reason; queue for Phase 17.2 backlog"` (HEREDOC body documenting WHICH outcome triggered the deferral and WHAT the next investigation step would be)
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && grep -c "Phase 17.1 deferred\|Phase 17.1 attempt" .planning/phases/17-ci-stabilization/17-TRIAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - GUARD-CONDITIONAL: Only runs if Tasks 1-3 did not produce a clean fix. If they did, this task is correctly SKIPPED.
    - If executed: `grep -c "Phase 17.1 deferred\|Phase 17.1 attempt" .planning/phases/17-ci-stabilization/17-TRIAGE.md` returns ≥ 1 NEW occurrence.
    - If executed: each affected spec file has the annotation pattern updated from `Phase 17 deferred` to `Phase 17.1 deferred` (verifiable via `grep -l "Phase 17.1 deferred" apps/web/e2e/admin-solver-tuning-*.spec.ts`).
    - If executed: zero spec files DELETED (per D-20: skip preserves the regression detector).
    - If executed: commit subject `docs(17.1-cluster-A): preserve skip-with-reason; queue for Phase 17.2 backlog`.
  </acceptance_criteria>
  <done>Deferral documented in TRIAGE + spec annotations; specs preserved (not deleted); user has clear hand-off for 17.2 follow-up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test E2E helper -> live API | curl repro crosses helper code -> HTTP -> API DTO validation -> Prisma -> DB. The 422 surfaces at the DTO validation layer. |
| Prisma seed data -> validateCrossReference | Cross-reference checks bind seed-data integrity to test fixture stability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-10 | Tampering | apps/api/prisma/seed.ts (Path-A) | mitigate | Seed data is dev-only (gitignored production secrets). Changes go through PR review. ASVS L1 N/A for test fixtures. |
| T-17.1-11 | Tampering | apps/api/prisma/schema.prisma | mitigate (forbidden) | This plan PROHIBITS schema modifications per CLAUDE.md hard rule. Acceptance criterion explicitly checks `git diff schema.prisma` returns zero lines. Any schema-required diagnosis must abort + escalate. |
| T-17.1-12 | Information Disclosure | curl JWT in command-line history | accept | The JWT is short-lived (15-min access token per Phase 01 D-12). Local-only stack. Risk acceptable for diagnostic work. Document mitigation: do not paste full JWT into TRIAGE.md (truncate to first 20 chars + "..."). |
| T-17.1-13 | DTO validation regression | apps/api/src/modules/timetable/dto/* (if Path-C surfaces) | mitigate | Path-C (DTO change) is OUT-OF-SCOPE for auto-fix. Surfaces to user for explicit triage. Prevents accidental loosening of DTO validation that would weaken input-validation security. |
</threat_model>

<verification>
After Tasks 1-4 complete (with appropriate skips per guards):

1. `grep "live-stack repro" .planning/phases/17-ci-stabilization/17-TRIAGE.md` returns ≥ 1 NEW occurrence (Task 1 evidence captured regardless of fix outcome).
2. If Tasks 2+3 executed: `pnpm --filter @schoolflow/web exec playwright test admin-solver-tuning-{restrictions,preferences,audit}.spec.ts --project=desktop --repeat-each=3` shows 15 passed + 0 failed.
3. If Task 4 executed: `grep -c "Phase 17.1 deferred\|Phase 17.1 attempt" .planning/phases/17-ci-stabilization/17-TRIAGE.md` returns ≥ 1 NEW occurrence.
4. ZERO modifications to `apps/api/prisma/schema.prisma` across the entire plan: `git diff <plan-base-sha> HEAD apps/api/prisma/schema.prisma | wc -l` returns 0.
5. Commits in this plan are atomic single-concern: `git log --oneline <plan-base-sha>..HEAD | wc -l` returns 1, 2, or 3 (one per fired task).
</verification>

<success_criteria>
- [ ] Task 1 — live-stack repro attempted within 30-min budget; outcome (a/b/c/d) documented in TRIAGE
- [ ] Task 2 — IF Task 1 was (b) or (c): targeted single-file fix landed within 30-min budget; OR correctly skipped
- [ ] Task 3 — IF Task 2 fix landed: 5 specs re-run 3/3 PASS, skip annotations removed, TRIAGE Resolution updated
- [ ] Task 4 — IF Tasks 1-3 did not produce a clean fix: skip-with-reason preserved (NOT deleted per D-20), TRIAGE updated, queued for 17.2
- [ ] ZERO `apps/api/prisma/schema.prisma` modifications across the entire plan (CLAUDE.md hard rule)
- [ ] No `--admin` overrides used; no force-pushes to main; no branch switching
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-ci-stabilization-followup/17.1-03-SUMMARY.md` with:
- Cluster A repro outcome (URI captured + curl evidence)
- Fix path taken (Path-A seed.ts / Path-B constraints.ts / unable-to-fix-deferred)
- Spec re-run result (15/15 pass OR partial OR all-deferred)
- Commits landed (subjects + SHAs) — typically 0-3 commits depending on outcome path
- Hand-off note: PHASE-17.1-COMPLETE-FOR-CLUSTER-A (if 15/15 pass) OR DEFERRED-TO-17.2-CLUSTER-A (if any unable-to-fix path taken)
</output>
