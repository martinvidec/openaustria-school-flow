---
phase: 17.1-ci-stabilization-followup
plan: 02
type: execute
wave: 2
depends_on:
  - 17.1-01
files_modified:
  - .planning/phases/17-ci-stabilization/17-TRIAGE.md
  - .planning/STATE.md
autonomous: false
requirements: []
tags:
  - orchestration
  - ci
  - verifier-gate
  - smoke-pr
  - phase-branch-discipline

must_haves:
  decisions_honored:
    - "D-01: required-tier scope (worktree-merge + smoke-PR-rerun is MANDATORY)"
    - "D-02: verifier-gate = green smoke-PR re-run; this plan owns the gate"
    - "D-03: Wave 2 single-plan; depends_on [17.1-01]"
    - "D-08: sequential merge order 17-01 → 17-02 → 17-03 → 17-04 → 17-05"
    - "D-09: separate PR per branch (NOT bundled mega-merge)"
    - "D-10: smoke-PR rebased on new main + CI re-triggered; outcome bucketed (GREEN / RED-CLUSTERS / RED-NEW)"
    - "D-18: 17-TRIAGE.md ## Smoke-PR result updated with new-run URL + commit hash"
    - "D-19: atomic STATE.md transition (Phase 17 → complete AND Phase 17.1 → complete in same commit)"
  truths:
    - "Smoke-PR chore/ci-smoke-noop re-run shows GREEN CI on the Run Playwright tests step WITHOUT --admin override (the SOLE Phase 17.1 verifier-gate truth from CONTEXT D-02)"
    - "All 5 worktree branches (17-01, 17-02, 17-03, 17-04, 17-05) merged to main IN ORDER (17-01 → 17-02 → 17-03 → 17-04 → 17-05) per CONTEXT D-08, each as a separate PR (D-09)"
    - "17-TRIAGE.md ## Smoke-PR result section updated with the green-PR run URL + commit hash (CONTEXT D-18)"
    - "STATE.md atomically transitions Phase 17 from done-with-followup to complete AND Phase 17.1 to complete in a single commit (CONTEXT D-19)"
  artifacts:
    - path: ".planning/phases/17-ci-stabilization/17-TRIAGE.md"
      provides: "Updated ## Smoke-PR result section with green run URL"
      contains: "GREEN"
    - path: ".planning/STATE.md"
      provides: "Phase 17 + Phase 17.1 transitioned to complete"
      contains: "Phase 17"
  key_links:
    - from: "Smoke-PR chore/ci-smoke-noop"
      to: "GitHub Actions playwright.yml"
      via: "PR push triggering CI run"
      pattern: "Run Playwright tests"
    - from: "5 worktree-merge PRs"
      to: "main branch"
      via: "ordered sequential merge (17-01..17-05)"
      pattern: "merge\\(17-0[1-5]\\):"
---

<objective>
Orchestrate the ordered merge of Phase 17 worktree branches (17-01 → 17-02 → 17-03 → 17-04 → 17-05) into main per CONTEXT D-08/D-09, then re-trigger the smoke-PR (`chore/ci-smoke-noop`, PR #9) on the new main HEAD per D-10. This is the verifier-gate plan: success = green CI re-run; failure = bucketing into best-effort plans 17.1-03..17.1-05 (or worse, Phase 17.2 spawn).

Purpose: This is the literal D-15 gate Phase 17 was meant to deliver. Closes the Phase 17 `done-with-followup` status by producing the green-PR evidence that proves CI works WITHOUT the `--admin` override. Per CONTEXT D-02: "Verifier-gate for Phase 17.1 done = green smoke-PR re-run."

Output:
- 5 merged PRs (PR titles `merge(17-NN): <plan-summary>`).
- Updated `## Smoke-PR result` section in `17-TRIAGE.md`.
- Atomic STATE.md update transitioning Phase 17 + Phase 17.1 both to `complete`.
- `## Smoke-PR result` either GREEN (best case → Plans 17.1-03..05 become OPTIONAL skip-and-defer) or RED with classified residual cluster failures (→ Plans 17.1-03..05 become MANDATORY for the surviving clusters).

Branch invariant: This plan does NOT switch branches in any task. The orchestrator (user) coordinates the worktree merges via the `gh` CLI. The executor's role is to drive the `gh` commands, observe CI results, and update the documentation artifacts — NOT to physically check out branches.
</objective>

<execution_context>
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-ci-stabilization-followup/17.1-CONTEXT.md
@.planning/phases/17-ci-stabilization/17-TRIAGE.md
@.planning/phases/17-ci-stabilization/VERIFICATION.md

# 5 worktree-branch SUMMARYs — informs PR descriptions
@.planning/phases/17-ci-stabilization/17-01-SUMMARY.md
@.planning/phases/17-ci-stabilization/17-02-SUMMARY.md
@.planning/phases/17-ci-stabilization/17-03-SUMMARY.md
@.planning/phases/17-ci-stabilization/17-04-SUMMARY.md
@.planning/phases/17-ci-stabilization/17-05-SUMMARY.md

# Plan 17.1-01 SUMMARY — confirms the TS-fix landed
@.planning/phases/17.1-ci-stabilization-followup/17.1-01-SUMMARY.md

<interfaces>
Worktree branch enumeration. Each is currently merged into the integration branch `gsd/phase-17-ci-stabilization` (the current checked-out branch per env), but NOT YET merged to main.

| Worktree branch | Plan | Summary (one-liner for PR title) | Files-touched headline |
|-----------------|------|----------------------------------|------------------------|
| (Plan 17-01)    | F    | Mobile spec selector md→sm + 17-TRIAGE.md scaffolding | 2 spec files + 3 source files (PeriodsEditor, school.settings, PeriodsEditor.spec) |
| (Plan 17-02)    | G    | WebKit-darwin Bus-Error-10 env-classification (docs-only) | 17-TRIAGE.md only |
| (Plan 17-03)    | A+B+C| Primitive lifts: PageShell breadcrumb 44px + tabs.tsx min-h-11 + radio-group.tsx 44px | 3 ui-primitive files |
| (Plan 17-04)    | D    | DataList migration of 5 admin list surfaces (Teacher/Student/Class/Subject/User) | 10 OLD files deleted, 5 NEW XList.tsx files, 5 routes updated |
| (Plan 17-05)    | E    | 14-spec sweep + skip-with-reason + Phase 17.1 SCOPE draft | 14 spec files + 17-TRIAGE.md + PHASE-17.1-SCOPE.md + deferred-items.md |

Per CONTEXT D-09 STRICT: "Each branch merge is a SEPARATE PR (do NOT bundle into one mega-merge). Each PR title: `merge(17-NN): <plan-summary>` to preserve atomic-commit lineage."

**Resolution:** Use `gh pr create` against EACH worktree branch (the .claude/worktrees/agent-* paths show 5 worktrees still locked — these are the source branches). If the worktree branches no longer exist as standalone refs, the executor MUST flag this as a precondition violation and ESCALATE — do NOT bundle.

Smoke-PR commands (D-10):
```
git fetch origin
git -C <smoke-pr-checkout> rebase origin/main
git -C <smoke-pr-checkout> push --force-with-lease origin chore/ci-smoke-noop
gh pr ready 9
gh workflow run playwright.yml --ref chore/ci-smoke-noop
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-merge precondition checks (verify Plan 17.1-01 landed; verify worktree branch refs exist)</name>
  <read_first>
    - .planning/phases/17.1-ci-stabilization-followup/17.1-01-SUMMARY.md (must exist; must reference the fix(dashboard) commit)
    - .planning/phases/17-ci-stabilization/17-01-SUMMARY.md through 17-05-SUMMARY.md (so PR descriptions can reference the actual ship-deltas)
  </read_first>
  <files>(none — read-only verification + git ref enumeration)</files>
  <action>
    1. Confirm Plan 17.1-01 fix landed:
       `git log --oneline -20 | grep "fix(dashboard): resolve UseQueryResult"` — must return exactly one line. If empty -> STOP, Plan 17.1-01 did not complete.

    2. Confirm `tsc -p tsconfig.app.json --noEmit` still exits 0 on current branch:
       `pnpm --filter @schoolflow/web exec tsc -p tsconfig.app.json --noEmit; echo "TSC=$?"` — must show TSC=0.

    3. Enumerate the 5 worktree branch refs. Per `git worktree list`:
       - .claude/worktrees/agent-a2c5bc8a9e6ef8e9d (commit c8b19b3)
       - .claude/worktrees/agent-a377918ffb615786f (commit d081047)
       - .claude/worktrees/agent-a990c22a8e43baf35 (commit e2fd94f)
       - .claude/worktrees/agent-aa3d392641273fc41 (commit 17e192e)
       - .claude/worktrees/agent-ae18f78ebce356848 (commit b78023b)

       Map each worktree HEAD commit -> owning plan (17-01..17-05) by reading the worktree branch HEAD commit messages:
       `for wt in .claude/worktrees/agent-*; do echo "=== $wt ==="; git -C "$wt" log --oneline -5; git -C "$wt" branch --show-current; done`

       Build a mapping table `{branch_name -> plan_id (17-NN) -> ship-summary one-liner}`. This drives PR titles in Task 2. If any worktree branch is unidentifiable, STOP and escalate to user — do NOT guess merge order.

    4. Verify branch protection on main:
       `gh api repos/martinvidec/openaustria-school-flow/branches/main/protection 2>/dev/null | head -20`

    5. Confirm no in-flight conflicting PRs:
       `gh pr list --state open --base main --json number,title,headRefName`
       Document. Flag any PR overlapping with the 5 worktree-plan files_modified sets.

    6. Confirm smoke-PR is open:
       `gh pr view 9 --json state,baseRefName,headRefName,mergeable` — expected state=OPEN, baseRefName=main, headRefName=chore/ci-smoke-noop. If state=CLOSED or MERGED, STOP and escalate.
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && git log --oneline -20 | grep -c "fix(dashboard): resolve UseQueryResult" && pnpm --filter @schoolflow/web exec tsc -p tsconfig.app.json --noEmit; echo "TSC=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -20 | grep -c "fix(dashboard): resolve UseQueryResult"` returns 1.
    - `pnpm --filter @schoolflow/web exec tsc -p tsconfig.app.json --noEmit` exits 0 (TSC=0).
    - 5 worktree branches individually identifiable; mapping table `{17-01: <branch>, ..., 17-05: <branch>}` documented in this task's notes / SUMMARY. **HARD GATE per plan-checker warning 1:** if ANY worktree-to-plan-id mapping is ambiguous after running the `git -C "$wt" log --oneline -5` enumeration, the executor MUST stop and escalate to the user with the message `"Unmappable worktree: <path> at <commit>. Cannot proceed with ordered merge per CONTEXT D-08."` Do NOT guess merge order — out-of-order merge invalidates D-08 and may corrupt the smoke-PR rebase.
    - `gh pr view 9 --json state` returns "state":"OPEN" or "DRAFT".
    - No open PR against main has overlap with the 5 worktree-plan files_modified sets.
  </acceptance_criteria>
  <done>Preconditions satisfied: TS-fix in, tsc clean, 5 worktree branches mapped, smoke-PR open, no conflicting in-flight PRs.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create + sequentially merge 5 PRs (17-01 → 17-02 → 17-03 → 17-04 → 17-05) per CONTEXT D-08/D-09</name>
  <read_first>
    - Task 1 mapping table (worktree -> plan-id -> ship-summary)
    - .planning/phases/17.1-ci-stabilization-followup/17.1-CONTEXT.md (D-08 fixed order; D-09 SEPARATE PRs, NO mega-merge, title `merge(17-NN): <summary>`; D-10 smoke-PR re-trigger AFTER all 5 merge)
    - User memory `feedback_phase_branch_discipline.md` (parallel branches OK if ordered-merge — this is the enforcement step)
  </read_first>
  <files>(none — orchestration only; PR squash-merges land squash commits on origin/main)</files>
  <action>
    Drive 5 sequential PR creations + merges via `gh` CLI. Each PR is independently created, CI-checked, and merged (or HALTED on RED). Human-driven (checkpoint) because per-PR CI failures need human triage decisions per CONTEXT D-09 (no --admin override on RED).

    For EACH of 5 worktree branches, in the LOCKED order 17-01 -> 17-02 -> 17-03 -> 17-04 -> 17-05:

    Step A — Create PR:
    `gh pr create --base main --head <branch-from-Task-1-mapping> --title "merge(17-NN): <one-line plan-summary from Task-1 mapping>" --body "<HEREDOC body referencing 17-NN-SUMMARY.md bullets + 'Part of Phase 17.1 ordered-merge per CONTEXT D-08/D-09' + Co-Authored-By trailer>"`

    Step B — Wait for CI:
    `gh pr checks <pr-number> --watch`

    Step C (GREEN) — Merge with squash (preserves PR title as merge-commit subject):
    `gh pr merge <pr-number> --squash --delete-branch=false`

    Step D (RED) — STOP per D-09. Add a row to 17-TRIAGE.md ## Smoke-PR result documenting the failure. Surface to user with the failing-step name + run URL. Do NOT proceed to next PR until user resolves. PROHIBITED: --admin override.

    Repeat for ALL 5 PRs. After all 5 merge, verify the merged-PR list with:
    `gh pr list --state merged --search 'merge(17-0' --base main --limit 10 --json number,title,mergedAt`
    Expected: ≥5 entries in chronological order matching 17-01 -> 17-05.

    See `<what-built>` for the full step-by-step gh-cli sequence and `<how-to-verify>` for post-merge verification.
  </action>
  <what-built>
    Five sequential PRs from worktree branches into main, in the LOCKED order 17-01 → 17-02 → 17-03 → 17-04 → 17-05. Each PR runs CI independently. Each PR title follows the format `merge(17-NN): <plan-summary>`.

    For each PR (in order):

    ```
    # Step A: Create PR for branch <17-NN>
    gh pr create \
      --base main \
      --head <branch-from-Task-1-mapping> \
      --title "merge(17-NN): <plan-summary>" \
      --body "$(cat <<'EOF'
    ## Summary

    Phase 17 worktree merge: Plan 17-NN
    - <bullet 1 from .planning/phases/17-ci-stabilization/17-NN-SUMMARY.md>
    - <bullet 2>
    - <bullet 3>

    ## Test plan
    - [ ] CI Build Web step passes (TS compile)
    - [ ] CI Run Playwright tests step passes (or fails only on previously-deferred clusters per 17-TRIAGE.md)

    Part of Phase 17.1 ordered-merge per CONTEXT D-08/D-09.

    Generated with Claude Code (https://claude.com/claude-code)
    EOF
    )"

    # Step B: Wait for CI to complete on this PR
    gh pr checks <pr-number> --watch

    # Step C (GREEN): merge with squash (preserves the 17-NN PR title as the merge commit subject):
    gh pr merge <pr-number> --squash --delete-branch=false

    # Step D (RED): STOP. Per D-09: "if any RED -> STOP, document, escalate (do NOT force-merge with --admin)"
    #   - Add a row to 17-TRIAGE.md ## Smoke-PR result documenting the failure
    #   - Surface to user with the failing-step name + run URL
    #   - Do NOT proceed to the next PR until the user resolves
    ```

    Repeat for ALL 5 PRs in the FIXED order.
  </what-built>
  <how-to-verify>
    After each PR merge, verify before proceeding to the next:

    1. Confirm merge landed: `gh pr view <pr-number> --json state,mergedAt | grep -E '"state":"MERGED"'`
    2. Confirm main HEAD advanced: `git fetch origin && git log origin/main --oneline -5` — most recent commit subject should match the merged PR title.
    3. Confirm CI on main is GREEN after each merge: `gh run list --branch main --limit 1 --json conclusion,workflowName` — expected `"conclusion":"success"`. If RED -> STOP, escalate, do NOT proceed.

    After ALL 5 PRs merge, verify the merged-PR list:
    `gh pr list --state merged --search 'merge(17-0' --base main --limit 10 --json number,title,mergedAt`
    Expected: at least 5 entries, in chronological order matching 17-01 -> 17-02 -> 17-03 -> 17-04 -> 17-05.

    Why this is a checkpoint:human-action: branch coordination requires user awareness because (a) PR creation may require auth refresh, (b) any RED CI on a per-PR basis is a STOP signal that needs human triage (per D-09 — no --admin), (c) the user owns the merge button if branch protection requires admin approval.
  </how-to-verify>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && gh pr list --state merged --search 'merge(17-0' --base main --limit 10 --json title -q '.[] | .title' 2>/dev/null | wc -l</automated>
  </verify>
  <resume-signal>Type "all 5 merged" once `gh pr list --state merged --search 'merge(17-0' --base main` returns 5 entries AND `gh run list --branch main --limit 1` shows the most recent main CI run is GREEN. If ANY PR went RED, type "halted at PR <N>" with the failing-step name.</resume-signal>
  <acceptance_criteria>
    - `gh pr list --state merged --search 'merge(17-0' --base main --limit 10 --json number,title,mergedAt | jq 'length'` returns ≥ 5.
    - `gh pr list --state merged --search 'merge(17-0' --base main --limit 10 --json title | jq -r '.[] | .title'` outputs 5 titles, each matching `merge\(17-0[1-5]\):` regex.
    - Chronological order of `mergedAt` timestamps matches the LOCKED order 17-01 -> 17-02 -> 17-03 -> 17-04 -> 17-05 (verify with `jq 'sort_by(.mergedAt) | .[] | .title'`).
    - `gh run list --branch main --limit 1 --json conclusion -q '.[0].conclusion'` returns `success`.
    - Zero `--admin` override flags appear in any merge command audit log (per D-09 prohibition).
  </acceptance_criteria>
  <done>All 5 PRs merged in correct order; main CI green at post-merge HEAD; no --admin overrides used.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Re-trigger smoke-PR on new main HEAD; observe CI; classify outcome (GREEN -> green-gate; RED -> cluster-bucketing) per CONTEXT D-10</name>
  <read_first>
    - .planning/phases/17.1-ci-stabilization-followup/17.1-CONTEXT.md (D-10 smoke-PR rebase + re-trigger; D-15 verifier-gate semantics; D-18 TRIAGE update)
    - .planning/phases/17-ci-stabilization/deferred-items.md (Cluster A-F catalog — used to classify any residual RED)
    - .planning/phases/17-ci-stabilization/17-TRIAGE.md (## Smoke-PR result section — current RED state to be replaced)
  </read_first>
  <files>
    .planning/phases/17-ci-stabilization/17-TRIAGE.md
  </files>
  <action>
    Drive the smoke-PR rebase + CI re-trigger + outcome classification per CONTEXT D-10. Human-driven (checkpoint) because (a) the smoke-PR is the LITERAL phase verifier-gate, (b) any unexpected rebase conflict needs human triage, (c) the GREEN/RED classification determines whether plans 17.1-03..05 become mandatory or optional — a scope decision the user should ratify.

    Step A — Update local main + rebase smoke-PR (use temp worktree to honor branch invariant; do NOT switch branches in main worktree):
    `git fetch origin`
    `git worktree add /tmp/smoke-pr-rebase chore/ci-smoke-noop`
    `cd /tmp/smoke-pr-rebase && git rebase origin/main`
    (If conflicts: STOP, escalate. The smoke-PR is a no-op diff so conflicts should not occur.)
    `git push --force-with-lease origin chore/ci-smoke-noop`
    `cd <project-root> && git worktree remove /tmp/smoke-pr-rebase`

    Step B — Wait for CI on the re-triggered run:
    `gh pr checks 9 --watch`

    Step C — Capture new run ID, conclusion, URL, and main SHA. **Per plan-checker warning 2:** use `gh run list --branch ... --workflow ... --limit 1` (newer-run guarantee) instead of `gh pr checks 9 --json link` which can race with stale runs:
    `EXPECTED_HEAD_SHA=$(git -C /tmp/smoke-pr-rebase rev-parse HEAD)` (capture the post-rebase SHA before /tmp cleanup)
    `NEW_RUN_JSON=$(gh run list --branch chore/ci-smoke-noop --workflow playwright.yml --limit 1 --json databaseId,conclusion,headSha,createdAt -q '.[0]')`
    `NEW_RUN_ID=$(echo "$NEW_RUN_JSON" | jq -r .databaseId)`
    `NEW_RUN_CONCLUSION=$(echo "$NEW_RUN_JSON" | jq -r .conclusion)`
    `NEW_RUN_HEAD_SHA=$(echo "$NEW_RUN_JSON" | jq -r .headSha)`
    `NEW_RUN_URL="https://github.com/martinvidec/openaustria-school-flow/actions/runs/$NEW_RUN_ID"`
    `NEW_MAIN_SHA=$(git rev-parse origin/main)`

    **Newer-run guarantee assertions** (FAIL-CLOSED — abort outcome classification if either fails):
    - `[[ "$NEW_RUN_HEAD_SHA" == "$EXPECTED_HEAD_SHA" ]]` — confirms the captured run is for the post-rebase SHA, NOT a stale pre-rebase run
    - `[[ "$NEW_RUN_ID" -gt 25251866945 ]]` — confirms the captured run ID is newer than the original failed run 25251866945 (numeric comparison)
    If EITHER assertion fails, the run-list query captured a stale run; wait 30s for the new run to register and re-query, OR escalate to user.

    Step D — Outcome classification + TRIAGE update per CONTEXT D-10/D-18:

    **GREEN path:** Truth 1 satisfied. Plans 17.1-03..05 become OPTIONAL skip-and-defer. Update 17-TRIAGE.md ## Smoke-PR result with NEW_RUN_URL + NEW_MAIN_SHA + `Status: GREEN`. Proceed to Task 4 (STATE.md transition).

    **RED with classified Cluster A-F failures (per deferred-items.md):** Truth 1 NOT yet satisfied. Update 17-TRIAGE.md documenting WHICH clusters fired (e.g., "Cluster A: 5 specs RED -> Plan 17.1-03 MANDATORY"; "Cluster B: 15 specs RED -> Plan 17.1-04 MANDATORY"). Plans 17.1-03..17.1-05 become MANDATORY for firing clusters only. Hand-off: Plan 17.1-02 is COMPLETE-WITH-RESIDUALS.

    **RED with NEW failures NOT in Cluster A-F:** Document each new failure as a new TRIAGE row. Per CONTEXT deferred-ideas (Phase 17.2): if surviving non-cluster failures cannot be deferred, spawn Phase 17.2. Surface to user for triage decision (defer vs spawn 17.2).

    Update 17-TRIAGE.md ## Smoke-PR result section regardless of outcome (GREEN or RED). The update is the D-18 deliverable.
  </action>
  <what-built>
    The smoke-PR `chore/ci-smoke-noop` (PR #9) is rebased onto the new `origin/main` HEAD (which now contains all 5 merged worktree fixes), CI is re-triggered, and the result is classified into one of three outcomes (GREEN / RED-CLUSTERS / RED-NEW-FAILURES). The 17-TRIAGE.md ## Smoke-PR result section is updated regardless of outcome (D-18 deliverable).
  </what-built>
  <how-to-verify>
    1. Confirm smoke-PR is rebased on new main: `gh pr view 9 --json baseRefOid,potentialMergeCommit` — baseRefOid should match the post-merge `git rev-parse origin/main` SHA.
    2. Confirm the most-recent CI run on `chore/ci-smoke-noop` is from AFTER the rebase push (timestamp newer than Plan 17.1-01 commit time).
    3. Open `https://github.com/martinvidec/openaustria-school-flow/pull/9` (or `gh pr view 9 --web`) and inspect the new CI run:
       - "Build Web" step → expected GREEN (regression closed by Plan 17.1-01)
       - "Run Playwright tests" step → may be GREEN (Truth 1) OR RED (residuals to bucket)
    4. Confirm 17-TRIAGE.md ## Smoke-PR result section is updated (current text says "Status: RED — D-16 fires"; this MUST flip to "Status: GREEN" + new run URL OR "Status: RED-WITH-CLUSTERS" + cluster classification).
    5. Get final conclusion: `gh pr checks 9 --json conclusion,name | jq -r '.[] | "\(.name): \(.conclusion)"'`
  </how-to-verify>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && gh pr checks 9 --json conclusion,name -q '.[] | select(.name | contains("Run Playwright tests")) | .conclusion' 2>/dev/null && grep -c "GREEN\|RED-WITH-CLUSTERS\|RED-NEW-FAILURES" .planning/phases/17-ci-stabilization/17-TRIAGE.md</automated>
  </verify>
  <resume-signal>Type one of: "GREEN" (Truth 1 satisfied; Plans 17.1-03..05 are optional), "RED-CLUSTERS-A,B,C..." (specify firing clusters from A-F; those plans become mandatory), or "RED-NEW-FAILURES" (paste new failure list — triggers Phase 17.2 spawn discussion). Confirm "TRIAGE updated" once the ## Smoke-PR result section reflects the new outcome.</resume-signal>
  <acceptance_criteria>
    - `gh pr view 9 --json baseRefOid -q .baseRefOid` matches post-merge `git rev-parse origin/main` SHA (rebase landed).
    - The new CI run on `chore/ci-smoke-noop` shows the "Build Web" step with `conclusion=success` (Plan 17.1-01 fix worked through CI).
    - `grep -c "GREEN\|RED-WITH-CLUSTERS\|RED-NEW-FAILURES" .planning/phases/17-ci-stabilization/17-TRIAGE.md` returns ≥ 1 in a NEW heading under `## Smoke-PR result`.
    - The new run URL `https://github.com/martinvidec/openaustria-school-flow/actions/runs/<NEW_RUN_ID>` AND new main SHA appear in updated `## Smoke-PR result` section.
    - If GREEN: `gh pr checks 9 --json conclusion -q '.[] | select(.name | contains("Run Playwright tests")) | .conclusion'` returns `success` (D-15 truth satisfied).
    - If RED: every firing failure is classified into Cluster A/B/C/D/E/F per deferred-items.md OR documented as a NEW failure with explicit "needs Phase 17.2 escalation" annotation.
  </acceptance_criteria>
  <done>Smoke-PR re-ran on new main HEAD; outcome classified (GREEN or RED-with-buckets); 17-TRIAGE.md ## Smoke-PR result section reflects current truth; user has acknowledged outcome.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Atomic STATE.md transition (Phase 17 -> complete + Phase 17.1 -> complete) per CONTEXT D-19 — only if GREEN</name>
  <read_first>
    - .planning/STATE.md (current state — Phase 17 currently `done-with-followup`; Phase 17.1 currently `executing`)
    - .planning/phases/17.1-ci-stabilization-followup/17.1-CONTEXT.md (D-19: BOTH transitions in same atomic STATE.md commit; CAUSALLY linked by green-PR evidence)
    - Task 3 outcome (GREEN vs RED — D-19 only fires on GREEN)
  </read_first>
  <files>.planning/STATE.md</files>
  <action>
    GUARD: Only execute this task if Task 3 result was GREEN. If RED-WITH-CLUSTERS or RED-NEW-FAILURES -> SKIP this task. Hand off to Plans 17.1-03..05 (mandatory clusters) OR escalate to Phase 17.2 (new failures). Do NOT update STATE.md to complete with red CI.

    If Task 3 was GREEN:

    1. Read current `.planning/STATE.md` to identify the Phase 17 + Phase 17.1 status entries.

    2. Update both transitions in a SINGLE commit (per D-19 atomic causality):
       - Phase 17: status -> `complete` (was `done-with-followup` or `executing`)
       - Phase 17.1: status -> `complete` (was `executing` or `planning`)
       - Update `last_activity` to today's date with note: `Phase 17.1 complete — smoke-PR <run-url> green`
       - Update `progress.completed_phases` count.

    3. Stage + commit:
       `git add .planning/STATE.md .planning/phases/17-ci-stabilization/17-TRIAGE.md`
       `git commit -m "docs(17.1): close Phase 17 + 17.1 — smoke-PR green, verifier-gate satisfied"` (HEREDOC body: Phase 17 done-with-followup -> complete; Phase 17.1 executing -> complete; Evidence: smoke-PR <NEW_RUN_URL> on main HEAD <NEW_MAIN_SHA> went GREEN at Run Playwright tests step WITHOUT --admin override; D-15 SATISFIED; D-16 closed; 17-TRIAGE.md ## Smoke-PR result updated per D-18; Co-Authored-By trailer)

    4. Confirm both transitions landed via post-commit `grep` of the file. The commit + push is independent — push happens later when user explicitly approves.
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && grep -E "^(stopped_at|status):" .planning/STATE.md | head -5 && git log --oneline -1 | grep "docs(17.1): close Phase 17 + 17.1"</automated>
  </verify>
  <acceptance_criteria>
    - GUARD-CONDITIONAL: Task 3 must have classified GREEN. If Task 3 was RED, this task is correctly SKIPPED — verify by checking Task 3 SUMMARY before flagging this acceptance as failed.
    - If GREEN executed: `git log --oneline -1` shows commit `docs(17.1): close Phase 17 + 17.1 — smoke-PR green, verifier-gate satisfied`.
    - If GREEN executed: `grep -A2 "Phase 17:" .planning/STATE.md | grep -c "complete"` returns ≥ 1.
    - If GREEN executed: `grep -A2 "Phase 17.1:" .planning/STATE.md | grep -c "complete"` returns ≥ 1 (note: depending on STATE.md format, may be in milestone progress block or accumulated context).
    - If GREEN executed: commit references the green-PR run URL and main SHA in the body (`git show HEAD --no-patch | grep -c "actions/runs/"` returns ≥ 1).
  </acceptance_criteria>
  <done>If GREEN: atomic STATE.md commit landed transitioning Phase 17 + 17.1 -> complete. If RED: task correctly skipped, hand-off to mandatory cluster plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo -> GitHub Actions | PR push triggers CI run; CI runs Playwright against ephemeral environment; CI conclusion is the verifier-gate signal |
| Worktree branches -> main | Code crosses from per-plan integration into the trunk; merge is the trust elevation point |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-04 | Tampering | gh pr merge command-line flags | mitigate | Per CONTEXT D-09: NEVER use `--admin` override on any merge in this plan. The whole point of the verifier-gate is to prove CI works WITHOUT admin override. Acceptance criteria explicitly check zero --admin flags in merge audit log. |
| T-17.1-05 | Tampering | git push --force-with-lease on chore/ci-smoke-noop | mitigate | `--force-with-lease` (NOT `--force`) ensures the push only succeeds if the remote ref matches the expected pre-rebase SHA. Prevents clobbering concurrent updates. The smoke-PR is a no-op diff branch with single owner — concurrent updates not expected. |
| T-17.1-06 | Repudiation | STATE.md transition with no green-PR evidence | mitigate | Per D-19: STATE.md commit body MUST reference the green-PR run URL + main SHA. Acceptance criterion explicitly greps for `actions/runs/` in the commit body — no transition without evidence. |
| T-17.1-07 | Elevation of Privilege | Branch protection bypass | mitigate | Task 1 step 4 verifies branch protection on main is active. If a merge succeeds without going through PR review, that's a protection-policy regression — surface to user (out-of-scope-for-this-plan to fix branch protection itself). |
| T-17.1-08 | Information Disclosure | n/a | accept | No production data, no secrets, no PII touched in this plan. PR descriptions reference plan summaries (public). |
| T-17.1-09 | Denial of Service | Mass-trigger of CI runs | accept | The 5-PR sequential merge triggers ≤5 sequential CI runs + 1 smoke-PR re-run = 6 total. Within normal CI budget. |
</threat_model>

<verification>
After Task 4 completes (or is correctly skipped):

1. Truth 1 (GREEN path): `gh pr checks 9 --json conclusion -q '.[] | select(.name | contains("Run Playwright tests")) | .conclusion'` returns `success` AND `gh pr view 9 --json baseRefOid -q .baseRefOid` matches current `origin/main` HEAD.
2. Truth 2: `gh pr list --state merged --search 'merge(17-0' --base main --limit 10 --json title -q '.[] | .title' | sort | uniq -c` shows 5 distinct titles each starting with `merge(17-0` and ending with the corresponding plan suffix.
3. Truth 3: `grep -c "GREEN\|RED-WITH-CLUSTERS" .planning/phases/17-ci-stabilization/17-TRIAGE.md` ≥ 1 in the `## Smoke-PR result` section AND the section now references the new run URL/SHA.
4. Truth 4 (GREEN path only): `grep -E "Phase 17.*complete" .planning/STATE.md` shows both Phase 17 + Phase 17.1 in `complete` status; `git log --oneline -3 | grep -c "docs(17.1): close"` returns 1.

If GREEN — Phase 17.1 verifier-gate satisfied; Plans 17.1-03..05 are OPTIONAL deferral candidates.
If RED-WITH-CLUSTERS — Plans 17.1-03..05 are MANDATORY for firing clusters; this plan COMPLETE-WITH-RESIDUALS.
If RED-NEW-FAILURES — escalate to Phase 17.2 spawn discussion with user.
</verification>

<success_criteria>
- [ ] Task 1 preconditions satisfied (Plan 17.1-01 commit present; tsc clean; 5 worktree branches mapped; smoke-PR open)
- [ ] Task 2 — 5 PRs created and merged in the LOCKED order 17-01 -> 17-02 -> 17-03 -> 17-04 -> 17-05 (D-08), each as a SEPARATE PR (D-09), zero `--admin` overrides
- [ ] Task 3 — smoke-PR rebased onto new main HEAD; CI re-triggered; outcome classified (GREEN or RED-with-buckets); 17-TRIAGE.md ## Smoke-PR result updated (D-18)
- [ ] Task 4 — IF GREEN: atomic STATE.md commit transitioning Phase 17 + 17.1 -> complete (D-19). IF RED: task skipped with documented reason; mandatory cluster plans queued.
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-ci-stabilization-followup/17.1-02-SUMMARY.md` with:
- Branch-to-plan mapping table from Task 1
- 5 merged PR numbers + URLs in chronological merge order
- Smoke-PR re-run URL + conclusion (GREEN or RED with cluster classification)
- Outcome decision: PHASE-COMPLETE (if GREEN) OR RESIDUALS-DEFERRED-TO-PLANS-17.1-03..05 (if RED-with-clusters) OR ESCALATED-TO-17.2 (if new failures)
- STATE.md transition status (executed if GREEN, correctly skipped if RED)
</output>
