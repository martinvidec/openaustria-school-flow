---
plan: 9.1-01
phase: 09.1-runtime-blockers-fix
title: Runtime Blockers Fix
wave: 1
depends_on: []
gap_closure: true
autonomous: true
files_modified:
  - apps/api/src/modules/import/import.controller.ts
  - docker/docker-compose.yml
  - docker/docker-compose.prod.yml
  - docker/.env.example
  - packages/shared/src/constants/permissions.ts
  - .planning/phases/06-substitution-planning/06-VERIFICATION.md
requirements:
  - IMPORT-01
  - IMPORT-02
  - TIME-04
---

# Plan 9.1-01: Runtime Blockers Fix

<objective>
Apply 5 targeted fixes from milestone audit findings: ImportController prefix bug, Solver callback URL config, PermissionSubject enum entries, env documentation, stale Phase 6 truth.
</objective>

<must_haves>
truths:
  - ImportController @Controller decorator does NOT contain 'api/v1' prefix
  - docker-compose.yml api service has API_INTERNAL_URL env var set
  - docker-compose.prod.yml api service has API_INTERNAL_URL env var set
  - .env.example documents API_INTERNAL_URL with comment
  - PermissionSubject enum contains HOMEWORK, EXAM, IMPORT, PUSH entries
  - 06-VERIFICATION.md Truth #7 status updated to PASSED
artifacts:
  - apps/api/src/modules/import/import.controller.ts
  - docker/docker-compose.yml
  - docker/.env.example
  - packages/shared/src/constants/permissions.ts
key_links:
  - from: docker-compose api service
    to: API_INTERNAL_URL env var
    via: environment block
</must_haves>

<task id="1" type="auto">
  <name>Fix ImportController double prefix and add API_INTERNAL_URL config</name>

  <files>
    - apps/api/src/modules/import/import.controller.ts
    - docker/docker-compose.yml
    - docker/docker-compose.prod.yml
    - docker/.env.example
  </files>

  <read_first>
    - apps/api/src/modules/import/import.controller.ts (current state, line 43)
    - apps/api/src/main.ts (verify global prefix is 'api/v1')
    - docker/docker-compose.yml (api service env block)
    - docker/docker-compose.prod.yml (api service env block)
    - docker/.env.example (current env vars)
    - .planning/v1.0-MILESTONE-AUDIT.md (Findings 1, 2, 8 for full context)
  </read_first>

  <action>
    1. **Fix import.controller.ts line 43:** Change `@Controller('api/v1/schools/:schoolId/import')` to `@Controller('schools/:schoolId/import')`. This is the same fix already applied to room.controller.ts, timetable.controller.ts, and others in Phase 5 Plan 10.

    2. **Update docker/docker-compose.yml:** Add to the api service environment block:
       ```yaml
       API_INTERNAL_URL: http://api:3000/api/v1/api/internal/solver
       ```
       (Note: The solver POSTs to {callbackUrl}{path} where path is /progress/{runId}, so the callback URL must include the prefix + controller path.)

    3. **Update docker/docker-compose.prod.yml:** Same env var addition.

    4. **Update docker/.env.example:** Add documented entry:
       ```
       # Internal URL the Java solver uses to call back to the NestJS API.
       # Must include the global prefix 'api/v1' AND the SolverCallbackController prefix 'api/internal/solver'.
       # In Docker Compose, this is the service name + port.
       API_INTERNAL_URL=http://api:3000/api/v1/api/internal/solver
       ```
  </action>

  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/apps/api && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "api/v1/schools/:schoolId/import" apps/api/src/modules/import/import.controller.ts` returns 0
    - `grep -c "@Controller('schools/:schoolId/import')" apps/api/src/modules/import/import.controller.ts` returns 1
    - `grep -c "API_INTERNAL_URL" docker/docker-compose.yml` returns at least 1
    - `grep -c "API_INTERNAL_URL" docker/.env.example` returns at least 1
    - TypeScript compiles cleanly (no new errors)
  </acceptance_criteria>

  <done>ImportController routes resolve correctly. API_INTERNAL_URL configured for solver callbacks in both dev and prod docker-compose files and documented in .env.example.</done>
</task>

<task id="2" type="auto">
  <name>Add Phase 8-9 entries to PermissionSubject enum and fix stale Phase 6 truth</name>

  <files>
    - packages/shared/src/constants/permissions.ts
    - .planning/phases/06-substitution-planning/06-VERIFICATION.md
  </files>

  <read_first>
    - packages/shared/src/constants/permissions.ts (current PermissionSubject enum)
    - .planning/phases/06-substitution-planning/06-VERIFICATION.md (Truth #7 around line 48)
    - apps/api/prisma/seed.ts (lines 130-132 to confirm subject names used at runtime)
  </read_first>

  <action>
    1. **Update PermissionSubject enum** in packages/shared/src/constants/permissions.ts: Add these entries to the enum (use lowercase string values matching seed.ts usage):
       ```typescript
       HOMEWORK = 'homework',
       EXAM = 'exam',
       IMPORT = 'import',
       PUSH = 'push',
       ```
       Place them after the existing CONVERSATION/MESSAGE/POLL entries.

    2. **Update 06-VERIFICATION.md Truth #7:** Find Truth #7 (around line 48) which currently shows FAILED status for ChangeIndicator tests. Change status to VERIFIED with note:
       ```
       Tests fixed in commit d2a5dc9 (2026-04-06) — ChangeIndicator now verifies styling-only wrapper. All 14 web tests pass.
       ```
       This is a documentation cleanup — the tests are already passing.

    3. **Run shared package build:** After enum changes, the shared package needs to be rebuilt for type consumers.
  </action>

  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && grep -c "HOMEWORK\|EXAM\|IMPORT\|PUSH" packages/shared/src/constants/permissions.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "HOMEWORK = 'homework'" packages/shared/src/constants/permissions.ts` returns 1
    - `grep -c "EXAM = 'exam'" packages/shared/src/constants/permissions.ts` returns 1
    - `grep -c "IMPORT = 'import'" packages/shared/src/constants/permissions.ts` returns 1
    - `grep -c "PUSH = 'push'" packages/shared/src/constants/permissions.ts` returns 1
    - 06-VERIFICATION.md Truth #7 no longer shows FAILED status
    - Backend tests still pass after shared package rebuild
  </acceptance_criteria>

  <done>PermissionSubject enum complete with all phase 8-9 subjects. Phase 6 verification documentation updated to reflect resolved test state.</done>
</task>

## Verification

After both tasks:
- `cd apps/api && pnpm test -- --run` — backend tests still pass
- `pnpm --filter @schoolflow/web tsc --noEmit` — frontend compiles
- Manual: `docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml config --quiet` exits 0
