---
phase: 10.3-e2e-harness-per-role-smoke
plan: 01
subsystem: testing
tags: [e2e, playwright, harness, login-helper, global-setup, multi-role]

# Dependency graph
requires:
  - phase: 10.2-e2e-admin-console-gap-closure
    provides: admin-only loginAsAdmin + getAdminToken helpers (apps/web/e2e/helpers/login.ts), Playwright desktop/mobile projects, CI workflow
provides:
  - Parametric loginAsRole(page, role) covering admin/schulleitung/lehrer/eltern/schueler with env-var overrides and seed-default fallbacks
  - getRoleToken(request, role) for API-level direct-access-grant in multi-role fixtures
  - Playwright globalSetup that health-checks API + Vite and fails fast with a readable message
  - Playwright globalTeardown stub reserved for future multi-role fixture cleanup
  - Explicit HTML reporter output at apps/web/playwright-report for CI artifacts
affects: [10.3-02-per-role-smokes, 10.4-admin-ops-people, 10.5-admin-ops-operations]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-role credential map (CREDENTIALS: Record<Role, Credential>) with E2E_<ROLE>_USER / E2E_<ROLE>_PASS env fallbacks"
    - "Generic login helpers NEVER navigate past Keycloak — specs own their post-login routing"
    - "Playwright globalSetup uses AbortController-backed fetch with 5s timeout and treats 401 as live-but-unauthorized"

key-files:
  created:
    - apps/web/e2e/helpers/global-setup.ts
    - apps/web/e2e/helpers/global-teardown.ts
    - .planning/phases/10.3-e2e-harness-per-role-smoke/deferred-items.md
  modified:
    - apps/web/e2e/helpers/login.ts
    - apps/web/playwright.config.ts
    - apps/web/e2e/admin-school-settings.spec.ts

key-decisions:
  - "Removed admin-specific sidebar click from the generic login helper — embedding admin navigation inside a multi-role helper would leak into schulleitung/lehrer/eltern/schueler smokes. Admin specs already call page.goto('/admin/school/settings') explicitly; the one spec that didn't (admin-school-settings.spec.ts beforeEach) got a 1-line goto restoring the pre-10.3 shape."
  - "CREDENTIALS kept as a top-level Record<Role, Credential> frozen literal instead of building per-call — resolves env vars once at module load time, matches how 10.2-era ADMIN_USER/ADMIN_PASS were declared."
  - "loginAsAdmin + getAdminToken kept as thin wrappers (not renamed) so zero 10.2-era spec imports break. Saved ~10 spec edits vs. a rename refactor."
  - "globalSetup lands on 401-as-success for health-check targets because the web root (/) redirects to Keycloak but the server IS up; treating 401 as failure would create a false-negative. API health returns 200 so only the Vite side could plausibly hit 401, and even there we want 'process alive' semantics, not 'endpoint reachable + public'."
  - "globalTeardown shipped as an explicit no-op (not omitted) so the hook is wired + documented — 10.3-02+ can fill it in without editing playwright.config.ts."
  - "Reporter config promoted to explicit list + html array. HTML output at playwright-report/ matches Plan 10.2-05 CI workflow assumption (artifact upload path)."

patterns-established:
  - "Login-helper neutrality: multi-role test helpers land callers on '/' post-login and let specs route themselves. Admin-specific post-login navigation (if any) lives in spec beforeEach, not in the helper."
  - "Seed-default fallback chain: CREDENTIALS[role].user = env ?? seed-default — makes E2E work out-of-box on a freshly seeded dev DB AND under CI-provided credential overrides, with no if/else branching."
  - "globalSetup as fail-fast gate: health-check API + web in parallel; ANY failure throws with label + url + reason. Prevents Keycloak redirect loops eating Playwright's 60s timeout when Vite is down."

requirements-completed: []  # Plan declares requirements: [] (E2E-hardening, no REQ-IDs)

# Metrics
duration: 8min
completed: 2026-04-21
---

# Phase 10.3 Plan 01: E2E Harness Extension Summary

**Playwright harness upgraded from admin-only to multi-role: loginAsRole(page, role) + getRoleToken(request, role) across all 5 seed personas, Playwright globalSetup health-checking API + Vite with fail-fast errors, globalTeardown stub for future fixture cleanup.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-04-21T19:29:31Z
- **Completed:** 2026-04-21T19:37:13Z
- **Tasks:** 3
- **Files modified:** 6 (3 created, 3 modified)

## Accomplishments

- `loginAsRole(page, role)` + `CREDENTIALS` map in `apps/web/e2e/helpers/login.ts` — accepts `admin | schulleitung | lehrer | eltern | schueler`, reads `E2E_<ROLE>_USER`/`_PASS` env vars, falls back to seed defaults (`admin123`/`direktor123`/`lehrer123`/`eltern123`/`schueler123`)
- `getRoleToken(request, role)` for API-level direct-access-grant in multi-role fixture helpers
- `loginAsAdmin` + `getAdminToken` kept as thin wrappers — zero import drift for 10.2-era specs
- `apps/web/e2e/helpers/global-setup.ts`: parallel health-checks of API (`http://localhost:3000/api/v1/health`) and Vite (`http://localhost:5173`) with 5s timeout each, 401 tolerated as "live-but-unauthorized"
- `apps/web/e2e/helpers/global-teardown.ts`: explicit no-op stub reserved for 10.3-02+ cleanup
- `playwright.config.ts` wires both hooks via relative-path strings (works under `"type": "module"` ESM), explicit reporter (list + html → `playwright-report/`), trace `retain-on-failure` and screenshot `only-on-failure` retained + commented
- Deferred-items.md logs 3 pre-existing admin-school-settings failures (confirmed via `git stash` + re-run: same 3 fail on pre-10.3 baseline) — 10.3-01 introduces zero new failures

## Task Commits

1. **Task 1: Extend login helper to loginAsRole** — `2308d1c` (refactor)
2. **Task 2: Playwright globalSetup + globalTeardown** — `4b7caab` (feat)
3. **Task 3: Regression verification + deferred-items.md** — `1039761` (docs)

## Files Created/Modified

- `apps/web/e2e/helpers/login.ts` — REWRITTEN: Role type, CREDENTIALS map, loginAsRole, getRoleToken + thin admin wrappers
- `apps/web/e2e/helpers/global-setup.ts` — NEW: API + Vite health-check with fail-fast error messages
- `apps/web/e2e/helpers/global-teardown.ts` — NEW: no-op stub for future cleanup
- `apps/web/playwright.config.ts` — +globalSetup, +globalTeardown, +explicit reporter config
- `apps/web/e2e/admin-school-settings.spec.ts` — +1-line `page.goto('/admin/school/settings')` in beforeEach (plan anticipated this edit)
- `.planning/phases/10.3-e2e-harness-per-role-smoke/deferred-items.md` — logs 3 pre-existing failures

## Decisions Made

See frontmatter `key-decisions`. Core trade-off: the admin-specific sidebar navigation that existed in the 10.2-era helper was moved OUT of the generic helper (would leak into multi-role smokes) and replaced with an explicit `page.goto` in the one spec that relied on implicit post-login routing. This trades 1 extra line in one spec for zero role contamination in 10.3-02+.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added explicit `page.goto('/admin/school/settings')` to `admin-school-settings.spec.ts` beforeEach**

- **Found during:** Task 1 regression (SCHOOL-01 landed on `/` after login and the heading assertion timed out)
- **Issue:** The 10.2-era `loginAsAdmin` helper ended with a click on the "Schulverwaltung" sidebar link. Plan 10.3-01 explicitly asked to remove this from the generic helper. The admin-school-settings.spec was the only consumer that relied on it (every other spec already called `page.goto(...)` after login).
- **Fix:** Added 1 line in the spec's beforeEach: `await page.goto('/admin/school/settings');` directly after `await loginAsAdmin(page);`. Plan explicitly called this out as the expected edit: _"If removing the admin sidebar navigation breaks any existing spec (likely SCHOOL-01), add an explicit `await page.goto('/admin/school/settings')` in that spec's beforeEach or test body."_
- **Files modified:** `apps/web/e2e/admin-school-settings.spec.ts`
- **Verification:** SCHOOL-01 + SCREENSHOT SCHOOL-01 green after fix; full desktop suite shows zero new failures vs. pre-10.3 baseline.
- **Committed in:** `2308d1c` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking — plan-anticipated spec edit)
**Impact on plan:** The plan explicitly predicted this edit would be needed; not true scope creep. No other changes.

## Issues Encountered

### Pre-existing failures (out of scope per Scope Boundary rule)

Final desktop regression: **16 passed / 3 failed (42.7s)**. The 3 failures — `SCHOOL-02`, `SCHOOL-03`, `SCHOOL-05` — all in `admin-school-settings.spec.ts` — were confirmed pre-existing via `git stash` + re-run on the 10.2-era helper (same 3 failures reproduced 1:1). The 10.3-01 refactor introduced **zero new failures**.

Full details + fix recommendations logged in `.planning/phases/10.3-e2e-harness-per-role-smoke/deferred-items.md`:

- **SCHOOL-02**: `strict mode violation` on `getByText('Unterrichtstage')` — caused by 10.2-02 markup promotion (h3 + explanatory paragraph), spec not updated
- **SCHOOL-03**: `Schuljahr angelegt` toast never appears — related to 10.2-03 deferred item #2 (`POST /school-years` with `isActive: true` returns 500)
- **SCHOOL-05**: `PrismaClientInitializationError` in `fixtures/orphan-year.ts` — Playwright worker subprocess env wiring

All three are tracked for a dedicated spec-hygiene follow-up (likely at the tail of Phase 10.2 or in Phase 10.4 harness hardening).

### Helper-consuming specs — all green

Every spec that consumes `loginAsAdmin` / `getAdminToken` passed after the refactor:
- `SCHOOL-01` (admin-school-settings.spec.ts + screenshots.spec.ts SCREENSHOT variant)
- `SCHOOL-04` (A/B toggle persist)
- `ZEIT-01` / `ZEIT-02` (Zeitraster save + dirty-gate)
- `YEAR-02` / `YEAR-03` (Schuljahre delete + activate-switch)
- `WOCH-01` (Wochentage toggle persistence)
- `SILENT-4XX-01..04` (silent 4xx E2E sweep)
- All remaining screenshots.spec.ts captures

## User Setup Required

None — no external service configuration required. The new env vars are additive with seed defaults; existing `.env` files keep working unchanged.

## Next Phase Readiness

10.3-01 delivers the harness extensions 10.3-02 needs:
- Per-role login helpers ready to import from any new spec file
- globalSetup prevents the "Keycloak redirect loop on dead stack" class of spurious failures
- globalTeardown stub is already wired — 10.3-02 can fill it in without config edits
- HTML reporter output at `playwright-report/` matches CI workflow expectations from 10.2-05

**Ready for 10.3-02** (per-role smoke specs: schulleitung opens Substitutions-Admin · lehrer opens Klassenbuch-Lesson · eltern opens Kind-Timetable · schueler opens persönliches Timetable).

---
*Phase: 10.3-e2e-harness-per-role-smoke*
*Completed: 2026-04-21*

## Self-Check: PASSED

All acceptance criteria verified:

- `grep -q "export async function loginAsRole" apps/web/e2e/helpers/login.ts` — MATCH
- `grep -q "export async function loginAsAdmin" apps/web/e2e/helpers/login.ts` — MATCH
- `grep -q "getRoleToken" apps/web/e2e/helpers/login.ts` — MATCH
- `grep -E "schulleitung|lehrer|eltern|schueler"` — 13 matches (all 4 role names present)
- `grep -E "direktor123|lehrer123|eltern123|schueler123"` — 4 matches (all seed-default passwords present)
- `test -f apps/web/e2e/helpers/global-setup.ts` — FOUND
- `test -f apps/web/e2e/helpers/global-teardown.ts` — FOUND
- `grep -q "globalSetup" apps/web/playwright.config.ts` — MATCH (2 occurrences)
- `grep -q "globalTeardown" apps/web/playwright.config.ts` — MATCH (2 occurrences)
- `grep -q "retain-on-failure" apps/web/playwright.config.ts` — MATCH
- SCHOOL-01 green with globalSetup active (stack UP, silent success)
- Fail-fast verified: `E2E_API_URL=http://localhost:19999/doesnotexist` produces `Error: API health-check failed at http://localhost:19999/doesnotexist: fetch failed` before any spec runs
- Full desktop regression: 16 passed / 3 failed; all 3 failures pre-existing (confirmed via `git stash` + re-run on pre-10.3 baseline), logged in deferred-items.md

Commit hashes verified present:
- `2308d1c` — FOUND in `git log --oneline`
- `4b7caab` — FOUND
- `1039761` — FOUND
