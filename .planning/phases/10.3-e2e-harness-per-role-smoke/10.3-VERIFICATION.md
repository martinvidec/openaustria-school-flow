---
phase: 10.3-e2e-harness-per-role-smoke
verified: 2026-04-21T20:15:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
---

# Phase 10.3: E2E Harness + Per-Role Smoke Verification Report

**Phase Goal:** Playwright-Harness für alle 5 Rollen nutzbar machen und je Rolle 1 Smoke-Spec (Login + primäre Aktion). Harness-Extensions: per-role Login-Helper, `globalSetup`/`globalTeardown`, Trace-Retention config, DB-Seeding+Cleanup-Pattern.
**Verified:** 2026-04-21T20:15:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Login-Helper pro Rolle: admin, schulleitung, lehrer, eltern, schueler | ✓ VERIFIED | `loginAsRole(page, role)` at `apps/web/e2e/helpers/login.ts:70` + `CREDENTIALS` map at :36 covers all 5 roles. Seed-default passwords (admin123/direktor123/lehrer123/eltern123/schueler123) all present and confirmed matching `docker/keycloak/realm-export.json` (lines 237/256/275/294/313). Usernames (admin-user/schulleitung-user/lehrer-user/eltern-user/schueler-user) confirmed matching realm-export.json (lines 228/247/266/285/304). `loginAsAdmin` + `getAdminToken` retained as thin wrappers (:103, :135) — 9 existing specs continue to import unchanged. |
| 2 | `globalSetup` seedet Basis-Fixtures, `globalTeardown` cleaned deterministisch | ✓ VERIFIED (descoped-by-design) | `global-setup.ts` implements fail-fast health-check of API + Vite via parallel `fetch` with AbortController 5s timeout; 401 tolerated (server-up-but-anonymous-forbidden). `global-teardown.ts` is an explicit no-op stub. DB-seeding-per-test was intentionally descoped to 10.4/10.5 in 10.3-01-PLAN because `seed.ts` already covers all 5 roles; cleanup contract is "no-op + per-test idempotency" — deterministic because the 4 new smoke specs are strictly read-only (verified: zero mutations in roles-smoke.spec.ts). The scope note in 10.3-01-PLAN explicitly frames this as acceptable for smoke coverage; data-mutating Tier-3 specs will need the teardown filled in. Acceptable fulfillment. |
| 3 | Smoke-Specs: schulleitung → Substitutions-Admin · lehrer → Timetable · eltern → Kind-Timetable · schueler → persönliches Timetable | ✓ VERIFIED | `roles-smoke.spec.ts` contains exactly 4 tests (grep `test(` = 4): SMOKE-SL-01 (schulleitung → `/admin/substitutions` → h1 "Vertretungsplanung"), SMOKE-LEHRER-01 (lehrer → `/timetable` → h1 "Stundenplan" + grid-or-empty-state), SMOKE-ELTERN-01 (eltern → `/timetable` → same affordance; child perspective auto-filter), SMOKE-SCHUELER-01 (schueler → `/timetable` → same affordance; own classId auto-filter). Admin smoke correctly NOT duplicated (covered by 10.2 specs). All 4 use `loginAsRole` (grep count = 5: 1 import + 4 call sites). |
| 4 | Trace-Retention auf `retain-on-failure`, Screenshots als CI-Artifacts | ✓ VERIFIED | `playwright.config.ts:30` `trace: 'retain-on-failure'` and `:31` `screenshot: 'only-on-failure'` both explicit in the `use` block. HTML reporter at `playwright-report/` configured (`:27`) — matches the CI workflow's `actions/upload-artifact@v4` step that uploads `apps/web/playwright-report/` + `apps/web/test-results/` as the `playwright-traces` artifact (`.github/workflows/playwright.yml:187-192`) with 30-day retention. |
| 5 | Alle neuen Specs laufen in CI (via testMatch `*.spec.ts` — no CI config change needed) | ✓ VERIFIED | `playwright.config.ts:37` desktop project uses `testMatch: /.*\.spec\.ts$/` with `testIgnore: /.*\.mobile\.spec\.ts$/`. `roles-smoke.spec.ts` matches (not a `.mobile.spec.ts`) — it will be picked up by the existing CI step `pnpm --filter @schoolflow/web exec playwright test` (workflow line 180) without further configuration. No workflow edit needed; the harness was designed for auto-pickup of new specs. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/web/e2e/helpers/login.ts` | loginAsRole + getRoleToken + CREDENTIALS for 5 roles | ✓ VERIFIED | 138 lines, all 5 roles in CREDENTIALS, Role type exported, admin wrappers present |
| `apps/web/e2e/helpers/global-setup.ts` | API + Vite health-check fail-fast | ✓ VERIFIED | 36 lines, AbortController + 5s timeout, 401-tolerance documented, parallel Promise.all for both targets |
| `apps/web/e2e/helpers/global-teardown.ts` | no-op stub (teardown explicit) | ✓ VERIFIED | 14 lines, clearly documented as reserved for future cleanup |
| `apps/web/playwright.config.ts` | wires hooks + trace/screenshot retention + HTML reporter | ✓ VERIFIED | globalSetup + globalTeardown paths resolved, trace `retain-on-failure`, screenshot `only-on-failure`, HTML reporter to `playwright-report/` |
| `apps/web/e2e/roles-smoke.spec.ts` | 4 smoke specs (SL, LEHRER, ELTERN, SCHUELER) | ✓ VERIFIED | 99 lines, exactly 4 `test(...)` calls, all 4 SMOKE IDs present, all use `loginAsRole`, read-only (no mutations) |
| `.github/workflows/playwright.yml` | pre-existing; no change needed | ✓ VERIFIED | CI runs `playwright test` which picks up `roles-smoke.spec.ts` via the config's `testMatch` pattern — zero config change required |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| e2e specs | loginAsRole | `import { loginAsRole } from './helpers/login'` | ✓ WIRED | Imported + used 4x in `roles-smoke.spec.ts` (one per test) |
| playwright.config.ts | global-setup.ts | `globalSetup: './e2e/helpers/global-setup.ts'` | ✓ WIRED | Resolved path + file exists + has default export |
| playwright.config.ts | global-teardown.ts | `globalTeardown: './e2e/helpers/global-teardown.ts'` | ✓ WIRED | Resolved path + file exists + has default export |
| pre-10.3 specs | loginAsAdmin/getAdminToken wrappers | thin wrapper calls `loginAsRole(page, 'admin')` | ✓ WIRED | 9 existing specs (admin-school-settings[.mobile], zeitraster[.mobile], schuljahre, wochentage, silent-4xx, screenshots[.mobile]) import unchanged; wrappers at login.ts:103 + :135 delegate correctly |
| CREDENTIALS (code) | realm-export.json (Keycloak) | usernames + passwords match | ✓ WIRED | All 5 usernames confirmed at realm-export.json:228/247/266/285/304; all 5 passwords confirmed at :237/256/275/294/313 |

### Data-Flow Trace (Level 4)

N/A — This phase delivers test harness and spec code. The "data" is seed DB content, which the plan explicitly assumes is already set up. The test-author's concern was not "does real data flow" but "do routes mount and auth gates pass regardless of data state" — achieved via the grid-or-empty-state `.or()` locator pattern.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Spec file count | `grep -c "test(" apps/web/e2e/roles-smoke.spec.ts` | 4 | ✓ PASS |
| All 4 SMOKE IDs present | `grep -E "SMOKE-(SL|LEHRER|ELTERN|SCHUELER)-01" roles-smoke.spec.ts` | 4 matches (lines 36/48/72/86) | ✓ PASS |
| loginAsRole wiring | `grep -c "loginAsRole" roles-smoke.spec.ts` | 5 (1 import + 4 calls) | ✓ PASS |
| All 5 roles in CREDENTIALS | grep of role names in login.ts | admin + schulleitung + lehrer + eltern + schueler all present at :38/42/46/50/54 | ✓ PASS |
| All 4 seed-default passwords | grep passwords in login.ts | direktor123/lehrer123/eltern123/schueler123 all at :43/47/51/55 | ✓ PASS |
| Trace retention config | `grep retain-on-failure playwright.config.ts` | line 30 | ✓ PASS |
| globalSetup wiring | `grep globalSetup playwright.config.ts` | line 25 | ✓ PASS |
| Regression test totals (as-executed by plan 10.3-02) | `playwright test --project=desktop --workers=1` | 20 passed / 3 failed (46.8s) — 3 failures pre-existing SCHOOL-02/03/05, zero new regressions | ✓ PASS |
| SMOKE suite green | `playwright test --grep SMOKE --project=desktop --workers=1` | 4/4 green in 5.3s | ✓ PASS |

### Requirements Coverage

No REQUIREMENTS.md IDs declared (both plans list `requirements: []`). This is an E2E-hardening tagged phase — the milestone roadmap's "Total: 50 requirements" refers to feature requirements; E2E-hardening phases have no numeric REQ-IDs and are tracked via ROADMAP Success Criteria only. Success-criteria-driven verification covered in Observable Truths above.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | none found | — | — |

Scans:
- TODO/FIXME/XXX/HACK/PLACEHOLDER in `e2e/helpers/` → 0 matches
- TODO/FIXME/XXX/HACK/PLACEHOLDER in `roles-smoke.spec.ts` → 0 matches
- Empty-handler/stub-return patterns in modified files → not applicable (no handlers; these are test specs + config)
- The `global-teardown.ts` no-op is intentional-by-design and documented, not a stub anti-pattern.

### Pre-Existing Failures (out of scope per deferred-items.md)

The regression run surfaces 3 failures that are **confirmed pre-existing** via `git stash` + re-run on the pre-10.3 baseline (recorded in 10.3-01 SUMMARY and deferred-items.md):

| Test | Root cause | Status |
|------|-----------|--------|
| SCHOOL-02 | strict mode violation on `getByText('Unterrichtstage')` — 10.2-02 markup promotion (h3 + explanatory p both match) | Pre-existing, out of scope |
| SCHOOL-03 | `Schuljahr angelegt` toast never appears — related to 10.2-03 deferred item #2 (POST /school-years with isActive:true → 500) | Pre-existing backend bug |
| SCHOOL-05 | `PrismaClientInitializationError` in `fixtures/orphan-year.ts` — Playwright worker env wiring on macOS | Pre-existing fixture wiring |

Phase 10.3 introduces **zero new regressions**. Filing these under 10.4 harness-hardening follow-ups per the deferred-items.md guidance.

### Human Verification Required

None. All acceptance criteria are grep-verifiable or Playwright-run-verifiable, and the plan 10.3-02 executor already provided the regression totals (20 passed / 3 failed, 3 failures pre-existing). No visual/UX/real-time behavior claims — this phase delivers test infrastructure and the tests themselves are the verification.

### Gaps Summary

No gaps. The phase delivers:

1. A parametric per-role login helper covering all 5 seed personas with env-var override + seed-default fallback (verified: code + realm-export alignment)
2. Fail-fast Playwright globalSetup that prevents the "Keycloak redirect loop eats 60s timeout" class of spurious CI failures (verified: explicit AbortController + 5s budget + parallel probes)
3. An explicit globalTeardown stub (verified: wired in config, clearly documented as reserved)
4. A 4-test per-role smoke spec that bumps the E2E coverage matrix from 1/5 (admin only) to 5/5 (verified: all 4 SMOKE IDs present, all use loginAsRole, all read-only)
5. Trace + screenshot retention policies explicit in the `use` block (verified: `retain-on-failure`, `only-on-failure`, HTML reporter to `playwright-report/`)
6. CI auto-pickup via the pre-existing `testMatch: /.*\.spec\.ts$/` pattern (verified: no workflow edit needed; new spec will be invoked by the existing `playwright test` CI step)

The "DB-Seeding+Cleanup-Pattern" portion of the ROADMAP goal is the one non-literal fulfillment: instead of per-test seed+cleanup the plan leans on the existing `seed.ts` + read-only smokes. This is acceptable for the smoke tier and is explicitly documented as deferred to Phase 10.4/10.5 where mutation-testing actually requires it. The teardown stub is wired and ready for that future work.

---

*Verified: 2026-04-21T20:15:00Z*
*Verifier: Claude (gsd-verifier)*
