---
phase: 04-timetable-viewing-editing-room-management
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/vitest.config.ts
  - apps/web/src/test/setup.ts
  - apps/web/src/components/timetable/TimetableGrid.test.tsx
  - apps/web/src/components/timetable/PerspectiveSelector.test.tsx
  - apps/web/src/hooks/useSocket.test.ts
  - apps/web/src/lib/colors.test.ts
  - apps/api/src/modules/timetable/timetable-export.service.spec.ts
  - apps/api/src/modules/room/room-booking.service.spec.ts
  - apps/api/src/modules/resource/resource.service.spec.ts
  - apps/api/src/modules/timetable/timetable.service.spec.ts
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-03, VIEW-04, VIEW-05, VIEW-06, TIME-08, ROOM-03, ROOM-04, ROOM-05]
must_haves:
  truths:
    - "Vitest config exists for the web workspace and runs without errors"
    - "Testing Library setup is configured with jest-dom matchers"
    - "All 10 test stub files exist and contain at least one pending/skipped test"
    - "pnpm test runs in both web and api workspaces without crashes"
  artifacts:
    - path: "apps/web/vitest.config.ts"
      provides: "Vitest configuration for the web workspace"
      contains: "defineConfig"
    - path: "apps/web/src/test/setup.ts"
      provides: "Testing Library setup with jest-dom matchers and cleanup"
      contains: "@testing-library/jest-dom"
    - path: "apps/web/src/components/timetable/TimetableGrid.test.tsx"
      provides: "Test stubs for VIEW-01, VIEW-02 (grid renders, class perspective)"
      contains: "describe"
    - path: "apps/web/src/hooks/useSocket.test.ts"
      provides: "Test stubs for VIEW-04, ROOM-05 (socket events, room-swap)"
      contains: "describe"
  key_links:
    - from: "apps/web/vitest.config.ts"
      to: "apps/web/src/test/setup.ts"
      via: "setupFiles reference"
      pattern: "setupFiles"
---

<objective>
Create the Nyquist Wave 0 test infrastructure: Vitest configuration for the web workspace, Testing Library setup, and all test stub files listed in VALIDATION.md. This must run before any implementation plans so that behavioral sampling is possible from the first task onward.

Purpose: Without this Wave 0, no plan can provide valid `<automated>` verification commands for frontend tests. All subsequent plans depend on these test stubs existing so executors can fill in real tests alongside implementations.
Output: 10 test stub files, Vitest web config, Testing Library setup, all runnable (stubs pass as skipped).
</objective>

<execution_context>
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-timetable-viewing-editing-room-management/04-VALIDATION.md
@.planning/phases/04-timetable-viewing-editing-room-management/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vitest config for web workspace and Testing Library setup</name>
  <files>
    apps/web/vitest.config.ts,
    apps/web/src/test/setup.ts
  </files>
  <read_first>
    - apps/web/vite.config.ts (if exists after 04-02 -- may not exist yet at Wave 0; create a standalone vitest config that works regardless)
    - apps/web/package.json (if exists -- check if test deps are already installed)
    - apps/web/tsconfig.app.json (if exists -- TypeScript config for path aliases)
    - apps/api/vitest.config.ts (existing API vitest config -- follow same conventions)
    - .planning/phases/04-timetable-viewing-editing-room-management/04-VALIDATION.md (test infrastructure requirements)
  </read_first>
  <action>
    **Important:** This Wave 0 plan may execute BEFORE Plan 04-02 (SPA scaffolding). If apps/web/ does not exist yet, create the minimal directory structure first. If it does exist (04-02 ran first due to wave dependency resolution), extend what is there.

    **1. Ensure apps/web/ exists with minimal package.json:**
    If apps/web/package.json does not exist, create a minimal one:
    ```json
    {
      "name": "@schoolflow/web",
      "version": "0.0.0",
      "private": true,
      "type": "module",
      "scripts": {
        "test": "vitest run"
      }
    }
    ```
    If it already exists, just ensure "test": "vitest run" is in scripts.

    **2. Install test dependencies:**
    ```bash
    cd apps/web && pnpm add -D vitest @testing-library/react @testing-library/user-event @testing-library/jest-dom @vitest/coverage-v8 jsdom
    ```

    **3. Create apps/web/vitest.config.ts:**
    ```typescript
    import { defineConfig } from 'vitest/config';
    import { resolve } from 'path';

    export default defineConfig({
      test: {
        globals: true,
        environment: 'jsdom',
        setupFiles: ['./src/test/setup.ts'],
        include: ['src/**/*.{test,spec}.{ts,tsx}'],
        coverage: {
          provider: 'v8',
          include: ['src/**/*.{ts,tsx}'],
          exclude: ['src/test/**', 'src/**/*.test.*', 'src/routeTree.gen.ts'],
        },
      },
      resolve: {
        alias: {
          '@': resolve(__dirname, './src'),
        },
      },
    });
    ```

    **4. Create apps/web/src/test/setup.ts:**
    ```typescript
    import '@testing-library/jest-dom/vitest';
    import { cleanup } from '@testing-library/react';
    import { afterEach } from 'vitest';

    afterEach(() => {
      cleanup();
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/apps/web && pnpm vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>Vitest config and Testing Library setup exist in apps/web, vitest run executes without config errors</done>
</task>

<task type="auto">
  <name>Task 2: Create all 10 Nyquist test stub files with pending tests</name>
  <files>
    apps/web/src/components/timetable/TimetableGrid.test.tsx,
    apps/web/src/components/timetable/PerspectiveSelector.test.tsx,
    apps/web/src/hooks/useSocket.test.ts,
    apps/web/src/lib/colors.test.ts,
    apps/api/src/modules/timetable/timetable.service.spec.ts,
    apps/api/src/modules/timetable/timetable-export.service.spec.ts,
    apps/api/src/modules/room/room-booking.service.spec.ts,
    apps/api/src/modules/resource/resource.service.spec.ts
  </files>
  <read_first>
    - apps/web/vitest.config.ts (from Task 1 -- confirms test setup)
    - apps/api/vitest.config.ts (API test config -- follow conventions for .spec.ts files)
    - .planning/phases/04-timetable-viewing-editing-room-management/04-VALIDATION.md (exact file list and requirement mapping)
  </read_first>
  <action>
    Create each test stub file with `describe` blocks and `it.todo()` or `it.skip()` tests. Each stub maps to a requirement from VALIDATION.md. The stubs will be filled in by the respective implementation plans.

    **1. apps/web/src/components/timetable/TimetableGrid.test.tsx (VIEW-01, VIEW-02):**
    ```tsx
    import { describe, it } from 'vitest';

    describe('TimetableGrid', () => {
      describe('VIEW-01: Teacher timetable view', () => {
        it.todo('renders a CSS grid with period rows and day columns');
        it.todo('displays lesson cells with subject abbreviation, teacher surname, room name');
        it.todo('renders break rows between periods with muted background');
        it.todo('merges consecutive same-subject periods into Doppelstunde cells');
      });

      describe('VIEW-02: Class timetable view', () => {
        it.todo('renders class perspective when perspective prop is "class"');
        it.todo('shows all lessons for the class regardless of teacher');
      });
    });
    ```

    **2. apps/web/src/components/timetable/PerspectiveSelector.test.tsx (VIEW-03):**
    ```tsx
    import { describe, it } from 'vitest';

    describe('PerspectiveSelector', () => {
      describe('VIEW-03: Admin perspective switching', () => {
        it.todo('renders dropdown for admin role');
        it.todo('shows teacher, class, and room option groups');
        it.todo('does not render for schueler role');
        it.todo('shows "Mein Stundenplan" for lehrer role');
        it.todo('calls onSelect with correct perspective and ID');
      });
    });
    ```

    **3. apps/web/src/hooks/useSocket.test.ts (VIEW-04, ROOM-05):**
    ```typescript
    import { describe, it } from 'vitest';

    describe('useTimetableSocket', () => {
      describe('VIEW-04: Real-time updates', () => {
        it.todo('connects to /timetable namespace with schoolId');
        it.todo('invalidates timetable queries on timetable:changed event');
        it.todo('shows toast notification on timetable change');
        it.todo('handles connection loss with reconnection toast');
      });

      describe('ROOM-05: Room change propagation', () => {
        it.todo('invalidates room availability queries on timetable:room-swap event');
        it.todo('shows room change toast on timetable:room-swap event');
      });
    });
    ```

    **4. apps/web/src/lib/colors.test.ts (VIEW-05):**
    ```typescript
    import { describe, it } from 'vitest';

    describe('Subject color utilities', () => {
      describe('VIEW-05: Subject color-coding', () => {
        it.todo('returns a color pair for any subject ID');
        it.todo('returns consistent colors for the same subject ID');
        it.todo('distributes colors across the palette');
        it.todo('color overrides take precedence over computed colors');
      });
    });
    ```

    **5. apps/api/src/modules/timetable/timetable.service.spec.ts (TIME-08):**
    Check if this file already exists (Phase 3 may have created it). If so, ADD a new describe block; do NOT overwrite existing tests.
    ```typescript
    // Add to existing file or create new:
    describe('TimetableService - validateMove', () => {
      it.todo('returns valid=true when no constraints are violated');
      it.todo('returns hard violation for teacher clash');
      it.todo('returns hard violation for room conflict');
      it.todo('returns soft warning for max hours per day exceeded');
      it.todo('blocks move when hard violations exist');
    });
    ```

    **6. apps/api/src/modules/timetable/timetable-export.service.spec.ts (VIEW-06):**
    ```typescript
    import { describe, it } from 'vitest';

    describe('TimetableExportService', () => {
      describe('VIEW-06: PDF export', () => {
        it.todo('generates a PDF buffer for teacher perspective');
        it.todo('includes school name and perspective name in title');
        it.todo('includes all lessons in grid layout');
      });

      describe('VIEW-06: iCal export', () => {
        it.todo('generates valid iCal string with VCALENDAR');
        it.todo('creates recurring events for each lesson');
        it.todo('uses Europe/Vienna timezone');
        it.todo('handles A/B week with INTERVAL=2');
      });
    });
    ```

    **7. apps/api/src/modules/room/room-booking.service.spec.ts (ROOM-03):**
    ```typescript
    import { describe, it } from 'vitest';

    describe('RoomBookingService', () => {
      describe('ROOM-03: Room booking', () => {
        it.todo('creates an ad-hoc room booking for a free slot');
        it.todo('rejects booking when slot is already occupied by lesson');
        it.todo('rejects booking when slot is already booked');
        it.todo('returns room availability grid for a given day');
        it.todo('filters availability by room type and capacity');
      });
    });
    ```

    **8. apps/api/src/modules/resource/resource.service.spec.ts (ROOM-04):**
    ```typescript
    import { describe, it } from 'vitest';

    describe('ResourceService', () => {
      describe('ROOM-04: Resource management', () => {
        it.todo('creates a resource with name, type, quantity');
        it.todo('enforces unique name per school');
        it.todo('updates resource quantity and description');
        it.todo('deletes resource and cascades to bookings');
        it.todo('creates resource booking for a room+period');
      });
    });
    ```

    Ensure all directories exist before creating files:
    ```bash
    mkdir -p apps/web/src/components/timetable
    mkdir -p apps/web/src/hooks
    mkdir -p apps/web/src/lib
    mkdir -p apps/api/src/modules/resource
    ```
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && pnpm --filter @schoolflow/web vitest run --reporter=verbose 2>&1 | tail -15 && pnpm --filter @schoolflow/api vitest run --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>All 10 test stub files exist with pending/todo tests. Vitest runs in both web and API workspaces without crashes. All stubs are discoverable by the test runner.</done>
</task>

</tasks>

<verification>
- `apps/web/vitest.config.ts` exists with jsdom environment and setupFiles
- `apps/web/src/test/setup.ts` exists with jest-dom import and cleanup
- All 8 test stub files exist (4 web, 4 API -- API timetable.service.spec.ts may be pre-existing)
- `pnpm --filter @schoolflow/web vitest run` exits without config errors
- `pnpm --filter @schoolflow/api vitest run` exits without config errors
- All `.todo()` tests are reported as pending/skipped (not failing)
</verification>

<success_criteria>
- Nyquist Wave 0 complete: test infrastructure ready for behavioral sampling
- Every requirement (VIEW-01 through VIEW-06, TIME-08, ROOM-03 through ROOM-05) has at least one test stub
- Plans 04-01 through 04-08 can reference test stubs in their `<verify>` blocks
- VALIDATION.md wave_0_complete can be set to true
</success_criteria>

<output>
After completion, create `.planning/phases/04-timetable-viewing-editing-room-management/04-00-SUMMARY.md`
</output>
