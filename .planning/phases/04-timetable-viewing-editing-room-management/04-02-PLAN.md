---
phase: 04-timetable-viewing-editing-room-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/vite.config.ts
  - apps/web/tsconfig.json
  - apps/web/tsconfig.app.json
  - apps/web/index.html
  - apps/web/src/main.tsx
  - apps/web/src/app.css
  - apps/web/src/lib/keycloak.ts
  - apps/web/src/lib/api.ts
  - apps/web/src/lib/query-client.ts
  - apps/web/src/stores/ui-store.ts
  - apps/web/src/routes/__root.tsx
  - apps/web/src/routes/_authenticated.tsx
  - apps/web/src/routes/_authenticated/timetable/index.tsx
  - apps/web/src/routes/index.tsx
  - apps/web/src/components/layout/AppSidebar.tsx
  - apps/web/src/components/layout/AppHeader.tsx
  - apps/web/components.json
  - apps/web/src/hooks/useAuth.ts
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-03]
must_haves:
  truths:
    - "React SPA boots at localhost:5173 with Vite dev server"
    - "Keycloak OIDC redirects unauthenticated users to login"
    - "Authenticated users see a sidebar with navigation items based on their role"
    - "TanStack Router provides file-based type-safe routing"
    - "TanStack Query client is configured and available via QueryClientProvider"
    - "Zustand store manages UI state (sidebar collapse, view mode)"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Web app package with all Phase 4 frontend dependencies"
      contains: "\"name\": \"@schoolflow/web\""
    - path: "apps/web/src/main.tsx"
      provides: "React root with Keycloak init, QueryClientProvider, RouterProvider"
      contains: "createRoot"
    - path: "apps/web/src/lib/keycloak.ts"
      provides: "Keycloak singleton with PKCE, silent SSO"
      contains: "new Keycloak"
    - path: "apps/web/src/routes/__root.tsx"
      provides: "Root layout with sidebar and header"
      contains: "createRootRoute"
  key_links:
    - from: "apps/web/src/main.tsx"
      to: "apps/web/src/lib/keycloak.ts"
      via: "keycloak.init() before createRoot"
      pattern: "keycloak\\.init"
    - from: "apps/web/src/routes/_authenticated.tsx"
      to: "apps/web/src/lib/keycloak.ts"
      via: "Auth gate redirect for unauthenticated users"
      pattern: "keycloak\\.authenticated"
---

<objective>
Scaffold the entire React SPA from scratch in apps/web/ using the locked stack: Vite + React 19 + TanStack Router + TanStack Query + Zustand + shadcn/ui + Tailwind CSS 4 + Keycloak OIDC authentication.

Purpose: This is the FIRST FRONTEND phase. No apps/web/ exists yet. This plan creates the complete SPA foundation that all UI plans (05-08 in this phase, plus Phases 5-9) build on.
Output: A running React SPA at localhost:5173 with Keycloak auth, role-based sidebar navigation, and placeholder timetable route.
</objective>

<execution_context>
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-timetable-viewing-editing-room-management/04-CONTEXT.md
@.planning/phases/04-timetable-viewing-editing-room-management/04-RESEARCH.md
@.planning/phases/04-timetable-viewing-editing-room-management/04-UI-SPEC.md

<interfaces>
<!-- Existing monorepo infrastructure -->

From pnpm-workspace.yaml:
```yaml
packages:
  - "apps/*"
  - "packages/*"
```

From turbo.json:
```json
{
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] },
    "dev": { "cache": false, "persistent": true },
    "test": { "dependsOn": ["^build"] }
  }
}
```

From packages/shared/package.json:
```json
{ "name": "@schoolflow/shared" }
```

From docker/keycloak/realm-export.json (Keycloak config):
- Realm: schoolflow
- Client ID for frontend: schoolflow-web (public client, PKCE)
- 5 roles: admin, schulleitung, lehrer, eltern, schueler
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold apps/web with Vite + React 19 + TypeScript 6.0 + Tailwind CSS 4 + shadcn/ui + TanStack Router + TanStack Query</name>
  <files>
    apps/web/package.json, apps/web/vite.config.ts, apps/web/tsconfig.json, apps/web/tsconfig.app.json,
    apps/web/index.html, apps/web/src/main.tsx, apps/web/src/app.css, apps/web/components.json,
    apps/web/src/lib/query-client.ts, apps/web/src/lib/keycloak.ts, apps/web/src/lib/api.ts,
    apps/web/src/hooks/useAuth.ts, apps/web/src/stores/ui-store.ts,
    apps/web/public/silent-check-sso.html
  </files>
  <read_first>
    - pnpm-workspace.yaml (confirms apps/* is a workspace)
    - turbo.json (build task configuration)
    - apps/api/package.json (existing workspace package for naming convention reference)
    - packages/shared/package.json (shared package name for dependency reference)
    - .planning/phases/04-timetable-viewing-editing-room-management/04-RESEARCH.md (installation commands, Keycloak pattern, API fetch pattern, frontend structure)
    - .planning/phases/04-timetable-viewing-editing-room-management/04-UI-SPEC.md (design system, font, colors, spacing, shadcn init sequence)
    - docker/keycloak/realm-export.json (Keycloak realm name, client ID for OIDC config)
  </read_first>
  <action>
    **Step 1: Create apps/web directory and package.json:**
    ```bash
    mkdir -p apps/web/src apps/web/public
    ```
    Create apps/web/package.json with name "@schoolflow/web", type "module", scripts: dev (vite), build (tsc -b && vite build), preview (vite preview). Dependencies:
    - react@^19.0.0, react-dom@^19.0.0
    - @tanstack/react-router@^1.168.0, @tanstack/react-query@^5.95.0
    - zustand@^5.0.0, sonner@^2.0.0, date-fns@^4.1.0
    - @dnd-kit/core@^6.3.0, @dnd-kit/sortable@^10.0.0, @dnd-kit/utilities@^3.2.0
    - socket.io-client@^4.8.0, keycloak-js@^26.2.0
    - lucide-react@latest, class-variance-authority@latest, clsx@latest, tailwind-merge@latest
    - @schoolflow/shared@workspace:*
    DevDependencies:
    - typescript@~6.0.0, @types/react@^19.0.0, @types/react-dom@^19.0.0
    - vite@^8.0.0, @vitejs/plugin-react@^6.0.0
    - @tanstack/router-plugin@^1.167.0, @tanstack/router-devtools@^1.168.0
    - tailwindcss@^4.2.0, @tailwindcss/vite@^4.2.0
    - vitest@^4.1.0, @testing-library/react@^16.3.0, @testing-library/user-event@^14.6.0, @testing-library/jest-dom@^6.9.0

    **Step 2: Create vite.config.ts:**
    ```typescript
    import { defineConfig } from 'vite';
    import react from '@vitejs/plugin-react';
    import tailwindcss from '@tailwindcss/vite';
    import { TanStackRouterVite } from '@tanstack/router-plugin/vite';
    import { resolve } from 'path';

    export default defineConfig({
      plugins: [
        TanStackRouterVite({ routesDirectory: './src/routes', generatedRouteTree: './src/routeTree.gen.ts' }),
        react(),
        tailwindcss(),
      ],
      resolve: {
        alias: { '@': resolve(__dirname, './src') },
      },
      server: {
        port: 5173,
        proxy: {
          '/api': { target: 'http://localhost:3000', changeOrigin: true },
          '/socket.io': { target: 'http://localhost:3000', ws: true },
        },
      },
    });
    ```

    **Step 3: Create tsconfig.json:**
    ```json
    {
      "files": [],
      "references": [{ "path": "./tsconfig.app.json" }]
    }
    ```

    **Step 4: Create tsconfig.app.json:**
    ```json
    {
      "compilerOptions": {
        "target": "ES2022",
        "lib": ["ES2023", "DOM", "DOM.Iterable"],
        "module": "ESNext",
        "moduleResolution": "bundler",
        "jsx": "react-jsx",
        "strict": true,
        "noEmit": true,
        "isolatedModules": true,
        "skipLibCheck": true,
        "esModuleInterop": true,
        "paths": { "@/*": ["./src/*"] },
        "baseUrl": "."
      },
      "include": ["src"]
    }
    ```

    **Step 5: Create index.html:**
    ```html
    <!DOCTYPE html>
    <html lang="de">
      <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>SchoolFlow</title>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet" />
      </head>
      <body>
        <div id="root"></div>
        <script type="module" src="/src/main.tsx"></script>
      </body>
    </html>
    ```

    **Step 6: Create src/app.css** (Tailwind v4 single import + custom theme):
    ```css
    @import "tailwindcss";

    @theme {
      --font-sans: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      --color-background: hsl(0 0% 100%);
      --color-foreground: hsl(240 10% 3.9%);
      --color-card: hsl(240 5% 96%);
      --color-primary: hsl(221 83% 53%);
      --color-destructive: hsl(0 84% 60%);
      --color-success: hsl(142 71% 45%);
      --color-warning: hsl(38 92% 50%);
      --color-muted: hsl(240 5% 96%);
      --color-border: hsl(240 5.9% 90%);
    }
    ```

    **Step 7: Create src/lib/keycloak.ts:**
    Keycloak instance with env vars VITE_KEYCLOAK_URL (default http://localhost:8080), VITE_KEYCLOAK_REALM (default schoolflow), VITE_KEYCLOAK_CLIENT_ID (default schoolflow-web). Export `keycloak` singleton and `initKeycloak()` async function using init({ onLoad: 'check-sso', silentCheckSsoRedirectUri: window.location.origin + '/silent-check-sso.html', pkceMethod: 'S256' }).

    **Step 8: Create public/silent-check-sso.html:**
    Minimal HTML page for Keycloak silent SSO check:
    ```html
    <!DOCTYPE html><html><body><script>parent.postMessage(location.href, location.origin);</script></body></html>
    ```

    **Step 9: Create src/lib/api.ts:**
    Export `apiFetch(path: string, options?: RequestInit): Promise<Response>` that:
    1. Calls `await keycloak.updateToken(30)` to ensure fresh token
    2. Sets `Authorization: Bearer ${keycloak.token}` header
    3. Sets `Content-Type: application/json` for non-GET requests
    4. Prepends `/api` if path does not start with `/api`
    5. Returns `fetch(url, mergedOptions)`

    **Step 10: Create src/lib/query-client.ts:**
    Export a TanStack QueryClient with defaultOptions: queries.staleTime = 30_000 (30s), queries.retry = 1, queries.refetchOnWindowFocus = true.

    **Step 11: Create src/hooks/useAuth.ts:**
    Custom hook returning { user: { id, email, username, roles, firstName, lastName }, isAuthenticated, login, logout } from keycloak instance. Roles extracted from keycloak.realmAccess.roles.

    **Step 12: Create src/stores/ui-store.ts:**
    Zustand store with:
    - sidebarCollapsed: boolean (default false)
    - toggleSidebar: () => void
    - viewMode: 'day' | 'week' (default 'week')
    - setViewMode: (mode) => void
    - selectedWeekType: 'A' | 'B' | 'BOTH' (default 'BOTH')
    - setSelectedWeekType: (type) => void

    **Step 13: Create src/main.tsx:**
    1. Import app.css
    2. Import keycloak, initKeycloak
    3. Import QueryClient, QueryClientProvider, queryClient
    4. Import RouterProvider, createRouter from @tanstack/react-router
    5. Import routeTree from ./routeTree.gen
    6. Create router = createRouter({ routeTree })
    7. declare module '@tanstack/react-router' { interface Register { router: typeof router } }
    8. Initialize keycloak first, then createRoot(document.getElementById('root')!).render(
       <QueryClientProvider client={queryClient}><RouterProvider router={router} /></QueryClientProvider>
    )
    9. Add Toaster from sonner at top level

    **Step 14: Install dependencies and verify build:**
    ```bash
    cd apps/web && pnpm install
    ```

    **Step 15: Initialize shadcn/ui:**
    Run `npx shadcn@latest init` inside apps/web. Configure components.json with:
    - style: "default"
    - tailwindCSS: true
    - aliases: components "@/components", utils "@/lib/utils"
    Then add the P0 components listed in UI-SPEC:
    ```bash
    npx shadcn@latest add button card tabs select dialog badge sonner
    ```
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/apps/web && pnpm install && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json exists with "name": "@schoolflow/web"
    - apps/web/package.json contains "react" and "@tanstack/react-router" in dependencies
    - apps/web/vite.config.ts contains `TanStackRouterVite` and `tailwindcss()` plugins
    - apps/web/index.html contains `lang="de"`
    - apps/web/src/app.css contains `@import "tailwindcss"`
    - apps/web/src/lib/keycloak.ts contains `new Keycloak`
    - apps/web/src/lib/api.ts contains `keycloak.updateToken`
    - apps/web/src/lib/query-client.ts contains `new QueryClient`
    - apps/web/src/hooks/useAuth.ts contains `keycloak.realmAccess`
    - apps/web/src/stores/ui-store.ts contains `create<` (Zustand store)
    - apps/web/src/main.tsx contains `QueryClientProvider` and `RouterProvider`
    - apps/web/public/silent-check-sso.html exists
    - apps/web/components.json exists (shadcn config)
    - apps/web/src/components/ui/button.tsx exists (shadcn component)
    - TypeScript compilation (`tsc --noEmit`) reports no errors
  </acceptance_criteria>
  <done>React SPA is scaffolded with all dependencies installed, Keycloak auth configured, TanStack Router + Query wired, shadcn/ui initialized with P0 components, and TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Create root layout with role-based sidebar navigation and authenticated route wrapper</name>
  <files>
    apps/web/src/routes/__root.tsx, apps/web/src/routes/_authenticated.tsx,
    apps/web/src/routes/index.tsx, apps/web/src/routes/_authenticated/timetable/index.tsx,
    apps/web/src/components/layout/AppSidebar.tsx, apps/web/src/components/layout/AppHeader.tsx
  </files>
  <read_first>
    - apps/web/src/main.tsx (router setup from Task 1)
    - apps/web/src/hooks/useAuth.ts (user/roles interface from Task 1)
    - apps/web/src/stores/ui-store.ts (sidebar state from Task 1)
    - apps/web/src/lib/keycloak.ts (auth methods from Task 1)
    - .planning/phases/04-timetable-viewing-editing-room-management/04-UI-SPEC.md (navigation structure, responsive breakpoints, copywriting contract)
  </read_first>
  <action>
    **1. Create src/routes/__root.tsx:**
    Root layout using createRootRoute. Renders:
    - AppSidebar on left side
    - Main content area with AppHeader at top and `<Outlet />` below
    - Toaster from sonner at root level
    Use flex layout: sidebar (w-64 when expanded, w-16 when collapsed on tablet) + flex-1 main content.
    On mobile (< 640px), sidebar is hidden by default, revealed via Sheet (drawer).

    **2. Create src/routes/index.tsx:**
    Index route that redirects to /timetable using createFileRoute('/')({ beforeLoad: () => { throw redirect({ to: '/timetable' }) } }).

    **3. Create src/routes/_authenticated.tsx:**
    Layout route that gates all child routes behind Keycloak authentication.
    Use createFileRoute('/_authenticated')({ beforeLoad: async () => check keycloak.authenticated, if not call keycloak.login() }).
    Renders `<Outlet />` when authenticated.

    **4. Create src/routes/_authenticated/timetable/index.tsx:**
    Placeholder timetable page. Renders a Card with heading "Stundenplan" (Display size: 28px, semibold per UI-SPEC). Body text: "Kein Stundenplan vorhanden" / "Es wurde noch kein Stundenplan generiert. Starten Sie die automatische Erstellung unter Verwaltung." (empty state copy from UI-SPEC copywriting contract).

    **5. Create src/components/layout/AppSidebar.tsx:**
    Role-based sidebar navigation. Uses the useAuth() hook to get user roles. Menu items per UI-SPEC navigation structure:
    - "Stundenplan" (Calendar icon from lucide-react) -> /timetable -- visible to ALL roles
    - "Raeume" (DoorOpen icon) -> /rooms -- visible to lehrer, admin, schulleitung
    - "Ressourcen" (Package icon) -> /admin/resources -- visible to admin, schulleitung
    - "Stundenplan bearbeiten" (PencilRuler icon) -> /admin/timetable-edit -- visible to admin, schulleitung
    - "Aenderungsverlauf" (History icon) -> /admin/timetable-history -- visible to admin, schulleitung

    Uses Zustand ui-store for collapsed state. On collapsed: shows icons only with tooltip on hover.
    Active route highlighted with accent color (--primary).
    Uses Link from @tanstack/react-router for navigation.
    Collapse toggle button at bottom of sidebar.

    **6. Create src/components/layout/AppHeader.tsx:**
    Top header bar with:
    - School name (hardcoded "SchoolFlow" for now, will be dynamic later)
    - User info (firstName lastName from useAuth)
    - Logout button (calls keycloak.logout())
    - Mobile: hamburger menu button that opens sidebar Sheet

    All copy in German with formal "Sie" per UI-SPEC copywriting conventions.
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/apps/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/routes/__root.tsx contains `createRootRoute`
    - apps/web/src/routes/__root.tsx contains `AppSidebar` and `AppHeader`
    - apps/web/src/routes/_authenticated.tsx contains `keycloak.authenticated` or `keycloak.login`
    - apps/web/src/routes/index.tsx contains `redirect` and `/timetable`
    - apps/web/src/routes/_authenticated/timetable/index.tsx contains `Stundenplan` and `Kein Stundenplan vorhanden`
    - apps/web/src/components/layout/AppSidebar.tsx contains `Calendar` and `DoorOpen` and `PencilRuler`
    - apps/web/src/components/layout/AppSidebar.tsx contains role check logic (e.g., `roles.includes`)
    - apps/web/src/components/layout/AppHeader.tsx contains `keycloak.logout`
    - TypeScript compilation (`tsc --noEmit`) reports no errors
  </acceptance_criteria>
  <done>Root layout with role-based sidebar, authenticated route gate, app header with logout, and placeholder timetable page are in place. The SPA renders with navigation structure matching UI-SPEC.</done>
</task>

</tasks>

<verification>
- `pnpm install` succeeds in apps/web
- `tsc --noEmit` reports no errors
- apps/web/src/routeTree.gen.ts is auto-generated by TanStack Router plugin
- shadcn components (Button, Card, Tabs, Select, Dialog, Badge, Sonner) exist in src/components/ui/
- Navigation shows correct items per role
</verification>

<success_criteria>
- React SPA at apps/web/ is fully scaffolded with locked stack
- Keycloak OIDC authentication gates all routes
- Role-based sidebar navigation matches UI-SPEC navigation structure
- TanStack Router provides file-based routing with type safety
- TanStack Query client is configured for server state management
- Zustand manages UI state (sidebar, view mode, week type)
- Tailwind CSS 4 with custom theme variables from UI-SPEC
- shadcn/ui P0 components installed
</success_criteria>

<output>
After completion, create `.planning/phases/04-timetable-viewing-editing-room-management/04-02-SUMMARY.md`
</output>
