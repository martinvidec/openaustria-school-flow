---
phase: 10.4-e2e-admin-ops-people
verified: 2026-04-22T08:15:00Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
---

# Phase 10.4: E2E Harness Hardening & 10.3 Deferred-Items Closure — Verification Report

**Phase Goal (from ROADMAP.md:150, verbatim):**
> Schließt die in 10.3 deferred 3 pre-existing Failures (SCHOOL-02/03/05) und härtet das E2E-Harness (CardTitle-Helper). Schafft die Voraussetzung, dass Phase 11/12 E2E-Authoring ungestört läuft. People-CRUD E2E wurde nach Phase 11 (Lehrer+Fächer) und Phase 12 (Schüler+Klassen) verschoben, da die zu testenden UIs erst dort geshipped werden.

**Verified:** 2026-04-22T08:15:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                     | Status      | Evidence                                                                                                                                                                                                                          |
| --- | --------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | SCHOOL-02 desktop E2E transitions red → green (heading-role selector, Mo-toggle bootstrap)                | VERIFIED    | `admin-school-settings.spec.ts:110` uses `getByRole('heading', { name: 'Unterrichtstage' })`; Mo-toggle bootstrap at lines 122-125; REGRESSION-REPORT §1.2 confirms 22/23 desktop green with SCHOOL-02 among the transitioned specs |
| 2   | SCHOOL-03 desktop E2E transitions red → green via atomic-demote backend fix + strengthened E2E            | VERIFIED    | `school-year.service.ts:48-56` wraps create in conditional `$transaction` when `isActive === true` with `updateMany(demote) → create(promote)`; unit tests Test 1b (invocationCallOrder) + Test 1c (no-op path) at spec.ts:76-120; E2E at `admin-school-settings.spec.ts:152-179` asserts single-active API invariant post-toast; throwaway `E2E-SCHOOL03-{Date.now()}` + describe-level `afterEach` cleanup at lines 34-74 |
| 3   | SCHOOL-05 desktop E2E transitions red → green via env-robust fixture (dotenv + Prisma 7 driver-adapter)   | VERIFIED    | `orphan-year.ts:15-28` uses `import 'dotenv/config'` + explicit `dotenvConfig({ path: fileURLToPath-rooted .env })`; `orphan-year.ts:82-86, 112-116` constructs `PrismaClient({ adapter: new PrismaPg({ connectionString }) })`; `apps/web/package.json:66` has `dotenv ^17.4.2` as devDep |
| 4   | E2E harness hardened with reusable `getByCardTitle` helper (ADR Option 4a — helper, no production change) | VERIFIED    | `apps/web/e2e/helpers/card.ts` (51 LoC) exports `getByCardTitle(scope, name)` targeting `.text-2xl.font-semibold.leading-none.tracking-tight` + hasText; production `apps/web/src/components/ui/card.tsx:32-45` still renders CardTitle as `<div>` (untouched, confirming non-invasive ADR); helper imported in `roles-smoke.spec.ts:33` and consumed 3× (LEHRER-01/ELTERN-01/SCHUELER-01) |
| 5   | Phase 11/12 E2E-Authoring unblocked — reusable harness patterns are available                             | VERIFIED    | Three reusable patterns shipped and consumed in-repo: (a) `getByCardTitle` helper at `e2e/helpers/card.ts`; (b) env-robust Prisma-direct fixture template at `e2e/fixtures/orphan-year.ts`; (c) `E2E-<TEST-ID>-{Date.now()}` throwaway + activate-seed-then-delete `afterEach` cleanup pattern at `admin-school-settings.spec.ts:34-74`. All three are referenced in Plan 10.4-01/02 SUMMARYs as "ready to copy for Phase 11/12" |
| 6   | ROADMAP records People-CRUD descope correctly (2 E2E ex-10.4 lines in Phase 11, 2 in Phase 12)            | VERIFIED    | ROADMAP.md:148 contains "RESCOPED 2026-04-22"; line 168 contains "Descope-Beschluss (2026-04-22)"; Phase 11 success criteria lines 204-205 carry "E2E (ex-10.4): Lehrpersonen-CRUD" and "E2E (ex-10.4): Fächer-CRUD"; Phase 12 success criteria lines 229-230 carry "E2E (ex-10.4): Schüler-CRUD" and "E2E (ex-10.4): Klassen-CRUD"; Orphan-Guard-Backend gap at Phase 11 line 209 + Phase 12 line 235; Elternlink-HTTP-Surface gap at Phase 12 line 234; Phase 10.4 block (148-167) contains zero Klassen/Lehrpersonen/Schüler/Fächer-CRUD strings (correctly removed from old scope) |

**Score:** 6/6 truths verified

### Required Artifacts (Four-Level Verification)

| Artifact                                                          | Expected                                                          | Status     | Details                                                                                                                                                        |
| ----------------------------------------------------------------- | ----------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/web/e2e/helpers/card.ts`                                    | NEW — exports `getByCardTitle(scope, name)`                       | VERIFIED   | L1 exists (51 LoC); L2 substantive (JSDoc + ADR citation + typed signature + non-trivial body targeting stable Tailwind class bundle); L3 wired (imported in roles-smoke.spec.ts:33); L4 N/A (helper, not dynamic-data renderer) |
| `apps/api/src/modules/school/school-year.service.ts`              | `create()` wraps in conditional `$transaction` demoting incumbent | VERIFIED   | L1 exists; L2 substantive (lines 48-56 contain `if (isActive === true)` → `$transaction(async tx => { updateMany(demote); create })`); L3 wired (buildData() closure + direct path preserved line 59); L4 N/A (service, data flow validated by unit tests) |
| `apps/api/src/modules/school/school-year.service.spec.ts`         | 2 new tests (Test 1b atomic order, Test 1c no-op path)            | VERIFIED   | L1 exists; L2 substantive (Test 1b at lines 76-104 asserts `invocationCallOrder` updateMany→create inside `$transaction`; Test 1c at lines 107-120 asserts zero `$transaction`/`updateMany` on `isActive:false`); regression report confirms 10/10 (8 pre-existing + 2 new) |
| `apps/web/e2e/admin-school-settings.spec.ts`                      | SCHOOL-02 heading selector; SCHOOL-03 strengthened; Mo-toggle     | VERIFIED   | L1 exists; L2 substantive (SCHOOL-02 at line 110 uses `getByRole('heading')`; SCHOOL-03 parameterized year name + API invariant at lines 137-179; describe-level `afterEach` cleanup at lines 34-74); L3 wired (spec runs in Playwright test target); L4 N/A (test code) |
| `apps/web/e2e/fixtures/orphan-year.ts`                            | env-robust with dotenv + Prisma 7 driver-adapter                  | VERIFIED   | L1 exists; L2 substantive (belt-and-braces dotenv at lines 15-28; PrismaPg adapter at lines 82-86 + 112-116; legacy-shape docblock preserves migration-path discoverability at lines 32-40); L3 wired (imported in admin-school-settings.spec.ts:13-17); L4 N/A (fixture) |
| `apps/web/e2e/roles-smoke.spec.ts`                                | Migrated from inline `getByText` to `getByCardTitle` helper       | VERIFIED   | L1 exists; L2 substantive (3 LEHRER/ELTERN/SCHUELER smokes at lines 49-99 use `getByCardTitle(page, 'Kein Stundenplan vorhanden')`); L3 wired (import from ./helpers/card at line 33; regression report confirms all 4 role smokes green)          |
| `apps/web/package.json`                                           | `dotenv ^17.4.2` in devDependencies                               | VERIFIED   | Line 66 confirms `"dotenv": "^17.4.2"`                                                                                                                         |
| `apps/web/src/components/ui/card.tsx`                             | UNTOUCHED (ADR Option 4a invariant)                               | VERIFIED   | CardTitle at lines 32-45 still renders as `<div>` with the exact class bundle the helper targets; confirms production code remained non-invasive                |
| `.planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md` | 244-line regression matrix + triage                            | VERIFIED   | File exists (244 lines); §1 three-suite test results matrix; §2 Deliverable 5 closure; §3 Deliverable 6 ROADMAP audit (14/14 GREEN); §4 git log evidence; §5 follow-up item triage with root cause + 3 options each; §6 automation-first honesty note on methodology |

### Key Link Verification

| From                               | To                                                                         | Via                                                                                                    | Status | Details                                                                                                                              |
| ---------------------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------ |
| `roles-smoke.spec.ts`              | `e2e/helpers/card.ts` (`getByCardTitle`)                                   | Named import at line 33, used 3×                                                                       | WIRED  | Grep confirms import + 3 call sites; helper is non-orphaned                                                                          |
| `admin-school-settings.spec.ts`    | `e2e/fixtures/orphan-year.ts` (`seedOrphanYear`, `cleanupOrphanYear`)      | Named import at lines 12-16, used in SCHOOL-05 at lines 211 + 228                                      | WIRED  | Import + call sites verified; fixture is non-orphaned                                                                                |
| `SchoolYearService.create`         | `Prisma.$transaction` → `updateMany` → `tx.schoolYear.create`              | Conditional `if (isActive === true)` branch at service.ts:48-56                                        | WIRED  | Branch exists; matches `activate()` pattern at lines 96-106; unit Test 1b asserts invocationCallOrder |
| `admin-school-settings.spec.ts` SCHOOL-03 | API `/schools/{id}/school-years` single-active invariant            | `request.get()` + filter + `expect(length === 1)` at lines 164-179                                     | WIRED  | HTTP call + response parsing + invariant assertion all present                                                                       |
| `orphan-year.ts`                   | `PrismaClient({ adapter: new PrismaPg({ connectionString }) })`            | `createRequire` pulls CJS client + adapter from `apps/api/node_modules` at lines 42-59                 | WIRED  | Both imports wired; `process.env.DATABASE_URL` populated by dotenv belt-and-braces                                                   |
| ROADMAP Phase 11/12                | Phase 10.4 descope                                                         | "E2E (ex-10.4):" receipts at lines 204-205, 229-230                                                    | WIRED  | 4 occurrences confirmed via Grep; matches REGRESSION-REPORT §3 audit (14/14 checks)                                                  |

### Data-Flow Trace (Level 4)

Level 4 does not apply to this phase — all shipped artifacts are test infrastructure (helpers, fixtures, specs) or a backend service whose data flow is exhaustively validated by unit Test 1b (mock.invocationCallOrder asserts updateMany→create inside `$transaction`) and by the E2E SCHOOL-03 API single-active invariant. No new UI components render dynamic data in this phase.

### Behavioral Spot-Checks

Spot-checks were delegated to the orchestrator-run regression suite captured in `.planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md` (authoritative per phase_context). Re-running the suite in this verifier session is explicitly prohibited.

| Behavior                                                              | Source                                       | Result                         | Status      |
| --------------------------------------------------------------------- | -------------------------------------------- | ------------------------------ | ----------- |
| API unit school-year.service.spec.ts passes 10/10                     | REGRESSION-REPORT §1.1                       | 10/10 (8 pre-existing + Test 1b + Test 1c) | PASS        |
| Desktop E2E SCHOOL-02 green                                           | REGRESSION-REPORT §1.2                       | red → green                    | PASS        |
| Desktop E2E SCHOOL-03 green (+ API single-active invariant)           | REGRESSION-REPORT §1.2                       | red → green                    | PASS        |
| Desktop E2E SCHOOL-05 green (at admin-school-settings.spec.ts:196)    | REGRESSION-REPORT §1.2                       | red → green                    | PASS        |
| All 4 roles-smoke specs green (SL/LEHRER/ELTERN/SCHUELER)             | REGRESSION-REPORT §1.2                       | all green (getByCardTitle migrated) | PASS        |
| Preserved desktop E2E specs stay green (20 specs)                     | REGRESSION-REPORT §1.2                       | 20/20 preserved                | PASS        |
| Schema/migration hygiene (CLAUDE.md hard rule)                        | `git status --porcelain apps/api/prisma/*`   | EMPTY                          | PASS        |
| Wave 1 commits exist with expected messages                           | `git log --oneline`                          | All 10 commits present         | PASS        |

### Requirements Coverage

Per phase_context, Phase 10.4 is a meta/test-infra phase not tied to a numbered `REQ-` ID in REQUIREMENTS.md — the phase's Requirements field is `E2E-hardening`. Coverage therefore maps to the phase goal and success criteria directly, which are fully verified under truths #1-#6 above.

### Anti-Patterns Found

Scanned phase-touched files for TODO/FIXME/stub patterns. Zero blockers or warnings found.

| File                                                          | Line | Pattern                                                                                  | Severity | Impact                                                                                                                                                                                |
| ------------------------------------------------------------- | ---- | ---------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/web/e2e/fixtures/orphan-year.ts`                        | 41, 56 | `eslint-disable @typescript-eslint/no-var-requires` on two `require()` calls            | Info     | Intentional — `createRequire` bridge is required for CJS-emitted Prisma client + adapter-pg consumption from ESM Playwright fixture. Alternative (pure ESM) unavailable until Prisma ships ESM generation. Documented in file docblock. |
| `apps/api/src/modules/school/school-year.service.ts`          | —    | Conditional `$transaction` only on `isActive === true`                                   | Info     | Intentional design choice — preserves direct-create fast path for common case (schools create inactive years, activate later). Decision recorded in plan 10.4-02 SUMMARY + service-file comment at line 44-47. |

No placeholder returns, no hardcoded empty arrays/objects, no console.log-only implementations, no empty `{}` handlers. All changes are substantive and purposeful.

### Human Verification Required

None.

**Rationale:** Per the user's standing directive "E2E-first — keine UAT bis Playwright-Coverage" (MEMORY: `feedback_e2e_first_no_uat.md`, 2026-04-21), the Playwright regression suite IS the UAT gate for this phase. All plan-target specs (SCHOOL-02, SCHOOL-03 at both unit + E2E layers, SCHOOL-05, all 4 role smokes) are GREEN per REGRESSION-REPORT §1. The three full-suite failures surfaced (multi-active backfill, screenshots SCHOOL-05, mobile-375 WebKit) are classified — with explicit root-cause evidence — as NOT Wave 1 regressions:

- **Multi-active backfill** (§5.1): test-DB-pollution from parallel-worker ordering; Plan 10.4-02's atomic-demote is working correctly by design.
- **Screenshots SCHOOL-05** (§5.2): same test-DB-pollution; `2027/2028` protected active-year state blocks the delete button (separate guard, not orphan-guard).
- **Mobile-375 WebKit Bus error** (§5.3): macOS 14.3 + frozen Playwright `mac14_arm64_special` WebKit binary (Playwright's own install output acknowledges the freeze); pre-existing environmental blocker documented in STATE.md 10.2-01; Wave 1 authored zero new mobile specs.

All three follow-ups are legitimately out of scope for Phase 10.4 closure and are flagged for the testing-infra tranche (candidate Phase 10.5 or a dedicated 10.4-patch). No human browser-clicking is requested.

---

## Gaps Summary

No gaps. All six observable truths derived from the phase goal + ROADMAP success criteria are verified by direct code inspection (verifier re-read `card.ts`, `school-year.service.ts`, `school-year.service.spec.ts`, `admin-school-settings.spec.ts`, `orphan-year.ts`, `roles-smoke.spec.ts`, `card.tsx`, `package.json`, `ROADMAP.md:148-238`, `STATE.md:6-31`) and cross-validated against the authoritative REGRESSION-REPORT.md triage. Git log confirms all 10 Wave 1 + Wave 2 commits landed with expected messages (`66514ba`, `a4b52d8`, `8c51ef2`, `5e22d50`, `993aa6f`, `cbd415a`, `1aba6d7`, `20a0460`, `a8459c0`, `6ca1398`). Schema/migration hygiene is EMPTY (CLAUDE.md hard rule trivially satisfied — zero schema change in this phase). ADR Option 4a invariant upheld — `apps/web/src/components/ui/card.tsx` is untouched while the helper achieves equivalent test-ergonomics. ROADMAP descope is consistent: 4 × "E2E (ex-10.4)" receipts in Phase 11 + Phase 12, zero People-CRUD residue inside the 10.4 block, explicit "RESCOPED 2026-04-22" + "Descope-Beschluss (2026-04-22)" markers.

The phase goal — "Schließt die 3 pre-existing Failures (SCHOOL-02/03/05), härtet das E2E-Harness (CardTitle-Helper), schafft die Voraussetzung für Phase 11/12 E2E-Authoring" — is achieved.

---

*Verified: 2026-04-22T08:15:00Z*
*Verifier: Claude (gsd-verifier)*
