---
phase: 10.4
plan: 03
type: execute
wave: 2
depends_on: ["10.4-01", "10.4-02"]
files_modified:
  - .planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md
autonomous: true
requirements: []
tags: [e2e, playwright, regression, verification, roadmap-audit, tier-3a]

must_haves:
  truths:
    - "Full desktop E2E suite green — 23/23 passing (20 previously green + 3 newly fixed: SCHOOL-02/03/05)"
    - "Mobile-375 E2E project runs clean — all .mobile.spec.ts specs pass (pre-existing state preserved, SCHOOL-02 heading fix inherits to the mobile spec if present)"
    - "API unit-test suite green for school-year module (8/8 tests, including the 2 new atomic-demote tests from Plan 10.4-02)"
    - "ROADMAP.md audited read-only: Phase 11 + Phase 12 success criteria contain the 'E2E (ex-10.4)' lines from the 2026-04-22 descope, Phase 10.4 block reflects the rescope, and no orphan references to the old 10.4 scope remain — Deliverable 6"
    - "Regression report file written under phase dir summarizing green-count, failure triage (none expected), and evidence links to CI run + local run"
  artifacts:
    - path: ".planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md"
      provides: "Human-readable regression evidence — green-count, pass/fail matrix per spec, git-log excerpts for the 10.4-01 + 10.4-02 commits, ROADMAP consistency audit checklist"
      min_lines: 60
  key_links:
    - from: "Plan 10.4-03 Task 1"
      to: "Playwright HTML report at apps/web/playwright-report/"
      via: "full-suite run with HTML reporter (already configured in playwright.config.ts)"
      pattern: "playwright-report/index.html"
    - from: "Plan 10.4-03 Task 2"
      to: ".planning/ROADMAP.md Phase 10.4 + 11 + 12 blocks"
      via: "read-only grep audit against the 2026-04-22 descope contract"
      pattern: "E2E \\(ex-10\\.4\\)"
---

<objective>
Close Phase 10.4 with a full-regression gate + a read-only ROADMAP consistency audit. This plan writes NO code — it runs the verification suite, triages results against the expected pass matrix, confirms the ROADMAP descope edits from 2026-04-22 are in place, and produces a regression report artifact for the SUMMARY.md step.

Purpose:
- **Deliverable 5 (LOCKED)** — prove all 4 previous changes (helper, SCHOOL-02 selector, SCHOOL-05 fixture, SCHOOL-03 atomic-demote) hold together as a regression-clean release. The 3 10.3-deferred specs MUST transition from red to green without breaking any of the 20 previously-green specs.
- **Deliverable 6 (LOCKED)** — confirm the 2026-04-22 descope ROADMAP edits are in place in Phase 11/12 success criteria. Per CONTEXT.md: "Plan MUSS nur verifizieren, dass die Edits in `git diff ROADMAP.md` sichtbar sind und im Test-Authoring-Abschnitt von 11/12 stehen. Keine Neuedits durch den Planner."

Output:
- `.planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md` — the regression evidence file (written in Task 2).
- A clean full-suite run (Task 1).
- A ROADMAP audit result embedded in the regression report (Task 2).

## Gating rule

This plan runs AFTER Plans 10.4-01 and 10.4-02 have landed. Wave 2. If EITHER upstream plan is incomplete, abort before running verification — the suite will surface false-red results against un-landed fixes.
</objective>

<execution_context>
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10.4-e2e-admin-ops-people/10.4-CONTEXT.md
@.planning/phases/10.4-e2e-admin-ops-people/10.4-RESEARCH.md
@.planning/phases/10.4-e2e-admin-ops-people/10.4-01-PLAN.md
@.planning/phases/10.4-e2e-admin-ops-people/10.4-02-PLAN.md
@.planning/phases/10.3-e2e-harness-per-role-smoke/deferred-items.md
@.planning/phases/10.3-e2e-harness-per-role-smoke/10.3-02-SUMMARY.md

<interfaces>
<!-- Previously-green specs (from 10.3-02 SUMMARY "20 passed / 3 failed") -->
Desktop green before 10.4: 20 specs including — SCHOOL-01, SCHOOL-04, ZEIT-01, ZEIT-02, YEAR-02, YEAR-03, WOCH-01, SILENT-4XX-01..04, SMOKE-SL-01, SMOKE-LEHRER-01, SMOKE-ELTERN-01, SMOKE-SCHUELER-01, 4 screenshot captures, plus admin-school-settings SCHOOL-01/SCHOOL-04.

Desktop red before 10.4 (to be fixed):
- SCHOOL-02 (strict-mode violation on getByText('Unterrichtstage')) — fixed in Plan 10.4-01 Task 2
- SCHOOL-03 (POST /school-years with isActive:true → 500) — fixed in Plan 10.4-02 Task 2
- SCHOOL-05 (PrismaClientInitializationError in orphan-year fixture) — fixed in Plan 10.4-01 Task 2

Expected post-10.4: 23/23 desktop green (20 preserved + 3 transitioned).

<!-- Mobile-375 project — testMatch /.*\.mobile\.spec\.ts$/ -->
Pre-existing mobile specs: zeitraster.mobile.spec.ts, admin-school-settings.mobile.spec.ts (if exists), screenshots.mobile.spec.ts
Plan 10.4-01 does NOT author new mobile specs; Plan 10.4-02 does NOT author new mobile specs.
Mobile pass-rate should remain at pre-10.4 baseline (no new failures introduced).

<!-- ROADMAP descope contract (from 2026-04-22, verify IN Phase 11 + Phase 12 success criteria) -->
Phase 11 success criteria MUST contain (VERIFIED in ROADMAP.md:202-203):
  - "**E2E (ex-10.4):** Lehrpersonen-CRUD ..."
  - "**E2E (ex-10.4):** Fächer-CRUD ..."

Phase 12 success criteria MUST contain (VERIFIED in ROADMAP.md:227-228):
  - "**E2E (ex-10.4):** Schüler-CRUD ..."
  - "**E2E (ex-10.4):** Klassen-CRUD ..."

Phase 10.4 block (VERIFIED in ROADMAP.md:148-166):
  - Title: "E2E Harness Hardening & 10.3 Deferred-Items Closure (Tier 3a — INSERTED, RESCOPED 2026-04-22)"
  - "Descope-Beschluss (2026-04-22)" paragraph present
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full regression suite — API unit + E2E desktop + E2E mobile-375</name>
  <files>NONE (read-only test execution; no files written in this task)</files>
  <read_first>
    - /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/apps/web/playwright.config.ts (verify projects list — desktop + mobile-375 are the two targets)
    - /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.planning/phases/10.4-e2e-admin-ops-people/10.4-VALIDATION.md §Sampling Rate (full-suite command: `pnpm --filter @schoolflow/web exec playwright test --project=desktop --project=mobile-375 && pnpm --filter @schoolflow/api test school-year`)
    - /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.planning/phases/10.3-e2e-harness-per-role-smoke/10.3-02-SUMMARY.md §"Issues Encountered" (pre-10.4 baseline: 20 passed / 3 failed on desktop, no mobile count documented — capture mobile baseline at start of this task for the report)
  </read_first>
  <action>
**Pre-flight:** Verify upstream plans landed:

```bash
git log --oneline -20 | grep -E "10\.4-(01|02)"
```

Expect to see commits from 10.4-01 (helper, SCHOOL-02 fix, SCHOOL-05 fixture, roles-smoke migration) and 10.4-02 (RED test commit, GREEN service commit, E2E strengthening). If any are missing → STOP, orchestrator hasn't run upstream waves.

**Also verify** the API is rebuilt (post-service-edit binding — memory `feedback_restart_api_after_migration.md` equivalent applies here: the API binds to Prisma at boot; after `school-year.service.ts` change, API must be rebuilt OR running in `--watch` mode):

```bash
# Check if API is running in watch mode (Nest dev auto-reloads on file change):
ps aux | grep -E "nest.*start.*watch" | grep -v grep
# If NOT in watch mode, rebuild:
pnpm --filter @schoolflow/api build
```

**Step 1 — API unit tests:**

```bash
pnpm --filter @schoolflow/api test school-year --run
```

Expected: ALL tests pass (6 pre-existing + 2 new from 10.4-02 = 8 total). Capture stdout to a scratch file:
```bash
pnpm --filter @schoolflow/api test school-year --run 2>&1 | tee /tmp/10.4-api-unit.log
```

**Step 2 — E2E desktop full suite:**

```bash
pnpm --filter @schoolflow/web exec playwright test --project=desktop 2>&1 | tee /tmp/10.4-desktop.log
```

Expected: 23/23 passing. Capture the final summary line (`N passed (Ns)` or `N failed, M passed`).

**If fewer than 23 pass:**
- Diff against the pre-10.4 baseline (20 passed / 3 failed = SCHOOL-02/03/05 red, everything else green).
- If a PREVIOUSLY-GREEN spec is now red → treat as regression. Investigate and either:
  - File it in `deferred-items.md` of phase 10.4 with root-cause sketch (if caused by a 10.4 change but too broad for this plan)
  - Or open a follow-up task in this plan's Task 2 report with specific failure details
- If SCHOOL-02 / SCHOOL-03 / SCHOOL-05 are still red → a 10.4-01 or 10.4-02 change did not land cleanly. Root-cause BEFORE proceeding.

**Step 3 — E2E mobile-375 full suite:**

```bash
pnpm --filter @schoolflow/web exec playwright test --project=mobile-375 2>&1 | tee /tmp/10.4-mobile.log
```

Expected: same pass-count as pre-10.4 baseline (no new failures; this phase does not author new mobile specs). If admin-school-settings.mobile.spec.ts exists and includes a SCHOOL-02 variant that uses `getByText('Unterrichtstage')`, the same heading-selector fix needs a parallel edit — check during triage. If so, that parallel edit is IN SCOPE for this plan's Task 2 addendum (single-line spec edit, mirrors Plan 10.4-01 Task 2).

**Sanity check — mobile SCHOOL-02 preflight:**
```bash
# If a mobile variant exists and contains the old selector, record it:
grep -Hn "getByText('Unterrichtstage')" apps/web/e2e/admin-school-settings.mobile.spec.ts 2>/dev/null || echo "no mobile SCHOOL-02 drift"
```

If output shows a match, the mobile spec needs the same `getByRole('heading', { name: 'Unterrichtstage' })` switch — apply it INLINE as part of Task 1 (one-line fix, already pre-blessed by 10.4-01's ADR), re-run the mobile suite, and note the parallel edit in the regression report.

**Step 4 — Summarize output:**

Keep all three log files (`/tmp/10.4-api-unit.log`, `/tmp/10.4-desktop.log`, `/tmp/10.4-mobile.log`) available for Task 2 to extract counts and any failure details into the regression report. Do NOT commit these logs — they're scratch files.

**Environment reminders from project memory:**
- "Always restart Vite after API rebuild" (`feedback_restart_vite.md`) — if this task rebuilds the API, also restart the Vite dev server OR confirm it's running.
- `docker compose up -d postgres redis keycloak` must be running (Playwright globalSetup fails fast otherwise — no silent redirect loops).
- `DATABASE_URL` must be in the Playwright runner env — but Plan 10.4-01 Task 2 made the fixture dotenv-robust, so local runs without `source .env` are now supported.
  </action>
  <verify>
    <automated>
test -f /tmp/10.4-api-unit.log \
  && grep -qE "^Test Files.*passed" /tmp/10.4-api-unit.log \
  && test -f /tmp/10.4-desktop.log \
  && grep -qE "passed" /tmp/10.4-desktop.log \
  && ! grep -qE "^\\s*[1-9][0-9]* failed" /tmp/10.4-desktop.log \
  && test -f /tmp/10.4-mobile.log \
  && ! grep -qE "^\\s*[1-9][0-9]* failed" /tmp/10.4-mobile.log
    </automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/10.4-api-unit.log` exists AND contains a success summary (`grep -qE "^Test Files.*passed" /tmp/10.4-api-unit.log` exits 0)
    - `/tmp/10.4-desktop.log` exists AND contains a passing summary (`grep -qE "passed" /tmp/10.4-desktop.log` exits 0)
    - `/tmp/10.4-desktop.log` does NOT contain any `N failed` line for N ≥ 1 (`! grep -qE "^\s*[1-9][0-9]* failed" /tmp/10.4-desktop.log` exits 0)
    - `/tmp/10.4-mobile.log` exists AND contains no `N failed` line for N ≥ 1
    - `grep -c "SCHOOL-02" /tmp/10.4-desktop.log | grep -qE "^[1-9]"` returns ≥ 1 (spec was executed)
    - `grep -c "SCHOOL-03" /tmp/10.4-desktop.log | grep -qE "^[1-9]"` returns ≥ 1
    - `grep -c "SCHOOL-05" /tmp/10.4-desktop.log | grep -qE "^[1-9]"` returns ≥ 1
    - If `apps/web/e2e/admin-school-settings.mobile.spec.ts` exists AND contained the old selector: `grep -c "getByText('Unterrichtstage')" apps/web/e2e/admin-school-settings.mobile.spec.ts` returns 0 after Task 1's inline fix
    - If API was rebuilt: `pnpm --filter @schoolflow/api build` exited 0 (no TypeScript errors introduced by 10.4-02)
  </acceptance_criteria>
  <done>
All three test suites (API unit, E2E desktop, E2E mobile-375) complete with zero failures. Pre-existing baseline preserved + 3 deferred specs transitioned green. Scratch logs available for Task 2 reporting.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write regression report + ROADMAP consistency audit (Deliverable 5 + 6)</name>
  <files>.planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md</files>
  <read_first>
    - /tmp/10.4-api-unit.log (from Task 1)
    - /tmp/10.4-desktop.log (from Task 1)
    - /tmp/10.4-mobile.log (from Task 1)
    - /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.planning/ROADMAP.md (Phase 10.4 block at lines 148-166, Phase 11 block at lines 189-211, Phase 12 block at lines 213-236 — full read required to audit success-criteria completeness)
    - /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.planning/phases/10.4-e2e-admin-ops-people/10.4-CONTEXT.md §Deliverable 6 (audit-only clause: "Keine Neuedits durch den Planner")
  </read_first>
  <action>
Create `.planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md` — a structured regression evidence file. This is NOT the SUMMARY.md — it's a focused artifact consumed by the Phase 10.4 SUMMARY.md step later.

**Structure (use these exact headings):**

```markdown
# Phase 10.4 — Regression Report

**Generated:** <YYYY-MM-DD from date command at execution time>
**Scope:** Full-suite verification gate for Plans 10.4-01 + 10.4-02.

---

## 1. Test Results Matrix

### 1.1 API Unit Tests (apps/api — school-year module)

| Module | Pre-10.4 | Post-10.4 | Delta | Source |
|--------|----------|-----------|-------|--------|
| school-year.service.spec.ts | 6/6 | 8/8 | +2 atomic-demote tests from Plan 10.4-02 Task 1 | /tmp/10.4-api-unit.log |

**Summary line from log:** <paste the `Test Files N passed | Tests N passed` summary from /tmp/10.4-api-unit.log>

### 1.2 E2E Desktop (apps/web — --project=desktop)

| Spec | Pre-10.4 | Post-10.4 | Delta | Notes |
|------|----------|-----------|-------|-------|
| SCHOOL-01 | green | green | no change | |
| SCHOOL-02 | **RED** | green | fixed (heading selector, Plan 10.4-01) | strict-mode violation resolved |
| SCHOOL-03 | **RED** | green | fixed (atomic demote, Plan 10.4-02) | incumbent active year demoted in same tx |
| SCHOOL-04 | green | green | no change | |
| SCHOOL-05 | **RED** | green | fixed (dotenv + datasources, Plan 10.4-01) | fixture env-robust |
| ZEIT-01 | green | green | no change | |
| ZEIT-02 | green | green | no change | |
| YEAR-02 | green | green | no change | |
| YEAR-03 | green | green | no change | |
| WOCH-01 | green | green | no change | |
| SILENT-4XX-01 | green | green | no change | |
| SILENT-4XX-02 | green | green | no change | |
| SILENT-4XX-03 | green | green | no change | |
| SILENT-4XX-04 | green | green | no change | |
| SMOKE-SL-01 | green | green | no change | |
| SMOKE-LEHRER-01 | green | green | migrated to getByCardTitle helper (Plan 10.4-01 Task 3) |
| SMOKE-ELTERN-01 | green | green | migrated to getByCardTitle helper (Plan 10.4-01 Task 3) |
| SMOKE-SCHUELER-01 | green | green | migrated to getByCardTitle helper (Plan 10.4-01 Task 3) |
| Screenshot captures (4) | green | green | no change | |

**Summary line from log:** <paste the final `N passed (Ns)` from /tmp/10.4-desktop.log>

**Total:** 20 → 23 green (all 3 pre-existing failures resolved, zero new failures).

### 1.3 E2E Mobile-375 (apps/web — --project=mobile-375)

| Spec | Pre-10.4 | Post-10.4 | Delta |
|------|----------|-----------|-------|
| <per-spec from log, no changes expected from this phase> | <green/red> | <same> | no change |

**Mobile SCHOOL-02 parallel fix** (if admin-school-settings.mobile.spec.ts had the old `getByText('Unterrichtstage')` selector): applied inline during Task 1. Confirm via `grep -c "getByRole('heading', { name: 'Unterrichtstage' })" apps/web/e2e/admin-school-settings.mobile.spec.ts` = 1 if applicable.

**Summary line from log:** <paste from /tmp/10.4-mobile.log>

---

## 2. Deliverable 5 Closure (LOCKED from CONTEXT.md)

- [x] Alle existierenden desktop E2E-Specs bleiben grün: 20 preserved.
- [x] SCHOOL-02 jetzt grün: selector switched to `getByRole('heading', { name: 'Unterrichtstage' })` (Plan 10.4-01 Task 2).
- [x] SCHOOL-03 jetzt grün: `SchoolYearService.create` wraps create in `$transaction` with conditional `updateMany` demote (Plan 10.4-02 Task 2).
- [x] SCHOOL-05 jetzt grün: `orphan-year.ts` loads `dotenv/config` + passes explicit `datasources.db.url` to `PrismaClient` (Plan 10.4-01 Task 2).
- [x] Mobile-375 SCHOOL-02 assertion passes (parallel spec edit if mobile variant exists; otherwise N/A).

---

## 3. Deliverable 6 Closure — ROADMAP Consistency Audit (READ-ONLY)

Per CONTEXT.md: "Plan MUSS nur verifizieren, dass die Edits in `git diff ROADMAP.md` sichtbar sind und im Test-Authoring-Abschnitt von 11/12 stehen. Keine Neuedits durch den Planner."

### 3.1 Phase 10.4 block consistency (ROADMAP.md:148-166)

- [x] Phase title contains "RESCOPED 2026-04-22": `grep -c "RESCOPED 2026-04-22" .planning/ROADMAP.md` = ≥ 1
- [x] Descope-Beschluss paragraph present: `grep -c "Descope-Beschluss (2026-04-22)" .planning/ROADMAP.md` = ≥ 1
- [x] Success criterion "CardTitle-Harness-Hardening" present: `grep -c "CardTitle-Harness-Hardening" .planning/ROADMAP.md` = ≥ 1
- [x] Phase 10.4 "Plans: 0 plans" line reflects pre-planning state OR is updated by plan-phase completion hook — <record actual value>

### 3.2 Phase 11 descope receipts (ROADMAP.md:189-211)

- [x] Success criteria contain Lehrpersonen-CRUD ex-10.4 line: `grep -c "E2E (ex-10.4):.*Lehrpersonen-CRUD" .planning/ROADMAP.md` = ≥ 1
- [x] Success criteria contain Fächer-CRUD ex-10.4 line: `grep -c "E2E (ex-10.4):.*Fächer-CRUD" .planning/ROADMAP.md` = ≥ 1
- [x] Orphan-Guard-Backend-Gap for Teacher/Subject DELETE noted in Phase 11 risks section (gap candidate): `grep -c "Orphan-Guard-Backend für Teacher/Subject DELETE" .planning/ROADMAP.md` = ≥ 1

### 3.3 Phase 12 descope receipts (ROADMAP.md:213-236)

- [x] Success criteria contain Schüler-CRUD ex-10.4 line: `grep -c "E2E (ex-10.4):.*Schüler-CRUD" .planning/ROADMAP.md` = ≥ 1
- [x] Success criteria contain Klassen-CRUD ex-10.4 line: `grep -c "E2E (ex-10.4):.*Klassen-CRUD" .planning/ROADMAP.md` = ≥ 1
- [x] Elternlink-HTTP-Surface gap noted in Phase 12 risks: `grep -c "Elternlink-HTTP-Surface" .planning/ROADMAP.md` = ≥ 1
- [x] Orphan-Guard-Backend-Gap for Student/SchoolClass DELETE noted: `grep -c "Orphan-Guard-Backend für Student/SchoolClass DELETE" .planning/ROADMAP.md` = ≥ 1

### 3.4 No orphan references to the OLD 10.4 People-CRUD scope

Guards against stale text that still describes the pre-2026-04-22 scope in Phase 10.4's block:

- [x] Phase 10.4 block does NOT claim to test classes/teachers/students/subjects CRUD: `sed -n '148,167p' .planning/ROADMAP.md | grep -ciE "Klassen-CRUD|Lehrpersonen-CRUD|Schüler-CRUD|Fächer-CRUD"` returns 0 (the inside-10.4-block count is 0; those strings now live in 11 + 12 blocks only)
- [x] Phase 10.4 "Depends on" = "Phase 10.3": `grep -A2 "### Phase 10.4:" .planning/ROADMAP.md | grep -c "Depends on.*Phase 10.3"` = 1

**If ANY of the checks above fail:** the planner must escalate to the user via `## CHECKPOINT REACHED — ROADMAP audit failure` at the top of the regression report, NOT attempt to re-edit the ROADMAP (per CONTEXT.md Deliverable 6 "Keine Neuedits durch den Planner"). Record the failing check with its actual grep output for the user.

---

## 4. Git History Evidence

### Commits introduced by Phase 10.4 (from `git log --oneline --since="2026-04-22" --grep="10.4"`):

<paste `git log --oneline --since="2026-04-22" --grep="10.4"` output here>

Expected commit shape (5 commits minimum):
- `test(10.4-01): ...` or `chore(10.4-01): ...` (helper file + related edits)
- `test(10.4-02): add failing atomic-demote tests for SchoolYearService.create` (RED phase)
- `feat(10.4-02): atomic-demote incumbent active year in SchoolYearService.create` (GREEN phase)
- `test(10.4-02): strengthen SCHOOL-03 with API single-active invariant + throwaway cleanup`
- `docs(10.4-03): regression report + ROADMAP audit` (this plan's commit, landed after this file is written)

### Schema / migration guard (CLAUDE.md hard rule)

- [x] `git status --porcelain apps/api/prisma/schema.prisma` returns empty (no schema change across Phase 10.4)
- [x] `git status --porcelain apps/api/prisma/migrations` returns empty (no new migration folder across Phase 10.4)

---

## 5. Follow-Up Items

**None blocking** — Phase 10.4 closes cleanly with all 3 deferred items resolved and no new follow-ups.

Potential Phase-10.5 or Phase-11 considerations (informational, NOT gates):

- **Seed DB enrichment for kc-* personas** — the smoke tests still exercise the empty-state branch (CardTitle helper covers it). Deferred per CONTEXT.md Deferred Ideas #1.
- **`getByCardTitle` helper adoption** — future Phase 11/12 specs asserting on CardTitle should import the helper from `apps/web/e2e/helpers/card.ts`. Surface this as a convention in the Phase 11 discuss-phase.
```

**Constraints:**
- Fill in EVERY `<paste ... here>` placeholder with actual output from the scratch logs or from `grep` / `git log` commands you run during this task.
- Do NOT edit `.planning/ROADMAP.md` — audit is read-only per Deliverable 6.
- If ANY of the Section 3 checks fail, prepend a `## ⚠ CHECKPOINT REACHED — ROADMAP audit failure` header to the report (do NOT create the report as if clean) and return control to the orchestrator via the structured return format.
- Use the `date +%Y-%m-%d` command to fill in the `**Generated:**` timestamp.

**Commit message:** `docs(10.4-03): regression report + ROADMAP audit for Phase 10.4 closure`
  </action>
  <verify>
    <automated>
test -f .planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md \
  && [ "$(wc -l < .planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md)" -ge 60 ] \
  && grep -c "Deliverable 5 Closure" .planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md | grep -q "^1$" \
  && grep -c "Deliverable 6 Closure" .planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md | grep -q "^1$" \
  && grep -c "SCHOOL-02" .planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md | grep -qE "^[2-9]|^[1-9][0-9]" \
  && grep -c "SCHOOL-03" .planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md | grep -qE "^[2-9]|^[1-9][0-9]" \
  && grep -c "SCHOOL-05" .planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md | grep -qE "^[2-9]|^[1-9][0-9]" \
  && grep -c "E2E (ex-10.4)" .planning/ROADMAP.md | grep -qE "^[4-9]" \
  && ! git status --porcelain apps/api/prisma/schema.prisma | grep -q . \
  && ! git status --porcelain apps/api/prisma/migrations | grep -q .
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md` exits 0
    - Report line count ≥ 60 (substantive content, not a stub)
    - `grep -c "Deliverable 5 Closure" .planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md` returns exactly 1
    - `grep -c "Deliverable 6 Closure" .planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md` returns exactly 1
    - `grep -c "Test Results Matrix" .planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md` returns exactly 1
    - Report mentions SCHOOL-02, SCHOOL-03, SCHOOL-05 each at least 2 times (in the matrix + in Deliverable 5 closure section)
    - `grep -c "E2E (ex-10.4)" .planning/ROADMAP.md` returns ≥ 4 (2 in Phase 11 + 2 in Phase 12 = 4 minimum per the descope contract)
    - `grep -c "Descope-Beschluss (2026-04-22)" .planning/ROADMAP.md` returns ≥ 1
    - `grep -c "RESCOPED 2026-04-22" .planning/ROADMAP.md` returns ≥ 1
    - `grep -c "Elternlink-HTTP-Surface" .planning/ROADMAP.md` returns ≥ 1
    - `grep -c "Orphan-Guard-Backend für Teacher/Subject DELETE" .planning/ROADMAP.md` returns ≥ 1
    - `grep -c "Orphan-Guard-Backend für Student/SchoolClass DELETE" .planning/ROADMAP.md` returns ≥ 1
    - `git status --porcelain apps/api/prisma/schema.prisma` returns empty (no schema change across Phase 10.4)
    - `git status --porcelain apps/api/prisma/migrations` returns empty (no new migration folders)
    - `git status --porcelain .planning/ROADMAP.md` returns empty AT END OF TASK (audit-only per Deliverable 6)
    - Report does NOT contain any `## ⚠ CHECKPOINT REACHED` header (all audit checks passed cleanly) — IF such header is present, the plan is in a failure state and orchestrator must resolve
    - Final commit matches `^docs\(10\.4-03\):` in `git log --oneline -1`
  </acceptance_criteria>
  <done>
Regression report file written with all log summaries embedded, Deliverable 5 + 6 audit checklists ticked (all green), no ROADMAP edits, no migration/schema drift, commit landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan runs read-only commands (test runners, grep, git status). No new trust boundaries introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.4-03-01 | Tampering | Scratch log files at /tmp/* could be tampered with between Task 1 and Task 2 | accept | /tmp is process-local, session-short-lived, and the verify commands for Task 2 grep the REPORT file (committed) not the raw logs; tampering a tmp file only affects the report content, which is still subject to automated acceptance criteria. |
| T-10.4-03-02 | Repudiation | Report could claim green without actually running the suite | mitigate | Task 1 `<automated>` block writes the logs AND checks them for absence of `N failed`. Task 2 acceptance criteria cross-reference ROADMAP grep counts + schema-clean guards. Both gates must pass. |
</threat_model>

<verification>
Phase-level gate (after this plan completes):
- [x] Full-suite (API unit + E2E desktop + E2E mobile-375) green
- [x] All 3 10.3-deferred SCHOOL-0X tests transition red → green
- [x] Regression report file exists and is ≥ 60 lines
- [x] ROADMAP audit produces zero failing checks
- [x] No schema drift, no new migration folders

Commands for /gsd:verify-work:
```bash
pnpm --filter @schoolflow/web exec playwright test --project=desktop && \
pnpm --filter @schoolflow/web exec playwright test --project=mobile-375 && \
pnpm --filter @schoolflow/api test school-year --run && \
test -f .planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md && \
! git status --porcelain apps/api/prisma/schema.prisma | grep -q . && \
! git status --porcelain apps/api/prisma/migrations | grep -q .
```
</verification>

<success_criteria>
- [x] Deliverable 5: Gesamt-Regression passed — 23/23 desktop + mobile baseline preserved + API units 8/8
- [x] Deliverable 6: ROADMAP audit checklist passed (all 14 consistency checks green) with zero planner-side ROADMAP edits
- [x] `.planning/phases/10.4-e2e-admin-ops-people/10.4-REGRESSION-REPORT.md` exists with structured matrix + audit receipts + git-log evidence
- [x] No schema.prisma drift, no new migration folders (CLAUDE.md hard rule satisfied across the entire phase)
- [x] Phase 10.4 commit history is linear (5+ commits: 10.4-01 × 1+, 10.4-02 × 3, 10.4-03 × 1)
</success_criteria>

<output>
After completion, create `.planning/phases/10.4-e2e-admin-ops-people/10.4-03-SUMMARY.md` summarizing the verification gate result and linking to the regression report.
</output>
