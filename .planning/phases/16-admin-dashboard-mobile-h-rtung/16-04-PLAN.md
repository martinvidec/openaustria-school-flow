---
phase: 16-admin-dashboard-mobile-h-rtung
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/ui/button.tsx
  - apps/web/src/components/ui/input.tsx
  - apps/web/src/components/ui/select.tsx
  - apps/web/src/components/ui/textarea.tsx
  - apps/web/src/components/ui/button.test.tsx
  - apps/web/src/components/ui/input.test.tsx
  - apps/web/src/components/ui/select.test.tsx
autonomous: true
requirements: [MOBILE-ADM-02]
must_haves:
  truths:
    - "Button (default/sm/icon) min-height is >= 44px at <sm viewport per D-17 global touch-target hardening"
    - "Input min-height is >= 44px at <sm viewport per D-17"
    - "Select trigger min-height is >= 44px at <sm viewport per D-17"
    - "Desktop heights remain unchanged (h-10 for default Button, Input, Select; h-9 for Button sm)"
    - "Touch-target lift uses Tailwind responsive classes (min-h-11 sm:min-h-{n}), NOT a new mobile-named variant — global lift per D-17 prevents per-form patches"
  artifacts:
    - path: "apps/web/src/components/ui/button.tsx"
      provides: "Button primitive with responsive min-h-11 floor on <sm"
    - path: "apps/web/src/components/ui/input.tsx"
      provides: "Input primitive with responsive min-h-11 floor on <sm"
    - path: "apps/web/src/components/ui/select.tsx"
      provides: "Select trigger with responsive min-h-11 floor on <sm"
    - path: "apps/web/src/components/ui/textarea.tsx"
      provides: "Textarea audit verification (already >= 80px — no edit unless audit fails)"
  key_links:
    - from: "apps/web/src/components/ui/button.tsx"
      to: "all 100+ Button call-sites"
      via: "size variant class output"
      pattern: "min-h-11"
    - from: "apps/web/src/components/ui/input.tsx"
      to: "all Input call-sites"
      via: "default class output"
      pattern: "min-h-11"
---

<objective>
Lift the touch-target floor on shadcn/ui primitives so EVERY existing call-site (100+ Buttons, dozens of Inputs/Selects) automatically satisfies the 44px Mobile-Härtung floor on viewports `<sm` (640px) — without breaking dense desktop layouts. Per CONTEXT D-17 + UI-SPEC § Spacing § Touch-target floors.

Purpose: A single primitive-level edit replaces N per-form patches and prevents drift in future forms. The lift is responsive: desktop pointer accuracy keeps existing heights (`h-9`/`h-10`); mobile (`<sm`) gets `min-h-11` (44px floor).
Output: Edits to button.tsx, input.tsx, select.tsx (textarea.tsx audited; edit only if needed). Unit DOM-assertion tests verify both viewports.
</objective>

<execution_context>
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-admin-dashboard-mobile-h-rtung/16-CONTEXT.md
@.planning/phases/16-admin-dashboard-mobile-h-rtung/16-RESEARCH.md
@.planning/phases/16-admin-dashboard-mobile-h-rtung/16-PATTERNS.md
@.planning/phases/16-admin-dashboard-mobile-h-rtung/16-UI-SPEC.md
@apps/web/src/components/ui/button.tsx
@apps/web/src/components/ui/input.tsx
@apps/web/src/components/ui/select.tsx
@apps/web/src/components/ui/textarea.tsx
@apps/web/src/lib/utils.ts

<interfaces>
<!-- Current primitives state (verified via PATTERNS.md lines 491-518). -->

`button.tsx:22-27` (current cva sizes object):
```typescript
size: {
  default: "h-10 px-4 py-2",
  sm: "h-9 rounded-md px-3",
  lg: "h-11 rounded-md px-8",
  icon: "h-10 w-10",
},
```

`input.tsx:13` (current default class):
```typescript
"flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background ..."
```

`select.tsx:22` (current default class — verified):
```typescript
"flex h-10 w-full items-center justify-between rounded-md border border-input bg-background px-3 py-2 text-sm ..."
```

`textarea.tsx:12` (current default class):
```typescript
"flex min-h-[80px] w-full rounded-md border border-input bg-background px-3 py-2 text-sm ..."
```

UI-SPEC § Spacing § Touch-target floors target classes (lines 67-76 + Pitfall #4 audit):
| Primitive | Replace | With |
|-----------|---------|------|
| Button default | `h-10 px-4 py-2` | `h-10 px-4 py-2 min-h-11 sm:min-h-10` |
| Button sm | `h-9 rounded-md px-3` | `h-9 rounded-md px-3 min-h-11 sm:min-h-9` |
| Button lg | `h-11 rounded-md px-8` | unchanged (already 44px) |
| Button icon | `h-10 w-10` | `h-10 w-10 min-h-11 min-w-11 sm:min-h-10 sm:min-w-10` |
| Input | `h-10 ...` | `h-10 ... min-h-11 sm:min-h-10` |
| Select trigger | `h-10 ...` | `h-10 ... min-h-11 sm:min-h-10` |
| Textarea | `min-h-[80px] ...` | unchanged (already >44px) — but verify in audit |

RESEARCH Open Question #4 resolution: keep `h-{n}` declared AND prepend `min-h-11 sm:min-h-{n}`. Tailwind merges via `cn()`. Caller-passed `className` last wins.

RESEARCH Pitfall #7: Lifting `Button size="sm"` from `h-9` to `min-h-11` on desktop would make admin tables look oversized. The responsive lift `min-h-11 sm:min-h-9` keeps desktop unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lift Button + Input + Select primitives + textarea audit</name>
  <files>apps/web/src/components/ui/button.tsx, apps/web/src/components/ui/input.tsx, apps/web/src/components/ui/select.tsx, apps/web/src/components/ui/textarea.tsx, apps/web/src/components/ui/button.test.tsx, apps/web/src/components/ui/input.test.tsx, apps/web/src/components/ui/select.test.tsx</files>
  <read_first>
    - apps/web/src/components/ui/button.tsx (lines 22-27 — cva size variants)
    - apps/web/src/components/ui/input.tsx (line 13 — default className)
    - apps/web/src/components/ui/select.tsx (line 22 — SelectTrigger className)
    - apps/web/src/components/ui/textarea.tsx (line 12 — default className)
    - apps/web/src/lib/utils.ts (cn utility)
    - .planning/phases/16-admin-dashboard-mobile-h-rtung/16-UI-SPEC.md (lines 60-79 — touch-target floors table)
    - .planning/phases/16-admin-dashboard-mobile-h-rtung/16-RESEARCH.md (Pitfall #4 — audit grep, Pitfall #7 — desktop regression)
    - .planning/phases/16-admin-dashboard-mobile-h-rtung/16-PATTERNS.md (lines 491-532 — verbatim lift table)
  </read_first>
  <behavior>
    button.test.tsx:
    - Test 1: `<Button>Click</Button>` (size=default) renders with className containing both `h-10` AND `min-h-11`
    - Test 2: `<Button size="sm">` renders with `h-9` AND `min-h-11`
    - Test 3: `<Button size="lg">` renders with `h-11` (unchanged) — no `min-h-11` duplication needed but acceptable
    - Test 4: `<Button size="icon">` renders with `h-10 w-10` AND `min-h-11 min-w-11`
    - Test 5: caller `className` override survives (`<Button className="h-12">` — `h-12` AND `min-h-11` both present after cn merge)

    input.test.tsx:
    - Test 1: `<Input />` renders with className containing `h-10` AND `min-h-11`
    - Test 2: `<Input className="h-14" />` retains both override and floor

    select.test.tsx:
    - Test 1: `<SelectTrigger>` renders with className containing `h-10` AND `min-h-11`
  </behavior>
  <action>
    1. Edit `apps/web/src/components/ui/button.tsx` — find the `cva` `size` object at line 22-27 and rewrite EXACTLY:
    ```typescript
    size: {
      default: "h-10 px-4 py-2 min-h-11 sm:min-h-10",
      sm: "h-9 rounded-md px-3 min-h-11 sm:min-h-9",
      lg: "h-11 rounded-md px-8",
      icon: "h-10 w-10 min-h-11 min-w-11 sm:min-h-10 sm:min-w-10",
    },
    ```

    2. Edit `apps/web/src/components/ui/input.tsx` — find the `cn(...)` call at line 13. Modify the default class string from `"flex h-10 w-full ..."` to `"flex h-10 min-h-11 sm:min-h-10 w-full ..."`. The exact replacement: insert `min-h-11 sm:min-h-10` immediately after `h-10` in the className string.

    3. Edit `apps/web/src/components/ui/select.tsx` — find the SelectTrigger className at line 22 (`"flex h-10 w-full ..."`) and modify identically: insert `min-h-11 sm:min-h-10` after `h-10`.

    4. Audit `apps/web/src/components/ui/textarea.tsx`:
       - Read line 12. The current `min-h-[80px]` is already > 44px, so NO edit is required.
       - If grep reveals other height-related issues (e.g. caller-overridable `h-{n}` that drops below 44px), document in summary; otherwise leave the file unchanged.

    5. Create `apps/web/src/components/ui/button.test.tsx` per behavior block. Use RTL `render(<Button>...)` + `getByRole('button')` + `expect(button).toHaveClass('h-10', 'min-h-11')`. Same pattern for input.test.tsx and select.test.tsx.

    6. Run a sweep grep to confirm NO unintended changes in other primitive files:
       ```bash
       grep -nE "h-(9|10)\b" apps/web/src/components/ui/{button,input,select,textarea}.tsx
       ```
       The output should ONLY include the 4 lines edited above (per Pitfall #4 — no other primitive heights silently regressed).
  </action>
  <verify>
    <automated>pnpm --filter @schoolflow/web test --run button.test input.test select.test && pnpm --filter @schoolflow/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/src/components/ui/button.tsx` contains all four size patterns with their floor classes: `grep -c "min-h-11" apps/web/src/components/ui/button.tsx | grep -v '^#'` returns ≥ 3 (default, sm, icon — not lg)
    - File contains `sm:min-h-10` AND `sm:min-h-9` (responsive desktop preservation)
    - File `apps/web/src/components/ui/input.tsx` contains `min-h-11 sm:min-h-10`
    - File `apps/web/src/components/ui/select.tsx` contains `min-h-11 sm:min-h-10`
    - File `apps/web/src/components/ui/textarea.tsx` contains `min-h-[80px]` (verifying audit found no edit needed; if edited, contains `min-h-11`)
    - All three tests pass: `pnpm --filter @schoolflow/web test --run button.test input.test select.test` exits 0
    - `pnpm --filter @schoolflow/web exec tsc --noEmit` exits 0
    - No new "size" variant added (pattern: `size: {` block contains only `default`, `sm`, `lg`, `icon` — no `mobile`-named variant per UI-SPEC line 79)
  </acceptance_criteria>
  <done>Touch-target floor lifted on Button (3 sizes), Input, Select; textarea audited; desktop heights preserved via sm: prefix; unit tests verify both class outputs; no new variants introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — pure CSS-class edit) | This plan has no auth/data/network surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-11 | (Accessibility / non-STRIDE) | shadcn primitives | mitigate | WCAG 2.1 AAA touch-target minimum 44×44px enforced on `<sm`. Plan 05 + Plan 07 verify via Playwright sweep with 43.5px tolerance for sub-pixel rounding. |

(Plan 04 has no STRIDE threats — it is a CSS-only edit. Listed for completeness; no security implications.)
</threat_model>

<verification>
- TypeScript clean: `pnpm --filter @schoolflow/web exec tsc --noEmit` exits 0
- Unit tests pass: `pnpm --filter @schoolflow/web test --run button.test input.test select.test` exits 0
- Visual smoke (manual): admin opens any form (e.g. school.settings) on desktop — buttons/inputs unchanged at 40px. Resize to <640px — buttons/inputs grow to 44px floor.
- Pitfall #4 grep verified: no other primitive files have stray h-9/h-10 that escaped audit (run `grep -nE "h-(9|10)\b" apps/web/src/components/ui/*.tsx` and document hits in summary)
- Pitfall #7 verified: desktop dense layouts (Phase 14 SubjectPreferencesTab, audit-log filter bar) render at unchanged height — verified by Plan 07 E2E desktop project regression
</verification>

<success_criteria>
- Button (default/sm/icon) has min-h-11 on <sm with sm:min-h-{original} preserving desktop
- Input has min-h-11 sm:min-h-10
- Select trigger has min-h-11 sm:min-h-10
- Textarea audited (likely no edit needed — min-h-[80px] already > 44px)
- 3 unit tests verify class outputs at both viewports
- No new shadcn variant introduced (D-17 mandates lift on existing primitives, not new variants)
- No call-site changes required across the 100+ Button/Input/Select consumers
</success_criteria>

<output>
After completion, create `.planning/phases/16-admin-dashboard-mobile-h-rtung/16-04-SUMMARY.md` per templates/summary.md. Document:
- Final classNames written to each primitive file (for quick reference by Plan 05/07)
- Whether textarea required an edit (RESEARCH expected no — verify and document)
- Pitfall #4 audit grep output: list of any OTHER primitives with sub-44px heights still lurking (e.g. radio-group, checkbox icon size, toggle) — these become Plan 05/07 follow-ups if confirmed sub-44px
- Pitfall #7 verification approach used (visual desktop check vs Plan 07 deferral)
</output>
