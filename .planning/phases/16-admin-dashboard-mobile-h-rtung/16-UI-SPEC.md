---
phase: 16
slug: admin-dashboard-mobile-h-rtung
status: draft
shadcn_initialized: true
preset: "default / neutral / cssVariables (apps/web/components.json)"
created: 2026-04-28
---

# Phase 16 — UI Design Contract

> Visual and interaction contract for the Admin-Dashboard (`/admin`) and the cross-cutting Mobile-Härtung sweep across Phase 10–15 admin surfaces. Generated by `gsd-ui-researcher` from `16-CONTEXT.md` (D-01..D-22), `REQUIREMENTS.md` (ADMIN-01/02/03 + MOBILE-ADM-01/02/03), `15-UI-SPEC.md` (carry-forward), and direct inspection of `apps/web/components.json` + `apps/web/src/app.css`.

Scope of this contract:

- **A) Admin-Dashboard surface** — single page at `/admin` with a vertical Setup-Completeness checklist of 10 categories plus role-aware login redirect from `/`.
- **B) Mobile-Härtung surface** — global touch-target lift, shared `<DataList>` component with column→card render-prop, and a 375px audit-and-fix sweep across all admin pages from Phases 10–15.

This phase introduces ZERO new design tokens, ZERO new shadcn primitives, and ZERO new color values. Every visual decision below references existing tokens already declared in `apps/web/src/app.css` (carry-forward from Phase 15).

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | `shadcn` (already initialised in `apps/web`) | `apps/web/components.json` |
| Preset | `style: default`, `baseColor: neutral`, `cssVariables: true`, `tsx: true` | `apps/web/components.json` |
| Component library | Radix UI primitives via shadcn copies in `apps/web/src/components/ui/` | 24 primitives present (carry-forward Phase 15) |
| Icon library | `lucide-react` (`iconLibrary: "lucide"`) | `apps/web/components.json` line 26 |
| Font | `Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif` | `apps/web/src/app.css` `--font-sans` |
| Tailwind | v4 `@import "tailwindcss"` + `@theme` block | `apps/web/src/app.css` |

**Phase 16 uses ONLY the existing shadcn copies. No new primitive added.**

In-scope primitives (all already present):
`card.tsx`, `badge.tsx`, `button.tsx`, `separator.tsx`, `scroll-area.tsx`, `sheet.tsx` (existing `MobileSidebar` drawer pattern), `tooltip.tsx`, `dialog.tsx` (used elsewhere — not by Phase 16 surfaces themselves), `sonner.tsx` (toasts).

**New shared component (NOT a shadcn primitive — internal helper):** `<DataList>` at `apps/web/src/components/shared/DataList.tsx`. This is a Phase-16 invention per CONTEXT D-12 — owned by this phase, NOT registry-vendored. Spec in § Component Inventory below.

**New shared hook:** `useIsMobile()` extracted from `apps/web/src/routes/__root.tsx:20-32` to `apps/web/src/hooks/useIsMobile.ts` (per CONTEXT D-13). Default breakpoint stays at `640px`. Behaviour-preserving move — no API change.

---

## Spacing Scale

8-point Tailwind scale (Phase 15 carry-forward — no new tokens). Multiples of 4 only. Phase 16 uses Tailwind utility tokens directly.

| Token | Value | Tailwind | Phase 16 usage |
|-------|-------|----------|---------------|
| xs | 4px | `gap-1`, `p-1` | Icon ↔ label gap inside checklist row; badge inner padding |
| sm | 8px | `gap-2`, `p-2` | Internal gap inside checklist row (icon → text block); mobile-card field-internal padding |
| md | 16px | `gap-4`, `p-4` | Default card padding; mobile-card horizontal padding; vertical gap between checklist rows |
| lg | 24px | `gap-6`, `p-6` | `PageShell` content padding; gap between `PageShell` header and checklist; gap between filter-toolbar and `<DataList>` |
| xl | 32px | `gap-8`, `mb-8` | Gap between major page sections (e.g. Dashboard intro paragraph → checklist) |
| 2xl | 48px | `gap-12`, `py-12` | Empty-state vertical padding inside `<DataList>` empty slot (`Keine Einträge`) |
| 3xl | 64px | `py-16` | Reserved — not used by Phase 16 surfaces. Listed for design-system completeness. |

### Touch-target floors (CONTEXT D-17 — global lift)

This is the headline Mobile-Härtung change. All interactive primitives MUST satisfy these floors after Phase 16 ships:

| Primitive | Today (per current source) | Phase 16 floor | Tailwind |
|-----------|---------------------------|----------------|----------|
| `Button` `size="default"` | `h-10` (40px) | `min-h-11` (44px) on `<sm` + keep `h-10` desktop | `h-10 sm:h-10 min-h-11 sm:min-h-10` |
| `Button` `size="sm"` | `h-9` (36px) | `min-h-11` (44px) on `<sm` | `h-9 min-h-11 sm:min-h-9` |
| `Button` `size="lg"` | `h-11` (44px) | unchanged | `h-11` |
| `Button` `size="icon"` | `h-10 w-10` (40×40) | `min-h-11 min-w-11` on `<sm` | `h-10 w-10 min-h-11 min-w-11 sm:min-h-10 sm:min-w-10` |
| `Input` | `h-10` (40px) per `input.tsx:13` | `min-h-11` (44px) on `<sm` | `h-10 min-h-11 sm:min-h-10` |
| Checklist row (Dashboard) | n/a (new) | `min-h-14` (56px) — comfortable on touch + room for icon + 2-line copy | `min-h-14` |
| Mobile-card row (`<DataList>`) | n/a (new) | `min-h-20` (80px) per CONTEXT specifics line 172 | `min-h-20` |

**Rationale for the responsive lift:** desktop pointer accuracy makes 40px targets perfectly usable; tightening desktop to 44px makes UI feel oversized in dense admin tables. The lift is `<sm` only (≤639px). This matches the `useIsMobile()` 640px breakpoint exactly so logic and layout agree.

**Edit strategy** — globally lift `Button` and `Input` defaults inside `apps/web/src/components/ui/button.tsx` + `input.tsx` using the responsive Tailwind classes above. Do NOT introduce a new `mobile`-named variant — keep the existing `size` enum and apply the floor unconditionally. This makes the lift transparent for all 100+ existing call-sites.

### Mobile-card density (CONTEXT specifics line 172)

A `<DataList>` mobile card target is **≤80px tall** with default content. This means: leading icon + 2 lines of body copy + trailing chevron, separated by a 1px border. Padding is `p-4` (16px); content gap is `gap-2` (8px).

### Spacing exceptions

- **Dashboard checklist row** internal layout: `flex items-center gap-3 px-4 py-3` (12px vertical to land `min-h-14`). Visual: leading icon (24×24) → title block (title + status text stacked) → trailing status badge → chevron. This is a single horizontal flex row at all viewports — the layout does NOT collapse to two lines on mobile. Status badge MAY shrink to icon-only at `<sm` (see Component Inventory § ChecklistItem).

---

## Typography

Carry-forward from Phase 15 — same 4 sizes, same 2 weights, no italic, no bold-700.

| Role | Size | Weight | Line height | Tailwind | Phase 16 usage |
|------|------|--------|-------------|----------|---------------|
| Body | 14px | 400 | 1.5 (`leading-6`) | `text-sm font-normal leading-6` | Checklist item description; mobile-card field values; intro paragraph copy; empty-state body |
| Label | 14px | 400 | 1.4 (`leading-5`) | `text-sm font-normal leading-5 text-muted-foreground` | Checklist secondary text ("Erledigt am 24.04.2026", "12 Lehrer"); mobile-card field labels; status-badge text. **Differentiation via `text-muted-foreground` — NOT weight.** |
| Heading | 20px | 600 | 1.3 (`leading-7`) | `text-xl font-semibold leading-7` | Checklist row title (e.g. "Schule", "Zeitraster"); section titles |
| Display | 28px | 600 | 1.2 (`leading-8`) | `text-2xl font-semibold leading-8` | `PageShell` page title ("Dashboard", "Setup-Übersicht") — already rendered via `PageShell` `text-2xl md:text-3xl` |

**Two weights only**: `font-normal` (400) for body and label, `font-semibold` (600) for heading and display. Status-badge text uses `font-medium` (500) inherits from the existing `Badge` primitive (`text-xs font-semibold` per `badge.tsx:7`); we keep that primitive untouched — the per-phase 2-weight rule applies to all NEW content. Existing primitives keep their internal weights.

**Mono**: not used by Phase 16 surfaces. Reserved by Phase 15 for `<JsonTree>` only.

---

## Color

The 60/30/10 split is defined against the live `@theme` block in `apps/web/src/app.css` — Phase 16 introduces NO new color tokens.

| Role | Value | Token | Usage |
|------|-------|-------|-------|
| Dominant (60%) | `hsl(0 0% 100%)` | `--color-background` | Page background; `<DataList>` row background; checklist row background |
| Secondary (30%) | `hsl(240 5% 96%)` | `--color-card` / `--color-muted` | `PageShell` outer container background; sidebar; mobile-card outer surface (when stacked); checklist hover state (`hover:bg-accent`) |
| Accent (10%) | `hsl(221 83% 53%)` | `--color-primary` | **RESERVED FOR** (explicit list — see below) |
| Destructive | `hsl(0 84% 60%)` | `--color-destructive` | Status badge `Fehlt` only |
| Success | `hsl(142 71% 45%)` | `--color-success` | Status badge `Erledigt` only |
| Warning | `hsl(38 92% 50%)` | `--color-warning` | Status badge `Unvollständig` only |

**Accent reserved for** (NEVER applied outside this list):
1. Active sidebar route highlight on the new `Dashboard` entry (existing `AppSidebar.tsx` `bg-primary/10 text-primary` pattern at line 244).
2. Checklist row trailing chevron icon `text-primary` only when the row is `hovered` or `focus-visible`. Idle chevron is `text-muted-foreground`.
3. Focus ring on every interactive element via existing `focus-visible:ring-ring` from button/input — no Phase-16-specific change.

Accent **NOT** used for: status badges (use the success/warning/destructive scale), card borders, mobile-card chevrons in idle state, or row hover background (which uses `hover:bg-accent` muted, NOT `hover:bg-primary`).

### Status badge color map (CONTEXT D-03 + specifics line 171)

This is the headline visual of the Dashboard. The 3-state model maps as:

| `Status` | Label (DE) | Badge styling | Token |
|----------|------------|---------------|-------|
| `done` | `Erledigt` | `bg-success/15 text-success border-success/30` (carry-forward Phase 15 job-status pattern) | `--color-success` |
| `partial` | `Unvollständig` | `bg-warning/15 text-warning border-warning/30` | `--color-warning` |
| `missing` | `Fehlt` | `bg-destructive/15 text-destructive border-destructive/30` | `--color-destructive` |

All three use the `<Badge>` primitive with a custom `className` override. Do NOT introduce a new `Badge` variant — the existing primitive's `outline` variant is the closest match; pass the color classes via `className`.

**Icon adjunct (optional, mobile only):** at `<sm`, the badge MAY collapse to icon-only — `<CheckCircle2>` / `<AlertCircle>` / `<XCircle>` (lucide). Text label is removed but the icon color matches the badge text color. This avoids horizontal overflow on 375px when the row has a long title (e.g. "Stundenplan-Generator").

### Mobile-Härtung color invariants (no change)

The mobile sweep does NOT introduce new colors. Every existing surface uses the same tokens; the sweep only changes layout density and touch-target sizes. Any color regression caught during the audit is a fix-task, not a re-design.

---

## Copywriting Contract

All UI copy in **German** (DACH school admin audience). Carry-forward from Phase 15. Executor copies verbatim.

### Page titles + sidebar

| Surface | Copy |
|---------|------|
| Sidebar entry (new, top of admin section) | `Dashboard` (icon `LayoutDashboard` from lucide) |
| `/admin` `PageShell` title | `Dashboard` |
| `/admin` `PageShell` subtitle | `Setup-Übersicht: prüfe, was für deine Schule schon eingerichtet ist und wo noch Schritte offen sind.` |
| `/admin` `PageShell` breadcrumb root | `Verwaltung` (no href — single segment is the title itself) |

### Sidebar position (CONTEXT D-01)

The Dashboard entry is the **first** admin item — it appears above `Datenimport` in `AppSidebar.tsx`'s admin-only block. Order:
1. `Dashboard` (NEW — admin only)
2. `Datenimport` (existing)
3. `Schulverwaltung` (existing)
4. ... (rest unchanged)

Same insertion in `MobileSidebar.tsx`.

### Checklist row labels (the 10 categories — CONTEXT D-06 order)

Exact display order is the natural onboarding order from CONTEXT specifics line 170. Each category gets a fixed lucide icon (Phase-16 selection — see § Component Inventory).

| # | Category key | Title (DE) | Icon | Deep-link `to=` |
|---|--------------|------------|------|-----------------|
| 1 | `school` | `Schule` | `Building2` | `/admin/school/settings` |
| 2 | `timegrid` | `Zeitraster` | `Clock` | `/admin/school/settings?tab=zeitraster` |
| 3 | `schoolyear` | `Schuljahr` | `Calendar` | `/admin/school/settings?tab=schuljahre` |
| 4 | `subjects` | `Fächer` | `BookOpen` | `/admin/subjects` |
| 5 | `teachers` | `Lehrer` | `GraduationCap` | `/admin/teachers` |
| 6 | `classes` | `Klassen` | `School` | `/admin/classes` |
| 7 | `students` | `Schüler:innen` | `UsersRound` | `/admin/students` |
| 8 | `solver` | `Solver-Konfiguration` | `SlidersHorizontal` | `/admin/solver-tuning` |
| 9 | `dsgvo` | `DSGVO` | `ShieldCheck` | `/admin/dsgvo` |
| 10 | `audit` | `Audit-Log` | `ScrollText` | `/admin/audit-log` |

Icons reuse those already imported in `AppSidebar.tsx` where possible (carry-forward consistency). Two are new: `LayoutDashboard` (sidebar), `Clock` (Zeitraster). All others are already in scope.

### Status secondary copy per category

This is the second line under each title — short, factual, no marketing voice.

| Category | `done` secondary | `partial` secondary | `missing` secondary |
|----------|------------------|---------------------|---------------------|
| Schule | `Stammdaten vollständig` | `Adresse oder Kontakt fehlt` | `Noch keine Schule angelegt` |
| Zeitraster | `{n} Perioden + Wochentage konfiguriert` | `Wochentage fehlen` | `Noch keine Perioden definiert` |
| Schuljahr | `Aktives Schuljahr: {label}` | `Schuljahr unvollständig` | `Noch kein Schuljahr` |
| Fächer | `{n} Fächer angelegt` | (n/a — binary state) | `Noch keine Fächer` |
| Lehrer | `{n} Lehrer:innen` | (n/a — binary state) | `Noch keine Lehrer:innen` |
| Klassen | `{n} Klassen` | (n/a — binary state) | `Noch keine Klassen` |
| Schüler:innen | `{n} Schüler:innen, alle einer Klasse zugeordnet` | `{n} ohne Klassenzuordnung` | `Noch keine Schüler:innen` |
| Solver-Konfiguration | `Konfiguriert + {n} erfolgreich generierte Pläne` | `Konfiguration vorhanden, noch kein Lauf erfolgreich` | `Noch keine Konfiguration` |
| DSGVO | `Aufbewahrung, DSFA und VVZ gepflegt` | `Aufbewahrung gesetzt, DSFA/VVZ unvollständig` | `Noch keine DSGVO-Einträge` |
| Audit-Log | `{n} protokollierte Aktionen` | (n/a — passive) | `Noch keine Aktionen protokolliert` |

The `{n}` placeholder is filled by the backend response. The `{label}` placeholder for Schuljahr is the year label as returned (e.g. `2026/2027`).

### Primary CTAs (button labels)

| Surface | CTA label |
|---------|-----------|
| Per checklist row | (no button — entire row is a link, trailing chevron `ArrowRight` indicates navigation) |
| Mobile sidebar trigger (existing in header) | (icon-only — no Phase-16 label change) |

Phase 16 has **no form**, **no dialog**, **no destructive action** of its own. The Dashboard is read-only and navigation-only. All destructive flows live behind the deep-links to Phase 10–15 surfaces.

### Empty states

| Surface | Heading | Body | Next-step copy |
|---------|---------|------|----------------|
| Dashboard — checklist load failed | `Setup-Status nicht verfügbar` | `Der Server konnte den Setup-Status gerade nicht laden. Versuche es in wenigen Sekunden erneut.` | (button) `Aktualisieren` |
| `<DataList>` no rows (filtered) | `Keine Einträge gefunden` | `Kein Eintrag passt zu den aktuellen Filtern.` | (button) `Filter zurücksetzen` (only when filters are active) |
| `<DataList>` no rows (no filters) | per-surface (existing copy carries forward; `<DataList>` accepts `emptyState` prop) | per-surface | per-surface |

The `<DataList>` empty state is a **slot** — the component does not hardcode copy. Each Phase 10–15 surface that migrates to `<DataList>` passes its existing empty-state copy via the `emptyState` prop. Phase 16 introduces NO new per-surface empty-state copy; we only standardise the SLOT.

### Error states

| Trigger | Toast / inline copy |
|---------|---------------------|
| Dashboard query 5xx (transient) | inline `<InfoBanner variant="warn">` text: `Status-Aktualisierung fehlgeschlagen — neuer Versuch in 30 Sekunden.` (NO toast — would spam during background refetch per Phase 15 § 10 #6 carry-forward) |
| Dashboard query 4xx (auth lost) | redirect to login via existing `_authenticated` route guard — no Phase-16 copy needed |
| Dashboard polling failure (background) | silent — TanStack Query logs to console; UI shows last successful state |

### Destructive confirmations

**None.** Phase 16 has no destructive actions of its own.

---

## Component Inventory (Phase 16)

| Component | File | Reuses | New code |
|-----------|------|--------|----------|
| `useIsMobile` | `apps/web/src/hooks/useIsMobile.ts` | extracted from `__root.tsx:20-32` | extract — behaviour-preserving |
| `DashboardChecklist` | `apps/web/src/components/admin/dashboard/DashboardChecklist.tsx` | `Card`, `Separator`, `<ChecklistItem>` | YES |
| `ChecklistItem` | `apps/web/src/components/admin/dashboard/ChecklistItem.tsx` | `Badge`, `Link` (TanStack Router), lucide icons | YES |
| `useDashboardStatus` | `apps/web/src/hooks/useDashboardStatus.ts` | TanStack Query (`refetchInterval: 30_000`, `staleTime: 10_000` per CONTEXT D-08/D-09) | YES |
| `DataList` | `apps/web/src/components/shared/DataList.tsx` | native `<table>` (desktop) + `<Card>` stack (mobile) + `useIsMobile` | YES — Phase-16 invention |
| `dashboard` route | `apps/web/src/routes/_authenticated/admin/index.tsx` | `PageShell`, `useDashboardStatus`, `<DashboardChecklist>` | YES |
| `index` route (login redirect) | `apps/web/src/routes/index.tsx` | TanStack Router `beforeLoad` + `useAuth` | edit — role-aware redirect |
| `AppSidebar` (extension) | `apps/web/src/components/layout/AppSidebar.tsx` | existing `roles: ['admin']` pattern | edit — insert `Dashboard` entry as first admin item |
| `MobileSidebar` (extension) | `apps/web/src/components/layout/MobileSidebar.tsx` | same | edit — same insertion |
| `Button` (touch-target lift) | `apps/web/src/components/ui/button.tsx` | (the primitive itself) | edit — add `min-h-11` on `<sm` to all sizes per CONTEXT D-17 |
| `Input` (touch-target lift) | `apps/web/src/components/ui/input.tsx` | (the primitive itself) | edit — add `min-h-11` on `<sm` per CONTEXT D-17 |

### `<DashboardChecklist>` layout (Linear-style — CONTEXT specifics line 169)

Single vertical list, no card grid, no hero header. Wrapped in a single outer `<Card>` with `divide-y divide-border` between rows. NO per-row `<Card>` — the checklist IS the card.

```
┌──────────────────────────────────────────────────────────┐
│  [icon] [title block]              [status badge]  [→]   │  56px
├──────────────────────────────────────────────────────────┤
│  [icon] [title block]              [status badge]  [→]   │  56px
├──────────────────────────────────────────────────────────┤
│  ...                                                      │
└──────────────────────────────────────────────────────────┘
```

Outer card: `rounded-lg border bg-card shadow-sm` (existing `<Card>` styling).
Row anatomy: `flex items-center gap-3 px-4 py-3 min-h-14 hover:bg-accent transition-colors`.
Title block: `flex flex-col` with title (`text-base font-semibold leading-6 text-foreground`) and secondary line (`text-sm text-muted-foreground leading-5`).
Trailing block on `<sm`: badge collapses to icon-only via the icon-adjunct rule. Chevron stays at all sizes.

Each row is wrapped in `<Link to={deepLink}>` from `@tanstack/react-router` so the entire row is the click/tap target — not just the title or chevron. This satisfies the 56px touch-target unconditionally.

### `<ChecklistItem>` props

```ts
interface ChecklistItemProps {
  icon: LucideIcon;
  title: string;
  status: 'done' | 'partial' | 'missing';
  secondary: string;       // already filled with {n}/{label}
  to: string;              // deep-link path including search params
  testId?: string;         // e.g. data-checklist-item="school"
}
```

`data-*` E2E selectors:
- Outer link: `data-checklist-item={category-key}` (e.g. `data-checklist-item="school"`)
- Status badge: `data-checklist-status={status}` (e.g. `data-checklist-status="done"`)

This locks the E2E contract for Phase 16 specs and matches the carry-forward Phase 15 D-21 attribute pattern.

### `<DataList>` API (CONTEXT D-12 + D-14)

```ts
interface DataListColumn<T> {
  key: string;
  header: string;             // German label, e.g. 'Name', 'Klasse'
  cell: (row: T) => ReactNode;
  className?: string;         // optional cell tailwind class (e.g. text-right)
  /** Hide this column on mobile when card-mode is active. Default: false (renders inside the card). */
  hiddenOnMobile?: boolean;
}

interface DataListProps<T> {
  rows: T[];
  columns: DataListColumn<T>[];
  /** Returns the row's stable identifier for React keys + data-testid suffix. */
  getRowId: (row: T) => string;
  /** data-testid value rendered on every row (table <tr> AND mobile card). */
  getRowTestId?: (row: T) => string;
  /** Required: how each row renders as a single mobile card. Receives the same row data. */
  mobileCard: (row: T) => ReactNode;
  /** Optional: forced viewport mode override. Default: detected via useIsMobile(). */
  mode?: 'desktop' | 'mobile' | 'auto';
  /** Optional empty-state slot. */
  emptyState?: ReactNode;
  /** Optional loading skeleton slot. */
  loading?: boolean;
  loadingSlot?: ReactNode;
  /** Optional row-click handler. When set, each row gets cursor-pointer. */
  onRowClick?: (row: T) => void;
}
```

Desktop rendering: native `<table class="w-full text-sm">` with `<thead>` + `<tbody>`. Header row uses `bg-muted/50 text-xs uppercase tracking-wide text-muted-foreground` (carry-forward Phase 15 table style). Body rows: `border-t border-border hover:bg-accent`. Each `<tr>` carries `data-testid` from `getRowTestId(row)`.

Mobile rendering: `<div class="flex flex-col gap-2">` with one `<Card>`-styled wrapper per row from `mobileCard(row)`. Each wrapper is `rounded-lg border bg-card p-4 min-h-20`. The `mobileCard` slot is fully owned by the migrating surface — `<DataList>` does NOT prescribe its layout beyond the wrapper.

`mobileCard` MUST internally include the `data-testid` from `getRowTestId(row)` on its outermost element so E2E selectors work in BOTH modes. The `<DataList>` component re-applies it on the wrapper as a backstop.

**Sort/filter/pagination:** out-of-scope for the `<DataList>` primitive itself per CONTEXT discretion. Each migrating surface keeps its own sort/filter UI ABOVE the `<DataList>`. Pagination strategy (cursor vs offset) is decided per-surface and is a planner detail.

### `<DataList>` mobile-card guidance (Phase 16 recommendation, not enforced)

For visual consistency across migrated surfaces, `mobileCard(row)` SHOULD render in this anatomy:

```
┌────────────────────────────────────────┐
│ [Title — text-base font-semibold]      │
│ [Subtitle — text-sm text-muted-fg]     │
│ [Optional row-meta line — text-xs]     │
│ [Optional status badge — top-right]    │
└────────────────────────────────────────┘
```

with `flex flex-col gap-1` inside the wrapper. Touch-action targets (the row itself + any in-card icon buttons) MUST satisfy the 44px floor.

### Login-redirect role-aware (CONTEXT D-02)

`apps/web/src/routes/index.tsx` `beforeLoad` reads the auth context and:
- If `userRoles.includes('admin')` → `throw redirect({ to: '/admin' })`
- Else → `throw redirect({ to: '/timetable' })` (unchanged for non-admin roles)

Implementation note: this MUST be a `beforeLoad` redirect, NOT a runtime `useEffect`, so the user never flashes an empty `/` page.

---

## Interaction Contracts

### Live-update (CONTEXT D-07/D-08/D-09/D-10)

- Single endpoint `GET /admin/dashboard/status` returns the full status object for all 10 categories in one round-trip.
- TanStack Query: `useDashboardStatus()` with `queryKey: ['dashboard-status']`, `staleTime: 10_000`, `refetchInterval: 30_000`.
- Sub-surface mutations across Phase 10–15 surfaces invalidate this query alongside their own:
  ```ts
  queryClient.invalidateQueries({ queryKey: ['dashboard-status'] });
  ```
- This is an audit-and-edit fan-out across mutations in Phases 10–15 (planner detail).

### Mutation invariants (carry-forward Phase 15 D-20)

Every `useMutation` in Phase 16 (currently zero — Dashboard is read-only) would follow:
- `onError: (err) => toast.error(extractMessage(err))`
- `onSuccess: () => { toast.success(...); queryClient.invalidateQueries(...); }`

The Mobile-Härtung sweep MUST NOT regress this invariant in any migrated surface. If a surface currently lacks the `onError → toast.error` pattern (silent 4xx), it is a Phase 16 fix-task per the Phase 10.2-04 invariant carry-forward.

### Sidebar (CONTEXT D-01 + D-20)

- One new entry inserted as the first admin-only item in BOTH `AppSidebar.tsx` and `MobileSidebar.tsx`.
- `roles: ['admin']` strict — no `schulleitung` access (matches Phase 14 D-03 / Phase 15 D-22 admin-only pattern).
- Icon: `LayoutDashboard` from lucide-react (new import).
- Active-route highlight uses the existing `bg-primary/10 text-primary` pattern at `AppSidebar.tsx:244`.

### MobileSidebar (CONTEXT D-18)

The existing `MobileSidebar.tsx` drawer pattern (right-side off `<sm`, `bg-black/50` backdrop, `slide-in-from-left` animation) is verified — no structural change beyond the `Dashboard` entry insertion. Phase 16 also confirms that ALL Phase 10–15 admin entries are present (audit-and-fix task in the planner).

### Touch-target lift (CONTEXT D-17 — global)

The lift is applied at the primitive layer (`button.tsx`, `input.tsx`) using responsive `min-h-11 sm:min-h-{original}` Tailwind classes. This avoids per-surface patches and is verified via Playwright `page.locator('button').evaluate(el => el.getBoundingClientRect().height)` in the mobile sweep.

### Mobile sweep (CONTEXT D-15 + D-16)

Migration order is sequential per dependency graph (planner detail). Each migrated surface gets:
1. A 375px Playwright viewport spec that asserts: every interactive element ≥44×44 px; the `<DataList>` renders cards (not table); the `MobileSidebar` opens via the existing trigger.
2. A `data-testid` audit ensuring every row in the migrated `<DataList>` has a stable selector (E2E carry-forward Phase 14/15 D-21).

The sweep is **audit-first** per CONTEXT D-16: the planner runs the Playwright mobile-viewport sweep over all 14 admin surfaces (`school.settings.tsx`, `subjects.index.tsx`, `teachers.index.tsx`, `teachers.$teacherId.tsx`, `classes.index.tsx`, `classes.$classId.tsx`, `students.index.tsx`, `students.$studentId.tsx`, `users.index.tsx`, `users.$userId.tsx`, `solver.tsx`, `solver-tuning.tsx`, `dsgvo.tsx`, `audit-log.tsx`, `import.tsx`, `resources.tsx`, `substitutions.tsx`, `timetable-edit.tsx`, `timetable-history.tsx`) and emits a gap-list. Each gap becomes a fix-task within Phase 16.

### Empty-state slot (`<DataList>`)

When `rows.length === 0` and `loading === false`, `<DataList>` renders the `emptyState` prop centered with `py-12` vertical padding. If `emptyState` is omitted, falls back to the prescribed copy in § Copywriting "no rows (filtered)". Migrating surfaces SHOULD pass their existing copy via the prop — keeping per-surface phrasing wins continuity over standardisation.

### Loading state

`<DataList>` accepts `loading: boolean` + `loadingSlot?: ReactNode`. When `loading === true`, render `loadingSlot` if provided, else a 5-row skeleton: 5 × `<div class="h-12 bg-muted animate-pulse rounded-md" />` separated by `gap-2`. Mobile mode renders the same 5 skeletons but at `min-h-20`.

The Dashboard checklist itself uses a 10-row skeleton (one per category) while `useDashboardStatus()` is fetching for the first time — `flex flex-col divide-y` of `<div class="h-14 px-4 animate-pulse">` rows inside the outer card.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | NONE NEW. Phase 16 reuses existing primitives in `apps/web/src/components/ui/` (`card`, `badge`, `button`, `separator`, `scroll-area`, `tooltip`, `sonner`). | Not required — already in repo, vetted in earlier phases. |
| Third-party | NONE | Not applicable. |

**No `npx shadcn add` calls in Phase 16.** No third-party registries. No new dependencies introduced.

The `<DataList>` component is a Phase-16 internal helper at `apps/web/src/components/shared/DataList.tsx` — NOT a registry vendoring. It is owned by the project, not by any external registry, and therefore the registry vetting gate is not applicable to it.

---

## Pre-Population Source Map

| Decision | Source | Notes |
|----------|--------|-------|
| Design system tooling | `apps/web/components.json` (existing) | `style: default`, `baseColor: neutral`, `lucide` icons |
| Color tokens (60/30/10) | `apps/web/src/app.css` `@theme` block | No new tokens; full carry-forward Phase 15 |
| Status badge colors | CONTEXT specifics line 171 + Phase 15 success/warning/destructive precedent | green/amber/red mapped to `--color-success`/`--color-warning`/`--color-destructive` |
| Status labels | CONTEXT specifics line 171 (`Erledigt` / `Unvollständig` / `Fehlt`) | German UI carry-forward |
| Spacing scale | Tailwind v4 default + Phase 15 carry-forward | No new spacing values |
| Touch-target floor | CONTEXT D-17 (`min-h-11` global) + specifics line 173 (current `h-9`/`h-10` insufficient) | Applied at primitive layer (`button.tsx`, `input.tsx`) responsively `<sm` only |
| Mobile breakpoint | CONTEXT D-13 (existing 640px) | Hook moved to `hooks/useIsMobile.ts`; behaviour preserved |
| Mobile-card density | CONTEXT specifics line 172 (~80px) | `min-h-20` on `<DataList>` mobile cards |
| Checklist row height | CONTEXT specifics line 169 + Linear visual reference | `min-h-14` (56px) — comfortable touch + 2-line copy |
| Setup order | CONTEXT specifics line 170 (Schule → Zeitraster → Schuljahr → Fächer → Lehrer → Klassen → Schüler → Solver → DSGVO → Audit) | NOT alphabetic — natural onboarding flow |
| 10 category icons | Discretion: chosen to match existing `AppSidebar.tsx` imports + add `LayoutDashboard` (sidebar) + `Clock` (Zeitraster) | All from `lucide-react` |
| `<DataList>` API | CONTEXT D-12 (`columns` schema + `mobileCard` render-prop) + CONTEXT D-14 (data-testid standardisation) | Phase-16 invention; owned internally |
| Login redirect | CONTEXT D-02 (role-aware `beforeLoad`) | TanStack Router `beforeLoad` + `useAuth` |
| Sidebar entry position | CONTEXT D-01 (top of admin section) | Insertion before `Datenimport` |
| Polling pattern | CONTEXT D-08 (30s) + D-09 (10s staleTime) + D-07 (cross-mutation invalidation) | TanStack Query `refetchInterval: 30_000`, `staleTime: 10_000` |
| Single aggregation endpoint | CONTEXT D-10 | `GET /admin/dashboard/status` returns all 10 categories |
| German copy | CONTEXT specifics line 171 + Phase 15 carry-forward | All labels and microcopy prescribed verbatim |
| `data-*` E2E selectors | CONTEXT D-14 + Phase 14/15 D-21 carry-forward | `data-checklist-item={key}`, `data-checklist-status={status}` |
| Toast invariant | CONTEXT D-19 (carry-forward Phase 10.2-04 + Phase 14 D-12 + Phase 15 D-20) | `onError → toast.error` enforced; Phase 16 surfaces are read-only so not exercised here, but the Mobile-Härtung sweep MUST NOT regress this in migrated surfaces |
| Typography 2-weight rule | Phase 15 carry-forward | 4 sizes × 2 weights only |
| Mobile parity scope | CONTEXT D-15/D-16/D-18 | All Phase 10–15 admin surfaces audited at 375px |
| No new shadcn blocks | Inventory check vs `apps/web/src/components/ui/` (24 files present) | Every primitive needed already in repo |
| Empty-state copy slot | CONTEXT discretion (per-surface kept) + Phase 16 standardised wrapper | `<DataList>` exposes `emptyState` prop |
| Linear "Setup checklist" vibe | CONTEXT specifics line 169 | Vertical list, no card grid, no hero header |
| Migration-hard-rule | CONTEXT D-22 + CLAUDE.md | If new Prisma model needed for status caching → real `prisma migrate dev`. Phase 16 currently has no schema change. |
| Socket.IO out-of-scope | CONTEXT D-11 + Phase 15 D-15 carry-forward | Polling + invalidation only |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS — all sidebar/title/subtitle/checklist/status/empty/error copy in German prescribed verbatim; zero CTA buttons, dialogs, or destructive actions in Phase 16's own surface; `<DataList>` empty-state slot delegates per-surface copy without breaking Phase 16's contract.
- [ ] Dimension 2 Visuals: PASS — component inventory enumerated; `<DashboardChecklist>`, `<ChecklistItem>`, `<DataList>`, `useIsMobile` defined; no new shadcn primitives added; new internal helpers owned by project.
- [ ] Dimension 3 Color: PASS — 60/30/10 mapped to existing `--color-*` tokens; accent reserved-for list explicit (3 surfaces only); status-badge color map locked; mobile-icon-adjunct rule preserves color semantics.
- [ ] Dimension 4 Typography: PASS — 4 sizes (14/14/20/28) declared with display reserved; 2 weights (400/600); label vs body separation via `text-muted-foreground` color (not weight); existing `<Badge>` primitive's internal `font-semibold` retained as scoped exception.
- [ ] Dimension 5 Spacing: PASS — Tailwind 8-point scale carry-forward; touch-target floor `min-h-11` applied at primitive layer responsively; `<DataList>` mobile-card density `min-h-20`; checklist row `min-h-14`.
- [ ] Dimension 6 Registry Safety: PASS — no third-party registries; no new shadcn blocks; safety gate not applicable.

**Approval:** pending (awaiting `gsd-ui-checker` validation)
