---
phase: 16-admin-dashboard-mobile-h-rtung
plan: 06
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - apps/web/src/hooks/useTeachers.ts
  - apps/web/src/hooks/useClasses.ts
  - apps/web/src/hooks/useStudents.ts
  - apps/web/src/hooks/useSubjects.ts
  - apps/web/src/hooks/useSchool.ts
  - apps/web/src/hooks/useTimeGrid.ts
  - apps/web/src/hooks/useSchoolYears.ts
  - apps/web/src/hooks/useDsfa.ts
  - apps/web/src/hooks/useVvz.ts
  - apps/web/src/hooks/useRetention.ts
  - apps/web/src/hooks/useConsents.ts
  - apps/web/src/hooks/useDsgvoExportJob.ts
  - apps/web/src/hooks/useDsgvoDeletionJob.ts
  - apps/web/src/hooks/useTeachers.test.ts
  - apps/web/src/hooks/useDsfa.test.ts
autonomous: true
requirements: [ADMIN-03]
must_haves:
  truths:
    - "Every mutation in 13 hook files invalidates ['dashboard-status'] queryKey on onSuccess"
    - "Existing onSuccess invalidations and toast.success calls are preserved (no regression)"
    - "Existing onError → toast.error invariant is preserved (Phase 10.2-04 silent-4xx invariant carry-forward per D-19)"
    - "Dashboard query refetches after admin creates/updates/deletes any entity in dashboard.service.ts scope"
    - "Total ~57 mutation sites across 13 files have the invalidation line added"
  artifacts:
    - path: "apps/web/src/hooks/useTeachers.ts"
      provides: "Cross-mutation invalidation wired in 6 mutation onSuccess sites"
    - path: "apps/web/src/hooks/useStudents.ts"
      provides: "Cross-mutation invalidation wired in 9 mutation onSuccess sites"
  key_links:
    - from: "every modified hook file"
      to: "apps/web/src/hooks/useDashboardStatus.ts"
      via: "dashboardKeys import"
      pattern: "from '@/hooks/useDashboardStatus'"
    - from: "every onSuccess callback"
      to: "TanStack Query cache key ['dashboard-status']"
      via: "qc.invalidateQueries"
      pattern: "invalidateQueries.*dashboardKeys.status"
---

<objective>
Wire cross-mutation invalidation across the 13 admin-mutation hook files (~57 mutation sites total per PATTERNS audit) so the Dashboard's `['dashboard-status']` query refetches after EVERY admin action that could change a Setup-Completeness category. Per CONTEXT D-07 hybrid pattern: mutations call `invalidateQueries` for both their own keys AND the shared dashboard key.

Purpose: ADMIN-03 success criterion — "Dashboard zeigt Live-Zustand pro Eintrag, aktualisiert sich ohne Reload nach jeder Admin-Aktion". The 30s polling backup (D-08) is a safety net; this plan is the primary live-update mechanism.
Output: 13 hook files edited with one-line additions per mutation `onSuccess`. Two regression unit tests verify the wiring on representative hooks.
</objective>

<execution_context>
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-admin-dashboard-mobile-h-rtung/16-CONTEXT.md
@.planning/phases/16-admin-dashboard-mobile-h-rtung/16-RESEARCH.md
@.planning/phases/16-admin-dashboard-mobile-h-rtung/16-PATTERNS.md
@.planning/phases/16-admin-dashboard-mobile-h-rtung/16-02-PLAN.md
@apps/web/src/hooks/useTeachers.ts
@apps/web/src/hooks/useClasses.ts
@apps/web/src/hooks/useStudents.ts
@apps/web/src/hooks/useSubjects.ts
@apps/web/src/hooks/useSchool.ts
@apps/web/src/hooks/useTimeGrid.ts
@apps/web/src/hooks/useSchoolYears.ts
@apps/web/src/hooks/useDsfa.ts
@apps/web/src/hooks/useVvz.ts
@apps/web/src/hooks/useRetention.ts
@apps/web/src/hooks/useConsents.ts
@apps/web/src/hooks/useDsgvoExportJob.ts
@apps/web/src/hooks/useDsgvoDeletionJob.ts

<interfaces>
<!-- Plan 02 produced this — every hook will import it. -->

```typescript
// apps/web/src/hooks/useDashboardStatus.ts (Plan 02)
export const dashboardKeys = { status: ['dashboard-status'] as const };
```

Existing onSuccess pattern (verified via PATTERNS.md line 593-597, useTeachers.ts:223-225):
```typescript
onSuccess: () => {
  qc.invalidateQueries({ queryKey: teacherKeys.all(schoolId) });
  toast.success('Lehrperson angelegt.');
},
```

Edit pattern — insert ONE LINE after the existing `qc.invalidateQueries`:
```typescript
onSuccess: () => {
  qc.invalidateQueries({ queryKey: teacherKeys.all(schoolId) });
  qc.invalidateQueries({ queryKey: dashboardKeys.status });   // ← NEW LINE
  toast.success('Lehrperson angelegt.');
},
```

Per-file mutation count (PATTERNS line 614-628 — verified by grep):
| Hook file | useMutation count | Mutations to extend |
|-----------|--------------------|----------------------|
| useTeachers.ts | 6 | useCreateTeacher, useUpdateTeacher, useDeleteTeacher, useLinkKeycloak, useUnlinkKeycloak, +1 |
| useClasses.ts | 5 | create, update, delete, +2 |
| useStudents.ts | 9 | create, update, delete, archive, restore, move, parent-link, parent-unlink, +1 |
| useSubjects.ts | 4 | create, update, delete, +1 |
| useSchool.ts | 3 | create, update, +1 |
| useTimeGrid.ts | 2 | update, +1 |
| useSchoolYears.ts | 9 | year-CRUD + holidays + autonomous days |
| useDsfa.ts | 4 | create, update, delete, +1 |
| useVvz.ts | 4 | create, update, delete, +1 |
| useRetention.ts | 4 | create, update, delete, +1 |
| useConsents.ts | 3 | create, update, +1 |
| useDsgvoExportJob.ts | 2 | start, cancel |
| useDsgvoDeletionJob.ts | 2 | start, cancel |
| **Total** | **57** | — |

Anti-pattern warning (RESEARCH § Anti-Patterns):
"Reverting a 'silent 4xx' pattern when migrating ... — every mutation has `onError → toast.error`. Migrating the layout component MUST NOT change the mutation hook signatures."
This plan ONLY adds invalidation lines. NEVER touch `onError` blocks. NEVER simplify or remove `toast.success` / existing invalidations.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dashboard invalidation to 13 hook files (~57 mutations)</name>
  <files>apps/web/src/hooks/useTeachers.ts, apps/web/src/hooks/useClasses.ts, apps/web/src/hooks/useStudents.ts, apps/web/src/hooks/useSubjects.ts, apps/web/src/hooks/useSchool.ts, apps/web/src/hooks/useTimeGrid.ts, apps/web/src/hooks/useSchoolYears.ts, apps/web/src/hooks/useDsfa.ts, apps/web/src/hooks/useVvz.ts, apps/web/src/hooks/useRetention.ts, apps/web/src/hooks/useConsents.ts, apps/web/src/hooks/useDsgvoExportJob.ts, apps/web/src/hooks/useDsgvoDeletionJob.ts</files>
  <read_first>
    - apps/web/src/hooks/useDashboardStatus.ts (Plan 02 — verify dashboardKeys export)
    - apps/web/src/hooks/useTeachers.ts (lines 223-225, 248-251, 270-273, 299-301, 322-324 — onSuccess pattern analog)
    - .planning/phases/16-admin-dashboard-mobile-h-rtung/16-PATTERNS.md (lines 588-632 — verbatim per-file mutation counts and edit pattern)
    - .planning/phases/16-admin-dashboard-mobile-h-rtung/16-RESEARCH.md (Anti-Patterns warning lines 333-340 — DO NOT touch onError blocks)
  </read_first>
  <action>
    For EACH of the 13 hook files:

    Step 1 — Add import (idempotent — only if not already present):
    ```typescript
    import { dashboardKeys } from '@/hooks/useDashboardStatus';
    ```
    Place near other hook imports at the top of the file.

    Step 2 — Find every `useMutation({ ... })` block. For each one, locate its `onSuccess` callback. Add EXACTLY ONE line after the existing `qc.invalidateQueries` (or as the first invalidation if no other exists):
    ```typescript
    qc.invalidateQueries({ queryKey: dashboardKeys.status });
    ```

    Per-file checklist (count BEFORE editing using `grep -c "onSuccess:" apps/web/src/hooks/<file>.ts`; the count BEFORE this edit equals the count of insertions you must make in that file):
    - useTeachers.ts: 6 mutations
    - useClasses.ts: 5 mutations
    - useStudents.ts: 9 mutations
    - useSubjects.ts: 4 mutations
    - useSchool.ts: 3 mutations
    - useTimeGrid.ts: 2 mutations
    - useSchoolYears.ts: 9 mutations
    - useDsfa.ts: 4 mutations
    - useVvz.ts: 4 mutations
    - useRetention.ts: 4 mutations
    - useConsents.ts: 3 mutations
    - useDsgvoExportJob.ts: 2 mutations
    - useDsgvoDeletionJob.ts: 2 mutations
    - **Total: 57**

    Step 3 — DO NOT TOUCH:
    - Existing `qc.invalidateQueries` calls for the hook's own keys
    - `toast.success(...)` calls
    - `onError: ...` blocks (silent-4xx invariant carry-forward — D-19)
    - `mutationFn`, `mutationKey`, parameter shapes, return types

    Step 4 — Per-file post-edit verification:
    ```bash
    # The count of dashboardKeys.status invalidations should equal the count of onSuccess blocks
    grep -c "dashboardKeys.status" apps/web/src/hooks/<file>.ts
    grep -c "onSuccess:" apps/web/src/hooks/<file>.ts
    ```
    These two counts MUST match per file. If they don't, an onSuccess was missed.

    Step 5 — Sweep verification (final — across all 13 files):
    ```bash
    # Total dashboardKeys.status references across the 13 hook files should equal 57 (one per mutation) + 13 (one import per file)
    grep -h "dashboardKeys" apps/web/src/hooks/{useTeachers,useClasses,useStudents,useSubjects,useSchool,useTimeGrid,useSchoolYears,useDsfa,useVvz,useRetention,useConsents,useDsgvoExportJob,useDsgvoDeletionJob}.ts | wc -l
    ```
    Expected: ≥ 70 (57 invalidation calls + 13 imports — exact total may vary if a file has additional dashboardKeys references, e.g. inside a comment).

    Step 6 — Run TypeScript:
    ```bash
    pnpm --filter @schoolflow/web exec tsc --noEmit
    ```
  </action>
  <verify>
    <automated>pnpm --filter @schoolflow/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All 13 hook files import `dashboardKeys`: `for f in apps/web/src/hooks/{useTeachers,useClasses,useStudents,useSubjects,useSchool,useTimeGrid,useSchoolYears,useDsfa,useVvz,useRetention,useConsents,useDsgvoExportJob,useDsgvoDeletionJob}.ts; do grep -q "dashboardKeys" "$f" || echo "MISSING: $f"; done` produces no MISSING output
    - Total invalidation calls match expected counts per file. For each file, count of `qc.invalidateQueries.*dashboardKeys.status` MUST equal count of `onSuccess:` blocks (off by at most 1 if a file has any non-mutation onSuccess; document any divergences in summary)
    - Aggregate count: `grep -hE "invalidateQueries\(\s*\{\s*queryKey:\s*dashboardKeys.status\s*\}" apps/web/src/hooks/{useTeachers,useClasses,useStudents,useSubjects,useSchool,useTimeGrid,useSchoolYears,useDsfa,useVvz,useRetention,useConsents,useDsgvoExportJob,useDsgvoDeletionJob}.ts | wc -l` returns ≥ 50 (target ~57; permit some flex if PATTERNS audit was approximate)
    - No `onError` block was modified across the 13 files. Verify via `git diff` review during execution — search for any modification inside an `onError:` callback.
    - No `toast.success` or non-dashboard `invalidateQueries` line was removed (verify via git diff)
    - TypeScript clean: `pnpm --filter @schoolflow/web exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>13 hook files have dashboardKeys imported; ~57 mutation onSuccess blocks call invalidate ['dashboard-status']; existing onSuccess/onError shapes preserved; TypeScript clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression unit tests for fan-out wiring (2 representative hooks)</name>
  <files>apps/web/src/hooks/useTeachers.test.ts, apps/web/src/hooks/useDsfa.test.ts</files>
  <read_first>
    - apps/web/src/hooks/useTeachers.ts (post-edit — verify mutation onSuccess includes dashboardKeys.status)
    - apps/web/src/hooks/useDsfa.ts (post-edit)
    - apps/web/src/hooks/useDashboardStatus.ts (Plan 02 — dashboardKeys constant)
    - any existing `*.test.ts` in apps/web/src/hooks/ for QueryClient mock pattern (`renderHook` + QueryClientProvider wrapper)
  </read_first>
  <behavior>
    useTeachers.test.ts:
    - Test 1: useCreateTeacher.onSuccess invalidates BOTH `teacherKeys.all(schoolId)` AND `dashboardKeys.status`
    - Test 2: useUpdateTeacher onSuccess invalidates dashboardKeys.status
    - Test 3: useDeleteTeacher onSuccess invalidates dashboardKeys.status

    useDsfa.test.ts:
    - Test 1: useCreateDsfa onSuccess invalidates dashboardKeys.status
    - Test 2: useUpdateDsfa onSuccess invalidates dashboardKeys.status
    - Test 3: useDeleteDsfa onSuccess invalidates dashboardKeys.status
  </behavior>
  <action>
    Use `renderHook` from `@testing-library/react` wrapped in a QueryClientProvider. Mock the API call via `vi.mock('@/lib/api')` so `apiFetch` returns a 200 response immediately.

    Spy on `queryClient.invalidateQueries` via:
    ```typescript
    const qc = new QueryClient();
    const spy = vi.spyOn(qc, 'invalidateQueries');
    const wrapper = ({ children }: PropsWithChildren) => (
      <QueryClientProvider client={qc}>{children}</QueryClientProvider>
    );
    const { result } = renderHook(() => useCreateTeacher(schoolId), { wrapper });
    await act(() => result.current.mutateAsync(somePayload));
    expect(spy).toHaveBeenCalledWith({ queryKey: ['dashboard-status'] });
    ```

    For each of the 6 tests, invoke the mutation, await success, then assert the spy was called with `{ queryKey: ['dashboard-status'] }` (or equivalent — the exact array form, since `dashboardKeys.status === ['dashboard-status']` per Plan 02).

    NOTE: Existing useTeachers / useDsfa may already have a `.test.ts` file. If so, ADD the new test cases to the existing file rather than overwriting. Verify before writing.
  </action>
  <verify>
    <automated>pnpm --filter @schoolflow/web test --run useTeachers.test useDsfa.test</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/src/hooks/useTeachers.test.ts` contains tests asserting `invalidateQueries` called with `{ queryKey: ['dashboard-status'] }` for at least 3 mutations
    - File `apps/web/src/hooks/useDsfa.test.ts` contains tests asserting same for at least 3 mutations
    - `pnpm --filter @schoolflow/web test --run useTeachers.test useDsfa.test` exits 0 with all tests passing
    - Tests do NOT replace or remove existing test cases; they ADD to the file's test suite (verify via git diff)
  </acceptance_criteria>
  <done>2 representative hooks have unit tests verifying the dashboard invalidation wiring; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → TanStack QueryClient | Cache invalidation triggers refetch on next read; no auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-14 | (Regression / non-STRIDE) | onError silent-4xx invariant | mitigate | Acceptance criteria explicitly forbid touching `onError` blocks. Manual git-diff review during execution + Plan 07 E2E spec asserts toast.error is shown on 4xx mutations (carry-forward Phase 10.2-04). |
| T-16-15 | D (DoS via cache thrashing) | Cross-mutation invalidation | accept | Plan 02 useDashboardStatus uses `staleTime: 10_000` (D-09) to deduplicate within window. Worst case: 10 rapid-fire teacher creates → 1 dashboard refetch (subsequent invalidations within 10s fold). Bounded. |
</threat_model>

<verification>
- TypeScript clean: `pnpm --filter @schoolflow/web exec tsc --noEmit` exits 0
- Unit tests pass: `pnpm --filter @schoolflow/web test --run useTeachers.test useDsfa.test` exits 0
- Aggregate sweep: `grep -hE "invalidateQueries\(\s*\{\s*queryKey:\s*dashboardKeys.status" apps/web/src/hooks/{useTeachers,useClasses,useStudents,useSubjects,useSchool,useTimeGrid,useSchoolYears,useDsfa,useVvz,useRetention,useConsents,useDsgvoExportJob,useDsgvoDeletionJob}.ts | wc -l` returns ≥ 50
- No onError modification: `git diff apps/web/src/hooks/*.ts | grep -E "^[-+].*onError" | grep -v "^---\|^+++"` returns 0 (no onError lines added or removed)
- E2E live-update verification deferred to Plan 07 (admin creates teacher → dashboard category 'teachers' transitions from missing/done within 5s)
</verification>

<success_criteria>
- 13 hook files import `dashboardKeys`
- ~57 mutation onSuccess sites add `qc.invalidateQueries({ queryKey: dashboardKeys.status })`
- Existing onSuccess/onError logic untouched
- 2 representative regression tests pass
- TypeScript clean across all 13 files
</success_criteria>

<output>
After completion, create `.planning/phases/16-admin-dashboard-mobile-h-rtung/16-06-SUMMARY.md` per templates/summary.md. Document:
- Per-file: actual mutation count vs PATTERNS-predicted count (if differs)
- Total dashboardKeys.status invalidations added across the 13 files
- Any onSuccess block that was NOT a mutation (e.g. a query's onSuccess) — those should be skipped; document any such cases
- Verification of unchanged onError blocks (carry-forward D-19)
</output>
