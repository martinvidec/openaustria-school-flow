---
phase: 10-schulstammdaten-zeitraster
plan: 05
type: execute
wave: 5
depends_on: [01a, 01b, 02, 03a, 03b]
files_modified:
  - apps/web/src/components/admin/school-settings/SchoolYearsTab.tsx
  - apps/web/src/components/admin/school-settings/SchoolYearCard.tsx
  - apps/web/src/components/admin/school-settings/CreateSchoolYearDialog.tsx
  - apps/web/src/components/admin/school-settings/ActivateSchoolYearDialog.tsx
  - apps/web/src/components/admin/school-settings/DeleteSchoolYearDialog.tsx
  - apps/web/src/components/admin/school-settings/HolidaysList.tsx
  - apps/web/src/components/admin/school-settings/AutonomousDaysList.tsx
  - apps/web/src/components/admin/school-settings/OptionsTab.tsx
  - apps/web/src/components/admin/school-settings/__tests__/SchoolYearsTab.spec.tsx
  - apps/web/src/components/admin/school-settings/__tests__/OptionsTab.spec.tsx
autonomous: true
requirements:
  - SCHOOL-03
  - SCHOOL-04
  - SCHOOL-05
user_setup: []
must_haves:
  truths:
    - "Schuljahre tab lists all years for the school as Cards with Aktiv-Badge on the active row"
    - "Aktiv-Swap Info Banner is rendered at the top of the tab when an active year exists"
    - "+ Neues Schuljahr anlegen opens CreateSchoolYearDialog with name, startDate, semesterBreak, endDate, isActive switch"
    - "Aktivieren Row-Action opens confirm dialog; on confirm, useActivateSchoolYear is called and InfoBanner updates"
    - "Loeschen Row-Action is disabled when year.isActive OR has references; tooltip shows the reason"
    - "On 409 SchoolYearOrphanError from delete, toast shows German message with reference count (D-10)"
    - "Holidays and AutonomousDays sub-lists are nested inside each Card via Collapsible; both support add/remove"
    - "Optionen tab shows A/B-Wochen-Modus toggle row + status line (current run state) + Info Banner"
    - "Toggling A/B switch sends PUT /api/v1/schools/:id with {abWeekEnabled} via dedicated inline mutation; toast shows 'Option gespeichert.' verbatim (NOT useUpdateSchool which toasts 'Aenderungen gespeichert.')"
    - "Status line text varies by current run state: aktiviert/deaktiviert/no-run (D-06)"
  artifacts:
    - path: "apps/web/src/components/admin/school-settings/SchoolYearsTab.tsx"
      provides: "Full Schuljahre tab — list + dialogs + nested holidays/autonomous days"
    - path: "apps/web/src/components/admin/school-settings/SchoolYearCard.tsx"
      provides: "Per-year Card with badge + actions + Collapsible nested sub-UIs"
    - path: "apps/web/src/components/admin/school-settings/CreateSchoolYearDialog.tsx"
      provides: "Dialog with 5 fields per UI-SPEC §5.4"
    - path: "apps/web/src/components/admin/school-settings/ActivateSchoolYearDialog.tsx"
      provides: "Confirm dialog per UI-SPEC §5.5"
    - path: "apps/web/src/components/admin/school-settings/DeleteSchoolYearDialog.tsx"
      provides: "Confirm dialog per UI-SPEC §7.3"
    - path: "apps/web/src/components/admin/school-settings/HolidaysList.tsx"
      provides: "Nested date-range CRUD"
    - path: "apps/web/src/components/admin/school-settings/AutonomousDaysList.tsx"
      provides: "Nested single-day CRUD with optional label"
    - path: "apps/web/src/components/admin/school-settings/OptionsTab.tsx"
      provides: "Full Optionen tab — A/B toggle + InfoBanner + status line"
  key_links:
    - from: "OptionsTab toggle"
      to: "PUT /api/v1/schools/:id with {abWeekEnabled}"
      via: "Inline TanStack Query mutation (apiFetch direct — NOT useUpdateSchool)"
      pattern: "apiFetch\\(`/api/v1/schools/"
    - from: "OptionsTab status line"
      to: "useActiveTimetableRun"
      via: "TanStack Query"
      pattern: "useActiveTimetableRun"
    - from: "SchoolYearCard delete button"
      to: "useDeleteSchoolYear"
      via: "SchoolYearOrphanError 409 handling"
      pattern: "SchoolYearOrphanError"
---

<objective>
Wave 5 sibling B (parallel with Plan 04): implement the Schuljahre tab fully (multi-year list with Aktiv-Badge, Aktivieren/Bearbeiten/Loeschen Row-Actions, nested Holidays + AutonomousDays sub-lists, Aktiv-Swap Info Banner) and the Optionen tab (A/B-Wochen toggle + status line + Info Banner). Replaces the placeholder SchoolYearsTab and OptionsTab from Plan 03.

Purpose: Closes SCHOOL-03 (Schuljahre + Aktiv), SCHOOL-04 (A/B toggle UI), and SCHOOL-05 (orphan-guard delete UI). Mounts the orphan-guard error path end-to-end (backend Plan 02 throws 409 + count; frontend Plan 03 hook converts to SchoolYearOrphanError; this plan renders the German toast). Mounts the abWeekEnabled UI end-to-end (backend Plan 02 accepts the field; this plan wires the toggle + status line + UI-SPEC §6.2 info banner via a dedicated inline mutation so the toast text matches "Option gespeichert." verbatim).

Output: Two fully-implemented tabs (SchoolYearsTab, OptionsTab) + 5 supporting components (SchoolYearCard, CreateSchoolYearDialog, ActivateSchoolYearDialog, DeleteSchoolYearDialog, HolidaysList, AutonomousDaysList); spec coverage for the activate-swap, orphan-delete-409, and A/B-toggle flows.
</objective>

<execution_context>
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-schulstammdaten-zeitraster/10-CONTEXT.md
@.planning/phases/10-schulstammdaten-zeitraster/10-RESEARCH.md
@.planning/phases/10-schulstammdaten-zeitraster/10-PATTERNS.md
@.planning/phases/10-schulstammdaten-zeitraster/10-UI-SPEC.md
@.planning/phases/10-schulstammdaten-zeitraster/10-03a-SUMMARY.md
@.planning/phases/10-schulstammdaten-zeitraster/10-03b-SUMMARY.md
@apps/web/src/components/admin/school-settings/SchoolDetailsTab.tsx
@apps/web/src/components/admin/shared/InfoBanner.tsx
@apps/web/src/components/admin/shared/StickyMobileSaveBar.tsx
@apps/web/src/hooks/useSchoolYears.ts
@apps/web/src/hooks/useSchool.ts
@apps/web/src/hooks/useActiveTimetableRun.ts

<interfaces>
<!-- All available imports from prior plans -->

From @schoolflow/shared:
```typescript
import { SchoolYearSchema, type SchoolYearInput, type SchoolYearDto } from '@schoolflow/shared';
```

From apps/web/src/hooks/useSchoolYears.ts (Plan 03):
```typescript
export function useSchoolYears(schoolId: string | undefined): UseQueryResult<SchoolYearDto[]>;
export function useCreateSchoolYear(schoolId: string): UseMutationResult<SchoolYearDto, Error, SchoolYearInput>;
export function useUpdateSchoolYear(schoolId: string): UseMutationResult<SchoolYearDto, Error, {yearId, dto}>;
export function useActivateSchoolYear(schoolId: string): UseMutationResult<SchoolYearDto, Error, string>;
export function useDeleteSchoolYear(schoolId: string): UseMutationResult<void, SchoolYearOrphanError | Error, string>;
export class SchoolYearOrphanError extends Error { referenceCount: number; }
```

From apps/web/src/hooks/useSchool.ts (Plan 03):
```typescript
export function useSchool(schoolId: string | undefined): UseQueryResult<SchoolDto | null>;
export const schoolKeys = { one: (schoolId: string) => ['school', schoolId] as const, list: () => ['schools'] as const };
// useUpdateSchool exists but is INTENTIONALLY NOT used in OptionsTab — it toasts "Aenderungen gespeichert." which violates UI-SPEC §6.3 ("Option gespeichert." verbatim).
```

From apps/web/src/hooks/useActiveTimetableRun.ts (Plan 03):
```typescript
export function useActiveTimetableRun(schoolId: string | undefined): UseQueryResult<ActiveRunDto | null>;
export interface ActiveRunDto { id: string; abWeekEnabled: boolean; status: string; }
```

shadcn primitives (all installed):
- @/components/ui/{card, dialog, button, input, label, switch, badge, collapsible, tooltip, separator}

Backend endpoints (from Plan 02):
- GET    /api/v1/schools/:schoolId/school-years
- POST   /api/v1/schools/:schoolId/school-years
- POST   /api/v1/schools/:schoolId/school-years/:yearId/activate
- DELETE /api/v1/schools/:schoolId/school-years/:yearId  (409 + {referenceCount} on orphan-guard)
- PUT    /api/v1/schools/:id  (accepts abWeekEnabled — confirmed PUT, NOT PATCH; school.controller.ts line 48)

Holiday + AutonomousDay endpoints (PINNED — added by Plan 02 Task 3, Wave 2):
- POST   /api/v1/schools/:schoolId/school-years/:yearId/holidays           (body: CreateHolidayDto {name, startDate, endDate} -> 201 Holiday)
- DELETE /api/v1/schools/:schoolId/school-years/:yearId/holidays/:holidayId (-> 204 No Content)
- POST   /api/v1/schools/:schoolId/school-years/:yearId/autonomous-days     (body: CreateAutonomousDayDto {date, reason?} -> 201 AutonomousDay)
- DELETE /api/v1/schools/:schoolId/school-years/:yearId/autonomous-days/:dayId (-> 204 No Content)

These endpoints DEFINITELY exist by Wave 5. The corresponding TanStack Query hooks (useCreateHoliday, useDeleteHoliday, useCreateAutonomousDay, useDeleteAutonomousDay) are exported from apps/web/src/hooks/useSchoolYears.ts (Plan 03b). DO NOT add backend code in this plan; DO NOT ship UI with disabled add/remove buttons (D-08 mandates the nested Sub-UI is required, not optional).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SchoolYearsTab + supporting components (list, card, 3 dialogs, holidays + autonomous days sub-lists)</name>
  <files>apps/web/src/components/admin/school-settings/SchoolYearsTab.tsx, apps/web/src/components/admin/school-settings/SchoolYearCard.tsx, apps/web/src/components/admin/school-settings/CreateSchoolYearDialog.tsx, apps/web/src/components/admin/school-settings/ActivateSchoolYearDialog.tsx, apps/web/src/components/admin/school-settings/DeleteSchoolYearDialog.tsx, apps/web/src/components/admin/school-settings/HolidaysList.tsx, apps/web/src/components/admin/school-settings/AutonomousDaysList.tsx, apps/web/src/components/admin/school-settings/__tests__/SchoolYearsTab.spec.tsx</files>
  <read_first>
    - apps/web/src/components/admin/school-settings/SchoolDetailsTab.tsx (Plan 03 — RHF/Zod pattern + Card frame)
    - apps/web/src/components/admin/shared/InfoBanner.tsx (Plan 03)
    - apps/web/src/hooks/useSchoolYears.ts (Plan 03 — all mutations available)
    - apps/web/src/components/ui/badge.tsx, dialog.tsx, collapsible.tsx (Plan 01 installs verified)
    - apps/api/src/modules/school/school.controller.ts (verify existing Holiday/AutonomousDay endpoints — paths, request body shapes)
    - apps/api/src/modules/school/dto/create-school-year.dto.ts (after Plan 02 — confirm isActive field present)
    - .planning/phases/10-schulstammdaten-zeitraster/10-PATTERNS.md "SchoolYearsTab.tsx" + "HolidaysList.tsx" + "AutonomousDaysList.tsx" sections
    - .planning/phases/10-schulstammdaten-zeitraster/10-UI-SPEC.md §5 (Schuljahr tab — list, card, dialogs, info banner) verbatim, §7.3 (Delete dialog), §9 (Orphan-Delete error presentation), §13 (German copy)
    - .planning/phases/10-schulstammdaten-zeitraster/10-RESEARCH.md §1.2 (multi-active semantics), §6.2 (permissions)
  </read_first>
  <behavior>
    - Test 1 (SchoolYearsTab.spec): given 3 SchoolYearDto items (one with isActive=true), renders 3 Card elements; the active one shows a Badge with text "Aktiv".
    - Test 2 (SchoolYearsTab.spec): when an active year exists, an InfoBanner appears at the top with text containing "ist aktiv seit" + the formatted date.
    - Test 3 (SchoolYearsTab.spec): clicking "+ Neues Schuljahr anlegen" opens CreateSchoolYearDialog with 5 fields; submitting valid input calls useCreateSchoolYear.mutate.
    - Test 4 (SchoolYearsTab.spec): clicking "Aktivieren" on a non-active year opens ActivateSchoolYearDialog with text containing the year name and confirmation copy; confirming calls useActivateSchoolYear.mutate.
    - Test 5 (SchoolYearsTab.spec): "Loeschen" button is disabled on the active year with tooltip "Aktives Schuljahr kann nicht geloescht werden".
    - Test 6 (SchoolYearsTab.spec): on 409 SchoolYearOrphanError from delete, toast.error fires with text containing "wird noch von" and the reference count.
    - Test 7 (SchoolYearsTab.spec): expanding the Collapsible reveals "Ferien" and "Schulautonome Tage" sub-sections; "+ Eintrag hinzufuegen" buttons are present in both.
  </behavior>
  <action>
    Step A — Confirm Holiday/AutonomousDay hook imports. The backend endpoints are PINNED and live as of Plan 02 Task 3 (Wave 2). The TanStack Query hooks `useCreateHoliday`, `useDeleteHoliday`, `useCreateAutonomousDay`, `useDeleteAutonomousDay` are exported from `apps/web/src/hooks/useSchoolYears.ts` (Plan 03b Step C). Import them in HolidaysList.tsx / AutonomousDaysList.tsx — DO NOT add backend code in this plan and DO NOT use direct `apiFetch` calls when the typed hook exists. If a hook is missing for any reason, treat that as a Wave 3 gap and surface it via a checker request rather than silently working around it (D-08 requires the nested Sub-UI; disabled buttons are NOT an acceptable fallback).

    Step B — Create apps/web/src/components/admin/school-settings/CreateSchoolYearDialog.tsx (per UI-SPEC §5.4):
    ```tsx
    import { useForm } from 'react-hook-form';
    import { zodResolver } from '@hookform/resolvers/zod';
    import { SchoolYearSchema, type SchoolYearInput } from '@schoolflow/shared';
    import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle } from '@/components/ui/dialog';
    import { Button } from '@/components/ui/button';
    import { Input } from '@/components/ui/input';
    import { Label } from '@/components/ui/label';
    import { Switch } from '@/components/ui/switch';

    interface Props { open: boolean; isSubmitting?: boolean; onClose: () => void; onSubmit: (dto: SchoolYearInput) => void; }
    export function CreateSchoolYearDialog({ open, isSubmitting, onClose, onSubmit }: Props) {
      const { register, handleSubmit, formState: { errors }, watch, setValue, reset } = useForm<SchoolYearInput>({
        resolver: zodResolver(SchoolYearSchema),
        defaultValues: { name: '', isActive: false } as any,
      });
      const isActive = watch('isActive');
      const submit = handleSubmit((v) => { onSubmit(v); reset(); });
      return (
        <Dialog open={open} onOpenChange={(o) => !o && onClose()}>
          <DialogContent>
            <DialogHeader><DialogTitle>Neues Schuljahr anlegen</DialogTitle></DialogHeader>
            <form onSubmit={submit} className="space-y-4">
              <div className="space-y-1.5">
                <Label htmlFor="sy-name">Name</Label>
                <Input id="sy-name" {...register('name')} placeholder="2026/2027" />
                {errors.name &amp;&amp; <p className="text-xs text-destructive">{errors.name.message}</p>}
              </div>
              <div className="space-y-1.5">
                <Label htmlFor="sy-start">Start</Label>
                <Input id="sy-start" type="date" {...register('startDate' as any, { valueAsDate: true } as any)} />
                {errors.startDate &amp;&amp; <p className="text-xs text-destructive">{(errors.startDate as any).message}</p>}
              </div>
              <div className="space-y-1.5">
                <Label htmlFor="sy-sem">Semesterwechsel</Label>
                <Input id="sy-sem" type="date" {...register('semesterBreak' as any, { valueAsDate: true } as any)} />
                {errors.semesterBreak &amp;&amp; <p className="text-xs text-destructive">{(errors.semesterBreak as any).message}</p>}
              </div>
              <div className="space-y-1.5">
                <Label htmlFor="sy-end">Ende</Label>
                <Input id="sy-end" type="date" {...register('endDate' as any, { valueAsDate: true } as any)} />
                {errors.endDate &amp;&amp; <p className="text-xs text-destructive">{(errors.endDate as any).message}</p>}
              </div>
              <div className="flex items-center justify-between">
                <Label htmlFor="sy-active">Als aktives Schuljahr setzen</Label>
                <Switch id="sy-active" checked={!!isActive} onCheckedChange={(v) => setValue('isActive', v as any)} />
              </div>
              <DialogFooter>
                <Button type="button" variant="ghost" onClick={onClose} disabled={isSubmitting}>Abbrechen</Button>
                <Button type="submit" disabled={isSubmitting}>{isSubmitting ? 'Wird angelegt...' : 'Anlegen'}</Button>
              </DialogFooter>
            </form>
          </DialogContent>
        </Dialog>
      );
    }
    ```

    Step C — Create apps/web/src/components/admin/school-settings/ActivateSchoolYearDialog.tsx (per UI-SPEC §5.5):
    ```tsx
    import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from '@/components/ui/dialog';
    import { Button } from '@/components/ui/button';

    interface Props { open: boolean; yearName: string; isSubmitting?: boolean; onCancel: () => void; onConfirm: () => void; }
    export function ActivateSchoolYearDialog({ open, yearName, isSubmitting, onCancel, onConfirm }: Props) {
      return (
        <Dialog open={open} onOpenChange={(o) => !o && onCancel()}>
          <DialogContent>
            <DialogHeader>
              <DialogTitle>Schuljahr aktivieren</DialogTitle>
              <DialogDescription>
                "{yearName}" wird zum aktiven Schuljahr. Bestehende Stundenplaene und Klassenbuch-Eintraege bleiben unveraendert.
              </DialogDescription>
            </DialogHeader>
            <DialogFooter>
              <Button variant="ghost" onClick={onCancel} disabled={isSubmitting} autoFocus>Abbrechen</Button>
              <Button onClick={onConfirm} disabled={isSubmitting}>{isSubmitting ? 'Wird aktiviert...' : 'Aktivieren'}</Button>
            </DialogFooter>
          </DialogContent>
        </Dialog>
      );
    }
    ```

    Step D — Create apps/web/src/components/admin/school-settings/DeleteSchoolYearDialog.tsx (per UI-SPEC §7.3):
    ```tsx
    import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from '@/components/ui/dialog';
    import { Button } from '@/components/ui/button';

    interface Props { open: boolean; yearName: string; isSubmitting?: boolean; onCancel: () => void; onConfirm: () => void; }
    export function DeleteSchoolYearDialog({ open, yearName, isSubmitting, onCancel, onConfirm }: Props) {
      return (
        <Dialog open={open} onOpenChange={(o) => !o && onCancel()}>
          <DialogContent>
            <DialogHeader>
              <DialogTitle>Schuljahr loeschen?</DialogTitle>
              <DialogDescription>"{yearName}" wird dauerhaft geloescht. Diese Aktion kann nicht rueckgaengig gemacht werden.</DialogDescription>
            </DialogHeader>
            <DialogFooter>
              <Button variant="outline" onClick={onCancel} disabled={isSubmitting} autoFocus>Abbrechen</Button>
              <Button variant="destructive" onClick={onConfirm} disabled={isSubmitting}>Loeschen</Button>
            </DialogFooter>
          </DialogContent>
        </Dialog>
      );
    }
    ```

    Step E — Create apps/web/src/components/admin/school-settings/HolidaysList.tsx and AutonomousDaysList.tsx. Each is a simple nested-list component. HolidaysList contract:
    ```tsx
    interface Holiday { id: string; startDate: string; endDate: string; name?: string | null; }
    interface Props { schoolId: string; yearId: string; holidays: Holiday[]; }
    // Renders: list of <div className="flex items-center gap-2 py-2 border-b last:border-b-0">
    //   <Input type="date" value={h.startDate} disabled />
    //   <span>–</span>
    //   <Input type="date" value={h.endDate} disabled />
    //   <Button variant="ghost" size="icon" className="h-8 w-8 md:h-8 md:w-8" onClick={() => deleteMut.mutate(h.id)}><Trash2/></Button>
    // </div>
    // Plus a "+ Eintrag hinzufuegen" button that opens an inline form (two date inputs + optional name + Save button).
    // Mobile: button h-11 w-11.
    ```
    Use `useCreateHoliday` and `useDeleteHoliday` mutation hooks imported from `apps/web/src/hooks/useSchoolYears.ts` (exported by Plan 03b). Both hooks already invalidate `schoolYearKeys.all(schoolId)` on success, so the parent SchoolYearsTab refetches automatically. AutonomousDaysList has the same shape with `useCreateAutonomousDay` + `useDeleteAutonomousDay`, a single `<Input type="date">`, and an optional label `<Input type="text" placeholder="z. B. Schulfest">` mapped to the `reason` field of CreateAutonomousDayDto.

    Step F — Create apps/web/src/components/admin/school-settings/SchoolYearCard.tsx (per UI-SPEC §5.2 + §5.3):
    - Header: year name (text-base font-semibold), date range "dd.MM.yyyy – dd.MM.yyyy" via date-fns format, Aktiv-Badge with CheckCircle2 icon when isActive.
    - Actions row: [Aktivieren] (only if !isActive), [Bearbeiten] (ghost, Pencil icon — opens inline edit OR a dialog; v1: dialog identical to CreateSchoolYearDialog with prefilled values), [Loeschen] (destructive, disabled when isActive OR hasReferences).
    - Tooltip on disabled Loeschen explains why ("Aktives Schuljahr kann nicht geloescht werden" / "Schuljahr wird noch verwendet und kann nicht geloescht werden").
    - Body: Collapsible with HolidaysList + AutonomousDaysList sub-sections.

    Step G — Implement apps/web/src/components/admin/school-settings/SchoolYearsTab.tsx (REPLACE Plan 03 placeholder):
    ```tsx
    import { useState } from 'react';
    import { useSchoolYears, useCreateSchoolYear, useActivateSchoolYear, useDeleteSchoolYear, SchoolYearOrphanError } from '@/hooks/useSchoolYears';
    import { Card } from '@/components/ui/card';
    import { Button } from '@/components/ui/button';
    import { Plus } from 'lucide-react';
    import { format } from 'date-fns';
    import { InfoBanner } from '@/components/admin/shared/InfoBanner';
    import { SchoolYearCard } from './SchoolYearCard';
    import { CreateSchoolYearDialog } from './CreateSchoolYearDialog';
    import { ActivateSchoolYearDialog } from './ActivateSchoolYearDialog';
    import { DeleteSchoolYearDialog } from './DeleteSchoolYearDialog';
    import { toast } from 'sonner';
    import type { SchoolYearDto, SchoolYearInput } from '@schoolflow/shared';

    interface Props { schoolId: string; onDirtyChange?: (d: boolean) => void; }

    export function SchoolYearsTab({ schoolId, onDirtyChange }: Props) {
      const yearsQuery = useSchoolYears(schoolId);
      const createMut = useCreateSchoolYear(schoolId);
      const activateMut = useActivateSchoolYear(schoolId);
      const deleteMut = useDeleteSchoolYear(schoolId);
      const [createOpen, setCreateOpen] = useState(false);
      const [activate, setActivate] = useState<SchoolYearDto | null>(null);
      const [del, setDel] = useState<SchoolYearDto | null>(null);

      const years = yearsQuery.data ?? [];
      const active = years.find((y) => y.isActive) ?? null;

      const handleCreate = async (dto: SchoolYearInput) => {
        await createMut.mutateAsync(dto);
        setCreateOpen(false);
      };
      const handleActivate = async () => {
        if (!activate) return;
        await activateMut.mutateAsync(activate.id);
        setActivate(null);
      };
      const handleDelete = async () => {
        if (!del) return;
        try {
          await deleteMut.mutateAsync(del.id);
          setDel(null);
        } catch (e) {
          // SchoolYearOrphanError handled inside the hook's onError; dialog stays open
          if (e instanceof SchoolYearOrphanError) setDel(null);
        }
      };

      return (
        <Card className="border-none shadow-none md:border md:shadow-sm p-6 md:p-8">
          <div className="mb-6">
            <h2 className="text-lg font-semibold">Schuljahre</h2>
            <p className="text-sm text-muted-foreground">Pflegen Sie Start- und Endzeitpunkte, Ferien und schulautonome Tage.</p>
          </div>

          {active &amp;&amp; (
            <div className="mb-4">
              <InfoBanner>
                <strong>{active.name}</strong> ist aktiv seit {format(new Date(active.startDate), 'dd.MM.yyyy')}. Neue Stundenplaene und Eintraege ordnen sich diesem Schuljahr zu.
              </InfoBanner>
            </div>
          )}

          <div className="flex justify-end mb-4">
            <Button onClick={() => setCreateOpen(true)} className="h-11 md:h-10 w-full md:w-auto">
              <Plus className="h-4 w-4 mr-2" /> Neues Schuljahr anlegen
            </Button>
          </div>

          {years.length === 0 ? (
            <div className="text-center py-8">
              <h3 className="text-lg font-semibold mb-2">Noch kein Schuljahr angelegt</h3>
              <p className="text-sm text-muted-foreground">Legen Sie das erste Schuljahr an, um mit der Planung zu beginnen.</p>
            </div>
          ) : (
            <div className="space-y-3">
              {years.map((y) => (
                <SchoolYearCard key={y.id} schoolId={schoolId} year={y} onActivate={() => setActivate(y)} onDelete={() => setDel(y)} />
              ))}
            </div>
          )}

          <CreateSchoolYearDialog open={createOpen} isSubmitting={createMut.isPending} onClose={() => setCreateOpen(false)} onSubmit={handleCreate} />
          <ActivateSchoolYearDialog open={!!activate} yearName={activate?.name ?? ''} isSubmitting={activateMut.isPending} onCancel={() => setActivate(null)} onConfirm={handleActivate} />
          <DeleteSchoolYearDialog open={!!del} yearName={del?.name ?? ''} isSubmitting={deleteMut.isPending} onCancel={() => setDel(null)} onConfirm={handleDelete} />
        </Card>
      );
    }
    ```

    Step H — Write apps/web/src/components/admin/school-settings/__tests__/SchoolYearsTab.spec.tsx covering all 7 behaviors. Mock `useSchoolYears` to return controlled data; mock the mutation hooks; mock `toast` via `vi.mock('sonner', () => ({ toast: { success: vi.fn(), error: vi.fn() } }))` for assertions on Test 6.
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/apps/web &amp;&amp; pnpm exec vitest run src/components/admin/school-settings/__tests__/SchoolYearsTab.spec.tsx &amp;&amp; pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Schuljahre" apps/web/src/components/admin/school-settings/SchoolYearsTab.tsx` returns at least 1 match (section title).
    - `grep -F "Neues Schuljahr anlegen" apps/web/src/components/admin/school-settings/SchoolYearsTab.tsx` returns 1 match.
    - `grep -F "ist aktiv seit" apps/web/src/components/admin/school-settings/SchoolYearsTab.tsx` returns 1 match (Aktiv-Swap Info Banner per UI-SPEC §5.6).
    - `grep -F "SchoolYearOrphanError" apps/web/src/components/admin/school-settings/SchoolYearsTab.tsx` returns at least 1 match.
    - `grep -F "Aktives Schuljahr kann nicht geloescht werden" apps/web/src/components/admin/school-settings/SchoolYearCard.tsx` returns 1 match.
    - `grep -F "Schuljahr aktivieren" apps/web/src/components/admin/school-settings/ActivateSchoolYearDialog.tsx` returns 1 match.
    - `grep -F "Schuljahr loeschen?" apps/web/src/components/admin/school-settings/DeleteSchoolYearDialog.tsx` returns 1 match.
    - `grep -F "Neues Schuljahr anlegen" apps/web/src/components/admin/school-settings/CreateSchoolYearDialog.tsx` returns 1 match (dialog title).
    - `grep -F "Als aktives Schuljahr setzen" apps/web/src/components/admin/school-settings/CreateSchoolYearDialog.tsx` returns 1 match.
    - `ls apps/web/src/components/admin/school-settings/HolidaysList.tsx apps/web/src/components/admin/school-settings/AutonomousDaysList.tsx` lists both files.
    - `cd apps/web &amp;&amp; pnpm exec vitest run src/components/admin/school-settings/__tests__/SchoolYearsTab.spec.tsx` exits 0 with all 7 behaviors passing.
    - `cd apps/web &amp;&amp; pnpm exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    Schuljahre tab fully implements UI-SPEC §5: list with Aktiv-Badge, Info Banner when active, +/Aktivieren/Bearbeiten/Loeschen Row-Actions with proper guard tooltips, 3 dialogs with verbatim copy, nested HolidaysList + AutonomousDaysList. 409 SchoolYearOrphanError surfaces a German toast with reference count. Vitest + tsc green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OptionsTab (A/B-Wochen toggle + status line + Info Banner)</name>
  <files>apps/web/src/components/admin/school-settings/OptionsTab.tsx, apps/web/src/components/admin/school-settings/__tests__/OptionsTab.spec.tsx</files>
  <read_first>
    - apps/web/src/hooks/useSchool.ts (Plan 03 — schoolKeys exported; useUpdateSchool exists but is intentionally NOT used here)
    - apps/web/src/hooks/useActiveTimetableRun.ts (Plan 03)
    - apps/web/src/lib/api.ts (apiFetch signature)
    - apps/web/src/components/admin/shared/InfoBanner.tsx (Plan 03)
    - apps/web/src/components/admin/school-settings/SchoolDetailsTab.tsx (Plan 03 — Card frame + Save button placement)
    - apps/web/src/components/ui/switch.tsx (Plan 01 install)
    - .planning/phases/10-schulstammdaten-zeitraster/10-PATTERNS.md "OptionsTab.tsx" section
    - .planning/phases/10-schulstammdaten-zeitraster/10-UI-SPEC.md §6 (Optionen tab — verbatim copywriting and dialog structure)
    - .planning/phases/10-schulstammdaten-zeitraster/10-CONTEXT.md (D-04, D-05, D-06)
  </read_first>
  <behavior>
    - Test 1 (OptionsTab.spec): renders section header "Optionen" + subtitle "Schulweite Einstellungen, die den Stundenplan-Solver beeinflussen.".
    - Test 2 (OptionsTab.spec): renders A/B-Wochen-Modus row with label + Switch reflecting school.abWeekEnabled.
    - Test 3 (OptionsTab.spec): given useActiveTimetableRun returns {abWeekEnabled: true}, status line shows "A/B-Wochen sind im aktuellen Stundenplan aktiviert.".
    - Test 4 (OptionsTab.spec): given useActiveTimetableRun returns {abWeekEnabled: false}, status line shows "A/B-Wochen sind im aktuellen Stundenplan deaktiviert.".
    - Test 5 (OptionsTab.spec): given useActiveTimetableRun returns null, status line shows "Es existiert noch kein Stundenplan.".
    - Test 6 (OptionsTab.spec): InfoBanner with text containing "gilt ab dem naechsten Stundenplan-Lauf" is ALWAYS present (per UI-SPEC §6.2 — always visible).
    - Test 7 (OptionsTab.spec): toggling the Switch calls `apiFetch` with `PUT /api/v1/schools/:id` and request body `{abWeekEnabled: <new value>}`; on success the schoolKeys.one(schoolId) and ['timetable-run:active', schoolId] queries are invalidated and `toast.success` is called with the literal string "Option gespeichert.". `useUpdateSchool` is NEVER called and is NOT imported in this component.
  </behavior>
  <action>
    Implement apps/web/src/components/admin/school-settings/OptionsTab.tsx (REPLACE Plan 03 placeholder).

    DESIGN CONSTRAINT (UI-SPEC §6.3): the success toast for the A/B toggle MUST read "Option gespeichert." verbatim. The shared `useUpdateSchool` mutation toasts "Aenderungen gespeichert." (Plan 03b) which violates this contract. Therefore OptionsTab DOES NOT use `useUpdateSchool` and DOES NOT import it. It uses a single dedicated inline mutation (`updateAbMut`) that PUTs the school directly via `apiFetch` and toasts the correct copy on success.

    There must be EXACTLY ONE mutation hook in the final component. Do NOT add a second mutation, do NOT call `useUpdateSchool`, do NOT have any reference to a `updateMut` variable.

    Final component (use this verbatim — single mutation hook, single toast string):

    ```tsx
    import { useEffect } from 'react';
    import { useMutation, useQueryClient } from '@tanstack/react-query';
    import { useSchool, schoolKeys } from '@/hooks/useSchool';
    import { useActiveTimetableRun } from '@/hooks/useActiveTimetableRun';
    import { apiFetch } from '@/lib/api';
    import { toast } from 'sonner';
    import { Card } from '@/components/ui/card';
    import { Switch } from '@/components/ui/switch';
    import { Label } from '@/components/ui/label';
    import { InfoBanner } from '@/components/admin/shared/InfoBanner';
    import type { SchoolDto } from '@schoolflow/shared';

    interface Props { schoolId: string; onDirtyChange?: (d: boolean) => void; }

    export function OptionsTab({ schoolId, onDirtyChange }: Props) {
      const schoolQuery = useSchool(schoolId);
      const runQuery = useActiveTimetableRun(schoolId);
      const qc = useQueryClient();

      // Dedicated inline mutation — toasts "Option gespeichert." per UI-SPEC §6.3.
      // We do NOT use useUpdateSchool here because it toasts "Aenderungen gespeichert." (wrong copy for this tab).
      const updateAbMut = useMutation({
        mutationFn: async (abWeekEnabled: boolean): Promise<SchoolDto> => {
          const res = await apiFetch(`/api/v1/schools/${schoolId}`, {
            method: 'PUT',
            body: JSON.stringify({ abWeekEnabled }),
          });
          if (!res.ok) throw new Error('Speichern fehlgeschlagen');
          return res.json();
        },
        onSuccess: () => {
          qc.invalidateQueries({ queryKey: schoolKeys.one(schoolId) });
          qc.invalidateQueries({ queryKey: ['timetable-run:active', schoolId] });
          toast.success('Option gespeichert.');
        },
        onError: (e: Error) => toast.error(e.message),
      });

      const school = schoolQuery.data;
      const run = runQuery.data;

      const statusLine = !run
        ? 'Es existiert noch kein Stundenplan.'
        : run.abWeekEnabled
          ? 'A/B-Wochen sind im aktuellen Stundenplan aktiviert.'
          : 'A/B-Wochen sind im aktuellen Stundenplan deaktiviert.';

      const handleToggle = (next: boolean) => {
        updateAbMut.mutate(next);
      };

      // Auto-save tab: never reports dirty state to the parent shell.
      useEffect(() => onDirtyChange?.(false), [onDirtyChange]);

      if (!school) return null;

      return (
        <Card className="border-none shadow-none md:border md:shadow-sm p-6 md:p-8">
          <div className="mb-6">
            <h2 className="text-lg font-semibold">Optionen</h2>
            <p className="text-sm text-muted-foreground">Schulweite Einstellungen, die den Stundenplan-Solver beeinflussen.</p>
          </div>

          <div className="flex items-start justify-between gap-4 py-3">
            <div className="flex flex-col">
              <Label htmlFor="ab-toggle" className="text-sm font-medium">A/B-Wochen-Modus</Label>
              <span className="text-xs text-muted-foreground mt-1">{statusLine}</span>
            </div>
            <Switch
              id="ab-toggle"
              checked={!!school.abWeekEnabled}
              onCheckedChange={handleToggle}
              disabled={updateAbMut.isPending}
              className="h-11 w-11 md:h-6 md:w-11"
              aria-label="A/B-Wochen-Modus aktivieren"
            />
          </div>

          <div className="mt-3">
            <InfoBanner>Eine Aenderung gilt ab dem naechsten Stundenplan-Lauf. Bestehende Stundenplaene bleiben unveraendert.</InfoBanner>
          </div>
        </Card>
      );
    }
    ```

    Then write apps/web/src/components/admin/school-settings/__tests__/OptionsTab.spec.tsx covering all 7 behaviors. Mock `useSchool`, `useActiveTimetableRun`, `apiFetch` (vi.mock '@/lib/api'), and `toast` (vi.mock 'sonner'). For Test 7 specifically: assert that `apiFetch` was called with `/api/v1/schools/${schoolId}` and `{method: 'PUT', body: JSON.stringify({abWeekEnabled: <new value>})}`, and that `toast.success` was called with the exact string `'Option gespeichert.'`.
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/apps/web &amp;&amp; pnpm exec vitest run src/components/admin/school-settings/__tests__/OptionsTab.spec.tsx &amp;&amp; pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "A/B-Wochen-Modus" apps/web/src/components/admin/school-settings/OptionsTab.tsx` returns at least 1 match.
    - `grep -F "A/B-Wochen sind im aktuellen Stundenplan aktiviert." apps/web/src/components/admin/school-settings/OptionsTab.tsx` returns 1 match.
    - `grep -F "A/B-Wochen sind im aktuellen Stundenplan deaktiviert." apps/web/src/components/admin/school-settings/OptionsTab.tsx` returns 1 match.
    - `grep -F "Es existiert noch kein Stundenplan." apps/web/src/components/admin/school-settings/OptionsTab.tsx` returns 1 match.
    - `grep -F "Eine Aenderung gilt ab dem naechsten Stundenplan-Lauf" apps/web/src/components/admin/school-settings/OptionsTab.tsx` returns 1 match (info banner verbatim per UI-SPEC §6.2).
    - `grep -F "Option gespeichert." apps/web/src/components/admin/school-settings/OptionsTab.tsx` returns 1 match.
    - `grep -F "useUpdateSchool" apps/web/src/components/admin/school-settings/OptionsTab.tsx` returns 0 matches (component MUST NOT import or call useUpdateSchool — per UI-SPEC §6.3 toast text contract).
    - `grep -F "abWeekEnabled" apps/web/src/components/admin/school-settings/OptionsTab.tsx` returns at least 2 matches.
    - `grep -F "apiFetch(`/api/v1/schools/" apps/web/src/components/admin/school-settings/OptionsTab.tsx` returns 1 match (inline PUT call).
    - `grep -F "method: 'PUT'" apps/web/src/components/admin/school-settings/OptionsTab.tsx` returns 1 match (school update is PUT, NOT PATCH — confirmed via apps/api/src/modules/school/school.controller.ts line 48 `@Put(':id')`).
    - `cd apps/web &amp;&amp; pnpm exec vitest run src/components/admin/school-settings/__tests__/OptionsTab.spec.tsx` exits 0 with all 7 behaviors passing.
    - `cd apps/web &amp;&amp; pnpm exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    Optionen tab fully implements UI-SPEC §6: section header + A/B-Wochen-Modus toggle + dynamic status line driven by useActiveTimetableRun + always-on Info Banner. Toggle persists via dedicated inline mutation that PUTs /api/v1/schools/:id with abWeekEnabled. Toast text matches UI-SPEC §6.3 ("Option gespeichert.") verbatim. Component does NOT import or call useUpdateSchool (which would toast the wrong copy). Vitest + tsc green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API (POST school-years/:id/activate) | Atomic state-transition request |
| Browser → API (DELETE school-years/:id) | Destructive request guarded by orphan-check |
| Browser → API (PUT schools/:id with abWeekEnabled) | Side-effect on future TimetableRun creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Tampering | Activate could race with create — two activates picking different "previously active" rows | mitigate | Backend Plan 02 wraps activate in $transaction with updateMany-then-update; partial unique index from Plan 01 enforces invariant at DB level even on race |
| T-10-05-02 | Information Disclosure | referenceCount on 409 leaks the magnitude of student/lesson activity | accept | Aggregated count only (no IDs); admin already has read access; necessary for clear UI guidance |
| T-10-05-03 | Tampering | Inline-edit on holidays could submit malformed dates bypassing client validation | mitigate | Server class-validator on Holiday DTO catches it; Zod schema (Plan 01) provides client-side gate |
| T-10-05-04 | Elevation of Privilege | Schueler/Eltern could attempt activate via direct API | mitigate | @CheckPermissions({subject:'school-year', action:'activate'}) on Plan 02 controller; seed grants only admin/schulleitung |
| T-10-05-05 | Repudiation | Activation has no separate audit row | accept | Existing AuditInterceptor (Phase 1 D-05) logs PATCH/POST mutations including activate; per CONTEXT.md Deferred Ideas explicit audit was deferred to Phase 15 |
| T-10-05-06 | Spoofing | Auto-save A/B toggle could be triggered by CSRF-style attack | mitigate | API uses bearer JWT (Plan 1 D-12) — no cookie-based session; CSRF not applicable; admin already authenticated via Keycloak |
| T-10-05-07 | Denial of Service | Rapid toggle-spam on A/B switch | accept | Single field PUT is sub-100ms; TanStack Query deduplicates concurrent identical mutations; not a meaningful attack vector |
</threat_model>

<verification>
1. Visit /admin/school/settings?tab=years — list renders all years; active row has Aktiv-Badge; Info Banner shows "ist aktiv seit ...".
2. Click "+ Neues Schuljahr anlegen" → 5-field dialog opens; submitting valid input creates the year.
3. Click Aktivieren on a non-active year → confirm dialog → on confirm, banner updates and Aktiv-Badge moves.
4. Click Loeschen on a year with references → dialog confirms → API returns 409 → toast shows "wird noch von N Eintraegen verwendet".
5. Visit ?tab=options → A/B toggle reflects school.abWeekEnabled; status line varies by current run state; Info Banner always visible.
6. Toggle A/B → toast "Option gespeichert."; subsequent solver run picks up the new value (verified via downstream Plan 06 E2E).
</verification>

<success_criteria>
- [ ] SchoolYearsTab renders Aktiv-Badge, Info Banner, list of Cards with proper Row-Action gating
- [ ] CreateSchoolYearDialog has 5 fields with Zod validation and Aktivieren switch
- [ ] ActivateSchoolYearDialog uses verbatim UI-SPEC §5.5 copy
- [ ] DeleteSchoolYearDialog uses verbatim UI-SPEC §7.3 copy
- [ ] HolidaysList + AutonomousDaysList enable nested CRUD per year (or have reasoned fallback documented in SUMMARY)
- [ ] OptionsTab renders A/B toggle + dynamic status line + always-on Info Banner
- [ ] OptionsTab toast text matches "Option gespeichert." per UI-SPEC §6.3
- [ ] OptionsTab does NOT use useUpdateSchool (single inline mutation only)
- [ ] All Vitest specs in this plan pass; apps/web tsc --noEmit green
</success_criteria>

<output>
After completion, create `.planning/phases/10-schulstammdaten-zeitraster/10-05-SUMMARY.md` documenting:
- Whether Holiday/AutonomousDay endpoints existed or had to be added (Step A decision)
- The exact useHolidays/useAutonomousDays hook surface added to useSchoolYears.ts (or a separate file)
- Any drift between server SchoolYearDto shape (especially `name` and date fields format) and the assumed Plan 03 contract
- Confirmation that OptionsTab uses ONLY the inline updateAbMut mutation (no useUpdateSchool reference); the Vitest assertion for the literal toast string passes
</output>
</output>
