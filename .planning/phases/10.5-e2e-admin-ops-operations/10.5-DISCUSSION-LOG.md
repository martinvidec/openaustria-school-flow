# Phase 10.5: E2E Admin-Ops Operations — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-22
**Phase:** 10.5-e2e-admin-ops-operations
**Mode:** discuss
**Areas discussed:** Räume-CRUD scope gap, Solver publish UI gap, Solver async strategy, Import fixtures location, Real-solver budget, Aktivieren UI shape, Plan splitting

---

## Räume-CRUD scope gap

**Context surfaced before question:** No `/admin/rooms` CRUD page exists in `apps/web` — only the booking grid (`/rooms`). ROADMAP §Phase 10.5 calls for "Räume-CRUD inkl. Booking-Conflict". Same blocker pattern as Phase 10.4 People-CRUD (see 10.4-CONTEXT.md, 10.4-RESEARCH.md Blocker B1).

| Option | Description | Selected |
|--------|-------------|----------|
| Descope CRUD, keep Booking-Conflict | Mirror 10.4 descope pattern. 10.5 ships only Room-Booking E2E (happy + 409). ROADMAP success-criteria edit + deferred-items.md note. | ✓ |
| Pure-API Räume-CRUD E2E | Playwright APIRequestContext POST/GET/PUT/DELETE — no UI. Inconsistent with 10.4 "no API-only E2E" precedent. | |
| Build minimal admin Räume-CRUD UI in 10.5 | Add /admin/rooms page (analogous to admin/resources.tsx). Scope creep: UI work in an E2E-hardening phase. | |

**User's choice:** Descope CRUD, keep Booking-Conflict (Recommended)
**Notes:** Identical scope-realignment pattern as 10.4. Räume-CRUD-UI-E2E migrates to a future admin-rooms phase. ROADMAP edit + deferred-items.md required (D-26 / D-27).

---

## Solver publish UI gap

**Context surfaced before question:** Backend `POST /timetable/runs/:runId/activate` exists (timetable.controller.ts:121), but no Aktivieren button in `apps/web/src/routes/_authenticated/admin/solver.tsx`. ROADMAP §Phase 10.5 calls for "trigger → result → publish".

| Option | Description | Selected |
|--------|-------------|----------|
| Add minimal Aktivieren button to solver.tsx | ~30 LoC UI add. E2E exercises full trigger → complete → activate → toast → GET active-run flow. | ✓ |
| Test only trigger → result; defer publish to Phase 14 | Strip publish from 10.5 scope. ROADMAP success-criteria edit needed. | |
| Test publish via APIRequestContext | Hybrid UI+API spec — UI trigger, API activate. Avoids UI work but inconsistent with 10.4 precedent. | |

**User's choice:** Add minimal Aktivieren button to solver.tsx (Recommended)
**Notes:** Plan 04 will edit `solver.tsx` (the only Production-Code-Touch in 10.5). Spec exercises the full UI loop.

---

## Solver async strategy

**Context surfaced before question:** Real solver runs take minutes by default (default `maxSolveSeconds: 300` in StartSolveDto). Tests need bounded time + deterministic completion.

| Option | Description | Selected |
|--------|-------------|----------|
| Stub the Timefold sidecar via API mock | page.route() on POST /timetable/solve + injected solve:complete WS event. Bounded, deterministic. | |
| Real solver, tiny seed data | maxSolveSeconds=30, real Timefold sidecar, real WS broadcast. True integration coverage. CI flake risk if sidecar slow. | ✓ |
| Mock at NestJS level (separate test mode) | Env-toggled SolverStubService. Backend change; not pure E2E. | |

**User's choice:** Real solver, tiny seed data
**Notes:** User explicitly chose the non-recommended option. Trade-off accepted: full integration coverage vs. CI flake risk. Mitigations: (a) globalSetup health-check on Timefold sidecar (D-11), (b) maxSolveSeconds=30 (D-10), (c) Playwright test timeout 60s, (d) tiny seed data minimizes solve duration.

---

## Import fixtures location

**Context surfaced before question:** Three test fixtures needed — Untis-XML, valid CSV, malformed CSV. Where do they live?

| Option | Description | Selected |
|--------|-------------|----------|
| Committed files under apps/web/e2e/fixtures/imports/ | 3 small files, version-controlled, reusable. Mirrors orphan-year.ts fixture pattern. | ✓ |
| Generated inline in the spec (string literals → tmp file) | Each spec writes its fixture to os.tmpdir() before upload. No checked-in artifacts. | |
| Reuse API test fixtures | Symlink/import from apps/api/src/modules/import/__tests__/fixtures/. Couples web E2E to API test fixtures. | |

**User's choice:** Committed files under apps/web/e2e/fixtures/imports/ (Recommended)
**Notes:** Three files: untis-happy.xml (~20 lines), csv-happy.csv (5 rows), csv-malformed.csv (1 broken row). Plan 03 creates the directory + files (D-09a).

---

## Real-solver E2E timing budget (follow-up)

**Context:** Given "Real solver" choice, what's the maxSolveSeconds and how is sidecar liveness ensured?

| Option | Description | Selected |
|--------|-------------|----------|
| 30s + globalSetup health-checks sidecar | maxSolveSeconds=30, GET :8080/q/health in global-setup.ts with 5s timeout. Fail-fast on sidecar down. | ✓ |
| 60s + skip-on-sidecar-unavailable | maxSolveSeconds=60. test.skip() if sidecar unreachable. | |
| 120s, no skip | Generous budget; hard-fail if sidecar down. | |

**User's choice:** 30s + globalSetup health-checks sidecar (Recommended)
**Notes:** Health-check label `[E2E-PRECHECK] Timefold sidecar` mirrors API/Vite check pattern from 10.3-01.

---

## Aktivieren button visible behavior (follow-up)

**Context:** What happens after the user clicks Aktivieren?

| Option | Description | Selected |
|--------|-------------|----------|
| Button on Letztes Ergebnis card; success toast + button disables | No navigation. Mirrors SchoolYearCard activate UX. | |
| Aktivieren button + redirect to /timetable | After activation, navigate to /timetable so admin sees the published plan immediately. | ✓ |
| Confirm-dialog before activate | "Aktiven Stundenplan ersetzen?" ConfirmDialog before POST. | |

**User's choice:** Aktivieren button + redirect to /timetable
**Notes:** Spec asserts toaster + URL change + grid render on /timetable. Confirm-dialog explicitly deferred to Phase 14 (D-28).

---

## Plan splitting (follow-up)

**Context:** How many plans for Phase 10.5?

| Option | Description | Selected |
|--------|-------------|----------|
| 4 plans, one per swimlane | 10.5-01 Booking-Conflict, 10.5-02 Ressourcen, 10.5-03 Imports, 10.5-04 Solver+Aktivieren. Disjoint files_modified → parallel waves. | ✓ |
| 3 plans — bundle Aktivieren UI + spec separately from other specs | 10.5-01 spec-only-fixes, 10.5-02 Aktivieren UI + Solver spec, 10.5-03 regression gate. | |
| 5 plans — split Imports into 3 (Untis happy, CSV happy, CSV error) | Maximum granularity; heavy planning overhead. | |

**User's choice:** 4 plans, one per swimlane (Recommended)
**Notes:** Plan 04 is the only one that touches production code (solver.tsx D-12). Plans 01–03 are spec-only. Wave parallelization possible (D-22..D-24).

---

## Claude's Discretion

Captured in CONTEXT.md `<decisions>` section as CD-01..CD-06:
- CD-01: Toast-string extraction verbatim from hooks
- CD-02: WS-wait pattern (waitForResponse vs poll lastResult) — planner choice with `pattern-established` doc
- CD-03: Aktivieren button disabled-logic shape (useMutation vs local state)
- CD-04: Untis-XML fixture source (existing API fixture vs hand-rolled stub)
- CD-05: Solver-sidecar URL source resolution (env var vs config file)
- CD-06: Mobile-spec separation (separate file vs cross-project run)

## Deferred Ideas

Captured in CONTEXT.md `<deferred>` section. Highlights:
- Räume-CRUD UI + E2E → future admin-rooms phase
- Solver Aktivieren confirm-dialog → Phase 14
- Solver-Run-History / multi-school / score chart / constraint tuning → Phase 14
- Imports error-path variants beyond malformed row → future hardening tranche
- Imports Mobile-Project → Plan 03 decides based on Wizard responsiveness
- Solver-Stub-Mode for CI → only if real-solver flake emerges
- People-CRUD remains → Phase 11/12
