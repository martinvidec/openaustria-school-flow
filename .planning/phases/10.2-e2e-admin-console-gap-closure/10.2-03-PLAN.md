---
phase: 10.2-e2e-admin-console-gap-closure
plan: 03
type: execute
wave: 2
depends_on: [10.2-01]
files_modified:
  - apps/web/e2e/schuljahre.spec.ts
autonomous: true
requirements: []
tags: [e2e, playwright, schuljahre, tier-1]

must_haves:
  truths:
    - "Playwright spec covers Schuljahr edit (rename, change dates) Happy-Path"
    - "Playwright spec covers Schuljahr delete (non-orphan — no holidays/autonomous-days/lessons) Happy-Path"
    - "Playwright spec covers active-year switch between two coexisting years (multi-active migration confirms only one can be active)"
    - "All three new tests use the same seed data as SCHOOL-03/05 — no new fixtures needed"
  artifacts:
    - path: "apps/web/e2e/schuljahre.spec.ts"
      provides: "E2E coverage for Schuljahr edit, delete (non-orphan), activate-switch paths — complements existing SCHOOL-03 (create) and SCHOOL-05 (orphan-delete)"
  key_links:
    - from: "schuljahre.spec.ts"
      to: "PATCH /api/v1/schools/:schoolId/school-years/:yearId"
      via: "admin opens edit dialog, changes name/dates, clicks Speichern"
      pattern: "Schuljahr aktualisiert"
    - from: "schuljahre.spec.ts"
      to: "DELETE /api/v1/schools/:schoolId/school-years/:yearId"
      via: "admin clicks Loeschen on a year with no dependents"
      pattern: "Schuljahr geloescht"
    - from: "schuljahre.spec.ts"
      to: "POST /api/v1/schools/:schoolId/school-years/:yearId/activate"
      via: "admin clicks Als aktiv setzen on a non-active year"
      pattern: "ist aktiv seit"
---

<objective>
Extend Schuljahre E2E coverage beyond create (SCHOOL-03) and orphan-delete (SCHOOL-05). Three new tests lock down edit, delete (non-orphan), and active-switch — the remaining Schuljahre mutations that a real admin uses daily.
</objective>

<task number="1" name="Desktop E2E — Schuljahr edit, delete, active-switch">
<read_first>
  - apps/web/e2e/admin-school-settings.spec.ts SCHOOL-03 lines 132-146 and SCHOOL-05 lines 162-196 (pattern reference, especially the dialog and card navigation)
  - apps/web/e2e/helpers/login.ts (10.2-01 Task 1)
  - apps/web/e2e/fixtures/orphan-year.ts (do NOT reuse — seedOrphanYear creates a year WITH dependents; here we need a clean year to delete)
  - apps/web/src/components/admin/school-settings/SchoolYearsTab.tsx (find the edit/delete/activate button role names and dialog labels — e.g. Schuljahr bearbeiten, Loeschen, Als aktiv setzen)
  - apps/web/src/hooks/useSchoolYears.ts (mutation endpoints — confirm the toast copy and success message)
  - apps/api/src/modules/school/school-year.controller.ts (confirm routes and DTOs)
</read_first>

<action>
Create `apps/web/e2e/schuljahre.spec.ts` with describe `Phase 10.2 — Schuljahre edit/delete/activate (desktop)` and three tests:

1. **Test `YEAR-01: create a clean year, edit its name, assert toast + reload persists`:**
   - `await loginAsAdmin(page)`
   - `await page.goto('/admin/school/settings?tab=years')`
   - Create a new year via the dialog using the SCHOOL-03 pattern — name e.g. `E2E-EDIT-YEAR-{Date.now()}`, start/end/semesterBreak reasonable
   - Open the newly created year's edit affordance (role `button` with name matching `bearbeiten` scoped to the card)
   - Change the name to `E2E-EDIT-YEAR-RENAMED`
   - Click Speichern
   - `await expect(page.getByText(/Schuljahr aktualisiert/)).toBeVisible()`
   - Reload — assert the new name is still present
   - Cleanup: delete via Task 2 pattern so the test is idempotent

2. **Test `YEAR-02: delete a dependent-free year, assert success toast + year disappears`:**
   - `await loginAsAdmin(page)`
   - `await page.goto('/admin/school/settings?tab=years')`
   - Create a throwaway year `E2E-DELETE-YEAR-{Date.now()}` — crucially, do NOT add holidays or autonomous days
   - Click the delete button scoped to that year's card
   - Confirm inside the destructive dialog (same pattern as SCHOOL-05)
   - `await expect(page.getByText(/Schuljahr geloescht/)).toBeVisible()`
   - Assert the year label is gone: `await expect(page.getByText('E2E-DELETE-YEAR-')).not.toBeVisible({ timeout: 3000 })`

3. **Test `YEAR-03: switch active year between two coexisting years, assert info banner updates`:**
   - `await loginAsAdmin(page)`
   - `await page.goto('/admin/school/settings?tab=years')`
   - Create a year `E2E-SWITCH-A-{ts}` with `Als aktives Schuljahr setzen` enabled
   - Create a year `E2E-SWITCH-B-{ts}` WITHOUT active flag
   - Find B's card and click `Als aktiv setzen` (role button)
   - `await expect(page.getByText(/ist aktiv seit/)).toBeVisible()` near the `E2E-SWITCH-B` label
   - Assert only one year shows the `aktiv` indicator (query the active badge, assert count 1)
   - Cleanup: delete both throwaway years

4. If any test flakes because of shared state or dialog timing, add a `test.afterEach` that uses the Keycloak admin token + direct API calls (`request.delete`) to purge any residual E2E-* year rows. Reuse `getAdminToken` from the shared helper.
</action>

<acceptance_criteria>
  - `test -f apps/web/e2e/schuljahre.spec.ts`
  - `grep -c "test(" apps/web/e2e/schuljahre.spec.ts` returns 3 (YEAR-01, YEAR-02, YEAR-03)
  - `grep -q "YEAR-01\|YEAR-02\|YEAR-03" apps/web/e2e/schuljahre.spec.ts`
  - `grep -q "Als aktiv setzen\|Als aktives Schuljahr" apps/web/e2e/schuljahre.spec.ts` (verifies the active-switch affordance is exercised)
  - `grep -q "Schuljahr geloescht" apps/web/e2e/schuljahre.spec.ts` (verifies the delete success-toast assertion exists)
  - `pnpm --filter web exec playwright test --project=desktop --grep YEAR` passes all 3 tests against a running dev stack
  - No new entries in the Schuljahre list after the full test run (cleanup works)
</acceptance_criteria>
</task>

<verification>
- `ls apps/web/e2e/schuljahre.spec.ts`
- `pnpm --filter web exec playwright test --grep "SCHOOL-03|SCHOOL-05|YEAR-"` runs 5 tests green (existing 2 + new 3, no regressions)
</verification>
