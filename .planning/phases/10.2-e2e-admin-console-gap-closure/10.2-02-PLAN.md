---
phase: 10.2-e2e-admin-console-gap-closure
plan: 02
type: execute
wave: 2
depends_on: [10.2-01]
files_modified:
  - apps/web/e2e/wochentage.spec.ts
  - apps/web/src/components/admin/school-settings/TimeGridTab.tsx
  - .planning/phases/10.2-e2e-admin-console-gap-closure/wochentage-decision.md
autonomous: true
requirements: []
tags: [e2e, playwright, wochentage, ux, tier-1]

must_haves:
  truths:
    - "Wochentage-UX gap from Phase 10.1 UAT is resolved — either via an inline label/hint improvement or documented as an intentional backlog item with justification"
    - "Playwright spec wochentage.spec.ts covers at minimum: admin sees Mo-Fr toggles pre-activated, admin can toggle Sa/So active, save persists, reload still shows state"
    - "Decision memo wochentage-decision.md commits the rationale (what was found in the UI, why the user said it was unclear, what the fix was or why it is deferred)"
  artifacts:
    - path: "apps/web/e2e/wochentage.spec.ts"
      provides: "E2E coverage for the Unterrichtstage (school days) toggle surface on the Zeitraster tab"
    - path: ".planning/phases/10.2-e2e-admin-console-gap-closure/wochentage-decision.md"
      provides: "Markdown memo: observed state, root-cause hypothesis for UAT feedback, fix-or-defer decision with rationale"
  key_links:
    - from: "wochentage.spec.ts"
      to: "schoolDays PUT round-trip"
      via: "admin toggles SATURDAY, clicks Save, reloads, asserts SATURDAY still active"
      pattern: "Unterrichtstag Sa"
---

<objective>
Close the Wochentage UX gap surfaced in Phase 10.1 UAT ("nicht erkennbar wie Wochentage angelegt/verwendet werden"). Investigate the current TimeGridTab render (the `SCHOOL-02` spec already asserts Mo-Fr toggles exist, so the affordance IS present — the gap is discoverability, not absence). Either improve the label/copy OR document why the current UX is sufficient, then lock behavior with a Playwright spec.
</objective>

<task number="1" name="Investigate current Wochentage UX and decide fix-or-defer">
<read_first>
  - apps/web/src/components/admin/school-settings/TimeGridTab.tsx (full file — find the Unterrichtstage/school-days toggle group)
  - apps/web/e2e/admin-school-settings.spec.ts lines 118-130 (SCHOOL-02 asserts Mo-Fr toggles visible)
  - .planning/phases/10-schulstammdaten-zeitraster/10-UI-SPEC.md §3.3 if it mentions school days
  - /Users/vid/.claude/projects/-Users-vid-Documents-GitHub-agentic-research-openaustria-school-flow/memory/project_phase10_new_findings.md (user's verbatim feedback — "nicht erkennbar wie Wochentage angelegt/verwendet werden")
</read_first>

<action>
1. Read the TimeGridTab render. Locate the school-days toggle group (probably under a heading `Unterrichtstage` or similar). Note:
   - What heading/label/hint text currently exists above the toggle group
   - Whether Sa/So toggles are visible by default or hidden
   - Whether there is ANY explanation of what the toggle does
   - What happens on toggle (optimistic? requires Save click?)
2. Form a hypothesis for why the user said "nicht erkennbar":
   - (a) Toggles are present but unlabeled/unexplained → **FIX**: add descriptive label + short hint
   - (b) Sa/So hidden behind something → **FIX**: always show all 7, grey out inactive
   - (c) Toggles work but save-flow is unclear (toggle changes local state only; user does not realize Save click is needed) → **FIX**: show "ungespeicherte Aenderungen" indicator or auto-save
   - (d) Current UX is fine, user just missed it → **DEFER**: backlog entry only
3. Write `.planning/phases/10.2-e2e-admin-console-gap-closure/wochentage-decision.md` with:
   - Observed state (heading, toggles, labels verbatim)
   - Hypothesis matched + brief user-experience reasoning
   - Decision: FIX or DEFER
   - If FIX: scope the fix in 1-3 bullets
   - If DEFER: add to backlog with `/gsd:plant-seed` or inline in the decision doc
4. If the decision is FIX, apply the UX improvement to `TimeGridTab.tsx` in the same commit as the decision doc. Keep it small (label + hint + a11y label — no new components, no new routes). Commit message: `feat(10.2-02): clarify Wochentage UX per 10.1 UAT feedback`.
5. If the decision is DEFER, commit the decision doc alone with `docs(10.2-02): document Wochentage UX decision — deferred to v1.2`.
</action>

<acceptance_criteria>
  - `test -f .planning/phases/10.2-e2e-admin-console-gap-closure/wochentage-decision.md`
  - The decision doc contains ALL of: `## Observed State`, `## Hypothesis`, `## Decision`, `## Rationale` section headings
  - Decision doc is committed
  - If FIX: `git log --oneline | grep -q "feat(10.2-02): clarify Wochentage UX"` AND `grep -q "Unterrichtstage" apps/web/src/components/admin/school-settings/TimeGridTab.tsx` (heading still present) AND the file diff shows a new label, hint, or a11y-attribute — NOT a component rewrite
  - If DEFER: `grep -q "DEFERRED\|deferred" .planning/phases/10.2-e2e-admin-console-gap-closure/wochentage-decision.md`
</acceptance_criteria>
</task>

<task number="2" name="Playwright spec for Wochentage toggle save round-trip">
<read_first>
  - apps/web/e2e/helpers/login.ts (from 10.2-01 Task 1)
  - apps/web/e2e/zeitraster.spec.ts (from 10.2-01 Task 2 — reuse the tab-navigation pattern)
  - Task 1 wochentage-decision.md (from this plan)
  - apps/api/src/modules/school/school-time-grid.service.ts lines 63-71 (schoolDays persistence logic)
</read_first>

<action>
Create `apps/web/e2e/wochentage.spec.ts` with one describe `Phase 10.2 — Wochentage toggle (desktop)` and one test:

1. **Test `WOCH-01: toggle Saturday active, save, reload, assert persistence`:**
   - `await loginAsAdmin(page)`
   - `await page.goto('/admin/school/settings?tab=timegrid')`
   - Assert all 5 weekday toggles Mo-Fr are visible: `for (const day of ['Mo', 'Di', 'Mi', 'Do', 'Fr']) { await expect(page.getByRole('button', { name: new RegExp(\`Unterrichtstag \${day}\`) })).toBeVisible() }`
   - Check that the Saturday toggle exists: `const saButton = page.getByRole('button', { name: /Unterrichtstag Sa/ })`
   - Record initial state: `const initiallyActive = (await saButton.getAttribute('aria-pressed')) === 'true'`
   - Click Sa → click Save → expect `Aenderungen gespeichert` toast
   - Reload page
   - Navigate back to Zeitraster tab (`?tab=timegrid`)
   - Assert Sa is now in the opposite state of initial: `expect((await saButton.getAttribute('aria-pressed')) === 'true').toBe(!initiallyActive)`
   - Cleanup: toggle Sa back to original state + Save (idempotent)
</action>

<acceptance_criteria>
  - `test -f apps/web/e2e/wochentage.spec.ts`
  - `grep -q "WOCH-01" apps/web/e2e/wochentage.spec.ts`
  - `grep -q "Unterrichtstag Sa" apps/web/e2e/wochentage.spec.ts`
  - `grep -q "aria-pressed" apps/web/e2e/wochentage.spec.ts`
  - `pnpm --filter web exec playwright test --project=desktop --grep WOCH-01` passes against a running dev stack
  - If task 1's decision was FIX and the fix changed the toggle's accessible name: update the regex/query in this spec accordingly and note it in the decision memo
</acceptance_criteria>
</task>

<verification>
- `ls apps/web/e2e/` includes `wochentage.spec.ts`
- `.planning/phases/10.2-e2e-admin-console-gap-closure/wochentage-decision.md` committed with clear FIX/DEFER decision
- `pnpm --filter web exec playwright test --grep "WOCH"` green
- If FIX landed: TimeGridTab diff is ≤20 lines; no new components; no new routes
</verification>
