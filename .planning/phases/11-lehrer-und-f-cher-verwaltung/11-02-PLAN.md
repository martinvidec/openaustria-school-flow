---
phase: 11
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - packages/shared/src/schemas/subject.schema.ts
  - packages/shared/src/schemas/subject.schema.spec.ts
  - packages/shared/src/constants/school-types.ts
  - packages/shared/src/stundentafel/index.ts
  - packages/shared/src/stundentafel/austrian-stundentafeln.ts
  - packages/shared/src/stundentafel/austrian-stundentafeln.spec.ts
  - packages/shared/src/index.ts
  - apps/api/src/modules/subject/subject.service.ts
  - apps/api/src/modules/subject/subject.service.spec.ts
  - apps/api/src/modules/subject/templates/austrian-stundentafeln.ts
  - apps/web/src/hooks/useSubjects.ts
  - apps/web/src/components/layout/AppSidebar.tsx
  - apps/web/src/components/layout/MobileSidebar.tsx
  - apps/web/src/routes/_authenticated/admin/subjects.index.tsx
  - apps/web/src/components/admin/subject/SubjectTable.tsx
  - apps/web/src/components/admin/subject/SubjectMobileCards.tsx
  - apps/web/src/components/admin/subject/SubjectFormDialog.tsx
  - apps/web/src/components/admin/subject/SubjectFormDialog.test.tsx
  - apps/web/src/components/admin/subject/DeleteSubjectDialog.tsx
  - apps/web/src/components/admin/subject/SubjectAffectedEntitiesDialog.tsx
  - apps/web/src/components/admin/subject/StundentafelVorlagenSection.tsx
  - apps/web/src/components/admin/subject/StundentafelVorlagenSection.test.tsx
autonomous: true
requirements: [SUBJECT-01, SUBJECT-02, SUBJECT-03, SUBJECT-05]
requirements_addressed: [SUBJECT-01, SUBJECT-02, SUBJECT-03, SUBJECT-05]
tags: [subject, stundentafel, orphan-guard, shared-templates, phase-11]

must_haves:
  truths:
    - "Admin sees /admin/subjects dense table (desktop) + mobile cards with search (Name oder Kürzel); empty-state hero 'Noch keine Fächer angelegt' + inline CTA 'Erstes Fach anlegen' (SUBJECT-01, D-09)"
    - "Admin clicks + Fach anlegen or row-click to open SubjectFormDialog with Name + Kürzel fields ONLY; Kürzel auto-transforms to uppercase on blur (SUBJECT-02)"
    - "SubjectFormDialog renders 2 input fields: Name + Kürzel; Farbe field is NOT in the DOM"
    - "Color swatch in table uses getSubjectColor(id) deterministic hash-to-SUBJECT_PALETTE mapping (existing v1.0 behavior, unchanged) — no schema migration needed"
    - "Fach Create/Edit returns 409 ConflictException with problem+json when [schoolId, shortName] Kürzel is already taken; UI shows inline error 'Dieses Kürzel ist bereits vergeben.' on the Kürzel field (Kürzel uniqueness — implementation constraint, not a REQ-ID)"
    - "Stundentafel-Vorlagen section renders under Fach-Liste with shadcn Tabs per Schultyp; each tab shows read-only Table (Fach | Kürzel | Jg. 1-4 Wochenstunden) from packages/shared/src/stundentafel/ (SUBJECT-03, D-10)"
    - "SubjectService.remove throws 409 ConflictException with RFC 9457 extensions.affectedEntities={classSubjects, teacherSubjects, timetableLessons, homework, exams} when ANY reference exists (SUBJECT-05, D-12, D-14)"
    - "DeleteSubjectDialog reuses AffectedEntitiesList from Plan 11-01 (new group labels: Fachzuordnungen, Klassen-Fach-Zuordnungen, Stundenplan-Einträge, Hausübungen, Prüfungen); classSubjects/timetableLessons/homework/exams deep-links disabled with 'Verfügbar ab Phase 12' tooltip"
    - "D-12 affected-entity deep-links: only affectedTeachers is live in Phase 11; classSubjects / timetableLessons / homework / exams entries render with disabled 'Verfügbar ab Phase 12' tooltip."
    - "Nutzung-count cell in SubjectTable is clickable, opens SubjectAffectedEntitiesDialog (non-destructive informational variant with Info icon, neutral framing, Schließen-only footer)"
    - "AppSidebar + MobileSidebar 'Personal & Fächer' group gains a second entry: Fächer → /admin/subjects with BookOpen icon (D-03, append to 11-01 scaffold)"
    - "packages/shared/src/stundentafel/ hosts AUSTRIAN_STUNDENTAFELN array (moved from apps/api) + StundentafelTemplate type; apps/api/src/modules/subject/templates/austrian-stundentafeln.ts becomes pure re-export from @schoolflow/shared"
    - "packages/shared/src/constants/school-types.ts exports SCHOOL_TYPES_LABELS map (7 Schultypen with deutsche Anzeigenamen) used by Stundentafel-Vorlagen tab labels — referenced in 11-UI-SPEC §3.4.1 but does not yet exist (Open Planner Note 4)"
    - "All useSubjects mutations wire explicit onError → red toast; silent-4xx invariant preserved (Phase 10.1-01 lock)"
    - "Wave 0 TDD stubs exist in SubjectFormDialog.test.tsx + StundentafelVorlagenSection.test.tsx + schema specs BEFORE implementation"

  rationale:
    - "Farbe descoped per REQUIREMENTS.md SUBJECT-02 update + 11-CONTEXT.md D-11 rollback (2026-04-22). Subject schema has NO color columns; colors remain auto-derived via existing v1.0 getSubjectColor(id)."
    - "Kürzel-uniqueness is an implementation constraint (existing [schoolId, shortName] @@unique Prisma index + inline error UX), NOT a standalone REQ-ID. REQUIREMENTS.md has no 'Kürzel uniqueness' requirement."
    - "SUBJECT-05 canonical semantics: 'Admin kann ungenutzte Fächer löschen (Orphan-Schutz gegen Fächer mit Zuordnungen)' — this plan's Orphan-Guard delete delivers SUBJECT-05 in full."
    - "SUBJECT-04 (Wochenstunden pro Fach pro Klassenstufe anpassen) is NOT in Phase 11 scope — moved to Phase 12 per ROADMAP update 2026-04-22, because ClassSubject junction model lives there."

  artifacts:
    - path: "packages/shared/src/schemas/subject.schema.ts"
      provides: "SubjectCreateSchema + SubjectUpdateSchema Zod (Name + Kürzel only per post-research descope)"
      contains: "SubjectCreateSchema"
    - path: "packages/shared/src/constants/school-types.ts"
      provides: "SCHOOL_TYPES_LABELS Record<SchoolType, string> with 7 deutsche Anzeigenamen (VS/NMS/AHS/BHS/BMS/PTS/ASO)"
      contains: "SCHOOL_TYPES_LABELS"
    - path: "packages/shared/src/stundentafel/austrian-stundentafeln.ts"
      provides: "Moved AUSTRIAN_STUNDENTAFELN + StundentafelTemplate type from apps/api; single source of truth"
      contains: "AUSTRIAN_STUNDENTAFELN"
    - path: "apps/api/src/modules/subject/subject.service.ts"
      provides: "Orphan-Guard in .remove via $transaction count queries; returns ConflictException with extensions.affectedEntities (SUBJECT-05)"
      contains: "ConflictException"
    - path: "apps/web/src/routes/_authenticated/admin/subjects.index.tsx"
      provides: "/admin/subjects route with PageShell + Fach-Table + Stundentafel-Vorlagen section"
      contains: "Stundentafel-Vorlagen"
    - path: "apps/web/src/components/admin/subject/SubjectFormDialog.tsx"
      provides: "Create + Edit dialog with Name + Kürzel fields, Kürzel uppercase auto-transform, uniqueness 409 inline error"
      contains: "Dieses Kürzel ist bereits vergeben"
    - path: "apps/web/src/components/admin/subject/StundentafelVorlagenSection.tsx"
      provides: "Read-only Tabs per Schultyp with Stundentafel table (Fach | Kürzel | Jg. 1-4)"
      contains: "Stundentafel-Vorlagen"
    - path: "apps/web/src/components/admin/subject/DeleteSubjectDialog.tsx"
      provides: "Happy + 409-blocked state; reuses AffectedEntitiesList from teacher folder (SUBJECT-05 orphan-safe delete)"
      contains: "Fach kann nicht gelöscht werden"
  key_links:
    - from: "apps/web/src/components/admin/subject/StundentafelVorlagenSection.tsx"
      to: "packages/shared/src/stundentafel/austrian-stundentafeln.ts"
      via: "AUSTRIAN_STUNDENTAFELN imported from @schoolflow/shared"
      pattern: "from ['\"]@schoolflow/shared['\"]"
    - from: "apps/api/src/modules/subject/subject.service.ts"
      to: "apps/web/src/lib/api.ts parseProblemJson"
      via: "RFC 9457 ConflictException with extensions.affectedEntities — consumed by DeleteSubjectDialog (SUBJECT-05)"
      pattern: "affectedEntities"
    - from: "apps/web/src/components/admin/subject/DeleteSubjectDialog.tsx"
      to: "apps/web/src/components/admin/teacher/AffectedEntitiesList.tsx"
      via: "Reuse AffectedEntitiesList component authored in Plan 11-01 Task 3"
      pattern: "AffectedEntitiesList"
    - from: "apps/web/src/components/layout/AppSidebar.tsx"
      to: "/admin/subjects"
      via: "Appended Fächer entry in 'Personal & Fächer' group (BookOpen icon)"
      pattern: "BookOpen"
---

<objective>
Ship the complete Fächer admin surface (SUBJECT-01, SUBJECT-02, SUBJECT-03, SUBJECT-05) + Stundentafel-Vorlagen read-out + SubjectService.remove Orphan-Guard gap-fix + shared move of AUSTRIAN_STUNDENTAFELN.

Per CONTEXT.md D-16 USER-OVERRIDE this is Plan 2 of the 3-plan bundle. Depends on Plan 11-01 (sidebar scaffold, AffectedEntitiesList, parseProblemJson extensions contract).

**Scope clarification (2026-04-22 revision):**
- SUBJECT-04 (Wochenstunden pro Fach pro Klassenstufe anpassen) is OUT of Phase 11 — moved to Phase 12 per ROADMAP update, because the ClassSubject junction model belongs to that phase.
- SUBJECT-05 (canonical: "Admin kann ungenutzte Fächer löschen / Orphan-Schutz") is the Orphan-Guard delete delivered here.
- Kürzel-uniqueness (existing `@@unique([schoolId, shortName])` + inline UI error) is an **implementation constraint**, NOT a REQ-ID — REQUIREMENTS.md does not contain a standalone uniqueness requirement.

**Post-research descope note (D-11 rolled back + A4 schoolType-mapping rolled back, 2026-04-22):** Subject schema has NO color columns and NO schoolType junction in v1.0 — the user descoped schema changes for Phase 11. Consequence: Free-Hex-Picker and Schultyp multi-select are both REMOVED from SubjectFormDialog. Colors remain auto-derived via existing v1.0 `getSubjectColor(id)` hash-to-SUBJECT_PALETTE mapping. Dialog ships with only Name + Kürzel fields + an information note (11-UI-SPEC §3.3 post-research block, lines 554-590).

**Purpose:** Close SUBJECT-01, SUBJECT-02, SUBJECT-03, SUBJECT-05 with production UI, fix silent-zombification defect in SubjectService.remove (cascades through ClassSubject → Homework + Exam per 11-RESEARCH Focus 4), and consolidate AUSTRIAN_STUNDENTAFELN into @schoolflow/shared so UI reads from the same source as the backend apply-template service.

**Output:**
- SHARED: Subject Zod schema (Name + Kürzel), SCHOOL_TYPES_LABELS constant map, AUSTRIAN_STUNDENTAFELN moved from apps/api
- API: SubjectService.remove Orphan-Guard + unit tests; apps/api templates file becomes pure re-export
- WEB: /admin/subjects route, Fach-Table + Mobile-Cards + FormDialog + DeleteDialog (with Orphan-Guard blocked state) + Stundentafel-Vorlagen section; AppSidebar append of Fächer entry

**Wave:** 2 (depends on 11-01 for sidebar scaffold, AffectedEntitiesList component, and parseProblemJson.extensions contract).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-lehrer-und-f-cher-verwaltung/11-CONTEXT.md
@.planning/phases/11-lehrer-und-f-cher-verwaltung/11-RESEARCH.md
@.planning/phases/11-lehrer-und-f-cher-verwaltung/11-UI-SPEC.md
@.planning/phases/11-lehrer-und-f-cher-verwaltung/11-VALIDATION.md
@.planning/phases/11-lehrer-und-f-cher-verwaltung/11-01-PLAN.md

# Reference code
@apps/api/src/modules/subject/subject.service.ts
@apps/api/src/modules/subject/subject.service.spec.ts
@apps/api/src/modules/subject/subject.controller.ts
@apps/api/src/modules/subject/dto/create-subject.dto.ts
@apps/api/src/modules/subject/dto/update-subject.dto.ts
@apps/api/src/modules/subject/templates/austrian-stundentafeln.ts
@apps/api/src/modules/subject/stundentafel-template.service.ts
@apps/api/prisma/schema.prisma
@packages/shared/src/schemas/school.schema.ts
@packages/shared/src/types/timetable.ts
@packages/shared/src/index.ts

<interfaces>
Prisma models (apps/api/prisma/schema.prisma lines 509-580):
```prisma
model Subject {
  id                       String      @id @default(uuid())
  schoolId                 String
  name                     String
  shortName                String
  subjectType              SubjectType   // PFLICHT | WAHLPFLICHT | FREIGEGENSTAND | UNVERBINDLICH
  lehrverpflichtungsgruppe String?
  werteinheitenFactor      Float?
  classSubjects            ClassSubject[]    // cascade
  teacherSubjects          TeacherSubject[]  // cascade
  @@unique([schoolId, shortName])
}
```
NOTE: NO colorBg/colorText/colorIndex columns. NO schoolType multi-select. These are deferred per 11-UI-SPEC post-research descope.

Subject dependents (Orphan-Guard must check — SUBJECT-05):
- ClassSubject.subjectId (Cascade — silent, DROPS Homework + Exam children)
- TeacherSubject.subjectId (Cascade — benign)
- TimetableLesson via ClassSubject.subjectId (indirect silent cascade)
- Homework via ClassSubject (indirect cascade)
- Exam via ClassSubject (NO ACTION declared at line 1382 — would throw DB-level error; must block via Orphan-Guard)

Current SubjectService.remove (apps/api/src/modules/subject/subject.service.ts:104-107):
```ts
async remove(id: string) {
  await this.findOne(id); // throws 404
  return this.prisma.subject.delete({ where: { id } });
}
```
NEW contract: MUST count dependents + findMany for affectedClasses/affectedTeachers (cap 50) first; if totalRefs > 0 throw ConflictException with RFC 9457 payload.

Existing apps/api templates (apps/api/src/modules/subject/templates/austrian-stundentafeln.ts):
```ts
export interface StundentafelTemplate {
  schoolType: string;
  yearLevel: number;
  displayName: string;
  subjects: Array<{name; shortName; subjectType; weeklyHours; lehrverpflichtungsgruppe}>;
  totalWeeklyHours: number;
}
export const AUSTRIAN_STUNDENTAFELN: StundentafelTemplate[] = [...]
```
This is consumed by `stundentafel-template.service.ts` and must keep working after move — via pure re-export from shared.

Shared existing (packages/shared/src/):
- `SCHOOL_TYPES` = ['VS','NMS','AHS','BHS','BMS','PTS','ASO'] (schemas/school.schema.ts line 7-15)
- `SUBJECT_PALETTE` + `getSubjectColor(id)` (types/timetable.ts line 95-121) — UNCHANGED in Phase 11
- NO `SCHOOL_TYPES_LABELS` exists yet — must be authored in this plan

Plan 11-01 contracts this plan depends on:
- AppSidebar groups refactor with `group: 'personalAndFaecher'` (Plan 11-01 Task 3 delivery)
- `AffectedEntitiesList` component at `apps/web/src/components/admin/teacher/AffectedEntitiesList.tsx` (Plan 11-01 Task 3 delivery) — this plan imports it directly
- `parseProblemJson` in `apps/web/src/lib/api.ts` returns `{ ..., extensions? }` (Plan 11-01 Task 3 extension if needed)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared move of AUSTRIAN_STUNDENTAFELN + SCHOOL_TYPES_LABELS + Subject Zod schema + Wave-0 test stubs</name>
  <files>packages/shared/src/stundentafel/austrian-stundentafeln.ts, packages/shared/src/stundentafel/austrian-stundentafeln.spec.ts, packages/shared/src/stundentafel/index.ts, packages/shared/src/constants/school-types.ts, packages/shared/src/schemas/subject.schema.ts, packages/shared/src/schemas/subject.schema.spec.ts, packages/shared/src/index.ts, apps/api/src/modules/subject/templates/austrian-stundentafeln.ts, apps/web/src/components/admin/subject/SubjectFormDialog.test.tsx, apps/web/src/components/admin/subject/StundentafelVorlagenSection.test.tsx, apps/api/src/modules/subject/subject.service.spec.ts</files>

  <read_first>
    - apps/api/src/modules/subject/templates/austrian-stundentafeln.ts (full file — this is what moves to shared VERBATIM)
    - apps/api/src/modules/subject/stundentafel-template.service.ts (verify it imports from templates/ path that will become re-export)
    - apps/api/src/modules/subject/dto/apply-stundentafel.dto.ts (verify SchoolType literals usage — out of scope but sanity-check no break)
    - packages/shared/src/schemas/school.schema.ts (SCHOOL_TYPES const array, Zod pattern)
    - packages/shared/src/index.ts (current re-exports — append new barrels)
    - apps/api/src/modules/subject/subject.service.spec.ts (existing unit-test structure — extend pattern)
    - apps/api/src/modules/subject/subject.service.ts (Kürzel-uniqueness error copy: "Ein Fach mit dem Kuerzel ... existiert bereits ...")
    - .planning/phases/11-lehrer-und-f-cher-verwaltung/11-VALIDATION.md (Wave-0 file list for Plan 11-02)
    - .planning/phases/11-lehrer-und-f-cher-verwaltung/11-UI-SPEC.md §3.3 post-research block (lines 554-590) + §3.4 stundentafel section
  </read_first>

  <action>
1. **Move AUSTRIAN_STUNDENTAFELN to @schoolflow/shared**:
   - Create `packages/shared/src/stundentafel/austrian-stundentafeln.ts` — copy full contents of `apps/api/src/modules/subject/templates/austrian-stundentafeln.ts` VERBATIM (same interface, same array, no modifications).
   - Create `packages/shared/src/stundentafel/index.ts`: `export * from './austrian-stundentafeln.js';`
   - Rewrite `apps/api/src/modules/subject/templates/austrian-stundentafeln.ts` to pure re-export: `export { AUSTRIAN_STUNDENTAFELN, type StundentafelTemplate } from '@schoolflow/shared';` — preserves existing import paths in `stundentafel-template.service.ts`.
   - Append to `packages/shared/src/index.ts`: `export * from './stundentafel/index.js';`

2. **Port stundentafel spec** — create `packages/shared/src/stundentafel/austrian-stundentafeln.spec.ts`:
   ```ts
   import { describe, it, expect } from 'vitest';
   import { AUSTRIAN_STUNDENTAFELN, type StundentafelTemplate } from './austrian-stundentafeln.js';

   describe('AUSTRIAN_STUNDENTAFELN', () => {
     it('contains templates for AHS_UNTER years 1-4', () => {
       const ahsYears = AUSTRIAN_STUNDENTAFELN
         .filter(t => t.schoolType === 'AHS_UNTER')
         .map(t => t.yearLevel)
         .sort();
       expect(ahsYears).toContain(1);
       expect(ahsYears).toContain(4);
     });
     it('each template totalWeeklyHours equals sum of subject weeklyHours', () => {
       for (const t of AUSTRIAN_STUNDENTAFELN) {
         const sum = t.subjects.reduce((a, s) => a + s.weeklyHours, 0);
         expect(sum).toBe(t.totalWeeklyHours);
       }
     });
     it('each subject has a non-empty shortName and a valid lehrverpflichtungsgruppe', () => {
       const VALID_LVG = ['I','II','III','IV','IVa','IVb','V','Va','VI'];
       for (const t of AUSTRIAN_STUNDENTAFELN) {
         for (const s of t.subjects) {
           expect(s.shortName.length).toBeGreaterThan(0);
           expect(VALID_LVG).toContain(s.lehrverpflichtungsgruppe);
         }
       }
     });
   });
   ```

3. **Author `packages/shared/src/constants/school-types.ts`** (referenced by 11-UI-SPEC §3.4.1 but not yet existing — Open Planner Note 4):
   ```ts
   import type { SchoolType } from '../schemas/school.schema.js';
   /**
    * Deutsche Anzeigenamen für die 7 österreichischen Schultypen.
    * Quelle: UI-SPEC §3.4.1 (Phase 11) + 11-CONTEXT.md D-10.
    */
   export const SCHOOL_TYPES_LABELS: Record<SchoolType, string> = {
     VS: 'Volksschule',
     NMS: 'Mittelschule',
     AHS: 'Allgemeinbildende Höhere Schule',
     BHS: 'Berufsbildende Höhere Schule',
     BMS: 'Berufsbildende Mittlere Schule',
     PTS: 'Polytechnische Schule',
     ASO: 'Allgemeine Sonderschule',
   };
   ```
   Append to `packages/shared/src/index.ts`: `export * from './constants/school-types.js';`

4. **Author `packages/shared/src/schemas/subject.schema.ts`** (Name + Kürzel only per post-research descope):
   ```ts
   import { z } from 'zod';
   export const SubjectTypeEnum = z.enum(['PFLICHT','WAHLPFLICHT','FREIGEGENSTAND','UNVERBINDLICH']);
   export const SubjectCreateSchema = z.object({
     schoolId: z.string().uuid(),
     name: z.string().min(1, 'Pflichtfeld').max(64),
     shortName: z.string().min(1, 'Pflichtfeld').max(8)
       .transform(s => s.toUpperCase()),  // Auto-uppercase on parse (mirrors UI onBlur transform)
     subjectType: SubjectTypeEnum.default('PFLICHT'),
     lehrverpflichtungsgruppe: z.string().optional(),
     werteinheitenFactor: z.number().optional(),
   });
   export const SubjectUpdateSchema = SubjectCreateSchema.partial();
   export type SubjectCreateInput = z.infer<typeof SubjectCreateSchema>;
   export type SubjectUpdateInput = z.infer<typeof SubjectUpdateSchema>;
   ```
   Append to `packages/shared/src/index.ts`: `export * from './schemas/subject.schema.js';`

5. **Author Subject Zod spec** (`subject.schema.spec.ts`) — ≥5 cases: happy path, Name empty → 'Pflichtfeld', shortName auto-uppercase transform works (`input 'm' → output 'M'`), shortName max 8 chars, subjectType enum default PFLICHT.

6. **Wave 0 TDD stubs**:
   - `apps/web/src/components/admin/subject/SubjectFormDialog.test.tsx`:
     - `it.todo('renders Name + Kürzel fields and hides Farbe field (post-research descope)')`
     - `it.todo('auto-transforms Kürzel to uppercase on blur')`
     - `it.todo('shows inline "Dieses Kürzel ist bereits vergeben." error on 409 response')`
     - `it.todo('shows color-palette info note referring to future phase')`
     - `it.todo('disables Speichern until Zod-valid')`
   - `apps/web/src/components/admin/subject/StundentafelVorlagenSection.test.tsx`:
     - `it.todo('renders one Tab per Schultyp using SCHOOL_TYPES_LABELS for labels')`
     - `it.todo('reads AUSTRIAN_STUNDENTAFELN from @schoolflow/shared (not apps/api path)')`
     - `it.todo('renders Table with columns Fach | Kürzel | Jg. 1 | Jg. 2 | Jg. 3 | Jg. 4 and footer totals')`
     - `it.todo('footer shows "Wochenstunden gesamt pro Jahrgang: {a} · {b} · {c} · {d}"')`
     - `it.todo('table is read-only (no inputs, no action buttons)')`

7. **Extend `apps/api/src/modules/subject/subject.service.spec.ts`** with new `describe('remove — Orphan-Guard (SUBJECT-05)')` containing stubs (implemented in Task 2):
   - `it.todo('deletes subject when zero dependents')`
   - `it.todo('throws ConflictException when ClassSubject references subject')`
   - `it.todo('throws ConflictException when TeacherSubject references subject')`
   - `it.todo('throws ConflictException when TimetableLesson references via ClassSubject')`
   - `it.todo('throws ConflictException when Homework references via ClassSubject')`
   - `it.todo('throws ConflictException when Exam references via ClassSubject')`
   - `it.todo('payload contains extensions.affectedEntities.{affectedClasses, affectedTeachers, lessonCount, homeworkCount, examCount}')`
   - `it.todo('affectedClasses + affectedTeachers arrays capped at 50 entries each')`
  </action>

  <acceptance_criteria>
    - `packages/shared/src/stundentafel/austrian-stundentafeln.ts` exports `AUSTRIAN_STUNDENTAFELN` AND `StundentafelTemplate` (grep)
    - `apps/api/src/modules/subject/templates/austrian-stundentafeln.ts` contains `from '@schoolflow/shared'` (pure re-export, grep)
    - `packages/shared/src/constants/school-types.ts` exports `SCHOOL_TYPES_LABELS` with all 7 keys (VS/NMS/AHS/BHS/BMS/PTS/ASO) and deutsche Werte (grep each key)
    - `packages/shared/src/schemas/subject.schema.ts` exports `SubjectCreateSchema` with `.transform(s => s.toUpperCase())` on shortName (grep)
    - `packages/shared/src/index.ts` contains `export * from './stundentafel/index.js'` AND `export * from './constants/school-types.js'` AND `export * from './schemas/subject.schema.js'` (grep all three)
    - `SubjectFormDialog.test.tsx` contains ≥5 `it.todo` (grep count)
    - `StundentafelVorlagenSection.test.tsx` contains ≥5 `it.todo` (grep count)
    - `subject.service.spec.ts` contains new `describe('remove — Orphan-Guard (SUBJECT-05)'` with ≥8 `it.todo` (grep)
    - `pnpm --filter @schoolflow/shared test:run -- stundentafel` green
    - `pnpm --filter @schoolflow/shared test:run -- subject` green
    - `pnpm --filter @schoolflow/api test:run -- subject.service` green (no regression from re-export)
    - `pnpm -r build` green
  </acceptance_criteria>

  <verify>
    <automated>pnpm --filter @schoolflow/shared test:run &amp;&amp; pnpm --filter @schoolflow/api test:run -- subject.service &amp;&amp; pnpm -r build</automated>
  </verify>

  <done>
    AUSTRIAN_STUNDENTAFELN lives in @schoolflow/shared (apps/api is pure re-export), SCHOOL_TYPES_LABELS + SubjectCreateSchema exported, Wave 0 it.todo() stubs in place, no behavior regression in stundentafel-template.service.
  </done>
</task>

<task type="auto">
  <name>Task 2: API — SubjectService.remove Orphan-Guard gap-fix + unit-test impls (SUBJECT-05)</name>
  <files>apps/api/src/modules/subject/subject.service.ts, apps/api/src/modules/subject/subject.service.spec.ts</files>

  <read_first>
    - apps/api/src/modules/subject/subject.service.ts (current .remove at lines 104-107)
    - apps/api/src/modules/subject/subject.service.spec.ts (Wave 0 stubs from Task 1)
    - apps/api/prisma/schema.prisma (Subject model line 509; ClassSubject line 530 cascade; TeacherSubject line 551 cascade; TimetableLesson line 720 via classSubject; Homework line 1346 via classSubject; Exam line 1369 via classSubject — with NO ACTION at line 1382)
    - .planning/phases/11-lehrer-und-f-cher-verwaltung/11-RESEARCH.md §Focus Area 4 (Subject Orphan-Guard Prisma code listing)
    - .planning/phases/11-lehrer-und-f-cher-verwaltung/11-UI-SPEC.md §4.3 (payload shape consumed by DeleteSubjectDialog)
    - apps/api/src/modules/teacher/teacher.service.ts (Plan 11-01 Task 2 delivered ConflictException pattern — mirror the same structure)
  </read_first>

  <action>
1. **Rewrite `SubjectService.remove`** in `apps/api/src/modules/subject/subject.service.ts` (replace lines 104-107). Verify `ConflictException` is already imported at top (it is — line 1). New implementation per 11-RESEARCH §Focus 4:

   ```ts
   async remove(id: string) {
     await this.findOne(id); // throws 404

     const [
       classSubjectCount,
       teacherSubjectCount,
       timetableLessonCount,
       homeworkCount,
       examCount,
       affectedClasses,
       affectedTeachers,
     ] = await this.prisma.$transaction([
       this.prisma.classSubject.count({ where: { subjectId: id } }),
       this.prisma.teacherSubject.count({ where: { subjectId: id } }),
       this.prisma.timetableLesson.count({
         where: { classSubject: { subjectId: id } },
       }),
       this.prisma.homework.count({
         where: { classSubject: { subjectId: id } },
       }),
       this.prisma.exam.count({
         where: { classSubject: { subjectId: id } },
       }),
       this.prisma.classSubject.findMany({
         where: { subjectId: id },
         select: { schoolClass: { select: { id: true, name: true } } },
         distinct: ['classId'],
         take: 50,
       }),
       this.prisma.teacherSubject.findMany({
         where: { subjectId: id },
         select: {
           teacher: {
             select: {
               id: true,
               person: { select: { firstName: true, lastName: true } },
             },
           },
         },
         take: 50,
       }),
     ]);

     const totalRefs =
       classSubjectCount + teacherSubjectCount
       + timetableLessonCount + homeworkCount + examCount;

     if (totalRefs > 0) {
       throw new ConflictException({
         type: 'https://schoolflow.dev/errors/subject-has-dependents',
         title: 'Fach hat Abhängigkeiten',
         status: 409,
         detail: 'Dieses Fach ist Klassen oder Lehrpersonen zugeordnet. Lösen Sie erst alle Zuordnungen.',
         extensions: {
           affectedEntities: {
             affectedClasses: affectedClasses.map((cs) => cs.schoolClass),
             affectedTeachers: affectedTeachers.map((ts) => ({
               id: ts.teacher.id,
               name: `${ts.teacher.person.firstName} ${ts.teacher.person.lastName}`,
             })),
             lessonCount: timetableLessonCount,
             homeworkCount,
             examCount,
           },
         },
       });
     }

     return this.prisma.subject.delete({ where: { id } });
   }
   ```

2. **Turn Wave-0 stubs into real tests** in `subject.service.spec.ts`:
   - Mock PrismaService using vi.fn() (follow existing spec file patterns).
   - Happy-case test: all counts return 0; both findMany return []; assert `prisma.subject.delete` called once.
   - Failing cases: mock each count to return 1 and remaining to 0; assert `rejects.toBeInstanceOf(ConflictException)` and inspect `error.getResponse()` for the RFC 9457 envelope shape (type/title/status/detail/extensions/affectedEntities).
   - Combined-failure test: multiple counts nonzero; assert totalRefs behavior and payload counts.
   - Array-cap test: mock findMany to return 100 items each; assert `affectedClasses.length <= 50` and `affectedTeachers.length <= 50` (Prisma's `take: 50` enforces this, but verify the contract).
  </action>

  <acceptance_criteria>
    - `apps/api/src/modules/subject/subject.service.ts` contains `ConflictException` AND `affectedEntities` AND `affectedClasses` AND `affectedTeachers` AND `take: 50` (grep all five)
    - `apps/api/src/modules/subject/subject.service.ts` contains `timetableLessonCount` AND `homeworkCount` AND `examCount` (grep)
    - `pnpm --filter @schoolflow/api test:run -- subject.service.spec` — all Orphan-Guard describe tests green (≥8 assertions)
    - Existing subject tests (create/findAll/findOne/update/addToClass/removeFromClass) remain green (no regression)
    - `pnpm --filter @schoolflow/api build` green
  </acceptance_criteria>

  <verify>
    <automated>pnpm --filter @schoolflow/api test:run -- subject.service &amp;&amp; pnpm --filter @schoolflow/api build</automated>
  </verify>

  <done>
    SubjectService.remove throws ConflictException with RFC 9457 extensions.affectedEntities on dependents (SUBJECT-05 orphan-safe delete closed). Orphan-Guard describe block is fully implemented with all stubs turned green. Existing tests untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Web — /admin/subjects route, SubjectTable, SubjectFormDialog, Stundentafel-Vorlagen section, DeleteSubjectDialog, sidebar append</name>
  <files>apps/web/src/hooks/useSubjects.ts, apps/web/src/components/layout/AppSidebar.tsx, apps/web/src/components/layout/MobileSidebar.tsx, apps/web/src/routes/_authenticated/admin/subjects.index.tsx, apps/web/src/components/admin/subject/SubjectTable.tsx, apps/web/src/components/admin/subject/SubjectMobileCards.tsx, apps/web/src/components/admin/subject/SubjectFormDialog.tsx, apps/web/src/components/admin/subject/SubjectFormDialog.test.tsx, apps/web/src/components/admin/subject/DeleteSubjectDialog.tsx, apps/web/src/components/admin/subject/SubjectAffectedEntitiesDialog.tsx, apps/web/src/components/admin/subject/StundentafelVorlagenSection.tsx, apps/web/src/components/admin/subject/StundentafelVorlagenSection.test.tsx</files>

  <checkpoint_protocol>
    **Context budget discipline:** This task spans ~12 files of FE surface. Target context consumption ≤35%. If context consumption exceeds **45%** at any point:
    1. STOP implementation mid-task.
    2. Snapshot current progress in an inline TODO comment block at the top of the last file touched (list: what's done, what's pending, next file to resume on).
    3. Emit a request-for-confirmation message: "Context budget exceeded 45% at {file}. Pausing for orchestrator decision: continue (risk degradation) / split into follow-up plan / resume after /clear."
    4. Do NOT silently compress or skip acceptance criteria.
    This codifies the discipline hinted at in 11-01 Task 3 ("stop and snapshot if exceeding 45%") for Plan 11-02 as well. User has accepted task bundling per D-16 — this protocol guards against quality degradation without splitting.
  </checkpoint_protocol>

  <read_first>
    - apps/web/src/components/layout/AppSidebar.tsx (Plan 11-01 Task 3 added grouping scaffold + Lehrer entry — APPEND Fächer here, do NOT duplicate the group header)
    - apps/web/src/components/layout/MobileSidebar.tsx (same append)
    - apps/web/src/routes/_authenticated/admin/teachers.index.tsx (Plan 11-01 Task 3 delivery — mirror structure for PageShell, empty-state, search input)
    - apps/web/src/components/admin/teacher/AffectedEntitiesList.tsx (Plan 11-01 Task 3 delivery — import directly, do NOT duplicate)
    - apps/web/src/components/admin/teacher/DeleteTeacherDialog.tsx (pattern reference for 409-blocked state transition)
    - apps/web/src/hooks/useTeachers.ts (Plan 11-01 Task 3 delivery — mirror useMutation onError pattern)
    - apps/web/src/lib/api.ts (apiFetch + parseProblemJson with extensions)
    - apps/web/src/components/admin/shared/PageShell.tsx
    - apps/web/src/components/admin/shared/WarnDialog.tsx
    - apps/web/src/components/ui/tabs.tsx (shadcn Tabs primitive)
    - apps/web/src/components/ui/dialog.tsx
    - apps/web/src/components/ui/dropdown-menu.tsx
    - .planning/phases/11-lehrer-und-f-cher-verwaltung/11-UI-SPEC.md §3 (Subject UI full) + §4 (Orphan-Guard dialog) + §11 (copy contract)
    - packages/shared/src/stundentafel/austrian-stundentafeln.ts (Plan 11-02 Task 1 delivery)
    - packages/shared/src/constants/school-types.ts (Plan 11-02 Task 1 delivery)
    - packages/shared/src/types/timetable.ts (getSubjectColor + SUBJECT_PALETTE — existing v1.0, unchanged)
  </read_first>

  <action>
**Target context ~30% for this task.**

**A. Sidebar append** — edit `AppSidebar.tsx` + `MobileSidebar.tsx` to add (within the existing 'personalAndFaecher' group array after the Lehrer entry from 11-01):
   ```ts
   {
     label: 'Fächer',
     href: '/admin/subjects',
     icon: BookOpen,
     roles: ['admin','schulleitung'],
     group: 'personalAndFaecher',
   }
   ```
   Import `BookOpen` from `lucide-react`. Do NOT touch the group-header rendering code (Plan 11-01 owns that).

**B. TanStack-Query hooks** `apps/web/src/hooks/useSubjects.ts`:
   ```ts
   export function useSubjects(schoolId: string, filters?: {search?: string})  // ['subjects', schoolId, filters]
   export function useSubject(id: string)                                       // ['subject', id]
   export function useCreateSubject(schoolId: string)                           // onSuccess → invalidate list + toast 'Fach angelegt.'
   export function useUpdateSubject(id: string)                                 // onSuccess → invalidate + toast 'Fach aktualisiert.'
   export function useDeleteSubject(id: string)                                 // onSuccess → invalidate + toast 'Fach gelöscht.' / onError handles 409
   ```
   All mutations MUST wire explicit `onError` (silent-4xx invariant). For create/update, extract 409 Kürzel-conflict and surface via form error: if `err.status === 409 && err.detail?.includes('Kuerzel')` then setError('shortName', {message: 'Dieses Kürzel ist bereits vergeben.'}). Otherwise `toast.error(err.detail ?? fallback)`.

**C. Route shell** `subjects.index.tsx`:
   ```
   [PageShell H1="Fächer", sub="Fachkatalog pflegen und Stundentafel-Vorlagen pro Schultyp einsehen."]
     ├── Header controls row: Search input placeholder "Name oder Kürzel suchen" + [+ Fach anlegen] button
     ├── SubjectTable (desktop) / SubjectMobileCards (mobile)
     ├── <Separator className="my-8" />
     └── <StundentafelVorlagenSection />
   ```
   Controlled `<SubjectFormDialog>` state (createMode | editMode with subjectId). Empty-state hero = `BookOpen` 48px + "Noch keine Fächer angelegt" + body + inline "Erstes Fach anlegen" button (same dialog).

**D. SubjectTable** desktop per 11-UI-SPEC §3.2 — 6 columns (Farbe swatch, Name, Kürzel pill, Schultyp, Nutzung clickable link, Aktion dropdown). Row click → open edit dialog. Color swatch: `style={{backgroundColor: getSubjectColor(subject.id).bg}}` using existing shared util (NO schema data). "Schultyp" column: leave as "—" or placeholder since no schoolType mapping exists; document as deferred in copy `text-muted-foreground`. Nutzung-count comes from `subject._count.classSubjects` (already on findAll response). Nutzung click → opens `<SubjectAffectedEntitiesDialog />`.

**E. SubjectMobileCards** per 11-UI-SPEC §3.2.1 — card with large color swatch (from getSubjectColor), name + kürzel pill inline, muted sub-line with count. Tap anywhere → edit dialog. MoreVertical trigger opens same dropdown.

**F. SubjectFormDialog** — shadcn Dialog `sm:max-w-md`, dynamic title "Fach anlegen" (create) / "Fach bearbeiten" (edit). Body fields in order:
   1. Name input (required, maxLength 64)
   2. Kürzel input (required, maxLength 8, onBlur auto-uppercase via `form.setValue('shortName', v.toUpperCase(), {shouldDirty: true, shouldValidate: true})`)
   3. Information note (11-UI-SPEC §3.3.2): `<div className="text-xs text-muted-foreground bg-muted/50 rounded-md p-3">Die Farbe wird automatisch aus der Standard-Palette vergeben. Manuelle Farbauswahl folgt in einer späteren Phase.</div>`
   Footer: Abbrechen (ghost, autoFocus) + dynamic primary button "Fach anlegen" / "Speichern" (disabled until zod-valid via RHF isValid).
   Uses `zodResolver(SubjectCreateSchema)` / `zodResolver(SubjectUpdateSchema)` from `@schoolflow/shared`.
   On 409 response (Kürzel uniqueness — implementation constraint): setError('shortName', {message: 'Dieses Kürzel ist bereits vergeben.'}) — inline error, DO NOT close dialog, DO NOT show toast.
   Make Wave-0 it.todo tests green (see `SubjectFormDialog.test.tsx` from Task 1).

**G. DeleteSubjectDialog (SUBJECT-05)** — mirrors Teacher pattern (11-01 Task 3 delivery). 2 states:
   - Happy: AlertTriangle amber + body `"${name}" wird dauerhaft gelöscht...` + Abbrechen + Löschen(destructive)
   - Blocked (409 received): title `Fach kann nicht gelöscht werden`, AlertTriangle red, body with totalCount, `<AffectedEntitiesList entities={...} kind="subject" />`, single Schließen footer.
   - Also fires toast.error on 409 per 11-UI-SPEC §4.3.

**H. Extend AffectedEntitiesList (Plan 11-01 component)** if it does NOT already handle Subject-side groups — since 11-01 delivered it for Teacher-side groups (klassenvorstandFor/timetableLessons/classbookEntries/gradeEntries/substitutions), extend with Subject-side keys: `affectedClasses → 'Klassen-Fach-Zuordnungen'`, `affectedTeachers → 'Fachzuordnungen'`, `lessonCount → 'Stundenplan-Einträge'`, `homeworkCount → 'Hausübungen'`, `examCount → 'Prüfungen'`. If 11-01 already authored it with union-of-both shapes, simply pass the extensions payload through. Deep-links: `affectedTeachers[].id` → `/admin/teachers/{id}` (functional in Phase 11); all other groups → disabled with `Verfügbar ab Phase 12` tooltip.

**I. SubjectAffectedEntitiesDialog** — shadcn Dialog, title `Zuordnungen zu "{name}"`, Info icon (neutral, not destructive), lead `"${name}" ist mit ${totalCount} Einträgen verknüpft.`, reuses `<AffectedEntitiesList />` with same entities payload, footer single Schließen ghost. Fetches via `useQuery({queryKey:['affected-entities', 'subject', id], queryFn: GET /subjects/{id}/affected-entities})` — if that endpoint does not yet exist, call `GET /subjects/{id}` and count from the `subject._count + classSubjects[]` include OR use a simulated HEAD-like pre-flight (call DELETE with a dry-run query param if supported; else the dialog shows `count` only from _count without the deep-link list). Pragmatic choice: for Phase 11, the dialog reuses `useSubject(id)` which returns `classSubjects` and `teacherSubjects` from the existing `findOne` include, so the count display and grouped lists are populated from that single GET — NO new endpoint needed.

**J. StundentafelVorlagenSection (SUBJECT-03)** per 11-UI-SPEC §3.4:
   - Section header h2 "Stundentafel-Vorlagen" + sub-line "Offizielle Austrian-Stundentafeln pro Schultyp..."
   - `<Tabs>` with one `<TabsTrigger>` per Schultyp that has entries in `AUSTRIAN_STUNDENTAFELN` — tab labels from `SCHOOL_TYPES_LABELS`
   - Per-Schultyp panel: group templates by `yearLevel` 1-4 and render merged table:
     | Fach | Kürzel | Jg. 1 | Jg. 2 | Jg. 3 | Jg. 4 |
     Each row is a unique `{name, shortName, lehrverpflichtungsgruppe}`; cell at Jg.X = sum of weeklyHours for that subject in yearLevel X templates (0 if absent, rendered `text-muted-foreground`).
   - Footer: `text-xs text-muted-foreground border-t pt-2 mt-2` with `Wochenstunden gesamt pro Jahrgang: {sum1} · {sum2} · {sum3} · {sum4}` (sum each column).
   - Mobile (<md): Stacked cards per Fach (11-UI-SPEC §3.4.2 end).
   - Imports `AUSTRIAN_STUNDENTAFELN`, `SCHOOL_TYPES_LABELS` from `@schoolflow/shared` (grep-verifiable path).
   - NO inputs, NO action buttons — read-only.
   - Disabled "Zur Klassenverwaltung →" link with tooltip "Verfügbar ab Phase 12" per 11-UI-SPEC §3.4.3.
   - Make Wave-0 it.todo tests green (see `StundentafelVorlagenSection.test.tsx` from Task 1).

**Copy compliance (grep-checked):** every German string from 11-UI-SPEC §3.3.2 + §3.4.3 + §4.5 + §11 appears VERBATIM. Examples: `Fach anlegen`, `Fach bearbeiten`, `Dieses Kürzel ist bereits vergeben.`, `Fach angelegt.`, `Fach aktualisiert.`, `Fach gelöscht.`, `Fach kann nicht gelöscht werden`, `Zuordnungen zu "{name}"`, `Stundentafel-Vorlagen`, `Wochenstunden gesamt pro Jahrgang`, `Verfügbar ab Phase 12`.
  </action>

  <acceptance_criteria>
    - `apps/web/src/routes/_authenticated/admin/subjects.index.tsx` exists with `/admin/subjects` route (grep `createFileRoute` or equivalent)
    - `apps/web/src/components/layout/AppSidebar.tsx` contains BOTH `GraduationCap` (from 11-01) AND `BookOpen` AND `/admin/subjects` (grep)
    - `apps/web/src/components/layout/MobileSidebar.tsx` contains `BookOpen` AND `/admin/subjects` (grep)
    - `apps/web/src/hooks/useSubjects.ts` exports `useSubjects`, `useSubject`, `useCreateSubject`, `useUpdateSubject`, `useDeleteSubject` (grep each name)
    - `apps/web/src/hooks/useSubjects.ts` contains at least 3 `onError:` entries (grep — silent-4xx invariant)
    - `SubjectFormDialog.tsx` contains `shortName` AND `toUpperCase` AND `Dieses Kürzel ist bereits vergeben` (grep all three — Kürzel uniqueness (implementation constraint))
    - `SubjectFormDialog.tsx` does NOT contain `colorBg` OR `colorText` OR `tinycolor2` OR `contrastRatio` (grep — 0 results, confirming D-11 descope)
    - `SubjectFormDialog.tsx` contains `Manuelle Farbauswahl folgt in einer späteren Phase` (grep — post-research info note)
    - `StundentafelVorlagenSection.tsx` imports `AUSTRIAN_STUNDENTAFELN` AND `SCHOOL_TYPES_LABELS` from `@schoolflow/shared` (grep `from ['"]@schoolflow/shared['"]` near both symbols)
    - `StundentafelVorlagenSection.tsx` contains `Stundentafel-Vorlagen` AND `Wochenstunden gesamt pro Jahrgang` (grep)
    - `DeleteSubjectDialog.tsx` contains `Fach kann nicht gelöscht werden` AND `extensions.affectedEntities` (grep)
    - `DeleteSubjectDialog.tsx` imports `AffectedEntitiesList` from `../teacher/AffectedEntitiesList` (grep — reuses Plan 11-01 component, does not duplicate)
    - `pnpm --filter @schoolflow/web test:run -- SubjectFormDialog` green
    - `pnpm --filter @schoolflow/web test:run -- StundentafelVorlagenSection` green
    - `pnpm --filter @schoolflow/web build` green
    - `pnpm --filter @schoolflow/web typecheck` introduces zero new errors
  </acceptance_criteria>

  <verify>
    <automated>pnpm --filter @schoolflow/web test:run -- "admin/subject" &amp;&amp; pnpm --filter @schoolflow/web build</automated>
  </verify>

  <done>
    Admin sees /admin/subjects in the "Personal & Fächer" sidebar group, table + mobile cards with getSubjectColor swatches, can create/edit via dialog (Name + Kürzel only, Kürzel auto-uppercase, 409 inline error), delete with Orphan-Guard 409 blocked-state, view Stundentafel-Vorlagen per Schultyp as read-only Tabs from shared templates. SUBJECT-01, SUBJECT-02, SUBJECT-03, SUBJECT-05 surfaced in UI. Sidebar now has both entries in the Personal & Fächer group.
  </done>
</task>

</tasks>

<verification>
- `pnpm -r build` green across workspace
- `pnpm --filter @schoolflow/shared test:run` — stundentafel + subject schema specs green
- `pnpm --filter @schoolflow/api test:run -- subject.service` — Orphan-Guard describe all green
- `pnpm --filter @schoolflow/web test:run -- "admin/subject"` — 2 new FE specs green
- Manual smoke: /admin/subjects loads, create dialog has Name + Kürzel only (NO color picker), Kürzel auto-uppercases, duplicate Kürzel shows inline red error, Stundentafel-Vorlagen Tabs render with 7 tabs
- 409 path: delete a subject with ClassSubject references → DeleteSubjectDialog transitions to blocked state with affected classes + teachers listed
</verification>

<success_criteria>
- SUBJECT-01 green: list with search + empty-state CTA ✓
- SUBJECT-02 green: create/edit via dialog (Name + Kürzel only per REQUIREMENTS.md updated definition — Farbe auto-palette) ✓
- SUBJECT-03 green: Stundentafel-Vorlagen Tabs per Schultyp (read-only, from shared) ✓
- SUBJECT-05 green: Orphan-Guard 409 with affectedEntities extensions + UI blocked-state (canonical "Admin kann ungenutzte Fächer löschen / Orphan-Schutz") ✓
- Kürzel uniqueness (implementation constraint, NOT a REQ-ID): 409 inline error surfaced ✓
- Sidebar: 'Personal & Fächer' has both Lehrer + Fächer entries ✓
- No schema migration (Subject.color descoped, Schultyp-mapping descoped) ✓
- AUSTRIAN_STUNDENTAFELN consolidated in @schoolflow/shared; apps/api is pure re-export ✓
- SUBJECT-04 explicitly NOT in this plan (moved to Phase 12 per ROADMAP 2026-04-22 — ClassSubject junction + Wochenstunden-editing live in Phase 12) ✓
</success_criteria>

<output>
Create `.planning/phases/11-lehrer-und-f-cher-verwaltung/11-02-SUMMARY.md` with: Orphan-Guard test count, shared move file diff summary, SCHOOL_TYPES_LABELS map, reuse of AffectedEntitiesList from 11-01, post-research D-11+A4 descope confirmation (zero schema changes), SUBJECT-04→Phase 12 scope transfer confirmation, outstanding items for Plan 11-03 (seeded ClassSubject fixture for SUBJECT-05 E2E error test).
</output>
