---
phase: 11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/schemas/teacher.schema.ts
  - packages/shared/src/schemas/availability.schema.ts
  - packages/shared/src/schemas/teaching-reduction.schema.ts
  - packages/shared/src/werteinheiten/index.ts
  - packages/shared/src/werteinheiten/werteinheiten.util.ts
  - packages/shared/src/werteinheiten/werteinheiten.util.spec.ts
  - packages/shared/src/index.ts
  - packages/shared/src/schemas/teacher.schema.spec.ts
  - packages/shared/src/schemas/availability.schema.spec.ts
  - packages/shared/src/schemas/teaching-reduction.schema.spec.ts
  - apps/api/src/modules/teacher/teacher.service.ts
  - apps/api/src/modules/teacher/teacher.service.spec.ts
  - apps/api/src/modules/teacher/teacher.controller.ts
  - apps/api/src/modules/teacher/teacher.module.ts
  - apps/api/src/modules/teacher/werteinheiten.util.ts
  - apps/api/src/modules/teacher/dto/link-keycloak.dto.ts
  - apps/api/src/modules/keycloak-admin/keycloak-admin.module.ts
  - apps/api/src/modules/keycloak-admin/keycloak-admin.service.ts
  - apps/api/src/modules/keycloak-admin/keycloak-admin.service.spec.ts
  - apps/api/src/modules/keycloak-admin/keycloak-admin.controller.ts
  - apps/api/src/modules/keycloak-admin/dto/keycloak-user-query.dto.ts
  - apps/api/src/modules/keycloak-admin/dto/keycloak-user-response.dto.ts
  - apps/api/src/app.module.ts
  - apps/api/package.json
  - pnpm-lock.yaml
  - apps/web/src/lib/api.ts
  - apps/web/src/hooks/useTeachers.ts
  - apps/web/src/hooks/useKeycloakUsers.ts
  - apps/web/src/components/layout/AppSidebar.tsx
  - apps/web/src/components/layout/MobileSidebar.tsx
  - apps/web/src/routes/_authenticated/admin/teachers.index.tsx
  - apps/web/src/routes/_authenticated/admin/teachers.$teacherId.tsx
  - apps/web/src/components/admin/teacher/TeacherListTable.tsx
  - apps/web/src/components/admin/teacher/TeacherMobileCards.tsx
  - apps/web/src/components/admin/teacher/TeacherCreateDialog.tsx
  - apps/web/src/components/admin/teacher/TeacherDetailTabs.tsx
  - apps/web/src/components/admin/teacher/StammdatenTab.tsx
  - apps/web/src/components/admin/teacher/StammdatenTab.test.tsx
  - apps/web/src/components/admin/teacher/LehrverpflichtungTab.tsx
  - apps/web/src/components/admin/teacher/LehrverpflichtungTab.test.tsx
  - apps/web/src/components/admin/teacher/VerfuegbarkeitsGrid.tsx
  - apps/web/src/components/admin/teacher/VerfuegbarkeitsGrid.test.tsx
  - apps/web/src/components/admin/teacher/VerfuegbarkeitsMobileList.tsx
  - apps/web/src/components/admin/teacher/ErmaessigungenList.tsx
  - apps/web/src/components/admin/teacher/ErmaessigungenList.test.tsx
  - apps/web/src/components/admin/teacher/KeycloakLinkSection.tsx
  - apps/web/src/components/admin/teacher/KeycloakLinkDialog.tsx
  - apps/web/src/components/admin/teacher/KeycloakLinkDialog.test.tsx
  - apps/web/src/components/admin/teacher/ArchiveTeacherDialog.tsx
  - apps/web/src/components/admin/teacher/DeleteTeacherDialog.tsx
  - apps/web/src/components/admin/teacher/AffectedEntitiesList.tsx
autonomous: true
requirements: [TEACHER-01, TEACHER-02, TEACHER-03, TEACHER-04, TEACHER-05, TEACHER-06]
requirements_addressed: [TEACHER-01, TEACHER-02, TEACHER-03, TEACHER-04, TEACHER-05, TEACHER-06]
tags: [teacher, admin, keycloak, werteinheiten, availability-grid, orphan-guard, zod, shared, phase-11]
user_setup:
  - service: keycloak
    why: "Admin-realm service account required for GET /admin/keycloak/users email search (D-08)"
    env_vars:
      - name: KEYCLOAK_ADMIN_CLIENT_ID
        source: "Keycloak Admin Console -> master realm -> Clients -> schoolflow-admin (or existing admin-cli client with service-account enabled)"
      - name: KEYCLOAK_ADMIN_CLIENT_SECRET
        source: "Same client -> Credentials tab -> Client secret"
    dashboard_config:
      - task: "Create confidential client `schoolflow-admin` in schoolflow realm with Service Accounts Enabled=ON and realm-management role `view-users` assigned"
        location: "Keycloak Admin Console -> schoolflow realm -> Clients -> Create"

must_haves:
  truths:
    - "Admin sees Lehrerliste at /admin/teachers with name/fach/email search + status filter + Fach filter, empty-state inline CTA 'Erste Lehrperson anlegen' when zero teachers (TEACHER-01, D-04)"
    - "Admin can create a Lehrperson via TeacherCreateDialog (Stammdaten fields) and the new row appears in list with 201 response (TEACHER-02)"
    - "Admin can edit Lehrperson Stammdaten on /admin/teachers/$teacherId?tab=stammdaten and PUT /teachers/:id returns the updated record, toast 'Änderungen gespeichert.' (TEACHER-02)"
    - "Werteinheiten tab shows live-computed total that updates synchronously as Beschäftigungsgrad / OEPU / Zusatz-WE / Fachzuordnungen inputs change — client and server compute identical values using shared werteinheiten util (TEACHER-03, D-05)"
    - "Verfügbarkeits-Grid (desktop >=md) renders Mo-Fr(-Sa) × periods, click toggles cell blocked/available, Save sends full AvailabilityRule[] in one PUT, diagonal-hatched + Lock-icon state for blocked cells (TEACHER-04, D-06)"
    - "Verfügbarkeit Mobile-375 (<md) renders Day-Picker Select + single-column period toggle list, each row h-11 (44px touch target) (TEACHER-04, D-06 mobile fallback)"
    - "Ermäßigungen tab row-add list (Grund Select + Anmerkung input + WE Number + Trash2) saves replace-all-in-transaction; PUT with reductions:[] replaces all existing rows (TEACHER-05, D-07)"
    - "Keycloak-Link dialog debounces email input 300ms, calls GET /admin/keycloak/users?email=, renders 404 'Kein Account mit dieser E-Mail gefunden' / match user-card / already-linked warning; Verknüpfen button issues PATCH /teachers/:id/keycloak-link (TEACHER-02, D-08)"
    - "TeacherService.remove throws 409 ConflictException with RFC 9457 extensions.affectedEntities={klassenvorstandFor, lessonCount, classbookCount, gradeCount, substitutionCount} when ANY reference exists — blocks silent zombification of denormalized-FK history (TEACHER-06, D-12, D-14)"
    - "AppSidebar + MobileSidebar render new group 'Personal & Fächer' (role-gated admin|schulleitung) with GraduationCap icon -> /admin/teachers entry (D-03)"
    - "Shared Zod schemas (teacher/availability/teaching-reduction) live under packages/shared/src/schemas/ with deutsche error copy + english field names, re-exported from @schoolflow/shared (D-15)"
    - "werteinheiten.util moved to packages/shared/src/werteinheiten/ and re-exported; apps/api/src/modules/teacher/werteinheiten.util.ts re-exports from shared so zero behavior change in existing consumers"
    - "Every new useMutation (useCreateTeacher/useUpdateTeacher/useDeleteTeacher/useLinkKeycloak/useUnlinkKeycloak) wires explicit onError that calls sonner.toast.error, never treats 4xx as success (Silent-4xx invariant from Phase 10.1-01/10.2-04)"
    - "Wave 0 TDD stubs exist as it.todo() in all 10 new test files BEFORE any implementation; planner/executor must not skip"
  artifacts:
    - path: "packages/shared/src/schemas/teacher.schema.ts"
      provides: "TeacherCreateSchema + TeacherUpdateSchema + StammdatenSchema Zod schemas; deutsche error strings, englische API-feldnamen"
      contains: "TeacherCreateSchema"
    - path: "packages/shared/src/schemas/availability.schema.ts"
      provides: "AvailabilityRuleSchema with ruleType/dayOfWeek/periodNumbers/isHard"
      contains: "AvailabilityRuleSchema"
    - path: "packages/shared/src/schemas/teaching-reduction.schema.ts"
      provides: "TeachingReductionSchema with reductionType enum (KLASSENVORSTAND/OEPU_FUNKTION/SUPPLIERREDUKTION/KUSTODIAT/MENTORING/SONSTIGES) + werteinheiten number + description conditional"
      contains: "TeachingReductionSchema"
    - path: "packages/shared/src/werteinheiten/werteinheiten.util.ts"
      provides: "calculateWerteinheiten + calculateMaxTeachingHours pure fns moved from apps/api"
      contains: "calculateWerteinheiten"
    - path: "apps/api/src/modules/teacher/teacher.service.ts"
      provides: "Orphan-Guard in .remove via $transaction count queries; .linkKeycloakUser(id, keycloakUserId); .unlinkKeycloakUser(id)"
      contains: "ConflictException"
    - path: "apps/api/src/modules/keycloak-admin/keycloak-admin.service.ts"
      provides: "findUsersByEmail via @keycloak/keycloak-admin-client with cached service-account token (re-auth lazy on expiry)"
      contains: "findUsersByEmail"
    - path: "apps/api/src/modules/keycloak-admin/keycloak-admin.controller.ts"
      provides: "GET /admin/keycloak/users?email= with @CheckPermissions manage teacher + @Throttle(30/min)"
      contains: "@Controller('admin/keycloak')"
    - path: "apps/web/src/routes/_authenticated/admin/teachers.index.tsx"
      provides: "Teacher list route + desktop table + mobile cards + search/filter controls + Create dialog trigger"
      contains: "TeacherListTable"
    - path: "apps/web/src/routes/_authenticated/admin/teachers.$teacherId.tsx"
      provides: "Teacher detail with 4 tabs (stammdaten|verpflichtung|verfuegbarkeit|ermaessigungen), Zod-validated search param ?tab="
      contains: "TeacherDetailTabs"
    - path: "apps/web/src/components/admin/teacher/VerfuegbarkeitsGrid.tsx"
      provides: "Visual Week-Grid (role=grid, aria-pressed, keyboard nav, hatch pattern + Lock icon for blocked)"
      contains: "role=\"grid\""
    - path: "apps/web/src/components/admin/teacher/VerfuegbarkeitsMobileList.tsx"
      provides: "Mobile-375 Day-Picker Select + 44px toggle list fallback"
      contains: "h-11"
    - path: "apps/web/src/hooks/useTeachers.ts"
      provides: "useTeachers, useTeacher, useCreateTeacher, useUpdateTeacher, useDeleteTeacher, useLinkKeycloak, useUnlinkKeycloak TanStack-Query hooks; all mutations wire explicit onError (silent-4xx invariant)"
      contains: "onError"
    - path: "apps/web/src/hooks/useKeycloakUsers.ts"
      provides: "useKeycloakUsers({email}) with enabled:email.length>=3 + staleTime 30_000"
      contains: "enabled"
    - path: "apps/web/src/components/layout/AppSidebar.tsx"
      provides: "Grouping support (group label row + children) + 'Personal & Fächer' -> Lehrer entry"
      contains: "Personal & Fächer"
  key_links:
    - from: "apps/web/src/routes/_authenticated/admin/teachers.index.tsx"
      to: "apps/web/src/hooks/useTeachers.ts"
      via: "useTeachers(schoolId, {search, fachId, status}) TanStack Query hook"
      pattern: "useTeachers"
    - from: "apps/web/src/components/admin/teacher/LehrverpflichtungTab.tsx"
      to: "packages/shared/src/werteinheiten/werteinheiten.util.ts"
      via: "calculateMaxTeachingHours imported from @schoolflow/shared (byte-identical FE/BE)"
      pattern: "from ['\"]@schoolflow/shared['\"]"
    - from: "apps/api/src/modules/teacher/teacher.service.ts"
      to: "apps/web/src/lib/api.ts parseProblemJson"
      via: "RFC 9457 ConflictException with extensions.affectedEntities consumed by DeleteTeacherDialog"
      pattern: "affectedEntities"
    - from: "apps/api/src/modules/keycloak-admin/keycloak-admin.service.ts"
      to: "@keycloak/keycloak-admin-client users.find({email})"
      via: "KeycloakAdminClient with cached service-account token"
      pattern: "KeycloakAdminClient"
    - from: "apps/web/src/components/admin/teacher/KeycloakLinkDialog.tsx"
      to: "apps/web/src/hooks/useKeycloakUsers.ts"
      via: "debounced (300ms) useKeycloakUsers({email}) query"
      pattern: "useKeycloakUsers"
    - from: "apps/web/src/components/layout/AppSidebar.tsx"
      to: "/admin/teachers"
      via: "Grouped nav item with GraduationCap icon, roles=['admin','schulleitung']"
      pattern: "GraduationCap"
---

<objective>
Ship the complete Teacher admin surface (TEACHER-01..06) + shared Zod/werteinheiten foundation + Keycloak-admin module + TeacherService.remove Orphan-Guard gap-fix.

Per CONTEXT.md D-16 USER-OVERRIDE this is the first of 3 bundled plans (instead of 5 parallel plans). Touches shared package, API (gap-fixes + greenfield keycloak-admin module), and web (routes, hooks, components, sidebar refactor).

**Purpose:** Close TEACHER-01..06 with a production-ready admin UI and fix the silent-zombification defect in TeacherService.remove that would otherwise leave stale teacherId references in TimetableLesson / ClassBookEntry / GradeEntry / Substitution history (11-RESEARCH.md Focus 4, Pitfall 3).

**Output:**
- SHARED: Zod schemas (teacher/availability/teaching-reduction), werteinheiten util move, index re-exports
- API: TeacherService.remove Orphan-Guard + .linkKeycloakUser/.unlinkKeycloakUser; new KeycloakAdmin module (service + controller + DTOs) wired into AppModule; @keycloak/keycloak-admin-client dependency added
- WEB: /admin/teachers + /admin/teachers/$teacherId routes, 4 detail tabs (Stammdaten/Lehrverpflichtung/Verfügbarkeit/Ermäßigungen), KeycloakLinkDialog, DeleteTeacherDialog with 409 Orphan-Guard blocked-state, ArchiveTeacherDialog, AppSidebar grouping refactor + "Personal & Fächer" group
- TESTS: Wave 0 it.todo() stubs (10 files) + Vitest impls + teacher.service.spec extended for Orphan-Guard

**Wave:** 1 (no dependencies — 11-02 depends on this for sidebar append, werteinheiten shared path, and Delete-dialog AffectedEntitiesList component; 11-03 depends on merged 11-01 + 11-02).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-lehrer-und-f-cher-verwaltung/11-CONTEXT.md
@.planning/phases/11-lehrer-und-f-cher-verwaltung/11-RESEARCH.md
@.planning/phases/11-lehrer-und-f-cher-verwaltung/11-UI-SPEC.md
@.planning/phases/11-lehrer-und-f-cher-verwaltung/11-VALIDATION.md

# Reference code (read these before editing)
@apps/api/src/modules/teacher/teacher.service.ts
@apps/api/src/modules/teacher/teacher.controller.ts
@apps/api/src/modules/teacher/teacher.service.spec.ts
@apps/api/src/modules/teacher/werteinheiten.util.ts
@apps/api/src/modules/teacher/dto/create-teacher.dto.ts
@apps/api/src/modules/teacher/dto/update-teacher.dto.ts
@apps/api/src/modules/teacher/dto/create-availability-rule.dto.ts
@apps/api/src/modules/teacher/dto/create-teaching-reduction.dto.ts
@apps/api/prisma/schema.prisma
@packages/shared/src/index.ts
@packages/shared/src/schemas/school.schema.ts
@packages/shared/src/types/timetable.ts
@apps/web/src/components/layout/AppSidebar.tsx
@apps/web/src/components/layout/MobileSidebar.tsx
@apps/web/src/components/admin/shared/PageShell.tsx
@apps/web/src/components/admin/shared/UnsavedChangesDialog.tsx
@apps/web/src/components/admin/shared/StickyMobileSaveBar.tsx
@apps/web/src/components/admin/shared/InfoBanner.tsx
@apps/web/src/components/admin/shared/WarnDialog.tsx
@apps/web/src/lib/api.ts
@apps/web/src/routes/_authenticated/admin/school.settings.tsx
@apps/web/src/stores/school-context-store.ts

<interfaces>
<!-- Key contracts the executor needs, extracted from codebase. -->

Prisma models (apps/api/prisma/schema.prisma lines 343-451):
```prisma
model Teacher {
  id                   String   @id @default(uuid())
  personId             String   @unique
  schoolId             String
  employmentPercentage Float    @default(100)
  werteinheitenTarget  Float    @default(20)
  qualifications       TeacherSubject[]     // cascade
  availabilityRules    AvailabilityRule[]   // cascade
  reductions           TeachingReduction[]  // cascade
  klassenvorstandClasses SchoolClass[] @relation("Klassenvorstand")  // nullable SetNull
  // ...
}

model Person {
  id              String  @id
  keycloakUserId  String? @unique @map("keycloak_user_id")
  firstName       String
  lastName        String
  email           String?
  phone           String?
  teacher         Teacher?
  // ...
}

model AvailabilityRule {
  teacherId     String
  ruleType      AvailabilityRuleType  // UNAVAILABLE | PREFERRED | MAX_DAYS_PER_WEEK | ...
  dayOfWeek     DayOfWeek?
  periodNumbers Int[]
  isHard        Boolean @default(true)
}

model TeachingReduction {
  teacherId     String
  reductionType ReductionType  // KLASSENVORSTAND | OEPU_FUNKTION | SUPPLIERREDUKTION | KUSTODIAT | MENTORING | SONSTIGES
  werteinheiten Float
  description   String?
  schoolYearId  String?
}
```

DENORMALIZED teacher references (string columns WITH NO FK — Orphan-Guard MUST check these):
- TimetableLesson.teacherId (String)
- ClassBookEntry.teacherId (String)
- GradeEntry.teacherId (String)
- Substitution.originalTeacherId (String)
- Substitution.substituteTeacherId (String, nullable)

Current TeacherService.remove (apps/api/src/modules/teacher/teacher.service.ts:207-213):
```ts
async remove(id: string) {
  const existing = await this.findOne(id);
  await this.prisma.teacher.delete({ where: { id } });  // cascades qualifications/rules/reductions
  await this.prisma.person.delete({ where: { id: existing.personId } });
}
```
NEW contract: MUST count denormalized refs + klassenvorstandClasses first; if any > 0 throw ConflictException with RFC 9457 payload.

Existing werteinheiten util (apps/api/src/modules/teacher/werteinheiten.util.ts):
```ts
export interface LehrverpflichtungsgruppeConfig { factor: number; fullHours: number; }
export const LEHRVERPFLICHTUNGSGRUPPEN: Record<string, LehrverpflichtungsgruppeConfig>;  // 9 groups
export function calculateWerteinheiten(weeklyHours: number, gruppe: string): number;
export function calculateMaxTeachingHours(
  werteinheitenTarget: number,
  reductions: Array<{ werteinheiten: number }>,
): number;
```

AppSidebar current shape (apps/web/src/components/layout/AppSidebar.tsx):
```ts
interface NavItem {
  label: string;
  href: string;
  icon: React.ComponentType<{ className?: string }>;
  roles: string[] | 'all';
}
// Currently a flat `navItems` array rendered by `visibleItems.map(...)`.
```
REFACTOR: extend NavItem with optional `group?: string` + render a group-label separator row before the first item of each group.

Phase 10 shared admin components (apps/web/src/components/admin/shared/):
- `PageShell` — H1/sub-line/breadcrumb wrapper
- `UnsavedChangesDialog` — per-tab dirty navigation
- `StickyMobileSaveBar` — fixed bottom save button on mobile
- `InfoBanner` — bg-muted/50 + Info icon + paragraph
- `WarnDialog` — AlertTriangle amber + cancel/confirm buttons

apps/web/src/lib/api.ts parseProblemJson handles RFC 9457 application/problem+json; access extensions via `problem.extensions?.affectedEntities`.

Subject color (existing v1.0, unchanged by Phase 11):
```ts
// @schoolflow/shared
export function getSubjectColor(subjectId: string): { bg: string; text: string };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — Shared Zod schemas + werteinheiten util move + test stubs (Nyquist TDD stubs for all Plan 11-01 test files)</name>
  <files>packages/shared/src/schemas/teacher.schema.ts, packages/shared/src/schemas/teacher.schema.spec.ts, packages/shared/src/schemas/availability.schema.ts, packages/shared/src/schemas/availability.schema.spec.ts, packages/shared/src/schemas/teaching-reduction.schema.ts, packages/shared/src/schemas/teaching-reduction.schema.spec.ts, packages/shared/src/werteinheiten/index.ts, packages/shared/src/werteinheiten/werteinheiten.util.ts, packages/shared/src/werteinheiten/werteinheiten.util.spec.ts, packages/shared/src/index.ts, apps/api/src/modules/teacher/werteinheiten.util.ts, apps/web/src/components/admin/teacher/StammdatenTab.test.tsx, apps/web/src/components/admin/teacher/LehrverpflichtungTab.test.tsx, apps/web/src/components/admin/teacher/VerfuegbarkeitsGrid.test.tsx, apps/web/src/components/admin/teacher/ErmaessigungenList.test.tsx, apps/web/src/components/admin/teacher/KeycloakLinkDialog.test.tsx, apps/api/src/modules/teacher/teacher.service.spec.ts</files>

  <read_first>
    - apps/api/src/modules/teacher/werteinheiten.util.ts (current implementation — move preserves behavior)
    - apps/api/src/modules/teacher/werteinheiten.util.spec.ts (existing spec — port tests to shared)
    - packages/shared/src/schemas/school.schema.ts (Zod pattern reference — deutsche Fehlermeldungen, englische API-feldnamen)
    - packages/shared/src/schemas/school-year.schema.ts (additional Zod pattern reference)
    - packages/shared/src/index.ts (current re-exports)
    - apps/api/src/modules/teacher/dto/create-teacher.dto.ts (class-validator source of truth for field list/types)
    - apps/api/src/modules/teacher/dto/create-availability-rule.dto.ts (AvailabilityRule field shape)
    - apps/api/src/modules/teacher/dto/create-teaching-reduction.dto.ts (TeachingReduction field shape)
    - apps/api/src/modules/teacher/teacher.service.spec.ts (existing unit test structure — extend pattern)
    - .planning/phases/11-lehrer-und-f-cher-verwaltung/11-VALIDATION.md (Wave-0 file list)
    - .planning/phases/11-lehrer-und-f-cher-verwaltung/11-RESEARCH.md §Validation Architecture
  </read_first>

  <action>
1. **Move werteinheiten util to @schoolflow/shared** (preserves byte-identical FE/BE compute per D-05):
   - Create `packages/shared/src/werteinheiten/werteinheiten.util.ts` — copy current contents VERBATIM from `apps/api/src/modules/teacher/werteinheiten.util.ts` (exports: `LEHRVERPFLICHTUNGSGRUPPEN`, `LehrverpflichtungsgruppeConfig`, `calculateWerteinheiten(weeklyHours, gruppe)`, `calculateMaxTeachingHours(werteinheitenTarget, reductions)`).
   - Create `packages/shared/src/werteinheiten/index.ts`: `export * from './werteinheiten.util.js';`
   - Rewrite `apps/api/src/modules/teacher/werteinheiten.util.ts` to pure re-export: `export { calculateWerteinheiten, calculateMaxTeachingHours, LEHRVERPFLICHTUNGSGRUPPEN, type LehrverpflichtungsgruppeConfig } from '@schoolflow/shared';` — so the existing `teacher.service.ts` import path keeps working unchanged.
   - Append to `packages/shared/src/index.ts`: `export * from './werteinheiten/index.js';`
   - Create `packages/shared/src/werteinheiten/werteinheiten.util.spec.ts` — port `apps/api/src/modules/teacher/werteinheiten.util.spec.ts` test cases AS-IS (same describe/it names, same expect values).

2. **Author shared Zod schemas** (D-15 defense-in-depth, deutsche UI-strings, englische API-Feldnamen):

   **`packages/shared/src/schemas/teacher.schema.ts`:**
   ```ts
   import { z } from 'zod';
   export const StammdatenSchema = z.object({
     firstName: z.string().min(1, 'Pflichtfeld').max(100),
     lastName: z.string().min(1, 'Pflichtfeld').max(100),
     academicTitle: z.string().max(32).optional(),
     email: z.string().email('Gültige E-Mail-Adresse eingeben'),
     phone: z.string().optional(),
     status: z.enum(['ACTIVE', 'ARCHIVED']).default('ACTIVE'),
   });
   export const LehrverpflichtungSchema = z.object({
     employmentPercentage: z.number().min(0, 'Wert muss zwischen 0 und 100 liegen').max(100, 'Wert muss zwischen 0 und 100 liegen'),
     oepuGroup: z.string().optional(),
     oepuFunctionHours: z.number().min(0).optional(),
     extraWerteinheiten: z.number().min(0).optional(),
     werteinheitenTarget: z.number().min(0).default(20),
     subjectIds: z.array(z.string().uuid()).default([]),
   });
   export const TeacherCreateSchema = StammdatenSchema.extend({
     schoolId: z.string().uuid(),
     employmentPercentage: z.number().min(0).max(100).default(100),
   });
   export const TeacherUpdateSchema = StammdatenSchema.partial().merge(LehrverpflichtungSchema.partial());
   export type StammdatenInput = z.infer<typeof StammdatenSchema>;
   export type TeacherCreateInput = z.infer<typeof TeacherCreateSchema>;
   export type TeacherUpdateInput = z.infer<typeof TeacherUpdateSchema>;
   ```

   **`packages/shared/src/schemas/availability.schema.ts`:**
   ```ts
   import { z } from 'zod';
   export const DayOfWeekEnum = z.enum(['MONDAY','TUESDAY','WEDNESDAY','THURSDAY','FRIDAY','SATURDAY','SUNDAY']);
   export const AvailabilityRuleTypeEnum = z.enum(['UNAVAILABLE','PREFERRED','MAX_DAYS_PER_WEEK','NO_LESSONS_AFTER','NO_LESSONS_BEFORE']);
   export const AvailabilityRuleSchema = z.object({
     ruleType: AvailabilityRuleTypeEnum,
     dayOfWeek: DayOfWeekEnum.optional(),
     periodNumbers: z.array(z.number().int().positive()).default([]),
     maxValue: z.number().int().optional(),
     dayPart: z.string().optional(),
     isHard: z.boolean().default(true),
   });
   export type AvailabilityRuleInput = z.infer<typeof AvailabilityRuleSchema>;
   ```

   **`packages/shared/src/schemas/teaching-reduction.schema.ts`:**
   ```ts
   import { z } from 'zod';
   export const ReductionTypeEnum = z.enum(['KLASSENVORSTAND','OEPU_FUNKTION','SUPPLIERREDUKTION','KUSTODIAT','MENTORING','SONSTIGES']);
   export const TeachingReductionSchema = z.object({
     reductionType: ReductionTypeEnum,
     werteinheiten: z.number().min(0, 'Wert muss >= 0 sein'),
     description: z.string().max(120).optional(),
     schoolYearId: z.string().uuid().optional(),
   }).refine(
     (r) => r.reductionType !== 'SONSTIGES' || (!!r.description && r.description.length > 0),
     { message: 'Anmerkung ist bei "Sonstiges" erforderlich', path: ['description'] },
   );
   export type TeachingReductionInput = z.infer<typeof TeachingReductionSchema>;
   ```

   Append to `packages/shared/src/index.ts`:
   ```ts
   export * from './schemas/teacher.schema.js';
   export * from './schemas/availability.schema.js';
   export * from './schemas/teaching-reduction.schema.js';
   ```

3. **Author Zod schema specs** — follow `packages/shared/src/schemas/school.schema.spec.ts` pattern. Each spec file has ≥5 cases: valid happy-path, required-field missing → 'Pflichtfeld', email format error, boundary (max len / min num), enum invalid. Use real `expect(...).toThrow` / `schema.safeParse(...)` pattern.

4. **Wave 0 TDD stubs** — author these AS `it.todo('...')` placeholders (no implementation yet). Each file imports vitest `it` and lists the test cases verbatim from 11-VALIDATION.md:

   - `apps/web/src/components/admin/teacher/StammdatenTab.test.tsx` — `it.todo('renders Stammdaten form fields in order Vorname/Nachname/Titel/Email/Phone/Status')`; `it.todo('blocks submit when email is invalid via RHF zodResolver')`; `it.todo('fires onSave with validated values (englische API-Feldnamen)')`.
   - `apps/web/src/components/admin/teacher/LehrverpflichtungTab.test.tsx` — `it.todo('live-computes WE total when Beschäftigungsgrad changes')`; `it.todo('matches calculateMaxTeachingHours output byte-for-byte')`; `it.todo('replaces TeacherSubject qualifications on save (full array)')`; `it.todo('shows Solver re-run InfoBanner')`.
   - `apps/web/src/components/admin/teacher/VerfuegbarkeitsGrid.test.tsx` — `it.todo('toggles cell isBlocked on click')`; `it.todo('arrow-key navigation moves focus between gridcells')`; `it.todo('Space toggles focused cell')`; `it.todo('column-header click triggers bulk-toggle confirm when ≥3 cells blocked')`; `it.todo('renders diagonal-hatch + Lock icon when blocked')`.
   - `apps/web/src/components/admin/teacher/ErmaessigungenList.test.tsx` — `it.todo('adds a new row on Ermäßigung hinzufügen click')`; `it.todo('removes row on Trash2 click')`; `it.todo('shows Anmerkung required error when Grund=SONSTIGES and empty')`; `it.todo('save sends full reductions[] array (replace-all)')`.
   - `apps/web/src/components/admin/teacher/KeycloakLinkDialog.test.tsx` — `it.todo('debounces email input 300ms before firing query')`; `it.todo('shows "Kein Account mit dieser E-Mail gefunden" on 404')`; `it.todo('renders user card + already-linked warning on 200 with alreadyLinkedToPersonId')`; `it.todo('disables Verknüpfen button until match selected')`.

5. **Extend `apps/api/src/modules/teacher/teacher.service.spec.ts`** with an additional `describe('remove — Orphan-Guard')` block containing `it.todo(...)` stubs (implemented in Task 2):
   - `it.todo('deletes teacher + person when zero dependents (204)')`
   - `it.todo('throws ConflictException when klassenvorstandId is set on SchoolClass')`
   - `it.todo('throws ConflictException when TimetableLesson.teacherId references teacher (denormalized)')`
   - `it.todo('throws ConflictException when ClassBookEntry.teacherId references teacher')`
   - `it.todo('throws ConflictException when GradeEntry.teacherId references teacher')`
   - `it.todo('throws ConflictException when Substitution.originalTeacherId references teacher')`
   - `it.todo('throws ConflictException when Substitution.substituteTeacherId references teacher')`
   - `it.todo('payload contains extensions.affectedEntities.{klassenvorstandFor, lessonCount, classbookCount, gradeCount, substitutionCount}')`
   - `it.todo('affectedEntities.klassenvorstandFor array is capped at 50 entries')`

6. **No behavior change to existing tests** — the werteinheiten move re-exports from the same path, so `pnpm --filter @schoolflow/api test:run -- werteinheiten` stays green.
  </action>

  <acceptance_criteria>
    - `packages/shared/src/werteinheiten/werteinheiten.util.ts` exists with `calculateWerteinheiten` AND `calculateMaxTeachingHours` exported (grep)
    - `apps/api/src/modules/teacher/werteinheiten.util.ts` contains `from '@schoolflow/shared'` (pure re-export, grep)
    - `packages/shared/src/index.ts` contains `export * from './werteinheiten/index.js'` AND `export * from './schemas/teacher.schema.js'` (grep)
    - `packages/shared/src/schemas/teacher.schema.ts` exports `StammdatenSchema`, `LehrverpflichtungSchema`, `TeacherCreateSchema`, `TeacherUpdateSchema` (grep)
    - `packages/shared/src/schemas/availability.schema.ts` exports `AvailabilityRuleSchema` (grep)
    - `packages/shared/src/schemas/teaching-reduction.schema.ts` exports `TeachingReductionSchema` + enum includes `'SONSTIGES'` (grep)
    - All 5 new FE test files exist with ≥3 `it.todo(` calls each (grep `it.todo` in each file, expect count ≥3)
    - `teacher.service.spec.ts` contains new `describe('remove — Orphan-Guard'` block with ≥9 `it.todo` entries (grep)
    - `pnpm --filter @schoolflow/shared test:run -- werteinheiten` green (ported tests)
    - `pnpm --filter @schoolflow/shared test:run -- schemas` green (new Zod spec files)
    - `pnpm --filter @schoolflow/api test:run -- teacher.service` green (no regression from re-export)
    - `pnpm -r build` succeeds (TypeScript compiles across shared+api+web workspace graph)
  </acceptance_criteria>

  <verify>
    <automated>pnpm --filter @schoolflow/shared test:run && pnpm --filter @schoolflow/api test:run -- teacher.service &amp;&amp; pnpm -r build</automated>
  </verify>

  <done>
    Shared Zod schemas live in @schoolflow/shared, werteinheiten util is moved + re-exported, all Wave-0 it.todo() stubs exist, existing werteinheiten specs pass from their new home, teacher.service.spec has Orphan-Guard describe block ready for Task 2 to implement. Workspace compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: API — TeacherService.remove Orphan-Guard gap-fix + Keycloak linkage endpoints + KeycloakAdmin module (greenfield)</name>
  <files>apps/api/src/modules/teacher/teacher.service.ts, apps/api/src/modules/teacher/teacher.service.spec.ts, apps/api/src/modules/teacher/teacher.controller.ts, apps/api/src/modules/teacher/teacher.module.ts, apps/api/src/modules/teacher/dto/link-keycloak.dto.ts, apps/api/src/modules/keycloak-admin/keycloak-admin.module.ts, apps/api/src/modules/keycloak-admin/keycloak-admin.service.ts, apps/api/src/modules/keycloak-admin/keycloak-admin.service.spec.ts, apps/api/src/modules/keycloak-admin/keycloak-admin.controller.ts, apps/api/src/modules/keycloak-admin/dto/keycloak-user-query.dto.ts, apps/api/src/modules/keycloak-admin/dto/keycloak-user-response.dto.ts, apps/api/src/app.module.ts, apps/api/package.json, pnpm-lock.yaml</files>

  <read_first>
    - apps/api/src/modules/teacher/teacher.service.ts (current .remove implementation at lines 207-213 — REPLACE with Orphan-Guard)
    - apps/api/src/modules/teacher/teacher.service.spec.ts (Wave 0 stubs to turn green)
    - apps/api/src/modules/teacher/teacher.controller.ts (existing controller — add PATCH/DELETE keycloak-link endpoints)
    - apps/api/src/modules/teacher/teacher.module.ts (register new controller dependencies)
    - apps/api/prisma/schema.prisma (lines 343-451 Teacher/AvailabilityRule/TeachingReduction; lines 720-760 TimetableLesson.teacherId; line 861 ClassBookEntry.teacherId; line 911 GradeEntry.teacherId; line 1072 Substitution teacherId fields — ALL denormalized strings with NO FK)
    - apps/api/src/app.module.ts (register KeycloakAdminModule)
    - apps/api/src/modules/auth/decorators/check-permissions.decorator.ts (existing CASL @CheckPermissions pattern)
    - apps/api/src/modules/auth/guards/ (existing APP_GUARD pattern)
    - apps/api/package.json (current dependencies — add @keycloak/keycloak-admin-client)
    - .planning/phases/11-lehrer-und-f-cher-verwaltung/11-RESEARCH.md §Focus Area 3 (Keycloak endpoint design) + §Focus Area 4 (Orphan-Guard Prisma queries — full code listing)
    - .planning/phases/11-lehrer-und-f-cher-verwaltung/11-UI-SPEC.md §2.6 + §4.3 (payload shape consumed by UI)
  </read_first>

  <action>
1. **Install Keycloak admin-client dependency** in api workspace:
   ```
   pnpm --filter @schoolflow/api add @keycloak/keycloak-admin-client
   ```
   Verify `apps/api/package.json` `dependencies` now lists `@keycloak/keycloak-admin-client`. Pin to caret range (^).

2. **Rewrite `TeacherService.remove`** in `apps/api/src/modules/teacher/teacher.service.ts` (lines 207-213) per 11-RESEARCH.md §Focus 4 code listing. Add these imports at top: `import { ConflictException, Injectable, NotFoundException } from '@nestjs/common';` (add `ConflictException`).

   New `remove` implementation:
   ```ts
   async remove(id: string) {
     const existing = await this.findOne(id); // throws 404

     const [
       klassenvorstandCount,
       lessonCount,
       classbookCount,
       gradeCount,
       originalSubCount,
       substituteSubCount,
       klassenvorstandClasses,
     ] = await this.prisma.$transaction([
       this.prisma.schoolClass.count({ where: { klassenvorstandId: id } }),
       this.prisma.timetableLesson.count({ where: { teacherId: id } }),
       this.prisma.classBookEntry.count({ where: { teacherId: id } }),
       this.prisma.gradeEntry.count({ where: { teacherId: id } }),
       this.prisma.substitution.count({ where: { originalTeacherId: id } }),
       this.prisma.substitution.count({ where: { substituteTeacherId: id } }),
       this.prisma.schoolClass.findMany({
         where: { klassenvorstandId: id },
         select: { id: true, name: true },
         take: 50,
       }),
     ]);

     const totalRefs = klassenvorstandCount + lessonCount + classbookCount
                     + gradeCount + originalSubCount + substituteSubCount;

     if (totalRefs > 0) {
       throw new ConflictException({
         type: 'https://schoolflow.dev/errors/teacher-has-dependents',
         title: 'Lehrperson hat Abhängigkeiten',
         status: 409,
         detail: 'Diese Lehrperson ist noch verplant. Lösen Sie erst alle Zuordnungen.',
         extensions: {
           affectedEntities: {
             klassenvorstandFor: klassenvorstandClasses,
             lessonCount,
             classbookCount,
             gradeCount,
             substitutionCount: originalSubCount + substituteSubCount,
           },
         },
       });
     }

     await this.prisma.teacher.delete({ where: { id } });
     await this.prisma.person.delete({ where: { id: existing.personId } });
   }
   ```

   Add two new service methods after `remove`:
   ```ts
   async linkKeycloakUser(teacherId: string, keycloakUserId: string) {
     const teacher = await this.findOne(teacherId);
     return this.prisma.person.update({
       where: { id: teacher.personId },
       data: { keycloakUserId },
     });
   }

   async unlinkKeycloakUser(teacherId: string) {
     const teacher = await this.findOne(teacherId);
     return this.prisma.person.update({
       where: { id: teacher.personId },
       data: { keycloakUserId: null },
     });
   }
   ```

3. **Implement Orphan-Guard unit tests** — turn the Wave-0 `it.todo` stubs into real Vitest tests in `apps/api/src/modules/teacher/teacher.service.spec.ts`. Pattern: mock `PrismaService` with `vi.fn()` (follow existing spec structure in the same file). Each failing-case test asserts `await expect(service.remove(id)).rejects.toBeInstanceOf(ConflictException)` AND `.rejects.toMatchObject({ response: { extensions: { affectedEntities: {...} } } })`. Happy-case test asserts `prisma.teacher.delete` + `prisma.person.delete` called exactly once each.

4. **Create `apps/api/src/modules/teacher/dto/link-keycloak.dto.ts`**:
   ```ts
   import { ApiProperty } from '@nestjs/swagger';
   import { IsString, MinLength } from 'class-validator';
   export class LinkKeycloakDto {
     @ApiProperty({ description: 'Keycloak user ID (sub claim)' })
     @IsString()
     @MinLength(1)
     keycloakUserId!: string;
   }
   ```

5. **Extend `apps/api/src/modules/teacher/teacher.controller.ts`** — add after the existing `remove` handler:
   ```ts
   @Patch(':id/keycloak-link')
   @CheckPermissions({ action: 'update', subject: 'teacher' })
   @ApiOperation({ summary: 'Link a Keycloak user to this teacher' })
   async linkKeycloak(@Param('id') id: string, @Body() dto: LinkKeycloakDto) {
     return this.teacherService.linkKeycloakUser(id, dto.keycloakUserId);
   }

   @Delete(':id/keycloak-link')
   @HttpCode(HttpStatus.NO_CONTENT)
   @CheckPermissions({ action: 'update', subject: 'teacher' })
   @ApiOperation({ summary: 'Remove the Keycloak link on this teacher' })
   async unlinkKeycloak(@Param('id') id: string) {
     await this.teacherService.unlinkKeycloakUser(id);
   }
   ```
   Add `Patch` to the imports from `@nestjs/common`.

6. **Create KeycloakAdmin module** under `apps/api/src/modules/keycloak-admin/`:

   `keycloak-admin.service.ts`:
   ```ts
   import { Injectable, Logger, OnModuleInit } from '@nestjs/common';
   import { ConfigService } from '@nestjs/config';
   import KeycloakAdminClient from '@keycloak/keycloak-admin-client';
   import { PrismaService } from '../../config/database/prisma.service';

   @Injectable()
   export class KeycloakAdminService implements OnModuleInit {
     private readonly logger = new Logger(KeycloakAdminService.name);
     private client!: KeycloakAdminClient;
     private tokenExpiresAt = 0;

     constructor(
       private readonly config: ConfigService,
       private readonly prisma: PrismaService,
     ) {}

     onModuleInit() {
       this.client = new KeycloakAdminClient({
         baseUrl: this.config.getOrThrow<string>('KEYCLOAK_URL'),
         realmName: this.config.getOrThrow<string>('KEYCLOAK_REALM'),
       });
     }

     private async ensureAuth(): Promise<void> {
       if (Date.now() < this.tokenExpiresAt - 30_000) return; // refresh 30s before expiry
       await this.client.auth({
         grantType: 'client_credentials',
         clientId: this.config.getOrThrow<string>('KEYCLOAK_ADMIN_CLIENT_ID'),
         clientSecret: this.config.getOrThrow<string>('KEYCLOAK_ADMIN_CLIENT_SECRET'),
       });
       // Default Keycloak admin-client token TTL is 5min; cache lazily
       this.tokenExpiresAt = Date.now() + 5 * 60 * 1000;
     }

     async findUsersByEmail(email: string) {
       await this.ensureAuth();
       const users = await this.client.users.find({ email, exact: false, max: 10 });
       // Enrich each with alreadyLinkedToPersonId lookup (Prisma side)
       const enriched = await Promise.all(
         users.map(async (u) => {
           const existing = u.id
             ? await this.prisma.person.findUnique({
                 where: { keycloakUserId: u.id },
                 select: { id: true, firstName: true, lastName: true },
               })
             : null;
           return {
             id: u.id!,
             email: u.email ?? '',
             firstName: u.firstName ?? '',
             lastName: u.lastName ?? '',
             enabled: u.enabled ?? true,
             alreadyLinkedToPersonId: existing?.id,
             alreadyLinkedToPersonName: existing
               ? `${existing.firstName} ${existing.lastName}`
               : undefined,
           };
         }),
       );
       return enriched;
     }
   }
   ```

   `keycloak-admin.controller.ts`:
   ```ts
   import { Controller, Get, Query } from '@nestjs/common';
   import { ApiBearerAuth, ApiTags } from '@nestjs/swagger';
   import { Throttle } from '@nestjs/throttler';
   import { KeycloakAdminService } from './keycloak-admin.service';
   import { KeycloakUserQueryDto } from './dto/keycloak-user-query.dto';
   import { CheckPermissions } from '../auth/decorators/check-permissions.decorator';

   @ApiTags('keycloak-admin')
   @ApiBearerAuth()
   @Controller('admin/keycloak')
   export class KeycloakAdminController {
     constructor(private readonly service: KeycloakAdminService) {}

     @Get('users')
     @CheckPermissions({ action: 'manage', subject: 'teacher' })
     @Throttle({ default: { ttl: 60_000, limit: 30 } })
     async findUsers(@Query() dto: KeycloakUserQueryDto) {
       return this.service.findUsersByEmail(dto.email);
     }
   }
   ```

   `dto/keycloak-user-query.dto.ts`:
   ```ts
   import { ApiProperty } from '@nestjs/swagger';
   import { IsEmail, IsString, MinLength } from 'class-validator';
   export class KeycloakUserQueryDto {
     @ApiProperty({ description: 'Email fragment (>=3 chars)' })
     @IsString()
     @MinLength(3)
     email!: string;
   }
   ```

   `dto/keycloak-user-response.dto.ts`:
   ```ts
   import { ApiProperty, ApiPropertyOptional } from '@nestjs/swagger';
   export class KeycloakUserResponseDto {
     @ApiProperty() id!: string;
     @ApiProperty() email!: string;
     @ApiProperty() firstName!: string;
     @ApiProperty() lastName!: string;
     @ApiProperty() enabled!: boolean;
     @ApiPropertyOptional() alreadyLinkedToPersonId?: string;
     @ApiPropertyOptional() alreadyLinkedToPersonName?: string;
   }
   ```

   `keycloak-admin.module.ts`:
   ```ts
   import { Module } from '@nestjs/common';
   import { KeycloakAdminController } from './keycloak-admin.controller';
   import { KeycloakAdminService } from './keycloak-admin.service';
   @Module({
     controllers: [KeycloakAdminController],
     providers: [KeycloakAdminService],
     exports: [KeycloakAdminService],
   })
   export class KeycloakAdminModule {}
   ```

7. **Register `KeycloakAdminModule`** in `apps/api/src/app.module.ts` imports array alongside existing modules (e.g. after `TeacherModule`).

8. **Unit-test `KeycloakAdminService`** in `keycloak-admin.service.spec.ts`:
   - `it('caches service-account token across calls (no re-auth if within TTL)')` — mock `client.auth` and assert called once across 2 consecutive `findUsersByEmail` calls.
   - `it('re-auths when token TTL expires')` — advance `Date.now` past expiry, assert `client.auth` called 2×.
   - `it('enriches results with alreadyLinkedToPersonId via Prisma lookup')` — mock Prisma `person.findUnique` to return `{id, firstName, lastName}`, assert response carries `alreadyLinkedToPersonId` + name.
   - `it('returns empty array when no users match')`.

9. **No migration needed** — zero schema changes (Subject.color descoped per 11-CONTEXT.md post-research; keycloakUserId already exists on Person).
  </action>

  <acceptance_criteria>
    - `apps/api/package.json` dependencies list contains `@keycloak/keycloak-admin-client` (grep)
    - `apps/api/src/modules/teacher/teacher.service.ts` contains `ConflictException` AND `affectedEntities` AND `klassenvorstandFor` (grep all three)
    - `apps/api/src/modules/teacher/teacher.service.ts` contains `linkKeycloakUser` AND `unlinkKeycloakUser` (grep)
    - `apps/api/src/modules/teacher/teacher.controller.ts` contains `PATCH.*keycloak-link` OR `@Patch.*keycloak-link` (grep) AND `@Delete.*keycloak-link`
    - `apps/api/src/modules/keycloak-admin/keycloak-admin.service.ts` exports `KeycloakAdminService` AND contains `findUsersByEmail` AND `ensureAuth` (grep)
    - `apps/api/src/modules/keycloak-admin/keycloak-admin.controller.ts` contains `@Controller('admin/keycloak')` AND `@Throttle` (grep)
    - `apps/api/src/app.module.ts` imports `KeycloakAdminModule` (grep)
    - `pnpm --filter @schoolflow/api test:run -- teacher.service.spec` — all Orphan-Guard describe tests green (minimum 9 assertions)
    - `pnpm --filter @schoolflow/api test:run -- keycloak-admin.service.spec` — all 4 tests green
    - `pnpm --filter @schoolflow/api build` succeeds
    - `pnpm --filter @schoolflow/api lint` green
  </acceptance_criteria>

  <verify>
    <automated>pnpm --filter @schoolflow/api test:run -- teacher.service keycloak-admin.service &amp;&amp; pnpm --filter @schoolflow/api build</automated>
  </verify>

  <done>
    TeacherService.remove throws ConflictException with RFC 9457 extensions.affectedEntities on dependents (TEACHER-06 gap-fix closed). New KeycloakAdmin module with GET /admin/keycloak/users endpoint live. Teacher controller has PATCH/DELETE :id/keycloak-link endpoints. All unit tests green. Existing tests non-regressed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Web — /admin/teachers routes, 4-tab detail page, sidebar grouping refactor, all hooks + components (TEACHER-01..06 UI)</name>
  <files>apps/web/src/components/layout/AppSidebar.tsx, apps/web/src/components/layout/MobileSidebar.tsx, apps/web/src/routes/_authenticated/admin/teachers.index.tsx, apps/web/src/routes/_authenticated/admin/teachers.$teacherId.tsx, apps/web/src/hooks/useTeachers.ts, apps/web/src/hooks/useKeycloakUsers.ts, apps/web/src/components/admin/teacher/TeacherListTable.tsx, apps/web/src/components/admin/teacher/TeacherMobileCards.tsx, apps/web/src/components/admin/teacher/TeacherCreateDialog.tsx, apps/web/src/components/admin/teacher/TeacherDetailTabs.tsx, apps/web/src/components/admin/teacher/StammdatenTab.tsx, apps/web/src/components/admin/teacher/StammdatenTab.test.tsx, apps/web/src/components/admin/teacher/LehrverpflichtungTab.tsx, apps/web/src/components/admin/teacher/LehrverpflichtungTab.test.tsx, apps/web/src/components/admin/teacher/VerfuegbarkeitsGrid.tsx, apps/web/src/components/admin/teacher/VerfuegbarkeitsGrid.test.tsx, apps/web/src/components/admin/teacher/VerfuegbarkeitsMobileList.tsx, apps/web/src/components/admin/teacher/ErmaessigungenList.tsx, apps/web/src/components/admin/teacher/ErmaessigungenList.test.tsx, apps/web/src/components/admin/teacher/KeycloakLinkSection.tsx, apps/web/src/components/admin/teacher/KeycloakLinkDialog.tsx, apps/web/src/components/admin/teacher/KeycloakLinkDialog.test.tsx, apps/web/src/components/admin/teacher/ArchiveTeacherDialog.tsx, apps/web/src/components/admin/teacher/DeleteTeacherDialog.tsx, apps/web/src/components/admin/teacher/AffectedEntitiesList.tsx, apps/web/src/lib/api.ts</files>

  <read_first>
    - apps/web/src/components/layout/AppSidebar.tsx (current flat navItems — refactor to support grouping)
    - apps/web/src/components/layout/MobileSidebar.tsx (same grouping refactor)
    - apps/web/src/routes/_authenticated/admin/school.settings.tsx (reference for TanStack Router admin route pattern, PageShell usage, tabs state-machine)
    - apps/web/src/components/admin/school-settings/TimeGridTab.tsx (reference for per-tab RHF + zodResolver + isDirty + StickyMobileSaveBar wiring)
    - apps/web/src/components/admin/school-settings/SchoolDetailsTab.tsx (reference for simple StammdatenTab-like form)
    - apps/web/src/components/admin/shared/PageShell.tsx (props contract)
    - apps/web/src/components/admin/shared/UnsavedChangesDialog.tsx (dirty-nav dialog contract)
    - apps/web/src/components/admin/shared/StickyMobileSaveBar.tsx (sticky save bar contract)
    - apps/web/src/components/admin/shared/InfoBanner.tsx
    - apps/web/src/components/admin/shared/WarnDialog.tsx
    - apps/web/src/lib/api.ts (apiFetch + parseProblemJson — extend or verify .extensions access)
    - apps/web/src/hooks/useResources.ts (reference pattern for useQuery + useMutation + onError silent-4xx wiring)
    - apps/web/src/hooks/useClassbook.ts (additional hook pattern reference)
    - apps/web/src/stores/school-context-store.ts (useSchoolContext().schoolId)
    - apps/web/src/components/ui/tabs.tsx, dialog.tsx, select.tsx, input.tsx, button.tsx, card.tsx, label.tsx, dropdown-menu.tsx, scroll-area.tsx, tooltip.tsx, separator.tsx, badge.tsx, toggle.tsx (shadcn primitives already installed)
    - .planning/phases/11-lehrer-und-f-cher-verwaltung/11-UI-SPEC.md §1 (PageShell/sidebar), §2 (Teacher UI), §4 (Orphan-Guard dialog), §6 (query keys), §7 (reused components), §8 (accessibility), §11 (copy contract)
    - .planning/phases/11-lehrer-und-f-cher-verwaltung/11-RESEARCH.md §Focus 1 (Week-Grid state model)
  </read_first>

  <action>
**This task is large (~25 files) but is the ONLY way to deliver TEACHER-01..06 as one coherent feature — splitting into sub-tasks would orphan RHF+dialog wiring. Target ~35% context; stop and snapshot if exceeding 45%.**

Implement in this order to keep incremental builds green:

**A. Sidebar grouping refactor** (AppSidebar + MobileSidebar). Extend `NavItem` with `group?: string` and optional `GROUPS: Record<string, { label: string; order: number }>`. Render ungrouped items first, then each group label (uppercase-small-caps separator row `px-3 pt-4 pb-1 text-xs font-medium uppercase tracking-wider text-muted-foreground`) followed by its children. Add two new items tagged `group: 'personalAndFaecher'`:
   - `{ label: 'Lehrer', href: '/admin/teachers', icon: GraduationCap, roles: ['admin','schulleitung'], group: 'personalAndFaecher' }`
   - (Leave the Fächer entry as a comment placeholder with a TODO referencing Plan 11-02 — 11-02 will append the second entry to avoid merge conflict per 11-RESEARCH §Focus 5 merge discipline.)

**B. TanStack-Query hooks** `apps/web/src/hooks/useTeachers.ts` + `useKeycloakUsers.ts`. All mutations MUST wire explicit `onError` that calls `toast.error(problemJson?.detail ?? 'Speichern fehlgeschlagen...')` — per Silent-4xx invariant. Query keys per 11-UI-SPEC §6.1.

   ```ts
   export function useTeachers(schoolId: string, filters: {search?: string; fachId?: string; status?: string}) {
     return useQuery({
       queryKey: ['teachers', schoolId, filters],
       queryFn: async () => {
         const res = await apiFetch(`/teachers?schoolId=${schoolId}&${new URLSearchParams({...filters}).toString()}`);
         if (!res.ok) throw await parseProblemJson(res);
         return res.json();
       },
       enabled: !!schoolId,
     });
   }
   export function useCreateTeacher(schoolId: string) {
     const qc = useQueryClient();
     return useMutation({
       mutationFn: async (dto: TeacherCreateInput) => { /* POST /teachers */ },
       onSuccess: () => { qc.invalidateQueries({queryKey:['teachers', schoolId]}); toast.success('Lehrperson angelegt.'); },
       onError: (err: ProblemJson) => { toast.error(err?.detail ?? 'Speichern fehlgeschlagen. Bitte prüfen Sie Ihre Eingaben.'); },
     });
   }
   // + useTeacher(id), useUpdateTeacher(id), useDeleteTeacher(id), useLinkKeycloak(id), useUnlinkKeycloak(id)
   ```

   `useKeycloakUsers({email})`:
   ```ts
   return useQuery({
     queryKey: ['keycloak-users', {email}],
     enabled: email.length >= 3,
     staleTime: 30_000,
     queryFn: async () => {
       const res = await apiFetch(`/admin/keycloak/users?email=${encodeURIComponent(email)}`);
       if (!res.ok) throw await parseProblemJson(res);
       return res.json();
     },
   });
   ```

**C. Ensure `apps/web/src/lib/api.ts` `parseProblemJson`** returns `{type, title, status, detail, extensions}` (check if extensions is already parsed; if not, add it). DeleteTeacherDialog consumes `problem.extensions.affectedEntities`.

**D. Route shell** `teachers.index.tsx`:
   - PageShell header "Lehrerinnen und Lehrer" + sub-line per 11-UI-SPEC §1.3
   - Header controls row (search/Fach/Status filter + `+ Lehrperson anlegen` button)
   - `<TeacherListTable />` (desktop, `hidden md:block`) + `<TeacherMobileCards />` (mobile, `md:hidden`)
   - Empty-state (unfiltered): `GraduationCap` 48px + heading "Noch keine Lehrerinnen und Lehrer" + body + `[Erste Lehrperson anlegen]` button
   - Empty-state (filtered): `SearchX` hero + "Keine Treffer" + "Filter zurücksetzen" button
   - `<TeacherCreateDialog />` controlled by local state

**E. TeacherListTable** desktop table per 11-UI-SPEC §2.1.2: 6 columns (Name, Fächer, Werteinheiten, Status, Keycloak icon, Aktion dropdown). Row click → `navigate({to: '/admin/teachers/$teacherId', params:{teacherId: row.id}, search:{tab:'stammdaten'}})`. Dropdown `Bearbeiten | Archivieren | Reaktivieren | Löschen` (visibility per §2.1.2 rules).

**F. TeacherMobileCards** per 11-UI-SPEC §2.1.3.

**G. TeacherCreateDialog** (shadcn Dialog, sm:max-w-md) — wraps `<StammdatenTab />` in controlled-form mode with submit label `Lehrperson anlegen`. On success: close dialog + navigate to detail page.

**H. Route shell** `teachers.$teacherId.tsx`:
   - `search: z.object({ tab: z.enum(['stammdaten','verpflichtung','verfuegbarkeit','ermaessigungen']).default('stammdaten') })` validated search param
   - PageShell dynamic title `{firstName} {lastName}` + sub-line per 11-UI-SPEC §1.3
   - `<TeacherDetailTabs teacher={teacher} activeTab={search.tab} />`

**I. TeacherDetailTabs** — shadcn Tabs desktop `>=md`, mobile Select `<md`. 4 tab panels render their respective components. Each tab owns its own RHF form + isDirty → triggers UnsavedChangesDialog on nav.

**J. StammdatenTab** — RHF form + zodResolver(StammdatenSchema), fields per 11-UI-SPEC §2.2 (Vorname, Nachname, Titel, Email, Phone, Status). Includes `<Separator className="my-6" />` then `<KeycloakLinkSection />`. Save button right-aligned desktop, StickyMobileSaveBar mobile. Make the tests in `StammdatenTab.test.tsx` green (convert `it.todo` → `it`).

**K. LehrverpflichtungTab** — RHF form + zodResolver(LehrverpflichtungSchema). Two-column grid (`grid-cols-1 md:grid-cols-2 gap-8`):
   - Left: Beschäftigungsgrad number (0-100 + % suffix), OEPU-Gruppe select (9 groups from LEHRVERPFLICHTUNGSGRUPPEN keys), OEPU-Funktionsstunden number, Zusatz-WE number, Fachzuordnungen row-add list
   - Right: Live-computed `<Card bg-muted/30>` breakdown using `calculateMaxTeachingHours` from `@schoolflow/shared/werteinheiten` — watches all inputs via `form.watch()` and recomputes synchronously
   - Solver re-run `<InfoBanner>` above save button
   - Tests green (see test file stubs from Task 1).

**L. VerfuegbarkeitsGrid** (desktop, `hidden md:block`) — per 11-RESEARCH §Focus 1:
   - State: `Set<CellKey>` where `CellKey = \`${DayOfWeek}-${PeriodNumber}\``
   - `<table role="grid">` with `<caption className="sr-only">Verfügbarkeits-Raster — {N} Perioden × {M} Tage</caption>`
   - Cells: `<button role="gridcell" aria-pressed={isBlocked} aria-label="{Dayname}, {PeriodNum}. Stunde, {verfügbar|geblockt}">` with `h-12` min-size
   - Blocked styling: `bg-muted` + `background-image: repeating-linear-gradient(45deg, transparent 0 4px, var(--color-muted-foreground) 4px 5px)` + `<Lock className="h-4 w-4" />` icon
   - Keyboard nav: local `focusedKey` state; onKeyDown handles `ArrowUp|ArrowDown|ArrowLeft|ArrowRight|Home|End|Space|Enter`
   - Column/row headers: clickable buttons that call `bulkToggle('col',day)` / `bulkToggle('row',periodNum)`; show AlertDialog confirm when `blockedCount >= 3` in target
   - Load: flatten `AvailabilityRule[]` where `ruleType==='UNAVAILABLE'` → for each `periodNumber` in `periodNumbers[]`, add `${dayOfWeek}-${periodNumber}` to the Set
   - Save: group back into one rule per day with ruleType='UNAVAILABLE', periodNumbers sorted, isHard=true
   - Uses `useTimeGrid(schoolId)` for periods and active schoolDays; empty-TimeGrid edge case renders `<InfoBanner>` with deep-link to `/admin/school/settings?tab=timegrid`

**M. VerfuegbarkeitsMobileList** (`md:hidden`) — Day-Picker shadcn Select (`h-11` full-width) labelled "Tag" + `<div>` of `h-11` rows per period. Each row uses `<Toggle className="h-11 w-11" aria-label="...">` with `Lock` icon. Per 11-UI-SPEC §2.4.2 — NO swipe.

**N. ErmaessigungenList** — RHF + `useFieldArray` for rows. Each row: Grund Select (6 enum values) + Anmerkung Input (required when Grund=SONSTIGES, validated via Zod refine in shared schema) + WE Number + `Trash2` button. `+ Ermäßigung hinzufügen` button at bottom. Summary row `{count} Ermäßigung(en) · Gesamt {sum} WE`. Save sends full reductions array (replace-all per Phase 2 D-04).

**O. KeycloakLinkSection** — renders Unlinked (dashed-border box + CTA `Keycloak-Account verknüpfen`) OR Linked (primary-tinted box + email + `Verknüpfung lösen` ghost-destructive button). Click CTA → open `<KeycloakLinkDialog />`; click unlink → open `<WarnDialog />` with copy from 11-UI-SPEC §2.6.3.

**P. KeycloakLinkDialog** — email Input autoFocus; debounced 300ms via `useDebounce` or `setTimeout` in effect; calls `useKeycloakUsers({email})`. State machine: Idle (empty/<3 chars) | Searching (isLoading) | NoMatch (404) | Match (200). On Match: user-card with firstName/lastName + email + Keycloak-ID first 8 chars + `alreadyLinkedToPersonName` amber warning if set. Footer: Abbrechen (ghost autoFocus) + Verknüpfen (primary, disabled until match loaded). On confirm: `useLinkKeycloak(teacherId).mutate({keycloakUserId: match.id})`.

**Q. ArchiveTeacherDialog** — reuses `<WarnDialog>` from Phase 10 shared with canonical copy from 11-UI-SPEC §7.2 (title "Lehrperson archivieren?", body per spec, confirm label "Archivieren" NOT destructive).

**R. DeleteTeacherDialog** — shadcn Dialog with 2 states:
   - Happy-state (pre-call or 2xx on DELETE): AlertTriangle amber + body + `Abbrechen` + `Löschen` (destructive) buttons
   - Blocked-state (409 received): title changes to "Lehrperson kann nicht gelöscht werden"; AlertTriangle red; renders `<AffectedEntitiesList entities={problem.extensions.affectedEntities} />`; footer single `Schließen` button
   - State transition: DO NOT close dialog on 409. Handle via mutation `onError` that inspects `err.status === 409` and sets local `blocked` state.
   - Also fires sonner `toast.error` with title + body per 11-UI-SPEC §4.3 "Also-surfaced toast".

**S. AffectedEntitiesList** — renders grouped lists inside `<ScrollArea className="max-h-64">`. Group labels per 11-UI-SPEC §4.3 table (teacherSubjects → "Fachzuordnungen", timetableLessons → "Stundenplan-Einträge", etc.). Deep-link to `/admin/teachers/{teacherId}` for `klassenvorstandFor`; disabled link with tooltip `Verfügbar ab Phase 12` for classbook/homework/exam categories.

**T. Convert Wave-0 it.todo stubs to real tests** (StammdatenTab, LehrverpflichtungTab, VerfuegbarkeitsGrid, ErmaessigungenList, KeycloakLinkDialog) — use `@testing-library/react` + `@testing-library/user-event`. Each described case gets a real assertion.

**Copy compliance:** Every German string MUST match 11-UI-SPEC §11 Copywriting Contract verbatim (`Lehrperson anlegen`, `Speichern`, `Lehrperson archivieren?`, `Kein Account mit dieser E-Mail gefunden`, etc.). Checker will grep for these.
  </action>

  <acceptance_criteria>
    - `apps/web/src/routes/_authenticated/admin/teachers.index.tsx` exists with `/admin/teachers` route exported (grep `createFileRoute.*admin/teachers` or equivalent)
    - `apps/web/src/routes/_authenticated/admin/teachers.$teacherId.tsx` exists with `tab` search-param schema (grep `z.enum.*stammdaten`)
    - `apps/web/src/components/layout/AppSidebar.tsx` contains `GraduationCap` AND `Personal & Fächer` AND `/admin/teachers` (grep all three)
    - `apps/web/src/components/layout/MobileSidebar.tsx` contains `Personal & Fächer` AND `/admin/teachers` (grep)
    - `apps/web/src/hooks/useTeachers.ts` exports `useTeachers`, `useTeacher`, `useCreateTeacher`, `useUpdateTeacher`, `useDeleteTeacher`, `useLinkKeycloak`, `useUnlinkKeycloak` (grep each name)
    - `apps/web/src/hooks/useTeachers.ts` contains at least 5 occurrences of `onError:` wiring (grep — one per mutation, silent-4xx invariant)
    - `apps/web/src/hooks/useKeycloakUsers.ts` contains `enabled: email.length >= 3` AND `staleTime: 30_000` (grep)
    - `VerfuegbarkeitsGrid.tsx` contains `role="grid"` AND `aria-pressed` AND `repeating-linear-gradient(45deg` (grep)
    - `VerfuegbarkeitsMobileList.tsx` contains `h-11` (44px touch target) AND uses `<Select` for day picker (grep)
    - `ErmaessigungenList.tsx` contains `SONSTIGES` AND `KLASSENVORSTAND` AND `Ermäßigung hinzufügen` (grep)
    - `KeycloakLinkDialog.tsx` contains `Kein Account mit dieser E-Mail gefunden` AND `alreadyLinkedToPerson` (grep)
    - `DeleteTeacherDialog.tsx` contains `Lehrperson kann nicht gelöscht werden` AND `extensions.affectedEntities` (grep)
    - `AffectedEntitiesList.tsx` contains `Verfügbar ab Phase 12` AND group labels `Stundenplan-Einträge` AND `Klassenbuch-Einträge` (grep)
    - `LehrverpflichtungTab.tsx` imports `calculateMaxTeachingHours` from `@schoolflow/shared` (grep `from ['"]@schoolflow/shared['"]` near `calculateMaxTeachingHours`)
    - `pnpm --filter @schoolflow/web test:run -- StammdatenTab` green
    - `pnpm --filter @schoolflow/web test:run -- LehrverpflichtungTab` green
    - `pnpm --filter @schoolflow/web test:run -- VerfuegbarkeitsGrid` green
    - `pnpm --filter @schoolflow/web test:run -- ErmaessigungenList` green
    - `pnpm --filter @schoolflow/web test:run -- KeycloakLinkDialog` green
    - `pnpm --filter @schoolflow/web build` succeeds (TSC + Vite)
    - `pnpm --filter @schoolflow/web typecheck` green (no new TS errors; pre-existing 10.1-01 deferred 8 errors unchanged, document if any new errors)
  </acceptance_criteria>

  <verify>
    <automated>pnpm --filter @schoolflow/web test:run -- "admin/teacher" &amp;&amp; pnpm --filter @schoolflow/web build</automated>
  </verify>

  <done>
    Admin can navigate to /admin/teachers, see list or empty-state CTA, create a Lehrperson via Dialog, open detail page, edit all 4 tabs (Stammdaten + Lehrverpflichtung with live WE compute + Verfügbarkeits-Grid with keyboard nav + Ermäßigungen row-add). Keycloak link dialog debounces email search and handles 404/200/already-linked. Delete dialog handles both happy (204) and 409 (blocked with AffectedEntities list). Mobile-375 renders day-picker fallback with 44px targets. Sidebar has new "Personal & Fächer" group. All 5 FE unit-test files green. TEACHER-01..06 requirements surfaced in UI.
  </done>
</task>

</tasks>

<verification>
- `pnpm -r build` succeeds across shared + api + web workspace
- `pnpm --filter @schoolflow/shared test:run` — werteinheiten + 3 schema specs green
- `pnpm --filter @schoolflow/api test:run -- teacher.service keycloak-admin` — Orphan-Guard + token caching green
- `pnpm --filter @schoolflow/web test:run -- "admin/teacher"` — 5 new component specs green
- Manual smoke in dev: `pnpm dev` → login as admin → /admin/teachers renders; create → detail page loads; 4 tabs switch without losing state; archive + delete dialogs work
- Silent-4xx invariant: mock API to return 422 on create → red toast shown, NO green toast (E2E asserted in Plan 11-03)
</verification>

<success_criteria>
- TEACHER-01 green: list with search/filter + empty-state CTA ✓
- TEACHER-02 green: create/edit via dialog + detail page ✓
- TEACHER-03 green: live-computed WE total, FE/BE byte-identical via shared util ✓
- TEACHER-04 green: visual week-grid desktop + day-picker mobile, keyboard nav, hatch pattern ✓
- TEACHER-05 green: row-add Ermäßigungen with replace-all save ✓
- TEACHER-06 green: Orphan-Guard 409 with affectedEntities extensions — blocks silent zombification of denormalized-FK history ✓
- Sidebar has "Personal & Fächer" group (role-gated admin|schulleitung) ✓
- Shared Zod schemas + werteinheiten util live under @schoolflow/shared ✓
- Silent-4xx invariant preserved: every mutation hook has explicit onError wiring ✓
- No new Prisma migration (Subject color descoped; keycloakUserId column already exists) ✓
</success_criteria>

<output>
After completion, create `.planning/phases/11-lehrer-und-f-cher-verwaltung/11-01-SUMMARY.md` documenting: Orphan-Guard tests (9 passing), werteinheiten-util path, Keycloak module env vars required, sidebar grouping refactor ADR, any pre-existing TSC/ESLint errors encountered, files touched count, and outstanding items for Plan 11-02 (Fächer sidebar entry append, AffectedEntitiesList reuse for Subject variants).
</output>
