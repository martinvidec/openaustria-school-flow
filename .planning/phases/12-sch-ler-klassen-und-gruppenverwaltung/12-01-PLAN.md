---
phase: 12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Shared Zod schemas + exports (Wave 0)
  - packages/shared/src/schemas/student.schema.ts
  - packages/shared/src/schemas/student.schema.spec.ts
  - packages/shared/src/schemas/parent.schema.ts
  - packages/shared/src/schemas/parent.schema.spec.ts
  - packages/shared/src/schemas/assign-parent.schema.ts
  - packages/shared/src/schemas/assign-parent.schema.spec.ts
  - packages/shared/src/schemas/move-student.schema.ts
  - packages/shared/src/schemas/move-student.schema.spec.ts
  - packages/shared/src/index.ts
  # Backend: migrations
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/20260424000000_student_archive_flag/migration.sql
  - apps/api/prisma/seed.ts
  # Backend: Student gap-fixes (archive, orphan-guard, parentIds)
  - apps/api/src/modules/student/student.controller.ts
  - apps/api/src/modules/student/student.service.ts
  - apps/api/src/modules/student/student.service.spec.ts
  - apps/api/src/modules/student/student.module.ts
  - apps/api/src/modules/student/dto/create-student.dto.ts
  - apps/api/src/modules/student/dto/update-student.dto.ts
  - apps/api/src/modules/student/dto/student-list-query.dto.ts
  - apps/api/src/modules/student/dto/assign-parent.dto.ts
  # Backend: Parent module (greenfield)
  - apps/api/src/modules/parent/parent.module.ts
  - apps/api/src/modules/parent/parent.service.ts
  - apps/api/src/modules/parent/parent.service.spec.ts
  - apps/api/src/modules/parent/parent.controller.ts
  - apps/api/src/modules/parent/dto/create-parent.dto.ts
  - apps/api/src/modules/parent/dto/update-parent.dto.ts
  - apps/api/src/modules/parent/dto/parent-list-query.dto.ts
  - apps/api/src/modules/parent/dto/parent-response.dto.ts
  - apps/api/src/app.module.ts
  # Frontend: hooks + routes + components
  - apps/web/src/hooks/useStudents.ts
  - apps/web/src/hooks/useParents.ts
  - apps/web/src/routes/_authenticated/admin/students.index.tsx
  - apps/web/src/routes/_authenticated/admin/students.$studentId.tsx
  - apps/web/src/components/admin/student/StudentListTable.tsx
  - apps/web/src/components/admin/student/StudentMobileCards.tsx
  - apps/web/src/components/admin/student/StudentFilterBar.tsx
  - apps/web/src/components/admin/student/StudentCreateDialog.tsx
  - apps/web/src/components/admin/student/StudentDetailTabs.tsx
  - apps/web/src/components/admin/student/StudentStammdatenTab.tsx
  - apps/web/src/components/admin/student/StudentStammdatenTab.test.tsx
  - apps/web/src/components/admin/student/StudentParentsTab.tsx
  - apps/web/src/components/admin/student/StudentParentsTab.test.tsx
  - apps/web/src/components/admin/student/StudentGroupsTab.tsx
  - apps/web/src/components/admin/student/ArchiveStudentDialog.tsx
  - apps/web/src/components/admin/student/RestoreStudentDialog.tsx
  - apps/web/src/components/admin/student/MoveStudentDialog.tsx
  - apps/web/src/components/admin/student/MoveStudentDialog.test.tsx
  - apps/web/src/components/admin/student/ParentSearchPopover.tsx
  - apps/web/src/components/admin/student/InlineCreateParentForm.tsx
  - apps/web/src/components/admin/student/DeleteStudentDialog.tsx
  # Shared UI: AffectedEntitiesList extension + shadcn primitives
  - apps/web/src/components/admin/teacher/AffectedEntitiesList.tsx
  - apps/web/src/components/ui/command.tsx
  - apps/web/src/components/ui/checkbox.tsx
  # Sidebar
  - apps/web/src/components/layout/AppSidebar.tsx
  - apps/web/src/components/layout/MobileSidebar.tsx
  - apps/web/package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [STUDENT-01, STUDENT-02, STUDENT-03, STUDENT-04]
requirements_addressed: [STUDENT-01, STUDENT-02, STUDENT-03, STUDENT-04]
tags: [student, parent, admin, orphan-guard, archive, move, shared-zod, migration, phase-12, d-01, d-02, d-03, d-04, d-05, d-13]

must_haves:
  truths:
    - "Admin sees Schülerliste at /admin/students as a dense table with Filter-Bar (Suche, Klassen-Dropdown inkl. 'Ohne Stammklasse', Status-Toggle Aktiv/Archiviert/Alle, Schuljahr-Dropdown default=aktiv); empty-state inline CTA 'Schüler:in anlegen' when zero students (STUDENT-01, D-01)"
    - "Row-click on student list navigates to /admin/students/$studentId; multi-select header/row checkboxes enable floating bulk-move toolbar 'Ausgewählte verschieben' when selection > 0 (D-01, D-05)"
    - "Student-Detail-Page renders 3 Tabs (Stammdaten | Erziehungsberechtigte | Gruppen) with Pro-Tab-Save, UnsavedChangesDialog on tab-switch with dirty state, and StickyMobileSaveBar on <640px (STUDENT-02, D-02)"
    - "Admin kann Schüler anlegen via StudentCreateDialog with Person-fields + classId picker + optional parentIds array; POST /students with parentIds creates Student + ParentStudent links in a single Transaktion (STUDENT-01, STUDENT-02, D-02, D-03, D-13.1)"
    - "Erziehungsberechtigte-Tab shows Parent-Liste with ParentSearchPopover (Command-Popover, Email-Autocomplete, 300ms debounce, GET /parents?email=) + Inline-Create-Fallback (Mini-Form firstName/lastName/email/phone → POST /parents with personType=PARENT + link via POST /students/:id/parents in single tx); Remove via Icon + Confirm-Dialog (parent record preserved if other children linked) (STUDENT-02, D-03, D-13.1)"
    - "STUDENT-04 Archivierung: Student.isArchived Boolean + archivedAt DateTime? via Prisma migration `20260424000000_student_archive_flag`; POST /students/:id/archive sets flag+archivedAt=now, POST /students/:id/restore clears both; findAll accepts ?archived=active|archived|all (default=active); UI row-action 'Archivieren'/'Reaktivieren' with WarnDialog confirmations (STUDENT-04, D-04, D-13.2)"
    - "STUDENT-03 Klassen-Umzug: (1) Single-row-action 'In andere Klasse verschieben' opens MoveStudentDialog mode='single' with Klassen-Picker + optional Notiz; (2) Multi-select toolbar 'Ausgewählte verschieben' opens same dialog mode='bulk' with avatar-stack preview; both use PUT /students/:id with new classId, bulk runs sequential with progress toast `{done}/{total}` (STUDENT-03, D-05)"
    - "StudentService.remove Orphan-Guard: $transaction counts AttendanceRecord, GradeEntry, StudentNote, AbsenceExcuse, GroupMembership, ParentStudent, ClassBookEntry (via TimetableLesson.classSubjectId → Student.classId is NOT direct; grep-verify at Wave 0) → ConflictException RFC 9457 with extensions.affectedEntities.{attendanceCount, gradeCount, studentNoteCount, excuseCount, groupMembershipCount, parentLinkCount} when any count > 0; Unit-Test coverage for happy + each dependency type (D-13.3)"
    - "AffectedEntitiesList discriminated-union extended to support kind:'student' with StudentAffectedEntities payload (attendanceCount, gradeCount, studentNoteCount, excuseCount, groupMembershipCount, parentLinkCount); Phase 11 backward-compat default (kind?:'teacher') + kind:'subject' preserved unchanged (D-13.3)"
    - "ParentModule greenfield: Controller + Service + Module + 4 DTOs at apps/api/src/modules/parent/; endpoints GET /parents (schoolId + email + name search pagination), GET /parents/:id, POST /parents (nested Person-Create personType=PARENT), PUT /parents/:id, DELETE /parents/:id (Orphan-Guard if ParentStudent refs), POST /students/:id/parents (link), DELETE /students/:id/parents/:parentId (unlink, Parent preserved) (D-03, D-13.1)"
    - "CreateStudentDto extended with optional parentIds: string[] (@IsOptional @IsArray @IsUUID each); StudentService.create accepts parentIds and creates ParentStudent rows in the same Prisma transaction with the Student; zero parentIds == no ParentStudent rows (backward-compat) (STUDENT-02, D-13.1)"
    - "Shared Zod schemas: packages/shared/src/schemas/student.schema.ts (StudentCreateSchema, StudentUpdateSchema, StudentListFiltersSchema), parent.schema.ts (ParentCreateSchema, ParentUpdateSchema, ParentSearchSchema), assign-parent.schema.ts (AssignParentSchema, UnlinkParentSchema), move-student.schema.ts (MoveStudentSchema, BulkMoveStudentSchema) — all with deutsche Fehlermeldungen, englische API-Feldnamen, re-exported from @schoolflow/shared (D-15)"
    - "TanStack Query hooks in useStudents.ts: useStudents(schoolId, filters), useStudent(studentId), useCreateStudent, useUpdateStudent, useArchiveStudent, useRestoreStudent, useDeleteStudent, useMoveStudent, useBulkMoveStudents — AND in useParents.ts: useParentsByEmail({email, enabled}), useCreateParent, useLinkParentToStudent, useUnlinkParentFromStudent; every useMutation wires explicit onError → sonner.toast.error (Silent-4XX invariant from Phase 10.1-01/10.2-04/11-01)"
    - "shadcn primitives installed: components/ui/command.tsx + components/ui/checkbox.tsx (confirmed MISSING per 12-UI-SPEC.md §Component Inventory + RESEARCH §7); installed via `pnpm --filter @schoolflow/web exec npx shadcn@latest add command checkbox`"
    - "AppSidebar + MobileSidebar: 'Personal & Fächer' group (Phase 11 D-03) gets new entry 'Schüler:innen' with UsersRound icon → /admin/students, role-gated ['admin', 'schulleitung'], ordering: Lehrer → Fächer → Schüler (Klassen appended by Plan 12-02) (D-16)"
    - "Wave 0 TDD: 3 FE test files (StudentStammdatenTab.test.tsx, StudentParentsTab.test.tsx, MoveStudentDialog.test.tsx) + extended student.service.spec.ts with describe('remove — Orphan-Guard') + new parent.service.spec.ts authored as it.todo() stubs BEFORE implementation (Phase 11-01 Nyquist Wave-0 parity)"
    - "Migration hygiene: `apps/api/prisma/migrations/20260424000000_student_archive_flag/migration.sql` committed in same PR (CLAUDE.md hard rule, scripts/check-migration-hygiene.sh enforces); seed.ts updated to set isArchived=false on existing students + adds one archived fixture student for filter verification"
    - "German copy verbatim from 12-UI-SPEC.md §Copywriting Contract (Archivieren/Reaktivieren dialogs, Verschieben dialog Single + Bulk, Parent unlink dialog, empty states, error toasts)"
  artifacts:
    - path: "packages/shared/src/schemas/student.schema.ts"
      provides: "StudentCreateSchema, StudentUpdateSchema, StudentListFiltersSchema with deutsche error strings + englische API-Feldnamen"
      contains: "StudentCreateSchema"
    - path: "packages/shared/src/schemas/parent.schema.ts"
      provides: "ParentCreateSchema, ParentUpdateSchema, ParentSearchSchema (email/name filter)"
      contains: "ParentCreateSchema"
    - path: "packages/shared/src/schemas/assign-parent.schema.ts"
      provides: "AssignParentSchema (link existing Parent via parentId) + UnlinkParentSchema"
      contains: "AssignParentSchema"
    - path: "packages/shared/src/schemas/move-student.schema.ts"
      provides: "MoveStudentSchema (single: targetClassId + notiz?) + BulkMoveStudentSchema (studentIds[] + targetClassId + notiz?)"
      contains: "MoveStudentSchema"
    - path: "apps/api/prisma/schema.prisma"
      provides: "Student.isArchived Boolean @default(false) + Student.archivedAt DateTime? fields (SUBJECT-04 fields not in this plan — in Plan 12-02)"
      contains: "isArchived"
    - path: "apps/api/prisma/migrations/20260424000000_student_archive_flag/migration.sql"
      provides: "SQL migration ALTER TABLE students ADD COLUMN is_archived boolean NOT NULL DEFAULT false + archived_at timestamp null"
      contains: "is_archived"
    - path: "apps/api/src/modules/student/student.service.ts"
      provides: "Orphan-Guard in .remove via $transaction counts; .archive(id); .restore(id); findAll filters by ?archived=active|archived|all + class + name search; .create accepts parentIds with ParentStudent tx-link; .linkParent(studentId, parentId) + .unlinkParent(studentId, parentId)"
      contains: "ConflictException"
    - path: "apps/api/src/modules/student/student.controller.ts"
      provides: "POST /students (with parentIds), GET /students (archived filter), GET /students/:id, PUT /students/:id, DELETE /students/:id (Orphan-Guard), POST /students/:id/archive, POST /students/:id/restore, POST /students/:id/parents, DELETE /students/:id/parents/:parentId"
      contains: "@Post(':id/archive')"
    - path: "apps/api/src/modules/student/dto/create-student.dto.ts"
      provides: "Existing Person+Student fields + new parentIds?: string[] (@IsOptional @IsArray @IsUUID each, ApiPropertyOptional)"
      contains: "parentIds"
    - path: "apps/api/src/modules/student/dto/student-list-query.dto.ts"
      provides: "Extends SchoolPaginationQueryDto with archived: 'active'|'archived'|'all' (default=active), classId?, search?"
      contains: "archived"
    - path: "apps/api/src/modules/parent/parent.module.ts"
      provides: "NestJS module registering ParentController + ParentService + DB access"
      contains: "ParentController"
    - path: "apps/api/src/modules/parent/parent.service.ts"
      provides: "ParentService with create (nested Person-Create personType=PARENT), findAll (schoolId + email/name filter), findOne, update, remove (Orphan-Guard on ParentStudent refs)"
      contains: "personType: 'PARENT'"
    - path: "apps/api/src/modules/parent/parent.controller.ts"
      provides: "GET/POST/PUT/DELETE /parents + @CheckPermissions({subject:'parent'})"
      contains: "@Controller('parents')"
    - path: "apps/api/src/app.module.ts"
      provides: "AppModule imports ParentModule"
      contains: "ParentModule"
    - path: "apps/web/src/hooks/useStudents.ts"
      provides: "TanStack Query hooks + studentKeys; every mutation wires explicit onError; imports Zod schemas from @schoolflow/shared"
      contains: "onError"
    - path: "apps/web/src/hooks/useParents.ts"
      provides: "useParentsByEmail (300ms debounce + enabled when email length >= 3 + staleTime 30_000), useCreateParent, useLinkParentToStudent, useUnlinkParentFromStudent"
      contains: "useParentsByEmail"
    - path: "apps/web/src/routes/_authenticated/admin/students.index.tsx"
      provides: "Student list route: PageShell + StudentFilterBar + StudentListTable (desktop) + StudentMobileCards (<640px) + floating bulk-move toolbar + StudentCreateDialog trigger"
      contains: "StudentListTable"
    - path: "apps/web/src/routes/_authenticated/admin/students.$studentId.tsx"
      provides: "Student detail route: PageShell + StudentDetailTabs with Zod-validated ?tab= search param"
      contains: "StudentDetailTabs"
    - path: "apps/web/src/components/admin/student/StudentDetailTabs.tsx"
      provides: "3-tab container (Stammdaten / Erziehungsberechtigte / Gruppen) with per-tab dirty state + UnsavedChangesDialog"
      contains: "Stammdaten"
    - path: "apps/web/src/components/admin/student/StudentParentsTab.tsx"
      provides: "Parent-Liste + ParentSearchPopover + InlineCreateParentForm + Unlink with Confirm-Dialog"
      contains: "ParentSearchPopover"
    - path: "apps/web/src/components/admin/student/ParentSearchPopover.tsx"
      provides: "shadcn Command-Popover with 300ms debounce + no-match Inline-Create CTA"
      contains: "Command"
    - path: "apps/web/src/components/admin/student/MoveStudentDialog.tsx"
      provides: "Dialog with mode: 'single'|'bulk'; bulk shows avatar-stack + progress on apply"
      contains: "mode"
    - path: "apps/web/src/components/admin/student/ArchiveStudentDialog.tsx"
      provides: "WarnDialog wrapper for archive action with deutsche copy verbatim per 12-UI-SPEC.md"
      contains: "Archivieren"
    - path: "apps/web/src/components/admin/student/DeleteStudentDialog.tsx"
      provides: "Delete dialog that catches 409 ConflictException and renders AffectedEntitiesList with kind='student'"
      contains: "affectedEntities"
    - path: "apps/web/src/components/admin/teacher/AffectedEntitiesList.tsx"
      provides: "Discriminated union extended with kind='student' + StudentAffectedEntities (attendanceCount/gradeCount/studentNoteCount/excuseCount/groupMembershipCount/parentLinkCount)"
      contains: "StudentAffectedEntities"
    - path: "apps/web/src/components/ui/command.tsx"
      provides: "shadcn Command primitive (Command/CommandInput/CommandList/CommandItem/CommandEmpty/CommandGroup)"
      contains: "CommandInput"
    - path: "apps/web/src/components/ui/checkbox.tsx"
      provides: "shadcn Checkbox primitive (Radix Checkbox wrapper)"
      contains: "Checkbox"
    - path: "apps/web/src/components/layout/AppSidebar.tsx"
      provides: "'Personal & Fächer' group includes Schüler:innen entry with UsersRound icon → /admin/students"
      contains: "Schüler"
  key_links:
    - from: "apps/web/src/routes/_authenticated/admin/students.index.tsx"
      to: "apps/web/src/hooks/useStudents.ts"
      via: "useStudents(schoolId, {search, classId, archived, schoolYearId}) TanStack Query hook"
      pattern: "useStudents"
    - from: "apps/web/src/components/admin/student/StudentParentsTab.tsx"
      to: "apps/web/src/hooks/useParents.ts"
      via: "debounced (300ms) useParentsByEmail({email}) + useCreateParent + useLinkParentToStudent mutations"
      pattern: "useParentsByEmail"
    - from: "apps/api/src/modules/student/student.service.ts"
      to: "apps/web/src/lib/api.ts parseProblemJson"
      via: "RFC 9457 ConflictException with extensions.affectedEntities consumed by DeleteStudentDialog"
      pattern: "affectedEntities"
    - from: "apps/api/src/modules/student/student.service.ts"
      to: "apps/api/src/modules/parent/parent.service.ts"
      via: "StudentService.create + .linkParent invoke ParentStudent Prisma ops inside shared Prisma transaction"
      pattern: "parentStudent"
    - from: "apps/web/src/components/layout/AppSidebar.tsx"
      to: "/admin/students"
      via: "Grouped nav item with UsersRound icon, roles=['admin','schulleitung']"
      pattern: "UsersRound"
    - from: "packages/shared/src/schemas/student.schema.ts"
      to: "apps/web/src/components/admin/student/StudentStammdatenTab.tsx"
      via: "zodResolver(StudentUpdateSchema) on RHF useForm"
      pattern: "zodResolver"
---

<objective>
Ship the complete Schüler admin surface (STUDENT-01..04) + ParentModule greenfield + StudentService.remove Orphan-Guard gap-fix + Student.isArchived Prisma migration — per CONTEXT.md D-16 the first of 3 bundled plans.

**Purpose:** Close STUDENT-01..04 with a production-ready admin UI (list + detail + archive + move + parent-link) and fix three v1.0 gaps surfaced in 12-RESEARCH.md §2.1–2.3: (1) missing Parent HTTP surface (ParentModule doesn't exist), (2) missing Student archivierung schema + endpoints, (3) missing Orphan-Guard in StudentService.remove which currently silently deletes Person + cascades records across Attendance/Grade/StudentNote/Excuse/GroupMembership/ParentStudent.

**Output:**
- SHARED: Zod schemas (student, parent, assign-parent, move-student) with deutsche Fehler + englische API-Feldnamen, re-exported from @schoolflow/shared
- API: Student.isArchived migration; StudentService.archive/.restore/.remove Orphan-Guard/.linkParent/.unlinkParent + findAll archived filter; ParentModule greenfield (controller + service + 4 DTOs + module); Student controller extensions (POST archive/restore + parent link/unlink); CreateStudentDto.parentIds option
- WEB: /admin/students + /admin/students/$studentId routes, 3 detail tabs (Stammdaten/Erziehungsberechtigte/Gruppen-read-mostly), StudentCreateDialog, Archive/Restore/Move/Delete dialogs, ParentSearchPopover + InlineCreateParentForm, Multi-select bulk-move toolbar, AppSidebar group entry Schüler
- SHARED-UI: AffectedEntitiesList extended with kind='student'; shadcn Command + Checkbox primitives installed
- TESTS: Wave 0 TDD stubs (3 FE component test files + extended student.service.spec + new parent.service.spec) + Vitest impls

**Wave:** 1 (no dependencies — 12-02 depends on this for sidebar group, AffectedEntitiesList kind-union, shadcn primitives, shared Zod foundation; 12-03 E2E depends on merged 12-01 + 12-02).

Scope boundaries:
- GroupDerivationRule model + CRUD → Plan 12-02 (arrives with class-side gap-fix)
- ClassService.remove Orphan-Guard → Plan 12-02
- SUBJECT-04 Wochenstunden + Apply-Stundentafel → Plan 12-02
- Class-CRUD UI + Gruppen-Tab → Plan 12-02
- All Playwright specs → Plan 12-03
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-sch-ler-klassen-und-gruppenverwaltung/12-CONTEXT.md
@.planning/phases/12-sch-ler-klassen-und-gruppenverwaltung/12-RESEARCH.md
@.planning/phases/12-sch-ler-klassen-und-gruppenverwaltung/12-UI-SPEC.md
@.planning/phases/12-sch-ler-klassen-und-gruppenverwaltung/12-VALIDATION.md

# Phase 11 plan set — this phase continues the same patterns
@.planning/phases/11-lehrer-und-f-cher-verwaltung/11-01-PLAN.md
@.planning/phases/11-lehrer-und-f-cher-verwaltung/11-02-PLAN.md
@.planning/phases/11-lehrer-und-f-cher-verwaltung/11-01-SUMMARY.md
@.planning/phases/11-lehrer-und-f-cher-verwaltung/11-02-SUMMARY.md

# Backend reference code (READ before editing)
@apps/api/prisma/schema.prisma
@apps/api/prisma/seed.ts
@apps/api/prisma/README.md
@apps/api/src/modules/student/student.controller.ts
@apps/api/src/modules/student/student.service.ts
@apps/api/src/modules/student/student.service.spec.ts
@apps/api/src/modules/student/student.module.ts
@apps/api/src/modules/student/dto/create-student.dto.ts
@apps/api/src/modules/student/dto/update-student.dto.ts
@apps/api/src/modules/student/dto/student-response.dto.ts
@apps/api/src/modules/teacher/teacher.service.ts
@apps/api/src/modules/teacher/teacher.controller.ts
@apps/api/src/common/dto/pagination.dto.ts
@apps/api/src/modules/auth/decorators/check-permissions.decorator.ts
@apps/api/src/app.module.ts

# Frontend reference code (READ before editing)
@apps/web/src/lib/api.ts
@apps/web/src/hooks/useTeachers.ts
@apps/web/src/hooks/useSubjects.ts
@apps/web/src/routes/_authenticated/admin/teachers.index.tsx
@apps/web/src/routes/_authenticated/admin/teachers.$teacherId.tsx
@apps/web/src/components/admin/teacher/TeacherListTable.tsx
@apps/web/src/components/admin/teacher/TeacherCreateDialog.tsx
@apps/web/src/components/admin/teacher/TeacherDetailTabs.tsx
@apps/web/src/components/admin/teacher/StammdatenTab.tsx
@apps/web/src/components/admin/teacher/ArchiveTeacherDialog.tsx
@apps/web/src/components/admin/teacher/DeleteTeacherDialog.tsx
@apps/web/src/components/admin/teacher/AffectedEntitiesList.tsx
@apps/web/src/components/layout/AppSidebar.tsx
@apps/web/src/components/layout/MobileSidebar.tsx
@apps/web/src/components/admin/shared/PageShell.tsx
@apps/web/src/components/admin/shared/UnsavedChangesDialog.tsx
@apps/web/src/components/admin/shared/StickyMobileSaveBar.tsx
@apps/web/src/components/admin/shared/WarnDialog.tsx
@apps/web/src/stores/school-context-store.ts

# Shared package — extend same structure
@packages/shared/src/index.ts
@packages/shared/src/schemas/teacher.schema.ts
@packages/shared/src/schemas/subject.schema.ts

<interfaces>
<!-- Key contracts the executor needs, extracted from codebase. -->

Prisma models referenced by this plan (apps/api/prisma/schema.prisma):

```prisma
// Lines 313-339 — Person (parent/teacher/student share)
model Person {
  id                   String     @id @default(uuid())
  schoolId             String
  keycloakUserId       String?    @unique
  personType           PersonType  // STUDENT | TEACHER | PARENT | ADMIN etc.
  firstName            String
  lastName             String
  email                String?
  phone                String?    // encrypted at app level
  address              String?    // encrypted
  dateOfBirth          String?    // encrypted, stringified
  socialSecurityNumber String?    // encrypted
  isAnonymized         Boolean    @default(false)
  anonymizedAt         DateTime?
  teacher              Teacher?
  student              Student?
  parent               Parent?
  consentRecords       ConsentRecord[]
}

// Lines 373-392 — Student (NO isArchived YET — this plan adds)
model Student {
  id             String   @id @default(uuid())
  personId       String   @unique
  person         Person   @relation(fields: [personId], references: [id], onDelete: Cascade)
  schoolId       String
  classId        String?
  schoolClass    SchoolClass? @relation(fields: [classId], references: [id], onDelete: SetNull)
  studentNumber  String?
  enrollmentDate DateTime?
  // ADD: isArchived Boolean @default(false) @map("is_archived")
  // ADD: archivedAt DateTime? @map("archived_at")
  groupMemberships GroupMembership[]
  parentStudents   ParentStudent[]
}

// Lines 396-408 — Parent
model Parent {
  id        String   @id @default(uuid())
  personId  String   @unique
  person    Person   @relation(fields: [personId], references: [id], onDelete: Cascade)
  schoolId  String
  children  ParentStudent[]
}

// Lines 412-421 — ParentStudent (M:N junction; delete-cascades BOTH sides)
model ParentStudent {
  id        String  @id @default(uuid())
  parentId  String
  parent    Parent  @relation(fields: [parentId], references: [id], onDelete: Cascade)
  studentId String
  student   Student @relation(fields: [studentId], references: [id], onDelete: Cascade)
  @@unique([parentId, studentId])
}
```

Student FK references across schema (ALL direct FKs to Student via studentId — Orphan-Guard MUST check):
- AttendanceRecord.studentId (String, NO FK declared — indexed)  — line 896
- GradeEntry.studentId (String, NO FK declared — indexed)  — line 915
- StudentNote.studentId (String, NO FK declared — indexed)  — line 951
- AbsenceExcuse.studentId (String, NO FK declared — indexed)  — line 968
- GroupMembership.studentId (String, CASCADE-FK via relation)  — line 498
- ParentStudent.studentId (String, CASCADE-FK via relation)  — line 416

NOTE: ClassBookEntry has NO direct student FK (it's lesson-level via classSubjectId, with per-student attendance in AttendanceRecord). Orphan-Guard for Student MUST NOT count ClassBookEntry directly; it counts AttendanceRecord (which IS per-student) + StudentNote (per-student) + GradeEntry (per-student). Re-verify via schema.prisma grep at Wave 0 (CONTEXT.md D-13.3 flags as "unknown — grep erforderlich").

Existing StudentService.remove (apps/api/src/modules/student/student.service.ts:154-158):
```ts
async remove(id: string) {
  const student = await this.findOne(id);
  // Deleting person cascades to student (onDelete: Cascade)
  await this.prisma.person.delete({ where: { id: student.personId } });
}
```
NEW contract: MUST count denormalized refs (AttendanceRecord/GradeEntry/StudentNote/AbsenceExcuse) + cascade-FK counts (GroupMembership/ParentStudent). If any > 0 throw ConflictException with RFC 9457 payload.

Existing AffectedEntitiesList discriminated union (apps/web/src/components/admin/teacher/AffectedEntitiesList.tsx):
```ts
export interface TeacherAffectedEntities { klassenvorstandFor; lessonCount; classbookCount; gradeCount; substitutionCount; }
export interface SubjectAffectedEntities { affectedClasses; affectedTeachers; lessonCount; homeworkCount; examCount; }
type Props = | { kind?: 'teacher'; entities: TeacherAffectedEntities } | { kind: 'subject'; entities: SubjectAffectedEntities };
```
EXTEND: `| { kind: 'student'; entities: StudentAffectedEntities }` with payload matching service response.

Phase 10/11 shared admin components already present (DO NOT re-create):
- `components/admin/shared/PageShell` — H1/sub/breadcrumb wrapper
- `components/admin/shared/UnsavedChangesDialog` — per-tab dirty navigation
- `components/admin/shared/StickyMobileSaveBar` — fixed bottom save (mobile)
- `components/admin/shared/WarnDialog` — AlertTriangle + confirm/cancel
- `lib/api.ts parseProblemJson` — RFC 9457 parser with extensions pass-through

shadcn primitives present (DO NOT install again): tabs, dialog, input, select, button, card, label, popover, dropdown-menu, badge, scroll-area, sheet, sonner (toast), switch, textarea, toggle, tooltip, separator, collapsible
shadcn primitives MISSING (install at Wave 0): command, checkbox

API convention (Phase 1 D-12/D-13/D-14):
- All endpoints under `/api/v1/` prefix — the global NestJS setGlobalPrefix handles this; controllers use `@Controller('students')` (no prefix in file)
- RFC 9457 problem+json on errors with `extensions` object for custom payload
- Pagination via `SchoolPaginationQueryDto extends PaginationQueryDto` (schoolId + page + limit; .skip getter)
- `@CheckPermissions({ action, subject })` CASL decorator — actions: create|read|update|delete|manage; subjects: 'student' | 'class' | 'parent' (new)

Sidebar group shape (apps/web/src/components/layout/AppSidebar.tsx, established by Phase 11-01):
```ts
interface NavItem { label: string; href: string; icon: React.ComponentType; roles: string[]|'all'; group?: string; }
// Group 'Personal & Fächer' already present with Lehrer + Fächer entries;
// Plan 12-01 APPENDS Schüler:innen entry to same group.
```

Bulk move API pattern: Plan 12-01 uses sequential PUT /students/:id calls in a for-loop with progress counter + toast. The API already supports classId update via PUT /students/:id (existing UpdateStudentDto has classId). No new endpoint required for bulk — only UI-side orchestration.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — Shared Zod schemas + Prisma migration + shadcn primitives + TDD stubs + AffectedEntitiesList kind extension</name>
  <files>packages/shared/src/schemas/student.schema.ts, packages/shared/src/schemas/student.schema.spec.ts, packages/shared/src/schemas/parent.schema.ts, packages/shared/src/schemas/parent.schema.spec.ts, packages/shared/src/schemas/assign-parent.schema.ts, packages/shared/src/schemas/assign-parent.schema.spec.ts, packages/shared/src/schemas/move-student.schema.ts, packages/shared/src/schemas/move-student.schema.spec.ts, packages/shared/src/index.ts, apps/api/prisma/schema.prisma, apps/api/prisma/migrations/20260424000000_student_archive_flag/migration.sql, apps/api/prisma/seed.ts, apps/web/src/components/ui/command.tsx, apps/web/src/components/ui/checkbox.tsx, apps/web/package.json, pnpm-lock.yaml, apps/web/src/components/admin/teacher/AffectedEntitiesList.tsx, apps/web/src/components/admin/student/StudentStammdatenTab.test.tsx, apps/web/src/components/admin/student/StudentParentsTab.test.tsx, apps/web/src/components/admin/student/MoveStudentDialog.test.tsx, apps/api/src/modules/student/student.service.spec.ts, apps/api/src/modules/parent/parent.service.spec.ts</files>

  <read_first>
    - apps/api/prisma/schema.prisma — Student model (lines 373-392), Parent + ParentStudent (396-421), Person (313-339), AttendanceRecord (892-907), GradeEntry (911-927), StudentNote (947-961), AbsenceExcuse (965-986), GroupMembership (494-505), ClassBookEntry (861-888)
    - apps/api/prisma/README.md — Migration hygiene policy (hard rule, shadow-DB setup)
    - CLAUDE.md §Conventions — Database migrations hard rule; `prisma db push` is FORBIDDEN; always `prisma migrate dev --name`; scripts/check-migration-hygiene.sh enforces
    - apps/api/prisma/seed.ts — existing seed; must set isArchived=false on existing students + 1 archived fixture
    - packages/shared/src/schemas/teacher.schema.ts — canonical Zod pattern (deutsche Fehler, englische API-Feldnamen, RHF/class-validator parity)
    - packages/shared/src/schemas/subject.schema.ts — second Zod analog (Phase 11-02)
    - packages/shared/src/index.ts — re-export list to extend
    - apps/web/src/components/admin/teacher/AffectedEntitiesList.tsx — discriminated union kind: 'teacher' | 'subject' (backward-compat default); MUST preserve existing shapes when adding 'student'
    - apps/web/components.json — shadcn config (default style + neutral + CSS vars)
    - apps/web/src/components/ui/popover.tsx — existing popover primitive (Command uses it underneath)
    - apps/web/src/components/admin/teacher/StammdatenTab.test.tsx — Vitest + React Testing Library pattern for FE component tests (reference for 3 new test stubs)
    - apps/api/src/modules/student/student.service.spec.ts — existing unit spec to EXTEND with Orphan-Guard describe block
    - .planning/phases/12-sch-ler-klassen-und-gruppenverwaltung/12-UI-SPEC.md §Copywriting Contract — canonical German strings for toasts/dialogs (used as error messages in Zod schemas where appropriate)
    - .planning/phases/12-sch-ler-klassen-und-gruppenverwaltung/12-RESEARCH.md §2.2 Orphan-Guard — exact dependency list for StudentService
    - .planning/phases/12-sch-ler-klassen-und-gruppenverwaltung/12-VALIDATION.md §Wave 0 Requirements — canonical test file list
  </read_first>

  <action>
1. **Confirm-grep StudentService orphan-guard scope** (CONTEXT.md D-13.3 flags this as "grep erforderlich"): run `grep -nE "studentId|student_id" apps/api/prisma/schema.prisma` and inspect. Authoritative list for this plan (confirmed via schema.prisma read in research):
   - AttendanceRecord.studentId (line 896) — per-student attendance row, denormalized string
   - GradeEntry.studentId (line 915) — per-student grade, denormalized string
   - StudentNote.studentId (line 951) — per-student note, denormalized string
   - AbsenceExcuse.studentId (line 968) — per-student excuse, denormalized string
   - GroupMembership.studentId (line 498) — cascade-FK but Orphan-Guard still counts for UI transparency
   - ParentStudent.studentId (line 416) — cascade-FK but Orphan-Guard still counts for UI
   ClassBookEntry itself has NO studentId (it's lesson-level via classSubjectId) — do NOT count it for Student orphan-guard. Attendance IS the per-student cross-cut (already included above).

2. **Prisma schema change** — in `apps/api/prisma/schema.prisma` under `model Student { ... }` (line 373-392), add two fields after `enrollmentDate`:
   ```prisma
   isArchived Boolean   @default(false) @map("is_archived")
   archivedAt DateTime? @map("archived_at")
   ```
   Leave everything else unchanged. Do NOT add SUBJECT-04 fields here (Plan 12-02 owns ClassSubject changes).

3. **Prisma migration via `prisma migrate dev`** (CLAUDE.md hard rule):
   ```sh
   pnpm --filter @schoolflow/api exec prisma migrate dev --name student_archive_flag
   ```
   Rename the generated folder to `20260424000000_student_archive_flag` (fixed timestamp for deterministic ordering in this plan). Verify `apps/api/prisma/migrations/20260424000000_student_archive_flag/migration.sql` exists and contains `ADD COLUMN "is_archived" boolean NOT NULL DEFAULT false` + `ADD COLUMN "archived_at" timestamp(3)`. Do NOT hand-edit the SQL — accept whatever Prisma emits.

4. **Update seed.ts** — set `isArchived: false` on all existing student fixtures (explicit for safety on re-seed), AND add one archived fixture student (`firstName: 'Fixture', lastName: 'Archived', isArchived: true, archivedAt: new Date('2026-01-15')`) scoped to the Phase 10 seed school — used by Plan 12-03 E2E fixture `admin-students-archive.spec.ts`. After edit, run `pnpm --filter @schoolflow/api exec prisma migrate reset --force --skip-seed` followed by `pnpm --filter @schoolflow/api exec prisma migrate deploy && pnpm --filter @schoolflow/api exec prisma db seed` to validate round-trip.

5. **Install shadcn primitives** (per 12-UI-SPEC.md §Component Inventory):
   ```sh
   pnpm --filter @schoolflow/web exec npx shadcn@latest add command checkbox
   ```
   Verify `apps/web/src/components/ui/command.tsx` + `apps/web/src/components/ui/checkbox.tsx` exist. If either file already exists after install (unlikely per Glob), leave intact. Commit `apps/web/package.json` + `pnpm-lock.yaml` diffs.

6. **Author shared Zod schemas** (deutsche Fehler, englische API-Feldnamen, per D-15):

   **`packages/shared/src/schemas/student.schema.ts`:**
   ```ts
   import { z } from 'zod';

   export const StudentStammdatenSchema = z.object({
     firstName: z.string().min(1, 'Pflichtfeld').max(100),
     lastName: z.string().min(1, 'Pflichtfeld').max(100),
     email: z.string().email('Gültige E-Mail-Adresse eingeben').optional().or(z.literal('')),
     phone: z.string().optional(),
     address: z.string().optional(),
     dateOfBirth: z.string().optional(),  // ISO YYYY-MM-DD, encrypted at DB layer
     socialSecurityNumber: z.string().optional(),
     studentNumber: z.string().optional(),
     classId: z.string().uuid('Ungültige Klassen-ID').optional(),
     enrollmentDate: z.string().optional(),
   });

   export const StudentCreateSchema = StudentStammdatenSchema.extend({
     schoolId: z.string().uuid(),
     firstName: z.string().min(1, 'Pflichtfeld').max(100),
     lastName: z.string().min(1, 'Pflichtfeld').max(100),
     parentIds: z.array(z.string().uuid()).optional().default([]),
   });

   export const StudentUpdateSchema = StudentStammdatenSchema.partial();

   export const StudentListFiltersSchema = z.object({
     schoolId: z.string().uuid(),
     search: z.string().optional(),
     classId: z.string().uuid().optional(),                 // explicit class
     noClass: z.boolean().optional(),                        // filter "Ohne Stammklasse"
     archived: z.enum(['active', 'archived', 'all']).default('active'),
     schoolYearId: z.string().uuid().optional(),
     page: z.number().int().min(1).default(1),
     limit: z.number().int().min(1).max(100).default(20),
   });

   export type StudentStammdatenInput = z.infer<typeof StudentStammdatenSchema>;
   export type StudentCreateInput = z.infer<typeof StudentCreateSchema>;
   export type StudentUpdateInput = z.infer<typeof StudentUpdateSchema>;
   export type StudentListFilters = z.infer<typeof StudentListFiltersSchema>;
   ```

   **`packages/shared/src/schemas/parent.schema.ts`:**
   ```ts
   import { z } from 'zod';

   export const ParentStammdatenSchema = z.object({
     firstName: z.string().min(1, 'Pflichtfeld').max(100),
     lastName: z.string().min(1, 'Pflichtfeld').max(100),
     email: z.string().email('Gültige E-Mail-Adresse eingeben'),
     phone: z.string().optional(),
   });

   export const ParentCreateSchema = ParentStammdatenSchema.extend({
     schoolId: z.string().uuid(),
   });

   export const ParentUpdateSchema = ParentStammdatenSchema.partial();

   export const ParentSearchSchema = z.object({
     schoolId: z.string().uuid(),
     email: z.string().optional(),
     name: z.string().optional(),
     page: z.number().int().min(1).default(1),
     limit: z.number().int().min(1).max(100).default(20),
   });

   export type ParentStammdatenInput = z.infer<typeof ParentStammdatenSchema>;
   export type ParentCreateInput = z.infer<typeof ParentCreateSchema>;
   export type ParentUpdateInput = z.infer<typeof ParentUpdateSchema>;
   ```

   **`packages/shared/src/schemas/assign-parent.schema.ts`:**
   ```ts
   import { z } from 'zod';
   export const AssignParentSchema = z.object({
     parentId: z.string().uuid('Ungültige Eltern-ID'),
   });
   export type AssignParentInput = z.infer<typeof AssignParentSchema>;
   ```

   **`packages/shared/src/schemas/move-student.schema.ts`:**
   ```ts
   import { z } from 'zod';
   export const MoveStudentSchema = z.object({
     targetClassId: z.string().uuid('Bitte Ziel-Klasse auswählen'),
     notiz: z.string().max(500).optional(),
   });
   export const BulkMoveStudentSchema = z.object({
     studentIds: z.array(z.string().uuid()).min(1, 'Keine Schüler:innen ausgewählt'),
     targetClassId: z.string().uuid('Bitte Ziel-Klasse auswählen'),
     notiz: z.string().max(500).optional(),
   });
   export type MoveStudentInput = z.infer<typeof MoveStudentSchema>;
   export type BulkMoveStudentInput = z.infer<typeof BulkMoveStudentSchema>;
   ```

   **Append to `packages/shared/src/index.ts`:**
   ```ts
   export * from './schemas/student.schema.js';
   export * from './schemas/parent.schema.js';
   export * from './schemas/assign-parent.schema.js';
   export * from './schemas/move-student.schema.js';
   ```

7. **Author Zod schema specs** — each `.spec.ts` file has ≥5 test cases following `packages/shared/src/schemas/teacher.schema.spec.ts` structure: valid happy-path, required-field missing → 'Pflichtfeld', email format error (where applicable), uuid format error, enum boundary (archived filter). Use `schema.safeParse(...)` patterns and assert `success` + `error.issues[0].message` strings.

8. **Extend AffectedEntitiesList discriminated union** — in `apps/web/src/components/admin/teacher/AffectedEntitiesList.tsx`:
   - Add interface `StudentAffectedEntities` with fields: `attendanceCount: number; gradeCount: number; studentNoteCount: number; excuseCount: number; groupMembershipCount: number; parentLinkCount: number;`
   - Extend Props union: `| { kind: 'student'; entities: StudentAffectedEntities }`
   - Add renderer `function StudentAffected({ entities }: { entities: StudentAffectedEntities })` with 6 categories mirroring TeacherAffected shape; German labels: 'Anwesenheiten', 'Noten-Einträge', 'Schüler:innen-Notizen', 'Abwesenheits-Entschuldigungen', 'Gruppen-Mitgliedschaften', 'Eltern-Verknüpfungen'.
   - Switch in main `AffectedEntitiesList` on `props.kind === 'student'`.
   - PRESERVE existing kind='teacher' (default) and kind='subject' branches byte-for-byte.

9. **Wave 0 TDD stubs — FE component test files** (all as `it.todo('...')` placeholders, NO implementation yet):

   **`apps/web/src/components/admin/student/StudentStammdatenTab.test.tsx`:**
   ```tsx
   import { describe, it } from 'vitest';
   describe('StudentStammdatenTab', () => {
     it.todo('renders Stammdaten form fields in order Vorname/Nachname/Email/Phone/Address/DateOfBirth/SVNR/StudentNumber/Klasse/EnrollmentDate');
     it.todo('blocks submit when lastName is empty via RHF zodResolver (Pflichtfeld)');
     it.todo('blocks submit when email is invalid via zodResolver');
     it.todo('shows Archivieren row-action only when status=active and hides Reaktivieren, inverse when status=archived');
     it.todo('fires onSave with validated values using englische API-Feldnamen (classId, studentNumber, enrollmentDate)');
   });
   ```

   **`apps/web/src/components/admin/student/StudentParentsTab.test.tsx`:**
   ```tsx
   import { describe, it } from 'vitest';
   describe('StudentParentsTab', () => {
     it.todo('renders empty-state "Keine Erziehungsberechtigten verknüpft" with CTA Erziehungsberechtigte:n verknüpfen');
     it.todo('ParentSearchPopover debounces email input 300ms before firing useParentsByEmail');
     it.todo('on 404 no-match shows "Keine Treffer. Neu:e Erziehungsberechtigte:n anlegen?" + inline-create CTA');
     it.todo('on 200 match shows user-row with CircleCheck and Enter triggers link-confirm dialog');
     it.todo('InlineCreateParentForm with firstName+lastName+email+phone submit creates parent + links in single request, toast success');
     it.todo('Unlink button opens WarnDialog "Verknüpfung entfernen?" with copy matching 12-UI-SPEC.md verbatim; confirms and calls DELETE /students/:id/parents/:parentId');
     it.todo('after unlink Parent row disappears but Parent record preserved (no 204 to DELETE /parents/:id issued)');
   });
   ```

   **`apps/web/src/components/admin/student/MoveStudentDialog.test.tsx`:**
   ```tsx
   import { describe, it } from 'vitest';
   describe('MoveStudentDialog', () => {
     it.todo('mode="single" renders single-student heading, class-picker and Notiz textarea');
     it.todo('mode="bulk" renders avatar-stack preview of selection (max 5 visible + "+N weitere") and plural heading');
     it.todo('blocks Verschieben button until targetClassId is selected (zodResolver MoveStudentSchema)');
     it.todo('bulk apply runs sequential PUT /students/:id with progress counter "{done}/{total}"');
     it.todo('on 4xx error during bulk stops at current index and shows toast "{done}/{total} verschoben. Fehler bei {name}."');
     it.todo('Silent-4xx invariant: explicit onError fires toast, never swallows via .catch(()=>{})');
   });
   ```

10. **Extend `apps/api/src/modules/student/student.service.spec.ts`** with 2 new describe blocks (authored as `it.todo()` stubs — implemented in Task 2):

    - **`describe('remove — Orphan-Guard')`:**
      - `it.todo('deletes person (cascading student) when zero dependents (204)')`
      - `it.todo('throws ConflictException when AttendanceRecord.studentId references student')`
      - `it.todo('throws ConflictException when GradeEntry.studentId references student')`
      - `it.todo('throws ConflictException when StudentNote.studentId references student')`
      - `it.todo('throws ConflictException when AbsenceExcuse.studentId references student')`
      - `it.todo('throws ConflictException when GroupMembership has student row')`
      - `it.todo('throws ConflictException when ParentStudent has student row')`
      - `it.todo('payload contains extensions.affectedEntities.{attendanceCount, gradeCount, studentNoteCount, excuseCount, groupMembershipCount, parentLinkCount}')`

    - **`describe('archive + restore')`:**
      - `it.todo('archive sets isArchived=true + archivedAt=now')`
      - `it.todo('restore sets isArchived=false + archivedAt=null')`
      - `it.todo('findAll default (archived=active) excludes archived students')`
      - `it.todo('findAll archived=archived returns ONLY archived students')`
      - `it.todo('findAll archived=all returns both')`

    - **`describe('create — parentIds')`:**
      - `it.todo('creates Student + ParentStudent rows in single transaction when parentIds provided')`
      - `it.todo('empty parentIds (or undefined) creates zero ParentStudent rows')`
      - `it.todo('invalid parentId throws validation error before tx')`

    - **`describe('linkParent + unlinkParent')`:**
      - `it.todo('linkParent creates ParentStudent row when (studentId, parentId) pair not yet linked')`
      - `it.todo('linkParent is idempotent (no duplicate, no error) when already linked')`
      - `it.todo('unlinkParent removes ParentStudent row but preserves Parent record')`

11. **Create `apps/api/src/modules/parent/parent.service.spec.ts`** (greenfield module — Wave 0 TDD stubs):
    - `describe('create')` — `it.todo('nested-creates Person with personType=PARENT then Parent row in single tx')`; `it.todo('returns parent with nested person')`; `it.todo('throws BadRequestException on invalid email')`;
    - `describe('findAll')` — `it.todo('filters by schoolId (required)')`; `it.todo('filters by email substring')`; `it.todo('filters by name substring (firstName OR lastName)')`; `it.todo('returns paginated response with meta')`;
    - `describe('findOne')` — `it.todo('returns parent with nested person + children count')`; `it.todo('throws NotFoundException when id not found')`;
    - `describe('update')` — `it.todo('updates Person fields (firstName/lastName/email/phone) via nested update')`;
    - `describe('remove — Orphan-Guard')` — `it.todo('deletes person (cascading parent) when zero ParentStudent refs')`; `it.todo('throws ConflictException with affectedEntities.linkedStudents when ParentStudent refs exist')`;

12. **Do NOT run the full test suite yet** — Task 2 turns stubs into real tests. Only verify the Zod schemas + schema specs are green at Wave 0:
  </action>

  <acceptance_criteria>
    - `packages/shared/src/schemas/student.schema.ts` exports StudentCreateSchema, StudentUpdateSchema, StudentStammdatenSchema, StudentListFiltersSchema (grep)
    - `packages/shared/src/schemas/parent.schema.ts` exports ParentCreateSchema, ParentUpdateSchema, ParentSearchSchema (grep)
    - `packages/shared/src/schemas/assign-parent.schema.ts` exports AssignParentSchema (grep)
    - `packages/shared/src/schemas/move-student.schema.ts` exports MoveStudentSchema, BulkMoveStudentSchema (grep)
    - `packages/shared/src/index.ts` contains 4 new `export * from './schemas/...'` lines (grep for 'student.schema.js', 'parent.schema.js', 'assign-parent.schema.js', 'move-student.schema.js')
    - `apps/api/prisma/schema.prisma` contains `isArchived Boolean` under `model Student` block (grep)
    - `apps/api/prisma/migrations/20260424000000_student_archive_flag/migration.sql` exists AND contains `"is_archived"` AND `"archived_at"` (grep)
    - `scripts/check-migration-hygiene.sh` exits 0 (`pnpm --filter @schoolflow/api exec bash ../../scripts/check-migration-hygiene.sh` or run from repo root)
    - `apps/web/src/components/ui/command.tsx` exists with `CommandInput` export (grep)
    - `apps/web/src/components/ui/checkbox.tsx` exists with `Checkbox` export (grep)
    - `apps/web/src/components/admin/teacher/AffectedEntitiesList.tsx` contains `StudentAffectedEntities` interface AND `kind: 'student'` in Props union AND `function StudentAffected` renderer (grep)
    - 3 FE test files exist with ≥5 `it.todo(` calls each (grep `it.todo` count per file)
    - `student.service.spec.ts` contains `describe('remove — Orphan-Guard'` AND `describe('archive + restore'` AND `describe('create — parentIds'` AND `describe('linkParent + unlinkParent'` (4 new describe blocks, grep)
    - `parent.service.spec.ts` exists with ≥10 `it.todo(` calls across 5 describe blocks (grep)
    - `pnpm --filter @schoolflow/shared test:run -- schemas` green (all 4 new schema specs pass)
    - `pnpm -r build` succeeds (TypeScript compiles across shared+api+web workspace graph)
    - `pnpm --filter @schoolflow/api exec prisma generate` succeeds (Student type now includes isArchived/archivedAt)
    - Seed script succeeds end-to-end (`pnpm --filter @schoolflow/api exec prisma migrate reset --force --skip-seed && pnpm --filter @schoolflow/api exec prisma db seed`)
  </acceptance_criteria>

  <verify>
    <automated>pnpm --filter @schoolflow/shared test:run -- schemas && pnpm -r build && pnpm --filter @schoolflow/api exec prisma validate && bash scripts/check-migration-hygiene.sh</automated>
  </verify>

  <done>
    Shared Zod schemas live in @schoolflow/shared, Student.isArchived migration is generated + committed, shadcn Command + Checkbox installed, AffectedEntitiesList supports kind='student', all Wave-0 it.todo() stubs authored in 3 FE test files + extended student.service.spec + new parent.service.spec, workspace compiles clean, migration hygiene check passes. Task 2 can now implement the backend gap-fixes and turn stubs green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Backend — ParentModule greenfield + StudentService gap-fixes (archive/restore/Orphan-Guard/parentIds/link+unlink) + controller endpoints + unit tests green</name>
  <files>apps/api/src/modules/parent/parent.module.ts, apps/api/src/modules/parent/parent.service.ts, apps/api/src/modules/parent/parent.service.spec.ts, apps/api/src/modules/parent/parent.controller.ts, apps/api/src/modules/parent/dto/create-parent.dto.ts, apps/api/src/modules/parent/dto/update-parent.dto.ts, apps/api/src/modules/parent/dto/parent-list-query.dto.ts, apps/api/src/modules/parent/dto/parent-response.dto.ts, apps/api/src/modules/student/student.service.ts, apps/api/src/modules/student/student.service.spec.ts, apps/api/src/modules/student/student.controller.ts, apps/api/src/modules/student/student.module.ts, apps/api/src/modules/student/dto/create-student.dto.ts, apps/api/src/modules/student/dto/update-student.dto.ts, apps/api/src/modules/student/dto/student-list-query.dto.ts, apps/api/src/modules/student/dto/assign-parent.dto.ts, apps/api/src/app.module.ts</files>

  <read_first>
    - apps/api/src/modules/student/student.service.ts — current .create (nested person-create, lines 11-40), .findAll (41-66), .update (114-152), .remove (154-158) — REPLACE with Orphan-Guard; ADD .archive, .restore, .linkParent, .unlinkParent
    - apps/api/src/modules/student/student.controller.ts — existing 5 endpoints (lines 1-60); APPEND archive/restore/parent-link/parent-unlink endpoints
    - apps/api/src/modules/student/student.service.spec.ts — Wave 0 stubs to turn green
    - apps/api/src/modules/student/dto/create-student.dto.ts — add parentIds field (@IsOptional @IsArray @IsUUID each, ApiPropertyOptional)
    - apps/api/src/modules/student/dto/update-student.dto.ts — no changes needed (PartialType already covers)
    - apps/api/src/modules/teacher/teacher.service.ts — reference for ConflictException RFC 9457 shape (Phase 11-01 pattern)
    - apps/api/src/modules/teacher/teacher.service.spec.ts — reference for orphan-guard unit test structure (vi.fn mocks on PrismaService)
    - apps/api/src/modules/subject/subject.service.ts — reference for Orphan-Guard $transaction pattern used in Phase 11-02
    - apps/api/src/modules/subject/subject.module.ts — NestJS module pattern to mirror for ParentModule
    - apps/api/src/modules/subject/subject.controller.ts — Controller pattern (@CheckPermissions per endpoint)
    - apps/api/prisma/schema.prisma — Student (lines 373-392 AFTER migration), Parent (396-408), ParentStudent (412-421), Person (313-339), AttendanceRecord (892-907), GradeEntry (911-927), StudentNote (947-961), AbsenceExcuse (965-986), GroupMembership (494-505)
    - apps/api/src/common/dto/pagination.dto.ts — PaginationQueryDto + SchoolPaginationQueryDto base
    - apps/api/src/modules/auth/decorators/check-permissions.decorator.ts — CheckPermissions decorator interface
    - apps/api/src/app.module.ts — imports list to extend
    - .planning/phases/12-sch-ler-klassen-und-gruppenverwaltung/12-RESEARCH.md §2 — full gap list
    - .planning/phases/12-sch-ler-klassen-und-gruppenverwaltung/12-CONTEXT.md D-13 — canonical gap-fix spec
  </read_first>

  <action>
1. **Extend `CreateStudentDto`** (`apps/api/src/modules/student/dto/create-student.dto.ts`) — append after `enrollmentDate?`:
   ```ts
   @ApiPropertyOptional({ description: 'Parent IDs to link on create', type: [String], format: 'uuid' })
   @IsOptional()
   @IsArray()
   @IsUUID('4', { each: true })
   parentIds?: string[];
   ```
   Import `IsArray` from `class-validator` at the top of the file.

2. **Create `apps/api/src/modules/student/dto/student-list-query.dto.ts`**:
   ```ts
   import { ApiPropertyOptional } from '@nestjs/swagger';
   import { IsEnum, IsOptional, IsString, IsUUID } from 'class-validator';
   import { SchoolPaginationQueryDto } from '../../../common/dto/pagination.dto';

   export type StudentArchiveFilter = 'active' | 'archived' | 'all';

   export class StudentListQueryDto extends SchoolPaginationQueryDto {
     @ApiPropertyOptional({ enum: ['active', 'archived', 'all'], default: 'active' })
     @IsOptional()
     @IsEnum(['active', 'archived', 'all'])
     archived: StudentArchiveFilter = 'active';

     @ApiPropertyOptional({ description: 'Filter by Stammklasse ID', format: 'uuid' })
     @IsOptional()
     @IsUUID()
     classId?: string;

     @ApiPropertyOptional({ description: 'Text search on firstName/lastName/email' })
     @IsOptional()
     @IsString()
     search?: string;

     @ApiPropertyOptional({ description: 'Filter by Schuljahr (via class.schoolYearId)', format: 'uuid' })
     @IsOptional()
     @IsUUID()
     schoolYearId?: string;
   }
   ```

3. **Create `apps/api/src/modules/student/dto/assign-parent.dto.ts`**:
   ```ts
   import { ApiProperty } from '@nestjs/swagger';
   import { IsUUID } from 'class-validator';

   export class AssignParentDto {
     @ApiProperty({ description: 'Existing Parent ID to link', format: 'uuid' })
     @IsUUID()
     parentId!: string;
   }
   ```

4. **Rewrite `StudentService`** (`apps/api/src/modules/student/student.service.ts`). Replace the full file with the following structure (preserves existing methods, adds Orphan-Guard/archive/restore/parent-link/unlink; extends create for parentIds; extends findAll for filters):

   ```ts
   import { Injectable, NotFoundException, ConflictException } from '@nestjs/common';
   import { PrismaService } from '../../config/database/prisma.service';
   import { CreateStudentDto } from './dto/create-student.dto';
   import { UpdateStudentDto } from './dto/update-student.dto';
   import { StudentListQueryDto } from './dto/student-list-query.dto';
   import { PaginatedResponseDto, PaginationQueryDto } from '../../common/dto/pagination.dto';

   @Injectable()
   export class StudentService {
     constructor(private prisma: PrismaService) {}

     async create(dto: CreateStudentDto) {
       return this.prisma.$transaction(async (tx) => {
         const person = await tx.person.create({
           data: {
             schoolId: dto.schoolId,
             personType: 'STUDENT',
             firstName: dto.firstName,
             lastName: dto.lastName,
             email: dto.email,
             phone: dto.phone,
             address: dto.address,
             dateOfBirth: dto.dateOfBirth,
             socialSecurityNumber: dto.socialSecurityNumber,
             student: {
               create: {
                 schoolId: dto.schoolId,
                 classId: dto.classId,
                 studentNumber: dto.studentNumber,
                 enrollmentDate: dto.enrollmentDate ? new Date(dto.enrollmentDate) : null,
               },
             },
           },
           include: { student: { include: { schoolClass: true } } },
         });

         const studentId = person.student!.id;
         if (dto.parentIds && dto.parentIds.length > 0) {
           await tx.parentStudent.createMany({
             data: dto.parentIds.map((parentId) => ({ parentId, studentId })),
             skipDuplicates: true,
           });
         }
         return person;
       });
     }

     async findAll(query: StudentListQueryDto): Promise<PaginatedResponseDto<any>> {
       const { schoolId, archived, classId, search, schoolYearId } = query;
       if (!schoolId) {
         throw new NotFoundException('schoolId query parameter is required');
       }

       const where: any = { schoolId };
       if (archived === 'active') where.isArchived = false;
       if (archived === 'archived') where.isArchived = true;
       // 'all' → no filter
       if (classId) where.classId = classId;
       if (schoolYearId) where.schoolClass = { schoolYearId };
       if (search) {
         where.person = {
           OR: [
             { firstName: { contains: search, mode: 'insensitive' } },
             { lastName: { contains: search, mode: 'insensitive' } },
             { email: { contains: search, mode: 'insensitive' } },
           ],
         };
       }

       const [data, total] = await Promise.all([
         this.prisma.student.findMany({
           where,
           include: { person: true, schoolClass: true },
           skip: query.skip,
           take: query.limit,
           orderBy: { person: { lastName: 'asc' } },
         }),
         this.prisma.student.count({ where }),
       ]);
       return {
         data,
         meta: { page: query.page, limit: query.limit, total, totalPages: Math.ceil(total / query.limit) },
       };
     }

     // findByClass — preserve existing signature (used internally by class module)

     async findOne(id: string) {
       const student = await this.prisma.student.findUnique({
         where: { id },
         include: {
           person: true,
           schoolClass: true,
           groupMemberships: { include: { group: true } },
           parentStudents: { include: { parent: { include: { person: true } } } },
         },
       });
       if (!student) throw new NotFoundException('Der angeforderte Schueler wurde nicht gefunden.');
       return student;
     }

     async update(id: string, dto: UpdateStudentDto) {
       // Preserve existing update impl (lines 114-152); no change required.
     }

     async archive(id: string) {
       await this.findOne(id);
       return this.prisma.student.update({
         where: { id },
         data: { isArchived: true, archivedAt: new Date() },
         include: { person: true, schoolClass: true },
       });
     }

     async restore(id: string) {
       await this.findOne(id);
       return this.prisma.student.update({
         where: { id },
         data: { isArchived: false, archivedAt: null },
         include: { person: true, schoolClass: true },
       });
     }

     async linkParent(studentId: string, parentId: string) {
       await this.findOne(studentId);
       // upsert-like: if already linked, do nothing
       await this.prisma.parentStudent.upsert({
         where: { parentId_studentId: { parentId, studentId } },
         update: {},
         create: { parentId, studentId },
       });
       return this.findOne(studentId);
     }

     async unlinkParent(studentId: string, parentId: string) {
       await this.findOne(studentId);
       await this.prisma.parentStudent.deleteMany({ where: { parentId, studentId } });
       return this.findOne(studentId);
     }

     async remove(id: string) {
       const student = await this.findOne(id);

       const [
         attendanceCount,
         gradeCount,
         studentNoteCount,
         excuseCount,
         groupMembershipCount,
         parentLinkCount,
       ] = await this.prisma.$transaction([
         this.prisma.attendanceRecord.count({ where: { studentId: id } }),
         this.prisma.gradeEntry.count({ where: { studentId: id } }),
         this.prisma.studentNote.count({ where: { studentId: id } }),
         this.prisma.absenceExcuse.count({ where: { studentId: id } }),
         this.prisma.groupMembership.count({ where: { studentId: id } }),
         this.prisma.parentStudent.count({ where: { studentId: id } }),
       ]);

       const total = attendanceCount + gradeCount + studentNoteCount + excuseCount + groupMembershipCount + parentLinkCount;

       if (total > 0) {
         throw new ConflictException({
           type: 'https://schoolflow.dev/errors/student-has-dependents',
           title: 'Schüler:in kann nicht gelöscht werden',
           status: 409,
           detail: 'Diese:r Schüler:in hat noch Verknüpfungen. Lösen Sie diese vorher oder archivieren Sie stattdessen.',
           extensions: {
             affectedEntities: {
               attendanceCount,
               gradeCount,
               studentNoteCount,
               excuseCount,
               groupMembershipCount,
               parentLinkCount,
             },
           },
         });
       }

       // Deleting person cascades to student + cascade-FK rows
       await this.prisma.person.delete({ where: { id: student.personId } });
     }
   }
   ```

5. **Turn student.service.spec.ts Wave 0 stubs green** — implement every `it.todo` added in Task 1. Follow existing spec structure in the same file (vi.fn mocks on PrismaService). For each Orphan-Guard failing case, mock exactly one `count` to return 1 and the rest 0, then assert:
   ```ts
   await expect(service.remove('id')).rejects.toBeInstanceOf(ConflictException);
   const err = await service.remove('id').catch((e) => e);
   expect(err.getResponse()).toMatchObject({ extensions: { affectedEntities: { attendanceCount: 1, gradeCount: 0, ... } } });
   ```

6. **Extend `apps/api/src/modules/student/student.controller.ts`** — replace findAll signature to use the new DTO and append 4 new endpoints (keep existing create/findOne/update/remove handlers unchanged except for DTO switch on findAll):

   ```ts
   import { StudentListQueryDto } from './dto/student-list-query.dto';
   import { AssignParentDto } from './dto/assign-parent.dto';

   // REPLACE findAll:
   @Get()
   @CheckPermissions({ action: 'read', subject: 'student' })
   @ApiOperation({ summary: 'List students by school with filters + pagination' })
   async findAll(@Query() query: StudentListQueryDto) {
     return this.studentService.findAll(query);
   }

   // APPEND after existing remove:
   @Post(':id/archive')
   @HttpCode(HttpStatus.OK)
   @CheckPermissions({ action: 'update', subject: 'student' })
   @ApiOperation({ summary: 'Archive a student (sets isArchived=true, archivedAt=now)' })
   async archive(@Param('id') id: string) {
     return this.studentService.archive(id);
   }

   @Post(':id/restore')
   @HttpCode(HttpStatus.OK)
   @CheckPermissions({ action: 'update', subject: 'student' })
   @ApiOperation({ summary: 'Restore an archived student (clears isArchived + archivedAt)' })
   async restore(@Param('id') id: string) {
     return this.studentService.restore(id);
   }

   @Post(':id/parents')
   @HttpCode(HttpStatus.OK)
   @CheckPermissions({ action: 'update', subject: 'student' })
   @ApiOperation({ summary: 'Link an existing Parent to this student' })
   async linkParent(@Param('id') id: string, @Body() dto: AssignParentDto) {
     return this.studentService.linkParent(id, dto.parentId);
   }

   @Delete(':id/parents/:parentId')
   @HttpCode(HttpStatus.OK)
   @CheckPermissions({ action: 'update', subject: 'student' })
   @ApiOperation({ summary: 'Unlink a Parent from this student (Parent record preserved)' })
   async unlinkParent(@Param('id') id: string, @Param('parentId') parentId: string) {
     return this.studentService.unlinkParent(id, parentId);
   }
   ```

7. **ParentModule greenfield** — create under `apps/api/src/modules/parent/`:

   **`dto/create-parent.dto.ts`:**
   ```ts
   import { ApiProperty, ApiPropertyOptional } from '@nestjs/swagger';
   import { IsEmail, IsOptional, IsString, IsUUID, MaxLength, MinLength } from 'class-validator';
   export class CreateParentDto {
     @ApiProperty({ format: 'uuid' }) @IsUUID() schoolId!: string;
     @ApiProperty({ minLength: 1, maxLength: 100 }) @IsString() @MinLength(1) @MaxLength(100) firstName!: string;
     @ApiProperty({ minLength: 1, maxLength: 100 }) @IsString() @MinLength(1) @MaxLength(100) lastName!: string;
     @ApiProperty({ format: 'email' }) @IsEmail() email!: string;
     @ApiPropertyOptional() @IsOptional() @IsString() phone?: string;
   }
   ```

   **`dto/update-parent.dto.ts`:**
   ```ts
   import { PartialType } from '@nestjs/swagger';
   import { CreateParentDto } from './create-parent.dto';
   export class UpdateParentDto extends PartialType(CreateParentDto) {}
   ```

   **`dto/parent-list-query.dto.ts`:**
   ```ts
   import { ApiPropertyOptional } from '@nestjs/swagger';
   import { IsOptional, IsString } from 'class-validator';
   import { SchoolPaginationQueryDto } from '../../../common/dto/pagination.dto';
   export class ParentListQueryDto extends SchoolPaginationQueryDto {
     @ApiPropertyOptional({ description: 'Email exact or substring search' })
     @IsOptional() @IsString()
     email?: string;
     @ApiPropertyOptional({ description: 'Name substring (firstName or lastName)' })
     @IsOptional() @IsString()
     name?: string;
   }
   ```

   **`dto/parent-response.dto.ts`:**
   ```ts
   import { ApiProperty } from '@nestjs/swagger';
   export class ParentResponseDto {
     @ApiProperty() id!: string;
     @ApiProperty() schoolId!: string;
     @ApiProperty() firstName!: string;
     @ApiProperty() lastName!: string;
     @ApiProperty() email!: string;
     @ApiProperty({ required: false, nullable: true }) phone?: string | null;
     @ApiProperty({ type: [String], description: 'Linked student IDs' }) linkedStudentIds!: string[];
   }
   ```

   **`parent.service.ts`:**
   ```ts
   import { Injectable, NotFoundException, ConflictException } from '@nestjs/common';
   import { PrismaService } from '../../config/database/prisma.service';
   import { CreateParentDto } from './dto/create-parent.dto';
   import { UpdateParentDto } from './dto/update-parent.dto';
   import { ParentListQueryDto } from './dto/parent-list-query.dto';
   import { PaginatedResponseDto } from '../../common/dto/pagination.dto';

   @Injectable()
   export class ParentService {
     constructor(private prisma: PrismaService) {}

     async create(dto: CreateParentDto) {
       return this.prisma.person.create({
         data: {
           schoolId: dto.schoolId,
           personType: 'PARENT',
           firstName: dto.firstName,
           lastName: dto.lastName,
           email: dto.email,
           phone: dto.phone,
           parent: { create: { schoolId: dto.schoolId } },
         },
         include: { parent: true },
       });
     }

     async findAll(query: ParentListQueryDto): Promise<PaginatedResponseDto<any>> {
       const { schoolId, email, name } = query;
       if (!schoolId) throw new NotFoundException('schoolId query parameter is required');
       const personWhere: any = {};
       if (email) personWhere.email = { contains: email, mode: 'insensitive' };
       if (name) personWhere.OR = [
         { firstName: { contains: name, mode: 'insensitive' } },
         { lastName: { contains: name, mode: 'insensitive' } },
       ];
       const where: any = { schoolId };
       if (Object.keys(personWhere).length > 0) where.person = personWhere;

       const [data, total] = await Promise.all([
         this.prisma.parent.findMany({
           where,
           include: { person: true, _count: { select: { children: true } } },
           skip: query.skip,
           take: query.limit,
           orderBy: { person: { lastName: 'asc' } },
         }),
         this.prisma.parent.count({ where }),
       ]);
       return {
         data,
         meta: { page: query.page, limit: query.limit, total, totalPages: Math.ceil(total / query.limit) },
       };
     }

     async findOne(id: string) {
       const parent = await this.prisma.parent.findUnique({
         where: { id },
         include: { person: true, children: { include: { student: { include: { person: true } } } } },
       });
       if (!parent) throw new NotFoundException('Der angeforderte Erziehungsberechtigte wurde nicht gefunden.');
       return parent;
     }

     async update(id: string, dto: UpdateParentDto) {
       const parent = await this.findOne(id);
       const personData: Record<string, unknown> = {};
       (['firstName','lastName','email','phone'] as const).forEach((k) => {
         if (dto[k] !== undefined) personData[k] = dto[k];
       });
       if (Object.keys(personData).length > 0) {
         await this.prisma.person.update({ where: { id: parent.personId }, data: personData });
       }
       return this.findOne(id);
     }

     async remove(id: string) {
       const parent = await this.findOne(id);
       const linkedStudents = await this.prisma.parentStudent.count({ where: { parentId: id } });
       if (linkedStudents > 0) {
         throw new ConflictException({
           type: 'https://schoolflow.dev/errors/parent-has-dependents',
           title: 'Erziehungsberechtigte:r kann nicht gelöscht werden',
           status: 409,
           detail: 'Es bestehen noch Verknüpfungen zu Schüler:innen. Lösen Sie diese zuerst.',
           extensions: { affectedEntities: { linkedStudents } },
         });
       }
       await this.prisma.person.delete({ where: { id: parent.personId } });
     }
   }
   ```

   **`parent.controller.ts`:**
   ```ts
   import { Body, Controller, Delete, Get, HttpCode, HttpStatus, Param, Post, Put, Query } from '@nestjs/common';
   import { ApiBearerAuth, ApiOperation, ApiResponse, ApiTags } from '@nestjs/swagger';
   import { ParentService } from './parent.service';
   import { CreateParentDto } from './dto/create-parent.dto';
   import { UpdateParentDto } from './dto/update-parent.dto';
   import { ParentListQueryDto } from './dto/parent-list-query.dto';
   import { CheckPermissions } from '../auth/decorators/check-permissions.decorator';

   @ApiTags('parents')
   @ApiBearerAuth()
   @Controller('parents')
   export class ParentController {
     constructor(private service: ParentService) {}

     @Post()
     @CheckPermissions({ action: 'create', subject: 'parent' })
     @ApiOperation({ summary: 'Create a new parent with person record' })
     async create(@Body() dto: CreateParentDto) { return this.service.create(dto); }

     @Get()
     @CheckPermissions({ action: 'read', subject: 'parent' })
     @ApiOperation({ summary: 'List parents by school with optional email/name filter' })
     async findAll(@Query() query: ParentListQueryDto) { return this.service.findAll(query); }

     @Get(':id')
     @CheckPermissions({ action: 'read', subject: 'parent' })
     @ApiOperation({ summary: 'Get a parent by ID with nested children students' })
     async findOne(@Param('id') id: string) { return this.service.findOne(id); }

     @Put(':id')
     @CheckPermissions({ action: 'update', subject: 'parent' })
     @ApiOperation({ summary: 'Update parent stammdaten' })
     async update(@Param('id') id: string, @Body() dto: UpdateParentDto) { return this.service.update(id, dto); }

     @Delete(':id')
     @HttpCode(HttpStatus.NO_CONTENT)
     @CheckPermissions({ action: 'delete', subject: 'parent' })
     @ApiOperation({ summary: 'Delete a parent (Orphan-Guard if linked to students)' })
     @ApiResponse({ status: 409, description: 'Parent is linked to one or more students' })
     async remove(@Param('id') id: string) { await this.service.remove(id); }
   }
   ```

   **`parent.module.ts`:**
   ```ts
   import { Module } from '@nestjs/common';
   import { PrismaService } from '../../config/database/prisma.service';
   import { ParentService } from './parent.service';
   import { ParentController } from './parent.controller';

   @Module({
     controllers: [ParentController],
     providers: [ParentService, PrismaService],
     exports: [ParentService],
   })
   export class ParentModule {}
   ```

8. **Turn parent.service.spec.ts Wave 0 stubs green** — implement each it.todo with vi.fn mocked PrismaService. Follow the pattern in `apps/api/src/modules/subject/subject.service.spec.ts` if present, otherwise mirror `teacher.service.spec.ts`.

9. **Register ParentModule in AppModule** — in `apps/api/src/app.module.ts` add `import { ParentModule } from './modules/parent/parent.module';` and add `ParentModule` to the `imports` array. Add the new subject to CASL abilities if the permission seed (if applicable) lives there; otherwise `@CheckPermissions({ subject: 'parent' })` will pass through the generic CASL factory (follow Phase 11-01 KeycloakAdminModule precedent — no ability registration required at this layer).

10. **Verify** with:
    - `pnpm --filter @schoolflow/api test:run -- student.service` (all describes green)
    - `pnpm --filter @schoolflow/api test:run -- parent.service` (all describes green)
    - `pnpm --filter @schoolflow/api build` (Nest compiles)
    - Smoke via `pnpm --filter @schoolflow/api exec nest start` (must boot without errors); kill after 10s.
  </action>

  <acceptance_criteria>
    - `apps/api/src/modules/student/student.service.ts` contains `async archive(` AND `async restore(` AND `async linkParent(` AND `async unlinkParent(` AND `ConflictException` AND `attendanceCount` inside .remove (grep)
    - `apps/api/src/modules/student/student.controller.ts` contains `@Post(':id/archive')` AND `@Post(':id/restore')` AND `@Post(':id/parents')` AND `@Delete(':id/parents/:parentId')` (grep)
    - `apps/api/src/modules/student/dto/create-student.dto.ts` contains `parentIds?:` AND `@IsArray()` AND `@IsUUID('4', { each: true })` (grep)
    - `apps/api/src/modules/student/dto/student-list-query.dto.ts` contains `archived: StudentArchiveFilter = 'active'` (grep)
    - `apps/api/src/modules/parent/parent.module.ts` exists AND exports `ParentModule` class (grep)
    - `apps/api/src/modules/parent/parent.service.ts` contains `personType: 'PARENT'` AND Orphan-Guard ConflictException for linkedStudents > 0 (grep)
    - `apps/api/src/modules/parent/parent.controller.ts` contains `@Controller('parents')` AND 5 decorated route handlers (grep `@(Get|Post|Put|Delete)` count ≥5)
    - `apps/api/src/app.module.ts` imports `ParentModule` (grep)
    - `pnpm --filter @schoolflow/api test:run -- student.service` green (ALL describe blocks incl. Orphan-Guard, archive, create-parentIds, link/unlink)
    - `pnpm --filter @schoolflow/api test:run -- parent.service` green (ALL 5 describe blocks)
    - `pnpm --filter @schoolflow/api build` succeeds
    - API starts without error (check stdout for 'Nest application successfully started' within 10s)
  </acceptance_criteria>

  <verify>
    <automated>pnpm --filter @schoolflow/api test:run -- student.service parent.service && pnpm --filter @schoolflow/api build</automated>
  </verify>

  <done>
    ParentModule is registered + all 5 endpoints work; StudentService has Orphan-Guard + archive/restore + parent link/unlink + create.parentIds; CreateStudentDto accepts parentIds; StudentController exposes 4 new endpoints; all unit tests (Wave 0 stubs from Task 1) are green; API boots clean. Task 3 wires the frontend against these endpoints.
  </done>
</task>

<task type="auto">
  <name>Task 3: Frontend — TanStack Query hooks + /admin/students route + Detail-Page 3 tabs + Create/Move/Archive/Delete dialogs + Parent search/create/link + Sidebar entry</name>
  <files>apps/web/src/hooks/useStudents.ts, apps/web/src/hooks/useParents.ts, apps/web/src/routes/_authenticated/admin/students.index.tsx, apps/web/src/routes/_authenticated/admin/students.$studentId.tsx, apps/web/src/components/admin/student/StudentListTable.tsx, apps/web/src/components/admin/student/StudentMobileCards.tsx, apps/web/src/components/admin/student/StudentFilterBar.tsx, apps/web/src/components/admin/student/StudentCreateDialog.tsx, apps/web/src/components/admin/student/StudentDetailTabs.tsx, apps/web/src/components/admin/student/StudentStammdatenTab.tsx, apps/web/src/components/admin/student/StudentParentsTab.tsx, apps/web/src/components/admin/student/StudentGroupsTab.tsx, apps/web/src/components/admin/student/ArchiveStudentDialog.tsx, apps/web/src/components/admin/student/RestoreStudentDialog.tsx, apps/web/src/components/admin/student/MoveStudentDialog.tsx, apps/web/src/components/admin/student/ParentSearchPopover.tsx, apps/web/src/components/admin/student/InlineCreateParentForm.tsx, apps/web/src/components/admin/student/DeleteStudentDialog.tsx, apps/web/src/components/layout/AppSidebar.tsx, apps/web/src/components/layout/MobileSidebar.tsx</files>

  <read_first>
    - apps/web/src/hooks/useTeachers.ts — reference for studentKeys pattern, every useMutation wires onError (Silent-4xx invariant)
    - apps/web/src/hooks/useSubjects.ts — second analog for SubjectApiError-like class + 409 AffectedEntities handling
    - apps/web/src/routes/_authenticated/admin/teachers.index.tsx — list route analog: PageShell + filter-bar + table + Create dialog
    - apps/web/src/routes/_authenticated/admin/teachers.$teacherId.tsx — detail route analog: Zod-validated search param ?tab= + TeacherDetailTabs
    - apps/web/src/components/admin/teacher/TeacherListTable.tsx — desktop table pattern (role="table", tabular numbers, row-click, action menu)
    - apps/web/src/components/admin/teacher/TeacherMobileCards.tsx — mobile stacked-cards pattern (<640px)
    - apps/web/src/components/admin/teacher/TeacherDetailTabs.tsx — 4-tab orchestrator with Zustand dirty-slice-per-tab + UnsavedChangesDialog
    - apps/web/src/components/admin/teacher/StammdatenTab.tsx — RHF + zodResolver + pro-tab-save pattern
    - apps/web/src/components/admin/teacher/TeacherCreateDialog.tsx — create dialog form pattern
    - apps/web/src/components/admin/teacher/ArchiveTeacherDialog.tsx — WarnDialog wrapper pattern
    - apps/web/src/components/admin/teacher/DeleteTeacherDialog.tsx — 409 Orphan-Guard blocked-state pattern with AffectedEntitiesList
    - apps/web/src/components/admin/shared/PageShell.tsx
    - apps/web/src/components/admin/shared/UnsavedChangesDialog.tsx
    - apps/web/src/components/admin/shared/StickyMobileSaveBar.tsx
    - apps/web/src/components/admin/shared/WarnDialog.tsx
    - apps/web/src/components/layout/AppSidebar.tsx — 'Personal & Fächer' group present (Phase 11-01); APPEND Schüler:innen entry
    - apps/web/src/components/layout/MobileSidebar.tsx — mirror the same append
    - apps/web/src/components/ui/command.tsx — shadcn Command primitive (installed in Task 1)
    - apps/web/src/components/ui/checkbox.tsx — shadcn Checkbox primitive (installed in Task 1)
    - apps/web/src/lib/api.ts — apiFetch + parseProblemJson (RFC 9457 with extensions.affectedEntities)
    - apps/web/src/stores/school-context-store.ts — useSchoolContext for schoolId + activeSchoolYearId
    - .planning/phases/12-sch-ler-klassen-und-gruppenverwaltung/12-UI-SPEC.md — FULL spec: §Copywriting Contract (verbatim German strings), §Color reserved-for, §Component Inventory, §Interaction Choreography (Parent Search-by-Email, Multi-select Bulk Move, Pro-Tab-Save)
    - .planning/phases/12-sch-ler-klassen-und-gruppenverwaltung/12-CONTEXT.md D-01..D-05 + D-13.1 — UI + parent-link behaviour specs
    - packages/shared/src/schemas/student.schema.ts + parent.schema.ts + assign-parent.schema.ts + move-student.schema.ts — Zod schemas for RHF zodResolver
  </read_first>

  <action>
1. **Create `apps/web/src/hooks/useStudents.ts`** mirroring `useSubjects.ts` structure:
   - Types: `StudentDto`, `StudentListFilters` (re-exported from @schoolflow/shared), `StudentApiError` class with `problem.extensions?.affectedEntities`
   - `studentKeys = { all: (schoolId) => ['students', schoolId], list: (schoolId, filters) => [...], detail: (id) => ['students','detail',id], affected: (id) => ['students','affected',id] }`
   - `useStudents(schoolId, filters)` — `useQuery` with queryFn calling `apiFetch('/api/v1/students?...' + query-string)`; `enabled: !!schoolId`
   - `useStudent(studentId)` — detail fetch
   - `useCreateStudent()` — POST /api/v1/students; onSuccess invalidate ['students', schoolId]; onError `toast.error(problem?.detail ?? 'Speichern nicht möglich')`
   - `useUpdateStudent(studentId)` — PUT /api/v1/students/:id; same pattern
   - `useArchiveStudent(studentId)` — POST /api/v1/students/:id/archive; success toast 'Schüler:in archiviert', invalidate list + detail
   - `useRestoreStudent(studentId)` — POST /api/v1/students/:id/restore; success toast 'Schüler:in reaktiviert'
   - `useDeleteStudent()` — DELETE /api/v1/students/:id; on 409 CATCH parses affectedEntities, throws typed `StudentApiError` WITHOUT green toast (dialog renders AffectedEntitiesList); on other errors → red toast
   - `useMoveStudent()` — wraps `useUpdateStudent` with classId-only payload
   - `useBulkMoveStudents()` — custom mutation that iterates studentIds sequentially, invoking PUT /api/v1/students/:id per row; aggregates results with progress callback; final toast `{done}/{total} Schüler:innen verschoben` on success, partial-failure toast `{done}/{total} verschoben. Fehler bei {name}.` on first error (stops then)
   - Every mutation MUST wire explicit onError → `toast.error(...)` (Silent-4xx invariant codified by Phase 10.2-04 + 11-01)

2. **Create `apps/web/src/hooks/useParents.ts`** mirroring same patterns:
   - `parentKeys = { all: (schoolId) => ['parents', schoolId], byEmail: (email) => ['parents','byEmail',email], detail: (id) => ['parents','detail',id] }`
   - `useParentsByEmail({ schoolId, email, enabled })` — GET /api/v1/parents?schoolId=...&email=... with `enabled: enabled && email.length >= 3 && !!schoolId` + `staleTime: 30_000` + `debounce 300ms` (use `useDebouncedValue` util — if not present, inline simple effect-based debounce)
   - `useCreateParent()` — POST /api/v1/parents with Person-nested-create via backend; on success invalidate parent lists
   - `useLinkParentToStudent(studentId)` — POST /api/v1/students/:id/parents body `{ parentId }`; on success invalidate student detail
   - `useUnlinkParentFromStudent(studentId)` — DELETE /api/v1/students/:id/parents/:parentId; on success invalidate student detail
   - All mutations wire explicit onError → `toast.error(...)`

3. **Create `apps/web/src/components/admin/student/StudentFilterBar.tsx`**:
   - Sticky top bar (inside PageShell content region) with 4 controls:
     - `Input` search (placeholder: `Name oder E-Mail suchen …`) — debounce 300ms
     - `Select` class (options: `Alle Klassen` default; dynamic list via `useClasses` stub — if Plan 12-02 hasn't shipped yet, use a plain fetch helper `apiFetch('/api/v1/classes?schoolId=...')` — or accept that the dropdown renders empty until Plan 12-02 lands; the filter backend already handles classId)
     - `Select` status (options: `Aktiv` (default) / `Archiviert` / `Alle`)
     - `Select` schoolYear (options populated from `useSchoolYears` — existing Phase 10 hook)
   - "Filter zurücksetzen" ghost button appears when any filter differs from defaults
   - Mobile <640px: collapses into `SlidersHorizontal` button → bottom-sheet Dialog with same controls
   - Copy verbatim from 12-UI-SPEC.md §Copywriting Contract §Inline micro-copy

4. **Create `apps/web/src/components/admin/student/StudentListTable.tsx`**:
   - Columns: Checkbox | Nachname | Vorname | Klasse | Status (Aktiv/Archiviert Badge) | Aktionen
   - Header checkbox triggers select-all-on-page; row checkboxes select individuals; selection state lifted to parent via props (parent owns Zustand)
   - Row click (NOT on checkbox/action cell) navigates to `/admin/students/$studentId` via `useNavigate`
   - Actions dropdown: Edit (link), Archivieren/Reaktivieren (by status), "In andere Klasse verschieben", Löschen
   - `tabular-nums` on any numeric column; Status badge uses `bg-success/10 text-success` for Aktiv and `bg-destructive/10 text-destructive` for Archiviert (per 12-UI-SPEC.md §Color)
   - ARIA: `role="table"`, `<th scope="col">`

5. **Create `apps/web/src/components/admin/student/StudentMobileCards.tsx`** — stacked Card-per-row for <640px with h-11 tap targets on actions + checkbox. Same row-click-to-detail. Same bulk-select integration.

6. **Create `apps/web/src/components/admin/student/StudentCreateDialog.tsx`** — shadcn Dialog + RHF (zodResolver using StudentCreateSchema from @schoolflow/shared). Fields: firstName, lastName, email, phone, address, dateOfBirth, socialSecurityNumber (masked), studentNumber, classId (Select driven by fetch or pre-computed list), enrollmentDate, optional parentIds (initial link via ParentSearchPopover embedded). Cancel/Create buttons; onSubmit invokes `useCreateStudent`. Mobile 375: full-screen sheet variant (reuse existing Sheet primitive).

7. **Create `apps/web/src/components/admin/student/StudentDetailTabs.tsx`** — 3-tab Tabs container mirroring TeacherDetailTabs:
   - Tabs: `stammdaten` | `parents` | `groups`
   - Zustand per-tab dirty-slice (`studentStammdatenDirty`, `studentParentsDirty`, `studentGroupsDirty`)
   - On tab-switch with dirty=true show UnsavedChangesDialog (copy from 12-UI-SPEC.md verbatim)
   - PageShell breadcrumb: `Admin → Schüler:innen → {firstName} {lastName}`
   - Route-param sync: `?tab=stammdaten|parents|groups` via Zod-validated search param (StudentDetailSearchSchema inline)

8. **Create `apps/web/src/components/admin/student/StudentStammdatenTab.tsx`** — RHF form (zodResolver(StudentUpdateSchema)). Fields layout per 12-UI-SPEC.md §Responsive (2-column ≥1024px, 1-column <1024px). Save button bottom-right (desktop); StickyMobileSaveBar on <640px. On dirty set store slice. On submit call `useUpdateStudent`.

9. **Create `apps/web/src/components/admin/student/StudentParentsTab.tsx`**:
   - Render list of linked parents (from `student.parentStudents`) — each row has firstName + lastName + email + phone + Unlink icon button
   - Above list: `ParentSearchPopover` trigger button "Erziehungsberechtigte:n verknüpfen"
   - Empty state per 12-UI-SPEC.md: `Keine Erziehungsberechtigten verknüpft` + `Suchen Sie per E-Mail nach bestehenden Eltern oder legen Sie neue an.` + CTA
   - Unlink click opens WarnDialog with copy from 12-UI-SPEC.md §Destructive confirmations; confirm calls `useUnlinkParentFromStudent`
   - Silent-4xx invariant asserted via tests (Task 1 stubs)

10. **Create `apps/web/src/components/admin/student/ParentSearchPopover.tsx`**:
    - Uses shadcn Command primitive inside Popover
    - Command Input: placeholder `E-Mail eingeben …`
    - Debounce 300ms → `useParentsByEmail({ schoolId, email: current, enabled: email.length >= 3 })`
    - 200 match: renders CommandItem with `{firstName} {lastName} · {email}` + green `CircleCheck`; Enter triggers a Confirm-Dialog `Erziehungsberechtigte:n verknüpfen` → on confirm call `useLinkParentToStudent`
    - 404/empty: `CommandEmpty` renders `Keine Treffer. Neu:e Erziehungsberechtigte:n anlegen?` + `Button` that swaps Command content to `InlineCreateParentForm`
    - On link/create success: close popover, toast `Verknüpfung angelegt`

11. **Create `apps/web/src/components/admin/student/InlineCreateParentForm.tsx`** — mini RHF form (zodResolver(ParentCreateSchema)): firstName, lastName, email (prefilled from search), phone. Submit calls `useCreateParent` then `useLinkParentToStudent` sequentially (inside a single async handler — if first fails, toast error and bail; if second fails, surface error but parent record survives). Cancel returns to CommandInput.

12. **Create `apps/web/src/components/admin/student/StudentGroupsTab.tsx`** — READ-MOSTLY per D-02:
    - List of GroupMembership rows grouped by group: group name + type badge (RELIGION/WAHLPFLICHT/LEISTUNG/LANGUAGE/CUSTOM) + level + member-count (this student visible) + `Auto` green badge or `Manuell` amber badge per membership
    - Deep-link per group: `<Link to="/admin/classes/$classId?tab=groups">` (target arrives in Plan 12-02)
    - Empty state: `Keine Gruppen-Mitgliedschaften` per 12-UI-SPEC.md

13. **Create Archive/Restore/Move/Delete dialogs** (WarnDialog wrappers + one full Move dialog):
    - `ArchiveStudentDialog.tsx` — title + body + primary `Archivieren` (destructive variant) per 12-UI-SPEC.md verbatim
    - `RestoreStudentDialog.tsx` — title + body + primary `Reaktivieren` (primary variant)
    - `DeleteStudentDialog.tsx` — calls `useDeleteStudent` → on 409 extract `problem.extensions.affectedEntities`, switch dialog to BLOCKED state with AffectedEntitiesList kind='student' + copy 'Diese:r Schüler:in hat noch Verknüpfungen. Öffnen Sie die Details oder archivieren Sie stattdessen.' + red toast via Silent-4xx invariant
    - `MoveStudentDialog.tsx` — props `{ mode: 'single' | 'bulk'; studentIds: string[]; currentClassId?: string; onSuccess: () => void }`; RHF(zodResolver(mode==='bulk'?BulkMoveStudentSchema:MoveStudentSchema)); single mode heading + body per 12-UI-SPEC.md; bulk mode avatar-stack max 5 + `+N weitere`; target class picker; optional `notiz` textarea; on submit invokes `useMoveStudent` (single) or `useBulkMoveStudents` (bulk, sequential with progress toast)

14. **Implement `apps/web/src/routes/_authenticated/admin/students.index.tsx`** with TanStack Router `createFileRoute`:
    - Search param Zod validation: `z.object({ search: z.string().optional(), classId: z.string().uuid().optional(), archived: z.enum(['active','archived','all']).default('active'), schoolYearId: z.string().uuid().optional(), page: z.number().int().min(1).default(1) })`
    - PageShell title `Schüler:innen`, subtitle `{total} Schüler:innen im Schuljahr {activeYearLabel}`
    - Right-top `Schüler:in anlegen` button (primary) opens StudentCreateDialog
    - StudentFilterBar wired to search params
    - StudentListTable (≥640px) / StudentMobileCards (<640px) driven by `useStudents(schoolId, filters)`
    - Selection state in local `useState<Set<string>>`; floating bulk toolbar visible when size > 0 with `Ausgewählte verschieben` (primary) + close-X
    - Pagination bar below table (reuse existing pagination primitive if available; else simple prev/next)

15. **Implement `apps/web/src/routes/_authenticated/admin/students.$studentId.tsx`** with `createFileRoute`:
    - Param `studentId` UUID
    - Search param `tab: z.enum(['stammdaten','parents','groups']).default('stammdaten')`
    - Fetch student via `useStudent(studentId)`; show skeleton while loading
    - PageShell title `{firstName} {lastName}` + Archiv badge if archived + Row-action menu top-right (Archivieren/Reaktivieren/Verschieben/Löschen)
    - StudentDetailTabs

16. **Sidebar updates** — in `apps/web/src/components/layout/AppSidebar.tsx` + `MobileSidebar.tsx`:
    - Find the `navItems` array entry for group `'Personal & Fächer'` (established by Phase 11-01)
    - APPEND new item:
      ```ts
      { label: 'Schüler:innen', href: '/admin/students', icon: UsersRound, roles: ['admin','schulleitung'], group: 'Personal & Fächer' }
      ```
    - Order inside group: Lehrer → Fächer → Schüler:innen (Klassen appended by Plan 12-02)
    - Import `UsersRound` from `lucide-react`

17. **Verify** with the full web test suite AND type-check. DO NOT run Playwright here (Plan 12-03 owns E2E).
  </action>

  <acceptance_criteria>
    - `apps/web/src/hooks/useStudents.ts` exports `useStudents`, `useStudent`, `useCreateStudent`, `useUpdateStudent`, `useArchiveStudent`, `useRestoreStudent`, `useDeleteStudent`, `useMoveStudent`, `useBulkMoveStudents` AND contains at least 9 `onError` occurrences (grep `onError` count ≥9 — Silent-4xx invariant)
    - `apps/web/src/hooks/useParents.ts` exports `useParentsByEmail`, `useCreateParent`, `useLinkParentToStudent`, `useUnlinkParentFromStudent` AND every mutation hook wires explicit onError (grep)
    - `apps/web/src/routes/_authenticated/admin/students.index.tsx` contains `StudentListTable` AND `StudentCreateDialog` AND `StudentFilterBar` AND `useStudents(` (grep)
    - `apps/web/src/routes/_authenticated/admin/students.$studentId.tsx` contains `StudentDetailTabs` AND `useStudent(` AND Zod search param `z.enum(['stammdaten','parents','groups'])` (grep)
    - `apps/web/src/components/admin/student/StudentDetailTabs.tsx` contains tab values `stammdaten`, `parents`, `groups` AND `UnsavedChangesDialog` (grep)
    - `apps/web/src/components/admin/student/ParentSearchPopover.tsx` contains `CommandInput` AND `useParentsByEmail(` AND `InlineCreateParentForm` (grep)
    - `apps/web/src/components/admin/student/MoveStudentDialog.tsx` contains `mode: 'single' | 'bulk'` AND `avatar-stack` OR `+N weitere` copy (grep)
    - `apps/web/src/components/admin/student/DeleteStudentDialog.tsx` contains `AffectedEntitiesList` AND `kind="student"` (grep)
    - `apps/web/src/components/admin/student/ArchiveStudentDialog.tsx` contains German copy `Archivieren` AND `archiviert markiert` (grep)
    - `apps/web/src/components/layout/AppSidebar.tsx` contains `UsersRound` AND `/admin/students` AND label `Schüler:innen` (grep)
    - `apps/web/src/components/layout/MobileSidebar.tsx` contains `UsersRound` AND `/admin/students` (grep)
    - StudentStammdatenTab.test.tsx / StudentParentsTab.test.tsx / MoveStudentDialog.test.tsx: all it.todo() converted to real `it(...)` (grep `it.todo` should be 0)
    - `pnpm --filter @schoolflow/web test:run` green (all new component tests pass)
    - `pnpm --filter @schoolflow/web typecheck` (or `pnpm -w typecheck` if configured) succeeds
    - `pnpm -r build` succeeds end-to-end
    - Manual smoke: starting `pnpm --filter @schoolflow/web dev` and visiting /admin/students renders table + opens Create dialog without runtime errors (executor MUST run this locally; CI skip OK)
  </acceptance_criteria>

  <verify>
    <automated>pnpm --filter @schoolflow/web test:run && pnpm -r build</automated>
  </verify>

  <done>
    /admin/students list + /admin/students/$id detail page fully functional with RHF+zodResolver forms, ParentSearchPopover + Inline-Create flow, bulk-move multi-select, Archive/Restore/Delete/Move dialogs wired, Sidebar + MobileSidebar show Schüler:innen entry with role-gating, every mutation has explicit onError (Silent-4xx invariant), all component tests green, web bundle compiles. Plan 12-02 can now append Klassen entry to same sidebar group and consume AffectedEntitiesList kind='student' if needed; Plan 12-03 can author E2E specs against the shipped UI.
  </done>
</task>

</tasks>

<verification>
Phase-level verification for Plan 12-01:

1. **Migration hygiene (CLAUDE.md hard rule):**
   - `bash scripts/check-migration-hygiene.sh` exits 0
   - `apps/api/prisma/migrations/20260424000000_student_archive_flag/migration.sql` tracked in git
   - `pnpm --filter @schoolflow/api exec prisma migrate status` reports `Database schema is up to date`

2. **Unit + integration green:**
   - `pnpm --filter @schoolflow/shared test:run` — all Zod specs pass
   - `pnpm --filter @schoolflow/api test:run` — student.service + parent.service pass
   - `pnpm --filter @schoolflow/web test:run` — 3 new component tests pass

3. **Workspace compiles:** `pnpm -r build` succeeds end-to-end.

4. **API boots:** `pnpm --filter @schoolflow/api exec nest start` prints 'Nest application successfully started' within 10s.

5. **UI smoke (manual):** visit `/admin/students` on dev server:
   - Empty state or seeded list renders (no runtime crash)
   - Create dialog opens with all fields
   - Filter bar + Schuljahr select populated
   - Click row → Detail page with 3 tabs, tab-switch without data-loss prompt (fresh)
   - Parent search popover: typing an email of length ≥3 fires request (observe network tab)
   - Archive action opens WarnDialog with German copy verbatim from 12-UI-SPEC.md

6. **Silent-4xx invariant:** grep `useMutation` in useStudents.ts + useParents.ts → every occurrence MUST be followed within the same block by `onError`. Test: `grep -c 'useMutation' apps/web/src/hooks/useStudents.ts` ≤ `grep -c 'onError' apps/web/src/hooks/useStudents.ts`.
</verification>

<success_criteria>
- All 4 requirement IDs (STUDENT-01, STUDENT-02, STUDENT-03, STUDENT-04) covered by shipped UI + backend + tests
- Backend gap-fixes shipped (ParentModule greenfield + CreateStudentDto.parentIds + archive/restore/linkParent/unlinkParent + StudentService.remove Orphan-Guard)
- Prisma migration committed with SQL file (scripts/check-migration-hygiene.sh green)
- AffectedEntitiesList extended with kind='student' (backward-compat preserved for teacher + subject)
- Shared Zod schemas live in @schoolflow/shared and consumed by both client (RHF zodResolver) and tests
- AppSidebar + MobileSidebar show 'Schüler:innen' entry in 'Personal & Fächer' group, role-gated
- shadcn Command + Checkbox primitives installed in apps/web/src/components/ui/
- Every new useMutation wires explicit onError → `sonner.toast.error(...)` (Silent-4xx invariant)
- German copywriting verbatim from 12-UI-SPEC.md §Copywriting Contract (no ad-hoc translations)
- Full workspace `pnpm -r build` green + all unit tests green
- Mobile-375 touch targets ≥44px on new list + dialog + tab surfaces (spot-check on dev server)
</success_criteria>

<output>
After completion, create `.planning/phases/12-sch-ler-klassen-und-gruppenverwaltung/12-01-SUMMARY.md` using the template at `.claude/get-shit-done/templates/summary.md`. Summary MUST include:
- REQ-IDs covered (STUDENT-01..04)
- Backend gap-fixes shipped (ParentModule, Student.isArchived migration, StudentService.remove Orphan-Guard, CreateStudentDto.parentIds)
- Files created/modified count (grouped shared / api / web)
- Any deviations from plan (with rationale)
- Explicit hand-off list for Plan 12-02: (a) AffectedEntitiesList discriminated union kinds remaining to add ('class', 'group', 'class-subject', 'group-membership', 'derivation-rule', 'parent-student'), (b) Sidebar 'Personal & Fächer' group now has Lehrer/Fächer/Schüler — Plan 12-02 appends Klassen, (c) shadcn Command/Checkbox available, (d) StudentService exposes parentIds + linkParent pattern reusable by Class-side workflows
</output>
