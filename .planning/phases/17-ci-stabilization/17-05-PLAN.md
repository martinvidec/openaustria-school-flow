---
phase: 17-ci-stabilization
plan: 05
type: execute
wave: 4
depends_on: [17-01, 17-02, 17-03, 17-04]
files_modified:
  - apps/web/e2e/admin-solver-tuning-restrictions.spec.ts
  - .planning/phases/17-ci-stabilization/17-TRIAGE.md
  - .planning/deferred-items.md
  # Additional spec files added during triage (TBD per actual failure list — executor will append to files_modified during execution)
autonomous: false
requirements: []
decisions_addressed: [D-01, D-05, D-06, D-07, D-08, D-12, D-13, D-14, D-15, D-16]
triage_ref: .planning/phases/17-ci-stabilization/17-TRIAGE.md
tags: [ci-stabilization, triage, fix-or-skip, smoke-pr, verification-gate]

must_haves:
  truths:
    - "Every remaining red E2E spec from PR #1 is classified in 17-TRIAGE.md as one of: real-bug (fixed), real-bug (deferred), CI-env (flake), CI-env (WebKit-darwin), missing-fixture, accessibility-lift (auto-resolved by 17-03), datalist-migration (auto-resolved by 17-04), or selector-drift (auto-resolved by 17-01)"
    - "Sample failure (admin-solver-tuning-restrictions, POST /constraint-templates 422) is investigated, classified, and either fixed or skipped with a reason annotation per CONTEXT D-13"
    - "30-min-fix-or-skip protocol applied per spec — no investigation longer than 30 minutes per failure (CONTEXT D-12)"
    - "Smoke-PR `chore/ci-smoke-noop` is created off main and CI passes WITHOUT --admin override (CONTEXT D-15)"
    - "If failures remain that are NOT classifiable as CI-env-flake -> Phase 17.1 spawned (CONTEXT D-16); otherwise Phase 17 verifier-gate is GREEN"
  artifacts:
    - path: ".planning/phases/17-ci-stabilization/17-TRIAGE.md"
      provides: "Master triage doc — fully populated with every remaining red spec classified"
      contains: "Resolution"
    - path: ".planning/deferred-items.md"
      provides: "Deferred parking lot — Phase-17-deferred E2E specs with reasons"
      contains: "Phase 17 deferred"
    - path: "apps/web/e2e/admin-solver-tuning-restrictions.spec.ts"
      provides: "Sample-failure investigation outcome (either fixed against backend, OR test.skip(true, 'Phase 17 deferred: ...'))"
      contains: "constraint-templates"
  key_links:
    - from: "Smoke-PR chore/ci-smoke-noop"
      to: "GitHub Actions Run Playwright tests step"
      via: "PR off main with empty change"
      pattern: "ci-smoke-noop"
    - from: "17-TRIAGE.md"
      to: ".planning/deferred-items.md"
      via: "deferred-items reference"
      pattern: "deferred-items"
---

<objective>
Plan E (CONTEXT D-12, D-13, D-14): Triage every remaining red E2E spec from PR #1 (run 25065085891) using the 30-minute-fix-or-skip protocol. Investigate the sample failure (admin-solver-tuning-restrictions, POST /constraint-templates 422) first as the canonical example. Then sweep the remaining specs that are NOT auto-resolved by Plans F (17-01), A/B/C (17-03), and D (17-04).

The PHASE-17-VERIFIER-GATE (CONTEXT D-15, D-16) is also handled here:
- Create the smoke-PR `chore/ci-smoke-noop` off main with a no-op change.
- Push and let CI run WITHOUT --admin override.
- If CI passes -> Phase 17 done.
- If CI still red AND failures are not CI-env-flake-classifiable -> spawn Phase 17.1 per CONTEXT D-16.

Purpose: Closes Phase 17. Either ships green CI baseline or hands off a clean Phase 17.1 scope. NO failure leaves Phase 17 unclassified.

Output:
- Sample-failure resolved (fixed OR skipped) with full triage row
- All remaining red specs classified
- Smoke-PR created and verified
- Phase 17.1 scope (if needed) drafted
</objective>

<execution_context>
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ci-stabilization/17-CONTEXT.md
@.planning/phases/17-ci-stabilization/17-PATTERNS.md
@.planning/phases/17-ci-stabilization/17-TRIAGE.md
@.planning/v1.1-OPEN-ITEMS-INVENTORY.md
@apps/web/e2e/admin-solver-tuning-restrictions.spec.ts
@apps/web/e2e/admin-solver-tuning-integration.spec.ts
@apps/web/e2e/admin-timetable-edit-dnd.spec.ts
@apps/api/src/modules/timetable/constraint-template.service.ts

<interfaces>
30-minute-fix-or-skip protocol (CONTEXT D-12):

```
For each red spec (in cluster order Phase 13 -> 14 -> 15 -> 10.5):
  start_timer(30min)
  step 1: Local repro — pnpm --filter @schoolflow/web exec playwright test <spec> --project=desktop --repeat-each=3
    if 0/3 fail -> classify CI-env (flake/timing); add to triage; CONTINUE to next spec
    if >=2/3 fail -> classify real-bug; proceed to step 2
    if 1/3 fail -> classify flake; add retry-strategy in test file; CONTINUE
  step 2: Investigate root cause (backend? frontend? fixture?)
  step 3a: If backend bug found AND fix fits in remaining time -> fix; verify; commit
  step 3b: If not -> annotate test.skip(true, 'Phase 17 deferred: <reason> — see 17-TRIAGE.md row N'); add to deferred-items.md
end_timer
```

Skip-with-reason annotation pattern (Pattern S6 from PATTERNS.md):

```typescript
// Phase 17 deferred: <reason> — see 17-TRIAGE.md row N
test.skip(true, 'Phase 17 deferred: <reason> — see 17-TRIAGE.md row N');
```

Sample failure context (CONTEXT D-13):
- Spec: apps/web/e2e/admin-solver-tuning-restrictions.spec.ts (POST /constraint-templates seed -> 422)
- Suspect: Phase-14 DTO drift in apps/api/src/modules/timetable/constraint-template.service.ts + apps/api/src/modules/timetable/dto/

Pre-existing-spec definition (CONTEXT D-14):
- Spec is "pre-existing" iff documented as failing/flaky in Phase 13/14/15 SUMMARY.
- If NOT pre-existing -> echte Regression durch Phase-16 Refactor -> already handled by Plan D (Wave 3) OR escalate to Phase 17.1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Triage the sample failure (admin-solver-tuning-restrictions) end-to-end as canonical demo</name>
  <read_first>
    - apps/web/e2e/admin-solver-tuning-restrictions.spec.ts (full — esp. line 42 POST /constraint-templates seed)
    - apps/api/src/modules/timetable/constraint-template.service.ts (full — find the POST handler)
    - apps/api/src/modules/timetable/dto/ (every DTO file in this directory — search for ConstraintTemplate-related DTOs)
    - apps/web/e2e/helpers/constraints.ts (helper that the spec uses)
    - .planning/phases/17-ci-stabilization/17-PATTERNS.md (Plan E section + Pattern S6 skip-with-reason)
    - .planning/phases/14-solver-tuning/14-03-SUMMARY.md (Phase 14 sub-plan 03 — likely owner of constraint-templates DTO)
    - .planning/phases/17-ci-stabilization/17-TRIAGE.md (current state from 17-01..17-04)
  </read_first>
  <files>apps/web/e2e/admin-solver-tuning-restrictions.spec.ts, .planning/phases/17-ci-stabilization/17-TRIAGE.md, .planning/deferred-items.md (if skip path chosen — and possibly apps/api/src/modules/timetable/constraint-template.service.ts + apps/api/src/modules/timetable/dto/* if backend-fix path chosen)</files>
  <action>
Apply the 30-minute-fix-or-skip protocol to the sample failure:

STEP 1 — Local repro (5 min budget):
```
pnpm --filter @schoolflow/web exec playwright test admin-solver-tuning-restrictions --project=desktop --repeat-each=3
```

STEP 2 — Classify based on outcome:
- 0/3 fail desktop -> classify as CI-env (flake/timing). Append row to 17-TRIAGE.md and STOP.
- 1/3 fail -> classify as flake. Add retry-strategy `test.describe.configure({ retries: 2 })` to the spec file. Append row. STOP.
- >=2/3 fail -> classify as real-bug. Proceed to STEP 3.

STEP 3 — Investigate root cause (15 min budget). Sample is a 422 on `POST /constraint-templates`. Likely paths:
  a) Open `apps/api/src/modules/timetable/constraint-template.service.ts` — find POST handler signature.
  b) Open every DTO under `apps/api/src/modules/timetable/dto/`. Look for `CreateConstraintTemplateDto` or similar. The 422 is class-validator rejecting payload.
  c) Read the spec's POST body — what fields does the spec send? `grep -n 'POST.*constraint-templates' apps/web/e2e/helpers/constraints.ts` to find helper.
  d) Diff: spec body keys vs DTO @IsNotEmpty / @IsEnum / @IsArray decorators. Find the missing or renamed field.

STEP 4 — Decision (10 min budget):
- If you find the drift AND the fix is a small DTO adjustment OR a small spec helper update -> apply the fix, run the spec to verify GREEN, commit. Append `real-bug (fixed)` row to 17-TRIAGE.md citing the commit hash and the specific DTO field name.
- If the fix would require larger backend changes (e.g. a missing endpoint, a new fixture, a Phase-14-architectural-decision) -> annotate the spec with skip-with-reason per Pattern S6:

```typescript
// Phase 17 deferred: POST /constraint-templates 422 — Phase-14 DTO drift requires architectural review
// See 17-TRIAGE.md row N
test.skip(true, 'Phase 17 deferred: POST /constraint-templates 422 — Phase-14 DTO drift requires architectural review');
```

  Then add an entry to `.planning/deferred-items.md` (create file if missing) under a new `## Phase 17 deferred` section:

```markdown
## Phase 17 deferred

- `apps/web/e2e/admin-solver-tuning-restrictions.spec.ts:42` — POST /constraint-templates 422 — Phase-14 DTO drift on field `<NAME>`. Requires architectural review of how the `restriction` payload shape changed between Phase 14-02 and Phase 14-03. Owner: Phase 14 follow-up sprint.
```

STEP 5 — Append the triage row to 17-TRIAGE.md (Phase 14 cluster, before any subsequent rows added in Task 2):

```
| `apps/web/e2e/admin-solver-tuning-restrictions.spec.ts:42` (E2E-SOLVER-04) | red on PR #1 (run 25065085891) | <local-repro-counts> | <real-bug-fixed | real-bug-deferred | CI-env-flake | flake-with-retry> | E | <commit-hash OR skip-reason> |
```

Use this row as the canonical TEMPLATE for Task 2.
  </action>
  <verify>
    <automated>grep -c "admin-solver-tuning-restrictions" .planning/phases/17-ci-stabilization/17-TRIAGE.md &amp;&amp; { grep -c "Phase 17 deferred" apps/web/e2e/admin-solver-tuning-restrictions.spec.ts || pnpm --filter @schoolflow/web exec playwright test admin-solver-tuning-restrictions --project=desktop --reporter=line; }</automated>
  </verify>
  <acceptance_criteria>
    - The sample spec has EITHER a passing local repro (3/3 desktop) AFTER a backend or helper fix, OR a `test.skip(true, 'Phase 17 deferred: ...')` annotation pointing to a 17-TRIAGE.md row.
    - 17-TRIAGE.md contains a row for `admin-solver-tuning-restrictions.spec.ts:42` with explicit classification + resolution.
    - If skip path chosen: `.planning/deferred-items.md` has a `## Phase 17 deferred` section with the spec listed.
    - 30-minute timer was respected (note remaining failures count in commit message).
  </acceptance_criteria>
  <done>Sample failure triaged end-to-end. The protocol now has a canonical example for Task 2 to mirror.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sweep all remaining red specs through the same protocol</name>
  <read_first>
    - .planning/phases/17-ci-stabilization/17-TRIAGE.md (every row — to know what's already classified)
    - /tmp/mobile-chrome-after-D.log if it exists from 17-04 Task 3 (post-Plan-D mobile snapshot)
    - .planning/v1.1-OPEN-ITEMS-INVENTORY.md §9.1 (full failure cluster list — Phase 13/14/15/10.5/Mobile-375)
    - apps/web/e2e/ (full directory — `ls apps/web/e2e/*.spec.ts | head -50` to enumerate)
    - .planning/phases/13-*/13-*-SUMMARY.md, 14-*/14-*-SUMMARY.md, 15-*/15-*-SUMMARY.md (find pre-existing-failure annotations per CONTEXT D-14)
    - apps/web/e2e/admin-solver-tuning-integration.spec.ts (lines 30-33 — env-gate analog) and apps/web/e2e/admin-timetable-edit-dnd.spec.ts (lines 40-43 — condition-gate analog)
  </read_first>
  <files>
    apps/web/e2e/*.spec.ts (specs touched are TBD — executor must update files_modified frontmatter during execution),
    .planning/phases/17-ci-stabilization/17-TRIAGE.md,
    .planning/deferred-items.md
  </files>
  <action>
First, enumerate the candidate failures. Run a desktop project full sweep to get the current red list:

```
pnpm --filter @schoolflow/web exec playwright test --project=desktop --reporter=line | tee /tmp/desktop-after-Dlast.log
```

Cross-reference with the original PR #1 cluster (CONTEXT failure clusters Phase 13/14/15/10.5):
- Phase 13 (User-Verwaltung): admin-user-overrides (5), admin-user-roles (4), admin-user-permissions (2), admin-user-person-link (3), admin-user-silent-4xx (2), admin-users-list (3+).
- Phase 14 (Solver-Tuning): admin-solver-tuning-audit, -preferences (2), -restrictions (2 — sample handled in Task 1), -integration (already gated on E2E_RUN_SOLVER).
- Phase 15 (DSGVO/Audit): admin-audit-log-csv, admin-audit-log-detail, admin-audit-log-filter, admin-dsgvo-* Suite.
- Phase 10.5 (Operations): admin-import (3 tests, einer mit 1m-timeout), admin-timetable-edit-dnd.

For each spec STILL on the red list:

1. Apply the same 30-min protocol from Task 1.
2. Classify per CONTEXT D-05 (real-bug-Threshold = 2/3 desktop fail).
3. Append row to 17-TRIAGE.md sorted into its phase cluster.
4. Cluster heuristic per CONTEXT specifics: if multiple Phase-13-User-Failures share a single root cause (e.g. shared loginAsRole fixture timeout) -> fix once, log multiple rows resolved by single commit.

Special cases:
- `admin-solver-tuning-integration.spec.ts` — already gated `process.env.E2E_RUN_SOLVER !== '1'`. Classification: missing-fixture (env-gate). Add a single row marking it as `missing-fixture (already-gated)` with resolution `leave as-is`.
- `admin-timetable-edit-dnd.spec.ts` — already condition-gated `({ isMobile }) => isMobile`. Classification: condition-gate (already-handled). Same treatment.
- Any spec where 0/3 fail desktop AND mobile-chrome-after-D snapshot showed it was previously red -> classification `regression-candidate -> auto-resolved by Plan D` (CONTEXT D-14 special case). No file change needed; just a triage row.
- For shared root-cause clusters (e.g. all Phase-13-User-Failures): if a single fixture fix lights up >3 specs at once, log it as ONE row with `Specs affected: <list>`, commit once.

Total sweep budget: 30 min × N specs. If a spec exceeds 30 min, skip-with-reason and move on (CONTEXT D-12 hard rule).

After sweep, the 17-TRIAGE.md table is FULLY populated — every red spec from the original PR #1 list has a Resolution column entry.
  </action>
  <verify>
    <automated>SPEC_COUNT=$(grep -E '\\.spec\\.ts' .planning/phases/17-ci-stabilization/17-TRIAGE.md | wc -l) &amp;&amp; test "$SPEC_COUNT" -ge 10 &amp;&amp; pnpm --filter @schoolflow/web exec playwright test --project=desktop --reporter=line | tee /tmp/desktop-after-E.log &amp;&amp; ! grep -E "passed|failed" /tmp/desktop-after-E.log | grep -v "0 failed" | grep "failed" | head -1 || echo "Some failures remain — verify they are all skip-annotated or pending Phase 17.1"</automated>
  </verify>
  <acceptance_criteria>
    - 17-TRIAGE.md has a Resolution column entry for every spec in the original PR #1 failure-cluster list (Phase 13/14/15/10.5).
    - Every UNRESOLVED-still-red spec in /tmp/desktop-after-E.log has a `test.skip(true, 'Phase 17 deferred: ...')` annotation OR a documented reason in 17-TRIAGE.md why it remains red (e.g. "regression-candidate -> escalated to Phase 17.1").
    - Total sweep respected the per-spec 30-min budget.
    - .planning/deferred-items.md `## Phase 17 deferred` section lists every skipped spec with reason.
  </acceptance_criteria>
  <done>17-TRIAGE.md is fully populated. Every PR-#1 red spec has a classification AND a resolution.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Create smoke-PR chore/ci-smoke-noop and verify CI passes without --admin override</name>
  <files>(no source files modified — branch creation + GitHub PR creation only)</files>
  <action>
Automation BEFORE the human verification step (executor performs all of these):
  1. Create a branch `chore/ci-smoke-noop` off `main` with a single trivial change (e.g. add a comment-only line to `.planning/STATE.md` `last_activity` field).
  2. `gh pr create --base main --head chore/ci-smoke-noop --title "chore(ci): smoke-test noop PR for Phase 17 verifier gate" --body "Phase 17 verifier-gate per CONTEXT D-15. Validates the Run Playwright tests step passes without --admin override. If green: Phase 17 is done. If red: Phase 17.1 spawns per D-16."`
  3. Wait for CI run to complete (`gh pr checks <pr-number> --watch`).
  4. Capture the `Run Playwright tests` step status + run ID and surface to the user in the checkpoint message.
  </action>
  <what-built>
    - Smoke-PR `chore/ci-smoke-noop` created off main with a no-op change.
    - GitHub Actions CI ran the full pipeline including `Run Playwright tests`.
    - Run URL + step status surfaced for human verification.
  </what-built>
  <how-to-verify>
    1. Open the PR URL printed by `gh pr create`.
    2. Wait for the GitHub Actions check `Run Playwright tests` to complete (typically 8-15 minutes).
    3. VERIFY:
       - The `Run Playwright tests` step status: green (success) or red (failure)?
       - Total failure count if red.
       - For each red spec: is it already skip-annotated OR listed in 17-TRIAGE.md as Phase-17.1-escalation?
    4. Decide:
       - Smoke-PR is GREEN -> Phase 17 verifier-gate PASSED. Approve the checkpoint with "approved — Phase 17 done".
       - Smoke-PR is RED with only specs already classified as CI-env-flake or already skip-annotated -> Approve with "approved with caveat — flakes documented".
       - Smoke-PR is RED with NEW unclassified failures -> Reject. Phase 17.1 must spawn per CONTEXT D-16.
  </how-to-verify>
  <verify>
    <automated>gh pr view chore/ci-smoke-noop --json statusCheckRollup --jq '.statusCheckRollup[] | select(.name=="Run Playwright tests") | .conclusion'</automated>
  </verify>
  <done>Smoke-PR exists; CI run completed; result recorded; user has approved (with or without caveat) OR rejected; Phase 17 status is unambiguous.</done>
  <resume-signal>Type "approved" or "approved with caveat — flakes documented" or describe rejection reason</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Finalise 17-TRIAGE.md + handle smoke-PR outcome</name>
  <read_first>
    - .planning/phases/17-ci-stabilization/17-TRIAGE.md (current state — has every Plan F/G/A/B/C/D/E row)
    - The smoke-PR check result (from Task 3 output)
    - .planning/ROADMAP.md (Backlog section + Phase 17 entry — for Phase 17.1 spawn if needed)
  </read_first>
  <files>.planning/phases/17-ci-stabilization/17-TRIAGE.md, .planning/STATE.md (if Phase 17 closes), .planning/ROADMAP.md (if Phase 17.1 spawns)</files>
  <action>
Branch on smoke-PR outcome from Task 3:

A) APPROVED (smoke-PR GREEN or APPROVED-WITH-CAVEAT):

1. Append a `## Smoke-PR result (CONTEXT D-15 verifier gate)` section to 17-TRIAGE.md:

```
## Smoke-PR result (CONTEXT D-15 verifier gate)

**PR:** chore/ci-smoke-noop — `<URL>`
**Status:** <GREEN | GREEN-WITH-DOCUMENTED-FLAKES>
**Run:** GitHub Actions run `<run-id>`
**Verified:** Subsequent PRs from any branch can merge to main WITHOUT `--admin` override.
**Phase 17 verifier-gate:** PASSED.
```

2. Update STATE.md `current_position` to indicate Phase 17 complete:
   - status: complete
   - last_activity: <date> - Phase 17 verifier gate PASSED via smoke-PR <run-id>

3. Update .planning/ROADMAP.md Phase 17 entry: tick off the success criteria boxes:
   - [x] CI-Run Playwright tests-Step grün auf einer leeren PR off main
   - [x] Folge-PRs können ohne --admin-Override gemerged werden
   - [x] Triage-Dokument im Phase-Verzeichnis kategorisiert jede Failure als real-bug/flake/env

B) REJECTED (smoke-PR RED with unresolved failures):

1. Append `## Smoke-PR result (CONTEXT D-15 verifier gate)` section to 17-TRIAGE.md with status RED + run URL + the specific unresolved spec list.

2. Spawn Phase 17.1 in .planning/ROADMAP.md per CONTEXT D-16. Add this entry under the Phase 17 entry:

```markdown
### Phase 17.1: CI Stabilization follow-up — unresolved failures from Phase 17 smoke-PR

**Goal:** Resolve the <N> red specs that survived Phase 17's 30-min-per-spec triage budget. Each spec is documented in 17-TRIAGE.md with classification "regression-candidate -> escalated to Phase 17.1" or "real-bug -> deferred (out of 30-min budget)".

**Triggered by:** Smoke-PR chore/ci-smoke-noop run <run-id> showed CI red after Phase 17.

**Requirements:** Tech-debt-closure follow-up — keine REQ-IDs.
**Depends on:** Phase 17 (Complete; 17-TRIAGE.md is the input artifact).
**Plans:** TBD (will be planned with /gsd:plan-phase 17.1)

**Specs to resolve:**
<copy from 17-TRIAGE.md every row classified as "real-bug -> deferred" or "regression-candidate -> escalated to Phase 17.1">

**Success criteria:**
- [ ] Smoke-PR re-run shows green CI without --admin override
- [ ] All previously-deferred specs either fixed or REMOVED from the test suite with documented rationale
```

3. Update STATE.md to indicate Phase 17 done-with-followup, Phase 17.1 next.

In BOTH cases:

- Run `gsd-sdk query frontmatter.validate <plan-path> --schema plan` for each Phase 17 plan — verify they all pass.
- Run `gsd-sdk query verify.plan-structure <plan-path>` for each plan.
- Generate `.planning/phases/17-ci-stabilization/17-05-SUMMARY.md` with the verifier-gate outcome.
  </action>
  <verify>
    <automated>grep -c "Smoke-PR result" .planning/phases/17-ci-stabilization/17-TRIAGE.md &amp;&amp; { grep -q "Phase 17 verifier-gate: PASSED" .planning/phases/17-ci-stabilization/17-TRIAGE.md || grep -q "Phase 17.1: CI Stabilization follow-up" .planning/ROADMAP.md; }</automated>
  </verify>
  <acceptance_criteria>
    - 17-TRIAGE.md contains a `## Smoke-PR result (CONTEXT D-15 verifier gate)` section with GREEN or RED status.
    - If GREEN: STATE.md + ROADMAP.md Phase 17 are marked complete; success criteria checked.
    - If RED: ROADMAP.md has a `Phase 17.1` entry with the unresolved spec list copied from 17-TRIAGE.md.
    - 17-05-SUMMARY.md exists.
  </acceptance_criteria>
  <done>Phase 17 either ships green or hands off a clean Phase 17.1 scope. No failure leaves Phase 17 unclassified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (varies) | If Task 1 chooses backend-fix path, the constraint-template.service.ts + DTOs may be touched. The DTO @IsX decorators ARE the trust boundary between client and API. |
| Smoke-PR -> main | The smoke-PR is a no-op; the CI environment is the trust boundary being validated, not a new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | T (Tampering) | constraint-template DTO (if backend-fix path chosen) | mitigate | Any DTO field added or relaxed must keep class-validator decorators (@IsString, @IsEnum, @IsArray, etc.). Do NOT replace strict validators with @IsOptional() to "make the test pass" — that would weaken the input validation boundary. |
| T-17-10 | E (Elevation of privilege) | Test fixture login (loginAsRole) | accept | E2E test fixtures do not run in production. Even if a Phase-13 cluster is rooted in a shared admin-login fixture, the fix changes test setup not production auth. ASVS L1 baseline preserved. |
| T-17-11 | I (Information disclosure) | 17-TRIAGE.md sweep — full red-spec list | accept | Triage doc names spec files; spec files do not embed secrets. Same OSS-public expectation as 17-01..17-04. |

Phase 17 produces no new attack surface. The verifier-gate validates the existing CI environment behaves as expected.
</threat_model>

<verification>
- 17-TRIAGE.md has a Resolution column entry for every PR-#1 red spec.
- .planning/deferred-items.md `## Phase 17 deferred` section is consistent with 17-TRIAGE.md skip rows.
- Smoke-PR `chore/ci-smoke-noop` was created, ran, and result is documented.
- Phase 17 status (complete or done-with-followup) is reflected in STATE.md AND ROADMAP.md.
- All 5 Phase-17 plans pass `gsd-sdk query frontmatter.validate --schema plan` and `gsd-sdk query verify.plan-structure`.
</verification>

<success_criteria>
- Sample failure (admin-solver-tuning-restrictions) end-to-end triaged with full row in 17-TRIAGE.md.
- Every remaining red spec from PR #1 is classified.
- Smoke-PR is created and CI is run.
- D-15 verifier gate: smoke-PR mergt OHNE --admin = Phase 17 done; OR Phase 17.1 spawns per D-16.
- 30-min-fix-or-skip protocol respected for every spec.
</success_criteria>

<output>
After completion, create .planning/phases/17-ci-stabilization/17-05-SUMMARY.md with:
- triage_ref: .planning/phases/17-ci-stabilization/17-TRIAGE.md
- decisions_addressed: [D-01, D-05, D-06, D-07, D-08, D-12, D-13, D-14, D-15, D-16]
- Brief recap: sample failure resolved (fixed | deferred), N specs swept through 30-min protocol, smoke-PR result, Phase 17 status (complete | spawned Phase 17.1).
</output>
