---
phase: 17-ci-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/e2e/admin-school-settings.mobile.spec.ts
  - apps/web/e2e/zeitraster.mobile.spec.ts
  - .planning/phases/17-ci-stabilization/17-TRIAGE.md
autonomous: true
requirements: []
decisions_addressed: [D-01, D-02, D-06, D-07, D-11]
triage_ref: .planning/phases/17-ci-stabilization/17-TRIAGE.md
tags: [ci-stabilization, e2e, playwright, mobile, selector-drift]

must_haves:
  truths:
    - "admin-school-settings.mobile.spec.ts queries div.sm\\:hidden.space-y-3, not div.md\\:hidden.space-y-3"
    - "zeitraster.mobile.spec.ts queries div.sm\\:hidden.space-y-3, not div.md\\:hidden.space-y-3"
    - "Both specs pass on local mobile-chrome project"
    - "17-TRIAGE.md exists and contains a row for each spec classified as selector-drift owned by Plan F"
  artifacts:
    - path: "apps/web/e2e/admin-school-settings.mobile.spec.ts"
      provides: "Mobile spec for school settings — selector aligned to Phase-16 sm: breakpoint"
      contains: "div.sm\\\\:hidden.space-y-3"
    - path: "apps/web/e2e/zeitraster.mobile.spec.ts"
      provides: "Mobile spec for Zeitraster — selector aligned to Phase-16 sm: breakpoint"
      contains: "div.sm\\\\:hidden.space-y-3"
    - path: ".planning/phases/17-ci-stabilization/17-TRIAGE.md"
      provides: "Master triage table for Phase 17 — created in this plan, extended by 17-02..17-05"
      contains: "| Spec | CI-State | Local-Repro | Classification | Owning Plan | Resolution |"
  key_links:
    - from: "apps/web/e2e/admin-school-settings.mobile.spec.ts"
      to: "PeriodsEditor mobile-card container"
      via: "page.locator('div.sm\\\\:hidden.space-y-3')"
      pattern: "div\\.sm\\\\\\\\:hidden\\.space-y-3"
    - from: "apps/web/e2e/zeitraster.mobile.spec.ts"
      to: "PeriodsEditor mobile-card container"
      via: "page.locator('div.sm\\\\:hidden.space-y-3')"
      pattern: "div\\.sm\\\\\\\\:hidden\\.space-y-3"
---

<objective>
Plan F (D-02 leverage-first): Fix the `md:hidden` → `sm:hidden` selector drift in the two mobile E2E specs that target the PeriodsEditor mobile-card container. Phase 16 standardised the responsive breakpoint at `sm:` (640px), but these two spec files still query the legacy `md:hidden` (768px) class, causing a cascade of mobile-chrome failures. This is also the plan where the master triage artifact `17-TRIAGE.md` is created — every subsequent Phase 17 plan extends it.

Purpose: Highest-leverage Phase 17 win — single-file mechanical edits should green ~15 mobile-cascade tests and shrink the failure surface that Plans D and E inherit. Establishes the triage doc shape (CONTEXT D-06/D-07).

Output:
- 2 spec files with corrected selector + comments
- `17-TRIAGE.md` created with table header + Plan-F rows
</objective>

<execution_context>
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/17-ci-stabilization/17-CONTEXT.md
@.planning/phases/17-ci-stabilization/17-PATTERNS.md
@apps/web/e2e/admin-school-settings.mobile.spec.ts
@apps/web/e2e/zeitraster.mobile.spec.ts
@apps/web/playwright.config.ts

<interfaces>
<!-- Triage table format (CONTEXT D-07) — every Phase 17 plan extends this same table. -->

`17-TRIAGE.md` row structure:
```markdown
| Spec | CI-State | Local-Repro | Classification | Owning Plan | Resolution |
|------|----------|-------------|----------------|-------------|------------|
| `<file>:<line>` | red on PR #1 (run <id>) | <repro-counts> | <real-bug \| flake \| CI-env \| selector-drift \| missing-fixture> | <A..G> | <commit-hash or skip-reason> |
```

Sort rows by Phase-Cluster: Phase 13 → Phase 14 → Phase 15 → Phase 10.5 → Mobile-375.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 17-TRIAGE.md skeleton with table header</name>
  <read_first>
    - .planning/phases/17-ci-stabilization/17-CONTEXT.md (D-06, D-07 — single master doc, 6-column format)
    - .planning/phases/17-ci-stabilization/17-PATTERNS.md (Triage Doc Format section)
    - .planning/phases/14-solver-tuning/14-03-SUMMARY.md (lines 178-219 — analog tabular format)
  </read_first>
  <files>.planning/phases/17-ci-stabilization/17-TRIAGE.md</files>
  <action>
Create `.planning/phases/17-ci-stabilization/17-TRIAGE.md` with this exact skeleton:

```markdown
# Phase 17: CI Stabilization — Master Triage Document

**Created:** 2026-05-02 (Plan 17-01)
**Reference CI run:** [GitHub Actions run 25065085891](https://github.com/martinvidec/openaustria-school-flow/actions/runs/25065085891) (PR #1 phase-15 baseline)
**Real-bug threshold (D-05):** ≥2/3 fail on `pnpm --filter @schoolflow/web exec playwright test <spec> --project=desktop --repeat-each=3`
**Owning plans:** 17-01 (Plan F), 17-02 (Plan G), 17-03 (Plans A+B+C), 17-04 (Plan D), 17-05 (Plan E)

## Triage Table

Sorted by Phase-Cluster: Phase 13 → Phase 14 → Phase 15 → Phase 10.5 → Mobile-375.

| Spec | CI-State | Local-Repro | Classification | Owning Plan | Resolution |
|------|----------|-------------|----------------|-------------|------------|
<!-- Plan-F rows appended in Task 3 of this plan. -->
<!-- Plan-G rows appended by 17-02. -->
<!-- Plans-A/B/C rows appended by 17-03. -->
<!-- Plan-D regression-candidate rows appended by 17-04. -->
<!-- Plan-E rows appended by 17-05. -->

## Classifications (legend)

- **real-bug** — Local repro ≥2/3 on desktop. Must be fixed in-plan or skipped with reason.
- **selector-drift** — Test uses outdated DOM selector after a refactor; mechanical update.
- **CI-env** — 0/3 fail locally + fail in CI. Flake / fixture / timing.
- **missing-fixture** — Test depends on env (e.g. `E2E_RUN_SOLVER`) or seed not present in default CI run.
- **regression-candidate** — May be a side-effect of an in-Phase-17 refactor (e.g. Plan D); re-evaluate after wave completes.

## Deferred items

(Populated by 17-05 when a Plan-E spec exhausts the 30-min fix budget — links to `.planning/deferred-items.md`.)
```

Do NOT include any rows yet — Task 3 of this plan adds the Plan-F rows. Subsequent plans (17-02..17-05) append their own rows to the same table.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-ci-stabilization/17-TRIAGE.md &amp;&amp; grep -q "| Spec | CI-State | Local-Repro | Classification | Owning Plan | Resolution |" .planning/phases/17-ci-stabilization/17-TRIAGE.md &amp;&amp; grep -q "Phase 17: CI Stabilization — Master Triage Document" .planning/phases/17-ci-stabilization/17-TRIAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/17-ci-stabilization/17-TRIAGE.md` exists.
    - Header row matches exactly: `| Spec | CI-State | Local-Repro | Classification | Owning Plan | Resolution |`
    - Skeleton mentions all 5 owning plans (17-01..17-05).
    - No data rows yet (Task 3 adds them).
  </acceptance_criteria>
  <done>17-TRIAGE.md skeleton committed; downstream plans can append rows without touching header/structure.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Switch md:hidden → sm:hidden in both mobile spec files</name>
  <read_first>
    - apps/web/e2e/admin-school-settings.mobile.spec.ts (lines 31-34 — locator + comment)
    - apps/web/e2e/zeitraster.mobile.spec.ts (lines 37-40 — locator + comment)
    - apps/web/playwright.config.ts (lines 33-82 — confirm mobile-chrome project name)
    - apps/web/src/components/admin/zeitraster/PeriodsEditor.tsx OR equivalent (verify the actual current breakpoint of the mobile container; per PATTERNS.md it has shifted to sm:hidden)
  </read_first>
  <files>apps/web/e2e/admin-school-settings.mobile.spec.ts, apps/web/e2e/zeitraster.mobile.spec.ts</files>
  <action>
In both spec files, replace EVERY occurrence of:

- `div.md\\:hidden.space-y-3` → `div.sm\\:hidden.space-y-3` (the locator string)
- Comment text: `// Mobile cards container uses .md:hidden.space-y-3.` → `// Mobile cards container uses .sm:hidden.space-y-3 (Phase 16 breakpoint standard).`
- Comment text: `// Desktop table uses .hidden.md:block on its wrapper → hidden at <md.` → `// Desktop table uses .hidden.sm:block on its wrapper → hidden at <sm.`
- Any other narrative `md:` references in these two files that describe the PeriodsEditor mobile container — leave selectors that target unrelated elements untouched.

Per CONTEXT D-11: this mirrors the Phase-16 `md:` → `sm:` convention; the PeriodsEditor mobile container has already shifted to `sm:hidden` in the source component. Per PATTERNS Pattern S7: do NOT rename the file — the `.mobile.spec.ts` suffix is what routes the test to the mobile-chrome / mobile-375 projects.

After edits, run the diff and verify only selector/comment lines changed:
```bash
git diff -- apps/web/e2e/admin-school-settings.mobile.spec.ts apps/web/e2e/zeitraster.mobile.spec.ts | grep '^[+-]' | grep -vE '^(\+\+\+|---)'
```

If a `grep -RIn 'md\\\\:hidden' apps/web/e2e/` finds further occurrences in OTHER mobile specs not in this plan's scope, log them in 17-TRIAGE.md as Plan-F-followups (Task 3) but do NOT fix in this plan — they belong to Phase 17.1 if the smoke-PR uncovers them per D-16.
  </action>
  <verify>
    <automated>grep -c 'div\\.sm\\\\:hidden\\.space-y-3' apps/web/e2e/admin-school-settings.mobile.spec.ts apps/web/e2e/zeitraster.mobile.spec.ts &amp;&amp; ! grep -E 'div\\.md\\\\:hidden\\.space-y-3' apps/web/e2e/admin-school-settings.mobile.spec.ts apps/web/e2e/zeitraster.mobile.spec.ts &amp;&amp; pnpm --filter @schoolflow/web exec playwright test admin-school-settings.mobile.spec.ts zeitraster.mobile.spec.ts --project=mobile-chrome --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'div\.sm\\:hidden\.space-y-3'` returns ≥1 in each of the two spec files.
    - `grep -E 'div\.md\\:hidden\.space-y-3'` returns ZERO matches in both files (the bare `grep -v '^#'` filter not needed — these are .ts files, not Markdown).
    - `pnpm --filter @schoolflow/web exec playwright test admin-school-settings.mobile.spec.ts zeitraster.mobile.spec.ts --project=mobile-chrome` exits 0.
    - No other files modified by this task (file diff limited to the two specs).
  </acceptance_criteria>
  <done>Both specs query the correct Phase-16 sm:hidden mobile container; mobile-chrome project run is green for these two specs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Append Plan-F rows to 17-TRIAGE.md</name>
  <read_first>
    - .planning/phases/17-ci-stabilization/17-TRIAGE.md (skeleton from Task 1)
    - .planning/phases/17-ci-stabilization/17-PATTERNS.md (Triage Doc Format — sample row for selector-drift)
  </read_first>
  <files>.planning/phases/17-ci-stabilization/17-TRIAGE.md</files>
  <action>
Append these two rows under the table header (after the placeholder comments, before the `## Classifications` section). Use the actual line numbers from the post-edit spec files (the locator line, not the comment line):

```markdown
| `apps/web/e2e/admin-school-settings.mobile.spec.ts:<LINE>` | red on PR #1 mobile-chrome (run 25065085891) | 0/3 fail desktop, 3/3 fail mobile-chrome before fix; 3/3 pass after fix | selector-drift | F | `md\:hidden` → `sm:hidden` — fixed in commit `<hash-from-Task-2>` |
| `apps/web/e2e/zeitraster.mobile.spec.ts:<LINE>` | red on PR #1 mobile-chrome (run 25065085891) | 0/3 fail desktop, 3/3 fail mobile-chrome before fix; 3/3 pass after fix | selector-drift | F | `md\:hidden` → `sm:hidden` — fixed in commit `<hash-from-Task-2>` |
```

Replace `<LINE>` with the actual locator-line numbers (use `grep -n 'div.sm\\\\:hidden' apps/web/e2e/admin-school-settings.mobile.spec.ts apps/web/e2e/zeitraster.mobile.spec.ts` to confirm). Replace `<hash-from-Task-2>` with the commit SHA of Task 2's commit.

Sort: both rows belong in the Mobile-375 cluster; place them at the end of the table (matching CONTEXT D-07 cluster ordering).
  </action>
  <verify>
    <automated>grep -c 'admin-school-settings\\.mobile\\.spec\\.ts' .planning/phases/17-ci-stabilization/17-TRIAGE.md &amp;&amp; grep -c 'zeitraster\\.mobile\\.spec\\.ts' .planning/phases/17-ci-stabilization/17-TRIAGE.md &amp;&amp; grep -c 'selector-drift' .planning/phases/17-ci-stabilization/17-TRIAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `17-TRIAGE.md` contains exactly 2 rows referencing the two specs.
    - Both rows have classification `selector-drift` and owning plan `F`.
    - Resolution column references a real commit hash (not `<hash-from-Task-2>` placeholder).
  </acceptance_criteria>
  <done>17-TRIAGE.md now has Plan-F evidence. Subsequent plans append below.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan modifies E2E test selectors and a planning doc; no production code, no new endpoints, no new data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | T (Tampering) | E2E spec selectors | accept | No new attack surface — phase modifies test query strings only. ASVS L1 baseline (no XSS regression) is unaffected because no UI source code is touched. |
| T-17-02 | I (Information disclosure) | 17-TRIAGE.md docs | accept | Triage doc lives in `.planning/`, no secrets, no PII. Public OSS repo expectation is met. |

Phase 17 produces no new attack surface — it modifies existing CI tests and creates a planning artifact.
</threat_model>

<verification>
- `git diff` after all 3 tasks shows changes ONLY to: 2 spec files + 1 triage doc.
- `pnpm --filter @schoolflow/web exec playwright test admin-school-settings.mobile.spec.ts zeitraster.mobile.spec.ts --project=mobile-chrome` exits 0.
- No other mobile specs in `apps/web/e2e/` regress (spot-check by running 1-2 unrelated mobile specs).
</verification>

<success_criteria>
- 2 mobile spec files updated; mobile-chrome project run green for both.
- 17-TRIAGE.md created with table skeleton + 2 Plan-F rows.
- Plans 17-02..17-05 can now append rows to the same triage doc without touching the structure.
- No production code modified (D-15 smoke-PR independence preserved).
</success_criteria>

<output>
After completion, create `.planning/phases/17-ci-stabilization/17-01-SUMMARY.md` with:
- `triage_ref: .planning/phases/17-ci-stabilization/17-TRIAGE.md` in frontmatter
- decisions_addressed: [D-01, D-02, D-06, D-07, D-11]
- Brief recap: 2 specs fixed, triage doc created, mobile-chrome cascade reduction count (TBD post-execution)
</output>
