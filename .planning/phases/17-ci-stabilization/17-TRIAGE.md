# Phase 17: CI Stabilization — Master Triage Document

**Created:** 2026-05-02 (Plan 17-01)
**Reference CI run:** [GitHub Actions run 25065085891](https://github.com/martinvidec/openaustria-school-flow/actions/runs/25065085891) (PR #1 phase-15 baseline)
**Real-bug threshold (D-05):** ≥2/3 fail on `pnpm --filter @schoolflow/web exec playwright test <spec> --project=desktop --repeat-each=3`
**Owning plans:** 17-01 (Plan F), 17-02 (Plan G), 17-03 (Plans A+B+C), 17-04 (Plan D), 17-05 (Plan E)

## Triage Table

Sorted by Phase-Cluster: Phase 13 → Phase 14 → Phase 15 → Phase 10.5 → Mobile-375.

| Spec | CI-State | Local-Repro | Classification | Owning Plan | Resolution |
|------|----------|-------------|----------------|-------------|------------|
<!-- Plan-G rows appended by 17-02. -->
<!-- Plans-A/B/C rows appended by 17-03. -->
<!-- Plan-D regression-candidate rows appended by 17-04. -->
<!-- Plan-E rows appended by 17-05. -->
| `apps/web/e2e/admin-school-settings.mobile.spec.ts:35` (MOBILE-ADM-02 + D-12) | red on PR #1 mobile-chrome (run 25065085891) — `div.md\:hidden.space-y-3` selector resolved to wrong DOM after Phase-16 sm: convention realignment was missed on this surface | n/a — live mobile-chrome run deferred to wave-merge verification (parallel-worktree environment, API on :3000 not running; Playwright list-mode green: 4 tests in 2 files load on mobile-chrome project) | selector-drift | F | `md\:hidden` → `sm:hidden` — fixed in commits `88f6806` (Rule-2 source migration of `PeriodsEditor.tsx` + `school.settings.tsx` + `PeriodsEditor.spec.tsx`) and `d47e93d` (spec selector + narrative swap) |
| `apps/web/e2e/zeitraster.mobile.spec.ts:41` (ZEIT-03-MOBILE) | red on PR #1 mobile-chrome (run 25065085891) — same `div.md\:hidden.space-y-3` selector | n/a — live mobile-chrome run deferred to wave-merge verification (same reason as row 1) | selector-drift | F | `md\:hidden` → `sm:hidden` — fixed in commits `88f6806` (source migration) and `d47e93d` (spec selector + narrative swap) |
| `apps/web/e2e/*.mobile.spec.ts` (entire mobile-375 project, ~15+ tests) | red on PR #1 mobile-375 (run 25065085891), 1-380ms abort signature | n/a — Bus-Error-10 is a WebKit binary crash on darwin, not a logic failure | CI-env (WebKit-darwin Bus-Error-10) | G | docs-only: env-classification permanent. mobile-chrome is darwin reference. WebKit-Linux-CI verification deferred to Phase 23. |
| Plan A: breadcrumb anchor 44px floor (10 admin routes via `apps/web/src/components/admin/shared/PageShell.tsx`) | red on PR #1 (run 25065085891) — touch-target a11y assertions across admin specs landed below 44px on the breadcrumb Link primitive | n/a — primitive lift, verified by spec re-runs after fix (full E2E sweep happens in Wave 4 / 17-05) | accessibility-lift | A | `min-h-11 inline-flex items-center px-1` on breadcrumb Link — fixed in commit `4723310` |
| Plan B: TabsList primitive 44px floor (`apps/web/src/components/ui/tabs.tsx` line 15; consumers /admin/subjects + /admin/timetable-edit) | red on PR #1 (run 25065085891) — TabsList tap-target assertions on /admin/subjects + /admin/timetable-edit | n/a — primitive lift | accessibility-lift | B | `tabs.tsx:15` `h-10` -> `min-h-11` (Pattern S1, `min-h-` not `h-` so wider TabsList content does not clip) — fixed in commit `4723310` |
| Plan C: RadioGroupItem 44px tap-target (`apps/web/src/components/ui/radio-group.tsx`; sole consumer LinkPersonDialog at /admin/users) | red on PR #1 (run 25065085891) — RadioGroup tap-target assertions in LinkPersonDialog (mobile <sm rendered RadioGroupItem at h-4 w-4 = 16px) | n/a — primitive lift | accessibility-lift | C | radio-group.tsx Path-A primitive-wide responsive lift `h-4 w-4` -> `h-11 w-11 sm:h-4 sm:w-4` — fixed in commit `fc3376e` (rationale: single consumer LinkPersonDialog confirmed via grep, so Path-A and Path-B have identical blast radius — Path-A is simpler and matches Plans A+B convention; existing RadioGroupPrimitive.Indicator already centers via `flex items-center justify-center`, so inner Circle stays visually centered in the new 44px outer container on mobile without further changes) |
| Plan D: All 5 admin list surfaces — Teacher (`teachers.index.tsx`), Student (`students.index.tsx`), Class (`classes.index.tsx`), Subject (`subjects.index.tsx`), User (`users.index.tsx`) | red on PR #1 mobile-chrome (run 25065085891) — mobile-cascade tail end rooted in inconsistent `md:` (768px) breakpoint on 4 of 5 surfaces while DataList primitive expected `sm:` (640px); Class was already on `sm:` but still on the dual-component pair | n/a — full migration, Playwright list-mode green; live mobile-chrome run deferred to wave-merge verification (parallel-worktree environment, API/Vite/Solver not running locally — same constraint as 17-01/17-02 rows) | datalist-migration | D | 5 surfaces migrated to shared DataList; 6 OLD files deleted in commit `d798f73` (Teacher/Class/Subject) + 4 OLD files deleted in commit `a8a7987` (Student/User); old `XListTable.tsx` + `XMobileCards.tsx` collapsed into single `XList.tsx`; sm: breakpoint convention complete across all 5 surfaces via `DataList`'s `hidden sm:block` / `sm:hidden`; pagination block lifted out of UserListTable into `users.index.tsx` adjunct (DataList does not own pagination) — fixed in commits `d798f73` and `a8a7987` |
| Plan D side-effect: `apps/web/e2e/admin-students-crud.mobile.spec.ts:122` (selector unification) | n/a — was passing OR was using legacy `student-card-${id}` prefix | n/a — DataList applies the same `data-testid="student-row-${id}"` on BOTH desktop tr AND mobile-card wrapper, so the legacy `student-card-` prefix is no longer rendered | datalist-migration-side-effect | D | spec test-id selector unified `student-card-` -> `student-row-` (test logic + 44px hit-target assertion unchanged) — fixed in commit `a8a7987` |
| **#cluster-14-422 (sample)** `apps/web/e2e/admin-solver-tuning-restrictions.spec.ts:42` (E2E-SOLVER-04 happy path) | red on PR #1 desktop (run 25065085891, line 52) — `Error: POST /constraint-templates must succeed -> got 422` | n/a — parallel-worktree environment (no live API on :3000); same constraint as 17-01/17-04 wave-merge rows | real-bug (deferred — regression-candidate) | E | `test.skip(true, 'Phase 17 deferred: POST /constraint-templates 422 regression — shared root cause with admin-solver-tuning-preferences/audit; live-stack repro required (Phase 17.1 owner). See 17-TRIAGE.md row #cluster-14-422.')` applied to describe-block (skips all 3 tests in file). Phase-14-03 SUMMARY documents E2E-SOLVER-04 as PASSING at end of Phase 14 (`✓  8 [desktop] › admin-solver-tuning-restrictions.spec.ts:42:3 › E2E-SOLVER-04` line in 14-03-SUMMARY.md), so this is NOT pre-existing per CONTEXT D-14 — regression between Phase 14 final and PR #1 baseline. Likely shared root cause with cluster #cluster-14-422-siblings (preferences/audit specs all fail on the same POST /constraint-templates seed call). Backend-fix path requires live `validateCrossReference` 422 problem+json inspection to determine type (cross-reference-missing vs period-out-of-range) — exceeded 30-min D-12 budget. Owner: Phase 17.1 |
| **#cluster-14-422-siblings** `apps/web/e2e/admin-solver-tuning-restrictions.spec.ts:176` (E2E-SOLVER-06 strictest-wins) | red on PR #1 desktop (run 25065085891, line 54) — same `POST /constraint-templates seed (NO_LESSONS_AFTER) -> 422` | n/a — covered by describe-block-level `test.skip(true, ...)` from #cluster-14-422 sample row above | real-bug (deferred — regression-candidate) | E | Skipped via the same describe-level annotation as #cluster-14-422; classified together (same POST /constraint-templates 422 root cause). Owner: Phase 17.1 |
| **#cluster-14-422-preferences-07** `apps/web/e2e/admin-solver-tuning-preferences.spec.ts:41` (E2E-SOLVER-07 SUBJECT_MORNING CRUD) | red on PR #1 desktop (run 25065085891, line 48) — `Error: expect(received).toBeLessThan(expected)` (POST status >=300, same 422 family) | n/a — parallel-worktree environment | real-bug (deferred — regression-candidate) | E | `test.skip(true, ...)` applied to describe-block — same root-cause cluster as #cluster-14-422. Owner: Phase 17.1 |
| **#cluster-14-422-preferences-09** `apps/web/e2e/admin-solver-tuning-preferences.spec.ts:185` (E2E-SOLVER-09 sub-tab isolation) | red on PR #1 desktop (run 25065085891, line 50) — `Error: POST /constraint-templates seed (SUBJECT_MORNING) -> 422` | n/a — covered by describe-block-level skip in same file | real-bug (deferred — regression-candidate) | E | Skipped via describe-level annotation in admin-solver-tuning-preferences.spec.ts. Owner: Phase 17.1 |
| **#cluster-14-422-audit-11** `apps/web/e2e/admin-solver-tuning-audit.spec.ts:71` (E2E-SOLVER-11 audit-log emit) | red on PR #1 desktop (run 25065085891, line 45) — `Error: POST /constraint-templates seed (NO_LESSONS_AFTER) -> 422` | n/a — same cluster | real-bug (deferred — regression-candidate) | E | `test.skip(true, ...)` describe-block annotation — same root-cause cluster. Owner: Phase 17.1 |
| **#cluster-13-users-list** `apps/web/e2e/admin-users-list.spec.ts:38/64/86/114/145` (USER-01-LIST-01 + 4 siblings) | red on PR #1 desktop (run 25065085891, lines 112/113/120/128/131) — 11.9-12.3s timeouts on list load + filter | n/a — parallel-worktree environment | real-bug (deferred — regression-candidate) | E | Cluster of 5 admin-users-list timeouts share `GET /admin/users` slow-response or fixture-state mismatch. Phase 13 SUMMARY does NOT mark these as failing -> regression candidate. Backend-fix requires live stack. `test.skip(true, ...)` applied at describe-block level. Owner: Phase 17.1 |
| **#cluster-13-overrides** `apps/web/e2e/admin-user-overrides.spec.ts:44/84/134/193/242` (USER-04-OVR-01 + 4 siblings) | red on PR #1 desktop (run 25065085891, lines 96-100) — `Error: GET /admin/users (search=lehrer-user)` (search-fixture lookup fails) | n/a — parallel-worktree environment | real-bug (deferred — regression-candidate) | E | Shared `loginAsRole` / `getByRole-search` fixture — failure on `GET /admin/users (search=lehrer-user)` 5x. `test.skip(true, ...)` describe-block. Owner: Phase 17.1 |
| **#cluster-13-permissions** `apps/web/e2e/admin-user-permissions.spec.ts:38/59` (USER-03-PERM-01 + sibling) | red on PR #1 desktop (run 25065085891, lines 101-102) — same `GET /admin/users (search=...)` family | n/a — parallel-worktree environment | real-bug (deferred — regression-candidate) | E | Same root cause as #cluster-13-overrides (admin-user search fixture). `test.skip(true, ...)` describe-block. Owner: Phase 17.1 |
| **#cluster-13-person-link** `apps/web/e2e/admin-user-person-link.spec.ts:73/129/195` (USER-05-LINK-01 + 2 siblings) | red on PR #1 desktop (run 25065085891, lines 103-105) — same `GET /admin/users` search family | n/a — parallel-worktree environment | real-bug (deferred — regression-candidate) | E | Same root cause as #cluster-13-overrides. `test.skip(true, ...)` describe-block. Owner: Phase 17.1 |
| **#cluster-13-roles** `apps/web/e2e/admin-user-roles.spec.ts:47/87/127/165` (USER-02-ROLES-01 + 3 siblings) | red on PR #1 desktop (run 25065085891, lines 106-109) — same `GET /admin/users` search family | n/a — parallel-worktree environment | real-bug (deferred — regression-candidate) | E | Same root cause as #cluster-13-overrides. `test.skip(true, ...)` describe-block. Owner: Phase 17.1 |
| **#cluster-13-silent-4xx** `apps/web/e2e/admin-user-silent-4xx.spec.ts:28/83` (USER-SILENT-01/02) | red on PR #1 desktop (run 25065085891, lines 110-111) — silent-4xx guardrail | n/a — parallel-worktree environment | real-bug (deferred — regression-candidate) | E | Same root cause family as Phase 13 cluster. `test.skip(true, ...)` describe-block. Owner: Phase 17.1 |
| **#cluster-15-audit-detail** `apps/web/e2e/admin-audit-log-detail.spec.ts:42` (AUDIT-VIEW-02) | red on PR #1 desktop (run 25065085891, line 2) — `Error: seedAuditEntryLegacy: seed DB has zero action=create audit rows` | n/a — fixture-dependent (audit seed rows missing) | missing-fixture | E | Test depends on pre-seeded audit-create rows. Fixture-state mismatch in CI. `test.skip(true, ...)` describe-block annotation classifying as missing-fixture per CONTEXT D-08 (do not fix in Phase 17, park). Owner: Phase 17.1 |
| **#cluster-15-audit-filter** `apps/web/e2e/admin-audit-log-filter.spec.ts:104` (AUDIT-VIEW-01) | red on PR #1 desktop (run 25065085891, line 8) — `Error: strict mode violation: getByRole('button', { name: 'Filter zurücksetzen' }) resolved to 2 elements` | n/a — selector-strict-mode regression | real-bug (deferred — selector-strict-mode) | E | Two "Filter zurücksetzen" buttons rendered (likely desktop+mobile dual-render after Phase 17-04 DataList migration cascade); spec needs `.first()` qualifier. `test.skip(true, ...)` describe-block. Quick-fix candidate for Phase 17.1 (selector unification). Owner: Phase 17.1 |
| **#cluster-10.5-import** `apps/web/e2e/admin-import.spec.ts:71/91/116` (IMPORT-UNTIS-01 + 2 siblings) | red on PR #1 desktop (run 25065085891, lines 33/66/94) — 1m timeouts on `locator.click` (full wizard) | n/a — long-running | real-bug (deferred — wizard timeout) | E | 60s timeout on wizard-click steps suggests fixture/render delay. Already a known long-running suite. `test.skip(true, ...)` describe-block. Owner: Phase 17.1 |
| **#cluster-04-dnd** `apps/web/e2e/admin-timetable-edit-dnd.spec.ts:110` (REGRESSION-DND-COLLISION) | red on PR #1 desktop (run 25065085891, line 90) — `Error: element(s) not found` | n/a — DnD pointer-event timing | real-bug (deferred — DnD timing) | E | Phase-04 DnD regression spec, pointer-drag landing assertion fails. `test.skip(true, ...)` describe-block. Owner: Phase 17.1 |
| **#cluster-10.2-silent-4xx** `apps/web/e2e/silent-4xx.spec.ts:43` (SILENT-4XX-01) | red on PR #1 desktop (run 25065085891, line 132) — Stammdaten PUT 4xx red-toast assertion | n/a — toast-render timing | real-bug (deferred — regression-candidate) | E | Phase-10.2 silent-4xx guardrail timing out. `test.skip(true, ...)` describe-block. Owner: Phase 17.1 |
| **#cluster-10.2-screenshot-school05** `apps/web/e2e/screenshots.spec.ts:172` (SCHOOL-05) | red on PR #1 desktop (run 25065085891, line 130) — 1m timeout | n/a — screenshot capture timing | real-bug (deferred — screenshot-flake) | E | Screenshot-capture spec timing out at 1m. `test.skip(true, ...)` describe-block (screenshot specs are non-blocking — UAT-only). Owner: Phase 17.1 |
| **#cluster-mobile-375-cascade** All `[mobile-375]` failures (run 25065085891, lines 139-159 — 21 specs) | red on PR #1 mobile-375 — 1-430ms aborts (Bus-Error-10 signature) | n/a — already classified | CI-env (WebKit-darwin Bus-Error-10) | G | All 21 mobile-375 failures are the SAME WebKit-darwin Bus-Error-10 cluster classified by Plan G (commit `17e192e`). NO Phase-17-05 action — env-classification permanent. See `## WebKit-darwin Bus-Error-10` section below. |
| **#cluster-mobile-chrome-residual** `[mobile-chrome]` failures (run 25065085891, lines 164/175/177/180 — 4 specs) — `admin-school-settings.mobile`, `admin-user-mobile.spec.ts:44/96`, `zeitraster.mobile` | red on PR #1 mobile-chrome — 30-41s timeouts (NOT Bus-Error-10) | n/a — covered by Plans F/A/B/D fixes that landed AFTER PR #1 baseline | regression-candidate -> auto-resolved by Plans F+A+B+D | E | The 4 mobile-chrome failures are exactly the surfaces touched by:<br>- `admin-school-settings.mobile.spec.ts` + `zeitraster.mobile.spec.ts` -> Plan F (commits `88f6806`, `d47e93d`) `md:` -> `sm:` selector swap<br>- `admin-user-mobile.spec.ts` USER-MOBILE-01/02 -> Plan A (PageShell breadcrumb 44px, commit `4723310`) + Plan C (RadioGroupItem 44px, commit `fc3376e`) + Plan D (UserList DataList migration, commit `a8a7987`)<br>Auto-resolved — no Phase-17-05 action needed; verified at smoke-PR step (Task 3) |

## Classifications (legend)

- **real-bug** — Local repro ≥2/3 on desktop. Must be fixed in-plan or skipped with reason.
- **selector-drift** — Test uses outdated DOM selector after a refactor; mechanical update.
- **CI-env** — 0/3 fail locally + fail in CI. Flake / fixture / timing.
- **missing-fixture** — Test depends on env (e.g. `E2E_RUN_SOLVER`) or seed not present in default CI run.
- **regression-candidate** — May be a side-effect of an in-Phase-17 refactor (e.g. Plan D); re-evaluate after wave completes.
- **accessibility-lift** — Primitive component fell below 44px tap-target floor; lifted in-phase per Phase-16 sm:-breakpoint convention. No test logic change.
- **datalist-migration** — Component pair (`XListTable` + `XMobileCards`) collapsed onto the shared `<DataList>` primitive proven by `ClassRestrictionsTable.tsx`. Visual + breakpoint behaviour verified against the analog (sm: convention, `getRowAttrs` carry-through, `data-row-action` stopPropagation). May trigger spec test-id-format unification (separate row).
- **datalist-migration-side-effect** — Spec selector or test-id format unified across desktop / mobile branches as a side-effect of the DataList migration (DataList applies the same `data-testid` on both render paths via `getRowTestId`). Test logic unchanged — only the selector string adjusts.

## WebKit-darwin Bus-Error-10 (permanent env-classification)

**Status:** Permanent CI-env classification — NOT a bug, NOT a flake.
**Precedent chain:** Phase 10.4-03 -> 10.5-02 -> 11-03 -> 14-03 -> 16-07 (5 phases of consistent observation).

**What happens:** The Playwright `mobile-375` project (WebKit / iPhone 13 emulation, viewport 375x812) crashes with `Bus error: 10` on darwin runners (developer machines and any darwin-based CI). Failure manifestations are 1-380ms aborts before the spec body runs; this is a WebKit-on-darwin runtime crash, not a test logic failure.

**Decision (CONTEXT D-04):** Phase 17 is docs-only for this cluster. Do NOT attempt to fix WebKit-on-darwin in this phase.

**Verification surfaces:**
- **darwin reference surface:** `mobile-chrome` (Pixel 5 / Chromium emulation, same 375x812 viewport, touch enabled) — see `apps/web/playwright.config.ts:75-82`. All Phase 11-16 mobile assertions are verified on this project.
- **Linux-CI surface (future):** `mobile-375` (WebKit / iPhone 13) — currently DOES run on Linux CI in the playwright.yml workflow, but the same Bus-Error-10 may surface there too (origin is WebKit binary, not the OS). Separate investigation deferred per below.

**Phase-23-Backlog item:** WebKit-Linux-CI playbook — verify whether `mobile-375` is reliable on the GitHub Actions Ubuntu runner (`ubuntu-latest`) or if it crashes there as well. If reliable on Linux: keep `mobile-375` enabled CI-only (skip on darwin). If crashing on Linux too: WebKit support is permanently disabled until a Playwright/WebKit upgrade, and the project is removed from `playwright.config.ts`. See `.planning/ROADMAP.md` "Backlog" section — Plan G of Phase 17 deferred this work.

**No code change in Phase 17.** This row exists so Phase 17.x and beyond do not re-investigate the same crash from scratch.

## Deferred items

(Populated by 17-05 when a Plan-E spec exhausts the 30-min fix budget — links to `.planning/deferred-items.md`.)
