---
phase: 17-ci-stabilization
plan: 02
type: execute
wave: 1
depends_on: [17-01]
files_modified:
  - .planning/phases/17-ci-stabilization/17-TRIAGE.md
autonomous: true
requirements: []
decisions_addressed: [D-01, D-04, D-06, D-07]
triage_ref: .planning/phases/17-ci-stabilization/17-TRIAGE.md
tags: [ci-stabilization, mobile-375, webkit, env-classification, docs-only]

must_haves:
  truths:
    - "17-TRIAGE.md contains a permanent CI-env classification row for the WebKit-darwin Bus-Error-10 cluster"
    - "17-TRIAGE.md documents that mobile-chrome (Pixel 5) is the darwin reference surface and mobile-375 (WebKit) is the Linux-CI surface"
    - "17-TRIAGE.md links the WebKit-Linux-CI setup as a Phase-23-Backlog item (per CONTEXT D-04 + Deferred)"
    - "No source code changes — playwright.config.ts is read-only verified, NOT edited"
  artifacts:
    - path: ".planning/phases/17-ci-stabilization/17-TRIAGE.md"
      provides: "Master triage doc — extended with Plan-G WebKit-darwin env-classification narrative"
      contains: "WebKit-darwin Bus-Error-10"
  key_links:
    - from: ".planning/phases/17-ci-stabilization/17-TRIAGE.md"
      to: "Phase 23 Backlog (WebKit-Linux-CI setup)"
      via: "deferred-items reference"
      pattern: "Phase 23"
---

<objective>
Plan G (CONTEXT D-04, docs-only): Document the WebKit-on-darwin Bus-Error-10 cluster as a permanent CI-environment classification — NOT a fix. The mobile-375 (WebKit) project crashes with Bus-Error-10 on darwin runners; mobile-chrome (Pixel 5 / Chromium emulation, same 375x812 viewport, touch enabled) is already the verified darwin reference surface. The actual WebKit-Linux-CI setup is explicitly deferred to Phase 23 backlog.

Purpose: Stops the mobile-375 cluster from being mis-classified as real bugs in the triage table. Without this row, every mobile-375 Bus-Error-10 would be re-investigated repeatedly.

Output:
- 17-TRIAGE.md extended with a narrative section + table rows for the WebKit-darwin Bus-Error-10 cluster
- Zero code changes
</objective>

<execution_context>
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ci-stabilization/17-CONTEXT.md
@.planning/phases/17-ci-stabilization/17-PATTERNS.md
@.planning/phases/16-admin-dashboard-mobile-h-rtung/16-07-SUMMARY.md
@apps/web/playwright.config.ts

<interfaces>
Existing playwright.config.ts mobile-project precedent (lines 67-82, READ ONLY for this plan):

```typescript
{
  name: 'mobile-375',
  use: { ...devices['iPhone 13'], viewport: { width: 375, height: 812 } },
  testMatch: /(.*\.mobile\.spec\.ts|.*-mobile\.spec\.ts)$/,
},
// Phase 11 Plan 11-03 — Chromium-emulated Pixel 5 mobile project.
// Accepted per 10.4-03/10.5-02 precedent: mobile-WebKit (iPhone 13) hits
// Bus-Error-10 on darwin runners, so Chromium-Pixel-5 emulation is the
// verification surface for Phase 11 Teacher + Subject mobile specs.
{
  name: 'mobile-chrome',
  use: { ...devices['Pixel 5'], viewport: { width: 375, height: 812 } },
  testMatch: /(.*\.mobile\.spec\.ts|.*-mobile\.spec\.ts)$/,
},
```

The precedent chain is already in the codebase (Phases 10.4 -> 10.5 -> 11 -> 14 -> 16). Plan G surfaces this in the triage doc so future agents stop reinvestigating.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append WebKit-darwin Bus-Error-10 narrative + table rows to 17-TRIAGE.md</name>
  <read_first>
    - .planning/phases/17-ci-stabilization/17-TRIAGE.md (current state from 17-01)
    - .planning/phases/17-ci-stabilization/17-CONTEXT.md (D-04: docs-only, NO WebKit-Linux-CI setup; D-06/D-07: triage format)
    - .planning/phases/17-ci-stabilization/17-PATTERNS.md (Plan G section + lines 213, 30, 44 of 16-07-SUMMARY for narrative wording)
    - .planning/phases/16-admin-dashboard-mobile-h-rtung/16-07-SUMMARY.md (lines 30, 44, 213 — origin of the narrative)
    - apps/web/playwright.config.ts (lines 33-82 — READ ONLY, do not edit)
  </read_first>
  <files>.planning/phases/17-ci-stabilization/17-TRIAGE.md</files>
  <action>
Append two pieces to `17-TRIAGE.md`.

PIECE A — narrative section (insert AFTER `## Classifications (legend)` and BEFORE `## Deferred items`):

```
## WebKit-darwin Bus-Error-10 (permanent env-classification)

**Status:** Permanent CI-env classification — NOT a bug, NOT a flake.
**Precedent chain:** Phase 10.4-03 -> 10.5-02 -> 11-03 -> 14-03 -> 16-07 (5 phases of consistent observation).

**What happens:** The Playwright `mobile-375` project (WebKit / iPhone 13 emulation, viewport 375x812) crashes with `Bus error: 10` on darwin runners (developer machines and any darwin-based CI). Failure manifestations are 1-380ms aborts before the spec body runs; this is a WebKit-on-darwin runtime crash, not a test logic failure.

**Decision (CONTEXT D-04):** Phase 17 is docs-only for this cluster. Do NOT attempt to fix WebKit-on-darwin in this phase.

**Verification surfaces:**
- **darwin reference surface:** `mobile-chrome` (Pixel 5 / Chromium emulation, same 375x812 viewport, touch enabled) — see `apps/web/playwright.config.ts:75-82`. All Phase 11-16 mobile assertions are verified on this project.
- **Linux-CI surface (future):** `mobile-375` (WebKit / iPhone 13) — currently DOES run on Linux CI in the playwright.yml workflow, but the same Bus-Error-10 may surface there too (origin is WebKit binary, not the OS). Separate investigation deferred per below.

**Phase-23-Backlog item:** WebKit-Linux-CI playbook — verify whether `mobile-375` is reliable on the GitHub Actions Ubuntu runner (`ubuntu-latest`) or if it crashes there as well. If reliable on Linux: keep `mobile-375` enabled CI-only (skip on darwin). If crashing on Linux too: WebKit support is permanently disabled until a Playwright/WebKit upgrade, and the project is removed from `playwright.config.ts`. See `.planning/ROADMAP.md` "Backlog" section — Plan G of Phase 17 deferred this work.

**No code change in Phase 17.** This row exists so Phase 17.x and beyond do not re-investigate the same crash from scratch.
```

PIECE B — table row (append to the triage table after the Plan-F rows from 17-01):

```
| `apps/web/e2e/*.mobile.spec.ts` (entire mobile-375 project, ~15+ tests) | red on PR #1 mobile-375 (run 25065085891), 1-380ms abort signature | n/a — Bus-Error-10 is a WebKit binary crash on darwin, not a logic failure | CI-env (WebKit-darwin Bus-Error-10) | G | docs-only: env-classification permanent. mobile-chrome is darwin reference. WebKit-Linux-CI verification deferred to Phase 23. |
```

Do NOT modify any other file. Verify with `git status` before committing that only `17-TRIAGE.md` is staged.
  </action>
  <verify>
    <automated>grep -c "WebKit-darwin Bus-Error-10" .planning/phases/17-ci-stabilization/17-TRIAGE.md &amp;&amp; grep -c "mobile-chrome is darwin reference" .planning/phases/17-ci-stabilization/17-TRIAGE.md &amp;&amp; grep -c "Phase 23" .planning/phases/17-ci-stabilization/17-TRIAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - Narrative section `## WebKit-darwin Bus-Error-10 (permanent env-classification)` exists in 17-TRIAGE.md.
    - Triage table contains at least one row classified `CI-env (WebKit-darwin Bus-Error-10)` with owning plan `G`.
    - `git diff --name-only HEAD` shows ONLY `.planning/phases/17-ci-stabilization/17-TRIAGE.md` modified — no apps/* changes.
    - Section references `Phase 23` for the deferred WebKit-Linux-CI work.
  </acceptance_criteria>
  <done>17-TRIAGE.md now permanently classifies the WebKit-darwin Bus-Error-10 cluster as CI-env, preventing repeated re-investigation. No code touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan modifies a planning doc only. No code, no endpoints, no data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | I (Information disclosure) | 17-TRIAGE.md narrative | accept | Public OSS planning doc; no secrets, no PII. ASVS L1 unaffected. |

Phase 17 produces no new attack surface. Plan G is purely documentation.
</threat_model>

<verification>
- `git diff --name-only HEAD` after task shows ONLY `.planning/phases/17-ci-stabilization/17-TRIAGE.md`.
- Narrative + table row both present.
- Smoke-PR (`chore/ci-smoke-noop`) on main DOES NOT regress — this plan changes only docs.
</verification>

<success_criteria>
- 17-TRIAGE.md contains the narrative section + at least one CI-env (WebKit-darwin Bus-Error-10) row.
- Phase 23 backlog reference present.
- Zero code changes — `apps/web/playwright.config.ts` untouched (CONTEXT D-04).
</success_criteria>

<output>
After completion, create `.planning/phases/17-ci-stabilization/17-02-SUMMARY.md` with:
- `triage_ref: .planning/phases/17-ci-stabilization/17-TRIAGE.md`
- decisions_addressed: [D-01, D-04, D-06, D-07]
- Brief recap: WebKit-darwin Bus-Error-10 documented as permanent CI-env, mobile-chrome confirmed as darwin reference, Phase-23-Backlog placeholder added.
</output>
