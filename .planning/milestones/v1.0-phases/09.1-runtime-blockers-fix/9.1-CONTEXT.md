# Phase 9.1: Runtime Blockers Fix - Context

**Gathered:** 2026-04-09
**Status:** Ready for planning
**Mode:** Gap closure (audit-driven)

<domain>
## Phase Boundary

Fix the 2 runtime blockers and 3 minor findings identified by v1.0-MILESTONE-AUDIT.md. Localized fixes only — no new features. All fixes have specific file paths and line numbers from the audit.

</domain>

<decisions>
## Implementation Decisions

### Audit Finding 1: ImportController Double API Prefix
- **D-01:** Change `apps/api/src/modules/import/import.controller.ts:43` from `@Controller('api/v1/schools/:schoolId/import')` to `@Controller('schools/:schoolId/import')`. Identical fix to the 5 other controllers fixed in Phase 5 Plan 10. Closes IMPORT-01, IMPORT-02.

### Audit Finding 2: Solver Callback URL Mismatch
- **D-02:** Set `API_INTERNAL_URL=http://api:3000/api/v1/api/internal/solver` in docker-compose.yml api service environment, and document in `docker/.env.example`. Solver POSTs to this base URL + path = correct route. Closes TIME-04.

### Audit Finding 9: PermissionSubject Enum Missing Phase 8-9 Subjects
- **D-03:** Add HOMEWORK, EXAM, IMPORT, PUSH entries to PermissionSubject enum in `packages/shared/src/constants/permissions.ts`. Runtime is already correct (CASL accepts strings); this is a type contract fix.

### Audit Finding 8: API_INTERNAL_URL Missing from .env.example
- **D-04:** Add `API_INTERNAL_URL` to `docker/.env.example` with comment explaining the solver callback URL purpose. Already partially covered by D-02.

### Audit Finding 7: Stale Verification Truth in Phase 6
- **D-05:** Update `06-VERIFICATION.md` Truth #7 to PASSED state (tests already fixed in commit d2a5dc9). Documentation cleanup only.

</decisions>

<code_context>
## Existing Code Insights

### Files to Modify
- `apps/api/src/modules/import/import.controller.ts` (line 43)
- `docker/docker-compose.yml` (api service environment)
- `docker/docker-compose.prod.yml` (api service environment)
- `docker/.env.example` (add API_INTERNAL_URL documentation)
- `packages/shared/src/constants/permissions.ts` (PermissionSubject enum)
- `.planning/phases/06-substitution-planning/06-VERIFICATION.md` (Truth #7)

### Verification
- After fix: `curl http://localhost:3000/api/v1/schools/seed-school-1/import/jobs` should return 200/401, not 404
- After fix: Solver completes → SolverCallbackController receives event → DB updated → /timetable WS emits

</code_context>

<specifics>
## Specific Ideas

All decisions are direct quotes from the audit report. No design choices needed.

</specifics>

<deferred>
## Deferred Ideas

None — all audit findings in Group A addressed.

</deferred>
