---
phase: 09-mobile-pwa-production-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/routes/_authenticated/timetable/index.tsx
  - apps/web/src/routes/_authenticated/classbook/$lessonId.tsx
  - apps/web/src/routes/_authenticated/messages/index.tsx
  - apps/web/src/routes/_authenticated/admin/substitutions.tsx
  - apps/web/src/routes/_authenticated/rooms/index.tsx
  - apps/web/src/routes/_authenticated/admin/import.tsx
  - apps/web/src/routes/_authenticated/excuses/index.tsx
  - apps/web/src/routes/_authenticated/statistics/absence.tsx
  - apps/web/src/routes/_authenticated/admin/resources.tsx
  - apps/web/src/routes/_authenticated/settings.tsx
  - apps/web/src/components/timetable/TimetableGrid.tsx
  - apps/web/src/components/timetable/TimetableCell.tsx
  - apps/web/src/components/classbook/AttendanceGrid.tsx
  - apps/web/src/components/classbook/GradeMatrix.tsx
  - apps/web/src/components/import/ImportColumnMapper.tsx
  - apps/web/src/routes/__root.tsx
autonomous: true
requirements:
  - MOBILE-01

must_haves:
  truths:
    - "All pages are fully usable at 375px viewport width (phone portrait)"
    - "All pages are fully usable at 768px viewport width (tablet portrait)"
    - "Touch targets on all interactive elements are at least 44x44px on mobile"
    - "Tables and grids scroll horizontally on mobile without breaking layout"
    - "Dialogs render as full-screen modals on mobile breakpoints"
  artifacts:
    - path: "apps/web/src/components/timetable/TimetableGrid.tsx"
      provides: "Responsive timetable grid with day-only view on mobile"
      contains: "sm:"
    - path: "apps/web/src/components/classbook/AttendanceGrid.tsx"
      provides: "Responsive attendance with list layout on mobile"
      contains: "sm:"
    - path: "apps/web/src/components/classbook/GradeMatrix.tsx"
      provides: "Horizontally scrollable grade matrix on mobile"
      contains: "overflow-x-auto"
  key_links:
    - from: "apps/web/src/routes/_authenticated/timetable/index.tsx"
      to: "TimetableGrid"
      via: "responsive prop or class overrides"
      pattern: "sm:|md:|lg:"
---

<objective>
Responsive audit and fixes across all existing pages to make every feature fully usable on smartphone and tablet.

Purpose: MOBILE-01 requires all features to work on mobile. The existing layout has AppSidebar/MobileSidebar with sm: breakpoint toggling, but individual page content and components need responsive adjustments for phone portrait (320-639px), tablet portrait (768px), and desktop (1024px+).

Output: All 10+ authenticated routes and their key components have responsive Tailwind classes ensuring usability across all breakpoints per the 09-UI-SPEC page-by-page responsive rules.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mobile-pwa-production-readiness/09-CONTEXT.md
@.planning/phases/09-mobile-pwa-production-readiness/09-UI-SPEC.md

<interfaces>
<!-- Key components that need responsive audit. Read each file to understand current layout before modifying. -->

From apps/web/src/routes/__root.tsx:
- RootLayout with AppSidebar (desktop) + MobileSidebar (mobile hamburger) + AppHeader
- Main content: `<main className="flex-1 p-4 md:p-6">`
- Toaster position: top-right (needs mobile override to bottom-center)

Breakpoint rules from 09-UI-SPEC:
- base (0-639px): Phone portrait. Force day view on timetable. Single-column forms. Full-screen dialogs.
- sm (640px): Phone landscape / small tablet.
- md (768px): Tablet portrait. Two-column where appropriate.
- lg (1024px+): Desktop. Full layouts.

Global responsive rules:
- Touch targets: min 44x44px at base/sm
- Tables: overflow-x-auto wrapper at base/sm
- Dialogs: full-screen (w-full h-full) at base/sm, centered max-width at md+
- Toasts: bottom-center on mobile, top-right on desktop
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Responsive fixes for timetable, classbook, and messaging pages</name>
  <files>
    apps/web/src/routes/_authenticated/timetable/index.tsx,
    apps/web/src/components/timetable/TimetableGrid.tsx,
    apps/web/src/components/timetable/TimetableCell.tsx,
    apps/web/src/routes/_authenticated/classbook/$lessonId.tsx,
    apps/web/src/components/classbook/AttendanceGrid.tsx,
    apps/web/src/components/classbook/GradeMatrix.tsx,
    apps/web/src/routes/_authenticated/messages/index.tsx,
    apps/web/src/routes/__root.tsx
  </files>
  <read_first>
    - apps/web/src/routes/_authenticated/timetable/index.tsx
    - apps/web/src/components/timetable/TimetableGrid.tsx
    - apps/web/src/components/timetable/TimetableCell.tsx
    - apps/web/src/routes/_authenticated/classbook/$lessonId.tsx
    - apps/web/src/components/classbook/AttendanceGrid.tsx
    - apps/web/src/components/classbook/GradeMatrix.tsx
    - apps/web/src/routes/_authenticated/messages/index.tsx
    - apps/web/src/routes/__root.tsx
    - .planning/phases/09-mobile-pwa-production-readiness/09-UI-SPEC.md
  </read_first>
  <action>
    Apply responsive fixes per D-01 and the 09-UI-SPEC page-by-page responsive rules:

    **Timetable (/timetable):**
    - At base/sm: Force day view. Hide week toggle button. Add day selector as horizontal scroll tabs. Reduce period row height to 48px (from 56px). TimetableCell shows subject abbreviation only (no teacher/room text -- show on tap/click via tooltip or expanded state).
    - At md: Allow day or week view. Week view shows 5 columns at compressed width. Full cell content.
    - At lg+: Full week view unchanged.

    **Classbook Lesson Detail (/classbook/$lessonId):**
    - At base/sm: Tabs as full-width horizontal scroll. AttendanceGrid renders as vertical list (one student per row, tap to cycle status) instead of grid. GradeMatrix wrapped in `overflow-x-auto` with frozen student name column (sticky left).
    - At md: AttendanceGrid as 2-column layout. GradeMatrix with horizontal scroll.
    - At lg+: Full grid layout unchanged.

    **Messaging (/messages):**
    - At base/sm: The mobile route (`/messages/$conversationId`) is already implemented per Phase 7 decision. Verify the list-only view works. ComposeDialog should be full-screen on mobile: add `className="sm:max-w-lg"` pattern and `h-[100dvh] sm:h-auto` on the Dialog content.
    - At md+: Side-by-side list-detail with `?id=` search param (already implemented).

    **Root Layout (__root.tsx):**
    - Update Sonner Toaster to use `position="bottom-center"` on mobile, `position="top-right"` on desktop. Use a responsive approach: detect viewport width or use Sonner's `mobilePosition` prop if available, otherwise use CSS media query on the Toaster wrapper.

    Use only existing Tailwind responsive utility classes (sm:, md:, lg:). Do NOT add new CSS files or frameworks.
  </action>
  <verify>
    <automated>pnpm --filter @schoolflow/web tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sm:\|md:\|lg:" apps/web/src/components/timetable/TimetableGrid.tsx` returns a number greater than 3
    - `grep "overflow-x-auto" apps/web/src/components/classbook/GradeMatrix.tsx` returns at least 1 match
    - `grep -c "sm:\|md:" apps/web/src/components/classbook/AttendanceGrid.tsx` returns a number greater than 2
    - `pnpm --filter @schoolflow/web tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Timetable grid is day-view-only on mobile with compressed cells, classbook attendance is list-layout on mobile, grade matrix scrolls horizontally, messaging ComposeDialog is full-screen on mobile, Toaster respects mobile/desktop positioning.</done>
</task>

<task type="auto">
  <name>Task 2: Responsive fixes for admin pages, excuses, statistics, rooms, and settings</name>
  <files>
    apps/web/src/routes/_authenticated/admin/substitutions.tsx,
    apps/web/src/routes/_authenticated/admin/import.tsx,
    apps/web/src/routes/_authenticated/admin/resources.tsx,
    apps/web/src/routes/_authenticated/rooms/index.tsx,
    apps/web/src/routes/_authenticated/excuses/index.tsx,
    apps/web/src/routes/_authenticated/statistics/absence.tsx,
    apps/web/src/routes/_authenticated/settings.tsx,
    apps/web/src/components/import/ImportColumnMapper.tsx
  </files>
  <read_first>
    - apps/web/src/routes/_authenticated/admin/substitutions.tsx
    - apps/web/src/routes/_authenticated/admin/import.tsx
    - apps/web/src/routes/_authenticated/admin/resources.tsx
    - apps/web/src/routes/_authenticated/rooms/index.tsx
    - apps/web/src/routes/_authenticated/excuses/index.tsx
    - apps/web/src/routes/_authenticated/statistics/absence.tsx
    - apps/web/src/routes/_authenticated/settings.tsx
    - apps/web/src/components/import/ImportColumnMapper.tsx
    - .planning/phases/09-mobile-pwa-production-readiness/09-UI-SPEC.md
  </read_first>
  <action>
    Apply responsive fixes per D-01 and the 09-UI-SPEC page-by-page responsive rules:

    **Admin Substitutions (/admin/substitutions):**
    - At base/sm: Tabs stack vertically (full-width). AbsenceForm fields stack single-column. CandidateList cards stack vertically. FairnessStatsPanel table wrapped in `overflow-x-auto`.
    - At md+: Current layout preserved.

    **Room Availability (/rooms):**
    - At base/sm: Room availability grid wrapped in `overflow-x-auto` with frozen time column (sticky left). Room booking dialog as full-screen modal.
    - At md+: Current layout preserved.

    **Import Wizard (/admin/import):**
    - At base/sm: Wizard steps show as horizontal progress dots (not step labels). ImportColumnMapper renders as stacked cards (one per column) instead of table. ImportDryRunPreview table wrapped in `overflow-x-auto`.
    - At md+: Current layout preserved with step labels and table column mapper.

    **Excuses (/excuses):**
    - At base/sm: ExcuseForm fields stack single-column (`flex-col` instead of `flex-row`). FileUploadField spans `w-full`.
    - At md+: Current layout preserved.

    **Statistics Absence (/statistics/absence):**
    - At base/sm: AbsenceStatisticsPanel table wrapped in `overflow-x-auto`. Date range pickers stack vertically.
    - At md+: Current layout preserved.

    **Admin Resources (/admin/resources):**
    - At base/sm: ResourceList table wrapped in `overflow-x-auto`. Edit dialog as full-screen modal.
    - At md+: Current layout preserved.

    **Settings (/settings):**
    - At base/sm: Settings sections stack vertically as full-width cards. All form controls span `w-full`.
    - At md+: Max-width 640px centered container for settings cards (`max-w-[640px] mx-auto`).

    Ensure all interactive elements at base/sm have min 44x44px touch targets (add `min-h-[44px] min-w-[44px]` where needed on buttons and tappable elements). Use `overflow-x-auto` wrappers on all tables at mobile breakpoints.
  </action>
  <verify>
    <automated>pnpm --filter @schoolflow/web tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "overflow-x-auto" apps/web/src/routes/_authenticated/admin/substitutions.tsx` returns at least 1 match
    - `grep "overflow-x-auto" apps/web/src/routes/_authenticated/rooms/index.tsx` returns at least 1 match
    - `grep "overflow-x-auto" apps/web/src/routes/_authenticated/statistics/absence.tsx` returns at least 1 match
    - `grep "max-w-\[640px\]\|max-w-2xl" apps/web/src/routes/_authenticated/settings.tsx` returns at least 1 match
    - `pnpm --filter @schoolflow/web tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>All admin, excuses, statistics, rooms, and settings pages have responsive layouts with horizontal scroll wrappers on tables, full-screen dialogs on mobile, and single-column form stacking. Settings page has max-width container on desktop. All interactive elements meet 44px touch target minimum.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes: `pnpm --filter @schoolflow/web tsc --noEmit`
- Responsive utility classes present across all modified files
- No layout breakage at any breakpoint (horizontal scroll only where specified)
</verification>

<success_criteria>
All 10 authenticated routes and their key components have responsive Tailwind classes. Day-only timetable view on mobile. Horizontal scroll on all data tables at mobile. Full-screen dialogs on mobile. 44px touch targets on all interactive elements. TSC passes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-mobile-pwa-production-readiness/09-01-SUMMARY.md`
</output>
