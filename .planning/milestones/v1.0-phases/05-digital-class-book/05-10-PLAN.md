---
phase: 05-digital-class-book
plan: 10
type: execute
wave: 5
depends_on: ["05-07", "05-08", "05-09"]
files_modified:
  - apps/web/src/routes/_authenticated/classbook/$lessonId.tsx
  - apps/web/src/components/classbook/AttendanceGrid.tsx
  - apps/web/src/components/classbook/GradeMatrix.tsx
  - apps/web/src/components/classbook/ExcuseForm.tsx
  - apps/web/src/components/classbook/ExcuseReviewList.tsx
  - apps/web/src/components/classbook/AbsenceStatisticsPanel.tsx
  - apps/web/src/components/classbook/StudentNoteList.tsx
autonomous: false
requirements:
  - BOOK-07

must_haves:
  truths:
    - "Lesson detail page is fully functional with all 4 tabs wired to real components"
    - "Attendance grid works on mobile with 44px touch targets"
    - "Grade matrix scrolls horizontally on mobile with sticky columns"
    - "Excuse form is usable on mobile with proper touch targets"
    - "Statistics table is readable on mobile"
    - "All classbook features pass visual inspection on desktop, tablet, and smartphone viewports"
  artifacts:
    - path: "apps/web/src/routes/_authenticated/classbook/$lessonId.tsx"
      provides: "Complete lesson detail page with all 4 tabs wired to real components"
      contains: "GradeMatrix"
  key_links:
    - from: "apps/web/src/routes/_authenticated/classbook/$lessonId.tsx"
      to: "apps/web/src/components/classbook/GradeMatrix.tsx"
      via: "Import and render in noten tab"
      pattern: "GradeMatrix"
    - from: "apps/web/src/routes/_authenticated/classbook/$lessonId.tsx"
      to: "apps/web/src/components/classbook/StudentNoteList.tsx"
      via: "Import and render in notizen tab"
      pattern: "StudentNoteList"
---

<objective>
Wire the Noten and Notizen tabs to real components (replacing placeholders from Plan 07), apply responsive polish across all classbook components, and verify mobile/tablet/desktop experience.

Purpose: Completes the full classbook feature set and ensures BOOK-07 compliance (works equally well on desktop, tablet, and smartphone). This is the integration plan that ties all frontend pieces together and polishes the responsive behavior.
Output: Fully functional lesson detail page with 4 tabs, responsive-polished components.
</objective>

<execution_context>
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-digital-class-book/05-CONTEXT.md
@.planning/phases/05-digital-class-book/05-UI-SPEC.md
@.planning/phases/05-digital-class-book/05-07-SUMMARY.md
@.planning/phases/05-digital-class-book/05-08-SUMMARY.md
@.planning/phases/05-digital-class-book/05-09-SUMMARY.md

<interfaces>
<!-- Components from Plans 07, 08, 09 -->

From apps/web/src/components/classbook/GradeMatrix.tsx:
```typescript
export function GradeMatrix({ classSubjectId, schoolId }: GradeMatrixProps)
```

From apps/web/src/components/classbook/StudentNoteList.tsx:
```typescript
export function StudentNoteList({ entryId, schoolId }: StudentNoteListProps)
```

<!-- Responsive breakpoints from UI-SPEC -->
Mobile: < 640px — Single column. Tabs scroll horizontally. Grade matrix horizontal scroll. 44px touch targets.
Tablet: 640-1023px — Tabs horizontal. Grade matrix fits ~6 columns.
Desktop: >= 1024px — Full layout. Grade matrix shows all columns.

<!-- Attendance grid responsive from UI-SPEC -->
Mobile: Full-width row. Name left, icon right. 48px row height. Icon only (no text label).
Tablet: Icon + abbreviated label ("A", "V", "E").
Desktop: Icon + full label ("Anwesend", "Verspaetet").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Noten and Notizen tabs + responsive polish on all components</name>
  <files>
    apps/web/src/routes/_authenticated/classbook/$lessonId.tsx,
    apps/web/src/components/classbook/AttendanceGrid.tsx,
    apps/web/src/components/classbook/GradeMatrix.tsx,
    apps/web/src/components/classbook/ExcuseForm.tsx,
    apps/web/src/components/classbook/ExcuseReviewList.tsx,
    apps/web/src/components/classbook/AbsenceStatisticsPanel.tsx,
    apps/web/src/components/classbook/StudentNoteList.tsx
  </files>
  <read_first>
    - apps/web/src/routes/_authenticated/classbook/$lessonId.tsx
    - apps/web/src/components/classbook/GradeMatrix.tsx
    - apps/web/src/components/classbook/StudentNoteList.tsx
    - apps/web/src/components/classbook/AttendanceGrid.tsx
    - apps/web/src/components/classbook/ExcuseForm.tsx
    - apps/web/src/components/classbook/ExcuseReviewList.tsx
    - apps/web/src/components/classbook/AbsenceStatisticsPanel.tsx
    - .planning/phases/05-digital-class-book/05-UI-SPEC.md
  </read_first>
  <action>
**1. Update `apps/web/src/routes/_authenticated/classbook/$lessonId.tsx`:**

Replace the placeholder content in the Noten and Notizen tabs with real components:

```typescript
import { GradeMatrix } from '@/components/classbook/GradeMatrix';
import { StudentNoteList } from '@/components/classbook/StudentNoteList';

// In the Noten tab:
<TabsContent value="noten" className="mt-4">
  <GradeMatrix classSubjectId={entry.classSubjectId} schoolId={schoolId} />
</TabsContent>

// In the Notizen tab:
<TabsContent value="notizen" className="mt-4">
  <StudentNoteList entryId={entry.id} schoolId={schoolId} />
</TabsContent>
```

Ensure the entry object provides `classSubjectId` (needed by GradeMatrix). If the ClassBookEntryDto doesn't include classSubjectId, it should be available from the entry data.

**2. Responsive polish on AttendanceGrid (BOOK-07):**

Review and ensure:
- On mobile (< 640px): Full-width rows with student name on the left, status icon on the right. Row height 48px. AttendanceStatusIcon in compact mode (icon only, no text labels). LateMinutesInput visible inline when status is LATE.
- On tablet (640-1023px): Comfortable spacing, abbreviated labels ("A", "V", "E") shown.
- On desktop (>= 1024px): Full status labels ("Anwesend", "Verspaetet") shown.
- "Alle anwesend" button full-width on mobile, auto-width on desktop.
- Ensure the grid container has `max-h-[70vh] overflow-y-auto` for scrolling on long student lists.

Add these Tailwind responsive classes if not already present:
- Container: `space-y-2 sm:space-y-3`
- Student row: `flex items-center justify-between h-12 px-3 sm:px-4 rounded-md`
- Student name: `text-sm truncate max-w-[60%] sm:max-w-none`

**3. Responsive polish on GradeMatrix (BOOK-07):**

Review and ensure:
- On mobile (< 640px): Student name column sticky left (120px wide). Grade columns in ScrollArea with horizontal scroll. Average ("Schnitt") column sticky right (64px). Row height 40px.
- Sticky columns use `position: sticky; left: 0; z-index: 10` for name and `position: sticky; right: 0; z-index: 10` for average.
- Grade cells: minimum width 56px per UI-SPEC. Font size 14px semibold for grade value, 12px for category headers.
- ScrollArea wraps the table on all viewports, enabling horizontal scroll.
- "Note hinzufuegen" button and filter badges full-width on mobile, inline on desktop.

**4. Responsive polish on ExcuseForm (BOOK-07):**

Review and ensure:
- Form fields stack vertically on mobile (single column).
- Date pickers: Von and Bis side by side on desktop (grid-cols-2), stacked on mobile.
- All interactive elements minimum 44px touch target.
- FileUploadField drag zone has sufficient padding on mobile.
- Submit button full-width on mobile, auto-width on desktop.

**5. Responsive polish on ExcuseReviewList (BOOK-07):**

Review and ensure:
- ExcuseCard uses full-width Card on mobile.
- Accept/Reject buttons are side by side on desktop, stacked on mobile (flex-col sm:flex-row).
- Review dialog is centered on mobile, not cut off.

**6. Responsive polish on AbsenceStatisticsPanel (BOOK-07):**

Review and ensure:
- Table uses horizontal scroll (ScrollArea) on mobile.
- Student name column sticky on mobile.
- Date range pickers stack on mobile.
- "Als PDF exportieren" button accessible on mobile.
- Column headers truncated with ellipsis on narrow viewports if needed.

**7. Responsive polish on StudentNoteList (BOOK-07):**

Review and ensure:
- Note cards are full-width on mobile.
- Actions dropdown accessible with touch.
- StudentNoteForm fields stack on mobile.
- All buttons minimum 44px touch target.

**8. Tab scrolling on mobile:**

Ensure TabsList in the lesson detail page has `overflow-x-auto flex-nowrap` classes so tab triggers scroll horizontally when they exceed mobile viewport width. Active tab should be visible without manual scrolling.
  </action>
  <verify>
    <automated>cd /Users/vid/Documents/GitHub/agentic-research/openaustria-school-flow && pnpm --filter web build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - classbook/$lessonId.tsx imports and renders GradeMatrix in noten tab (not placeholder text)
    - classbook/$lessonId.tsx imports and renders StudentNoteList in notizen tab (not placeholder text)
    - classbook/$lessonId.tsx does NOT contain "werden in einem naechsten Schritt implementiert" placeholder text
    - AttendanceGrid uses responsive classes (sm: or lg: prefixes for different breakpoints)
    - AttendanceGrid has min-h or min-w of 44px (or equivalent) on status icon buttons
    - GradeMatrix uses ScrollArea or overflow-x-auto for horizontal scrolling
    - GradeMatrix has sticky left column (student names) using `sticky` and `left-0`
    - ExcuseForm has responsive grid (grid-cols-1 sm:grid-cols-2 or similar for date pickers)
    - AbsenceStatisticsPanel has horizontal scroll on mobile
    - TabsList in lesson page has overflow-x-auto
    - `pnpm --filter web build` exits with code 0
  </acceptance_criteria>
  <done>All 4 classbook tabs wired to real components. Responsive polish applied across all classbook features for desktop, tablet, and smartphone viewports per BOOK-07.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete classbook workflow across devices</name>
  <files>apps/web/src/routes/_authenticated/classbook/$lessonId.tsx</files>
  <action>
Human verifies the complete digital classbook feature set across desktop, tablet, and smartphone viewports. This checkpoint validates BOOK-01 through BOOK-07 are working end-to-end.

What was built:
- Lesson detail page with 4 tabs (Anwesenheit, Inhalt, Noten, Notizen)
- Quick-tap attendance grid with status cycling and late minutes
- Lesson content form with auto-save (Thema, Lehrstoff, Hausaufgabe)
- Grade matrix with Austrian 1-5 Notensystem, weighted averages, filtering, sorting
- Student notes with private flag visibility
- Parent excuse submission with file upload
- Klassenvorstand excuse review with accept/reject
- Absence statistics with date range filter and sortable table
- Sidebar navigation updates (Entschuldigungen, Abwesenheit)
- Timetable cell click to classbook navigation for teachers
- Responsive layout across all components (BOOK-07)

Verification steps:
1. Start dev environment: `docker compose up -d && pnpm --filter api dev` and `pnpm --filter web dev`
2. Login as teacher at http://localhost:5173
3. Click a lesson cell in the timetable -> verify navigation to /classbook/{lessonId}
4. Verify 4 tabs: Anwesenheit, Inhalt, Noten, Notizen
5. Anwesenheit: Tap status icons to cycle, set LATE with minutes, "Alle anwesend", verify save toast
6. Inhalt: Type in fields, blur -> verify "Gespeichert" indicator
7. Noten: Add grade with 2+ notation, verify matrix and weighted average
8. Notizen: Add note with Privat checkbox, verify lock icon
9. Check sidebar for Entschuldigungen and Abwesenheit nav items
10. Navigate to /excuses and /statistics/absence
11. Resize browser to mobile width (< 640px) -> verify responsive layout
12. On mobile: verify 44px touch targets, grade matrix horizontal scroll
  </action>
  <verify>Human visual inspection across desktop/tablet/mobile viewports</verify>
  <done>User confirms all classbook features work correctly on all device sizes, or provides list of issues to fix.</done>
</task>

</tasks>

<verification>
- `pnpm --filter web build` passes
- All 4 tabs render real components (no placeholders)
- Responsive behavior verified at mobile/tablet/desktop widths
- All interactive elements have 44px touch targets on mobile
</verification>

<success_criteria>
- All 4 lesson detail tabs wired to real components (BOOK-01, BOOK-02, BOOK-03, BOOK-04)
- Responsive layout works at mobile (< 640px), tablet (640-1023px), desktop (>= 1024px) (BOOK-07)
- Touch targets minimum 44px on all interactive elements
- Grade matrix horizontal scroll on mobile with sticky columns
- Human verified: complete classbook workflow functional
</success_criteria>

<output>
After completion, create `.planning/phases/05-digital-class-book/05-10-SUMMARY.md`
</output>
