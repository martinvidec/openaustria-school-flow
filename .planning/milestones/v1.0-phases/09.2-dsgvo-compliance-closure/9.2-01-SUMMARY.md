---
phase: 09.2-dsgvo-compliance-closure
plan: 9.2-01
subsystem: dsgvo
tags: [dsgvo, gdpr, prisma, retention, anonymization, art-15, art-17, gap-closure]

# Dependency graph
requires:
  - phase: 02-school-data-model-dsgvo
    provides: DataExportService, DataDeletionService, RetentionService, PersonExportData, DEFAULT_RETENTION_DAYS
  - phase: 05-digital-class-book
    provides: AttendanceRecord, GradeEntry, AbsenceExcuse, StudentNote tables
  - phase: 07-communication
    provides: Message, MessageRecipient tables
  - phase: 08-homework-exams-data-import
    provides: Homework, Exam, CalendarToken, SisApiKey tables
  - phase: 09-mobile-pwa-production-readiness
    provides: PushSubscription, Notification tables
provides:
  - DSGVO Art. 15 subject access export covering Phase 5-8 personal data
  - DSGVO Art. 17 right-to-erasure anonymization across cross-phase linked records
  - Retention enforcement (deleteMany) for noten / anwesenheit / kommunikation categories
  - cleanupExpiredGrades / cleanupExpiredAttendance / cleanupExpiredMessages helpers on RetentionService
affects: [v1.0 milestone re-audit, future DSGVO admin UI, deletion request processor]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-category 50-row cap on subject access export (extends Phase 2 D-14 PDF cap to all Phase 5-8 categories)"
    - "Anonymize PII at the source Person row, preserve statistical FKs for grades/attendance (Schulunterrichtsgesetz audit value)"
    - "FK-safe deletion order in transactions (MessageRecipient before Message)"
    - "kc-scoped vs id-scoped data split — class book uses Person.id, communication/notifications/homework use keycloakUserId"

key-files:
  created:
    - .planning/phases/09.2-dsgvo-compliance-closure/9.2-01-SUMMARY.md
  modified:
    - apps/api/src/modules/dsgvo/export/data-export.service.ts
    - apps/api/src/modules/dsgvo/export/data-export.service.spec.ts
    - apps/api/src/modules/dsgvo/export/pdf-export.service.ts
    - apps/api/src/modules/dsgvo/deletion/data-deletion.service.ts
    - apps/api/src/modules/dsgvo/deletion/data-deletion.service.spec.ts
    - apps/api/src/modules/dsgvo/retention/retention.service.ts
    - apps/api/src/modules/dsgvo/retention/retention.service.spec.ts

key-decisions:
  - "Phase 5 grades/attendance NOT row-anonymized -- studentId already points to anonymized Person; preserves Schulunterrichtsgesetz statistical value"
  - "StudentNote.content replaced with [Anonymisiert] for both studentId and authorId matches (free-text could leak PII)"
  - "Message.body replaced with [Geloeschte Nachricht] -- preserves conversation structure for the other party's audit trail"
  - "PushSubscription / CalendarToken / SisApiKey cascade-deleted on anonymization -- pure session/credential data with no value once user is gone"
  - "kommunikation deletion uses an explicit transaction with MessageRecipient deletes BEFORE Message (FK-safe order, schema does not declare onDelete: Cascade on every relation)"
  - "Per-category 50-row cap on Phase 5-8 export sections matches existing Phase 2 D-14 cap on audit log entries"
  - "AbsenceExcuse OR query uses parentId (not createdById from plan draft) -- schema field name verified before implementation"

patterns-established:
  - "addRecordSection() helper in PdfExportService renders any Phase 5-8 category with the 50-row cap and a one-line key=value summary per row"
  - "cleanupExpired* helpers on RetentionService are independently callable -- enables future DSGVO admin UI to surface per-category counts and trigger manual cleanup"

requirements-completed:
  - DSGVO-04
  - DSGVO-05
  - DSGVO-06

# Metrics
duration: 26min
completed: 2026-04-09
---

# Phase 9.2 Plan 9.2-01: DSGVO Compliance Closure Summary

**DSGVO Art. 15 export, Art. 17 anonymization, and retention deletion now extend across Phase 5-8 personal data tables (grades, attendance, messages, notifications, homework, exams, calendar tokens).**

## Performance

- **Duration:** 26 min
- **Started:** 2026-04-09T13:00:42Z
- **Completed:** 2026-04-09T13:26:06Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- Subject access requests now return the complete personal data set (DSGVO Art. 15) — closing Audit Finding 4
- `anonymizePerson()` propagates erasure into Phase 5-8 linked records (DSGVO Art. 17) — closing Audit Finding 6
- Retention cron actually deletes expired grades, attendance, and messages instead of count-only reporting — closing Audit Finding 5
- Backend test suite grew from 413 → 423 passing (+10 tests, 0 failures)

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend data-export.service for Phase 5-8 personal data** — `0eea233` (feat)
2. **Task 2: Extend deletion service anonymization and retention service deletion** — `d36deaf` (feat)

## Files Created/Modified

- `apps/api/src/modules/dsgvo/export/data-export.service.ts` — Query AttendanceRecord, GradeEntry, AbsenceExcuse, StudentNote, Notification, Message, MessageRecipient, Homework, Exam, CalendarToken; include in JSON export with 50-row cap
- `apps/api/src/modules/dsgvo/export/data-export.service.spec.ts` — Mock new Prisma tables; +3 tests for Phase 5/7/8 export coverage
- `apps/api/src/modules/dsgvo/export/pdf-export.service.ts` — Extended `PersonExportData` interface with 10 new optional fields; new `addRecordSection()` helper renders each category in PDF with 50-row cap
- `apps/api/src/modules/dsgvo/deletion/data-deletion.service.ts` — Anonymize StudentNote.content (studentId + authorId), Message.body (senderId); cascade-delete PushSubscription / CalendarToken / SisApiKey
- `apps/api/src/modules/dsgvo/deletion/data-deletion.service.spec.ts` — Mock Phase 5-8 anonymization targets; +3 tests covering note anonymization, message+session cleanup, and kc-userId-absent skip path
- `apps/api/src/modules/dsgvo/retention/retention.service.ts` — Three new helpers (`cleanupExpiredGrades`, `cleanupExpiredAttendance`, `cleanupExpiredMessages`) wired into `checkExpiredRecords()`; messages helper uses an explicit FK-safe transaction
- `apps/api/src/modules/dsgvo/retention/retention.service.spec.ts` — Mock GradeEntry / AttendanceRecord / Message tables; +4 tests covering each new deletion path including the FK-safe order assertion

## Decisions Made

See `key-decisions` in frontmatter. Notable:
- **Schema verification before implementation:** The plan draft used `createdById` for AbsenceExcuse; the actual schema field is `parentId`. Verified all FK names against `prisma/schema.prisma` before writing queries to avoid silent runtime failures.
- **No row-level anonymization for grades/attendance:** Anonymizing the Person row itself removes the only PII (name, email, etc.) while leaving statistical/audit value intact for Schulunterrichtsgesetz compliance.

## Deviations from Plan

None - plan executed exactly as written. Field names corrected against schema (AbsenceExcuse uses `parentId` not `createdById`) which is a normal "use actual schema names" instruction explicit in the plan, not a deviation.

## Issues Encountered

None. Both tasks completed cleanly on first compile + first test run. Baseline 413 tests stayed green; the 10 new tests passed immediately.

## User Setup Required

None - no external service configuration required. All changes are internal service extensions to existing DSGVO infrastructure that was already wired in Phase 2.

## Next Phase Readiness

- All 3 DSGVO compliance gaps from the v1.0 audit are now closed (DSGVO-04, DSGVO-05, DSGVO-06)
- Combined with Phase 9.1 (runtime blockers), the v1.0 milestone is ready for re-audit
- The `cleanupExpired*` helpers are independently callable and ready for future DSGVO admin UI work (deferred per CONTEXT.md)

## Self-Check: PASSED

Created files:
- FOUND: .planning/phases/09.2-dsgvo-compliance-closure/9.2-01-SUMMARY.md (this file)

Modified files (verified via git diff):
- FOUND: apps/api/src/modules/dsgvo/export/data-export.service.ts
- FOUND: apps/api/src/modules/dsgvo/export/data-export.service.spec.ts
- FOUND: apps/api/src/modules/dsgvo/export/pdf-export.service.ts
- FOUND: apps/api/src/modules/dsgvo/deletion/data-deletion.service.ts
- FOUND: apps/api/src/modules/dsgvo/deletion/data-deletion.service.spec.ts
- FOUND: apps/api/src/modules/dsgvo/retention/retention.service.ts
- FOUND: apps/api/src/modules/dsgvo/retention/retention.service.spec.ts

Commits:
- FOUND: 0eea233 (Task 1)
- FOUND: d36deaf (Task 2)

Verification:
- TypeScript: pnpm tsc --noEmit clean (0 errors)
- Tests: 423 passed | 65 todo | 7 skipped (488 total) — was 413 before this plan, +10 new
- DSGVO module: 72 tests passed (was 65 before this plan, +7 new)
- Acceptance criteria grep counts:
  - data-export.service: Phase 5 refs=7, Phase 6/7 refs=12, Phase 8 refs=8 (>= 4/3/2 required)
  - retention.service: deletion refs=4 (>= 3 required)
  - data-deletion.service: anonymization refs=5 (>= 3 required)

---
*Phase: 09.2-dsgvo-compliance-closure*
*Completed: 2026-04-09*
