import { describe, it } from 'vitest';

describe('ResourceService', () => {
  describe('ROOM-04: Resource management', () => {
    it.todo('creates a resource with name, type, quantity');
    it.todo('enforces unique name per school');
    it.todo('updates resource quantity and description');
    it.todo('deletes resource and cascades to bookings');
    it.todo('creates resource booking for a room+period');
  });
});
