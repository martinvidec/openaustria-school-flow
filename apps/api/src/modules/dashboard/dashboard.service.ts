import { Injectable } from '@nestjs/common';

/**
 * STUB — full implementation lands in Task 2 (`getStatus` + `resolveAdminSchoolId`).
 * Decorated `@Injectable()` so `DashboardModule` providers array compiles cleanly.
 */
@Injectable()
export class DashboardService {}
