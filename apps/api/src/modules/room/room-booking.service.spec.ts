import { describe, it } from 'vitest';

describe('RoomBookingService', () => {
  describe('ROOM-03: Room booking', () => {
    it.todo('creates an ad-hoc room booking for a free slot');
    it.todo('rejects booking when slot is already occupied by lesson');
    it.todo('rejects booking when slot is already booked');
    it.todo('returns room availability grid for a given day');
    it.todo('filters availability by room type and capacity');
  });
});
