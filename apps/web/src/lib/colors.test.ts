import { describe, it } from 'vitest';

describe('Subject color utilities', () => {
  describe('VIEW-05: Subject color-coding', () => {
    it.todo('returns a color pair for any subject ID');
    it.todo('returns consistent colors for the same subject ID');
    it.todo('distributes colors across the palette');
    it.todo('color overrides take precedence over computed colors');
  });
});
