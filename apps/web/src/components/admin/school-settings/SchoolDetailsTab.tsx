interface Props {
  onDirtyChange?: (d: boolean) => void;
}

// Placeholder — Plan 10-03b replaces with the full SCHOOL-01 Stammdaten implementation
// (empty-flow inline-create, edit mode with 7 Schultyp options, RHF + zodResolver).
export function SchoolDetailsTab(_props: Props) {
  return <div className="p-6 text-muted-foreground">Stammdaten — Plan 03b implementiert.</div>;
}
