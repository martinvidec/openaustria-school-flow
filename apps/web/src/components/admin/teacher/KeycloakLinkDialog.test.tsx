import { describe, it } from 'vitest';

describe('KeycloakLinkDialog', () => {
  it.todo('debounces email input 300ms before firing query');
  it.todo('shows "Kein Account mit dieser E-Mail gefunden" on 404');
  it.todo('renders user card + already-linked warning on 200 with alreadyLinkedToPersonId');
  it.todo('disables Verknüpfen button until match selected');
});
