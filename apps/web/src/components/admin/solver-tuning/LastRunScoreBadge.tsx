/**
 * STUB — fleshed out in Task 3.
 *
 * Renders the Tuning-Page header badge: relative time + Hard/Soft score
 * + deep-link to /admin/timetable-history. See UI-SPEC §Tuning-Page header
 * copy for the four states (no run / feasible / infeasible / drift).
 */
interface Props {
  schoolId: string;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function LastRunScoreBadge(_: Props) {
  return null;
}
