import { describe, it } from 'vitest';

describe('PerspectiveSelector', () => {
  describe('VIEW-03: Admin perspective switching', () => {
    it.todo('renders dropdown for admin role');
    it.todo('shows teacher, class, and room option groups');
    it.todo('does not render for schueler role');
    it.todo('shows "Mein Stundenplan" for lehrer role');
    it.todo('calls onSelect with correct perspective and ID');
  });
});
